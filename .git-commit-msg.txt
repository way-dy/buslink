feat: 2026-05-28~06-01 세션 — SaaS Phase 1 + ETA 진척률 안분 + stopArrivals onCall 우회

## 핵심 변경
- **SaaS Phase 1.1~1.4**: companyResolver(동적 companyId) + 슈퍼관리자 회사 관리 콘솔
  (createCompany/listCompanies/toggleCompanyActive + 회사 운영 메뉴 4종: 편집/비번/관리자정보/영구삭제)
  + Phase 1.3 협력사 운영 포털(4섹션) + Phase 1.4 협력사 공지 발송(sendPartnerNotice rate-limit)
- **admin별 협력사 권한 인프라 Phase A**: users.allowedPartnerCodes + CF 4종
  (createCompanyAdmin/updateCompanyAdminPermissions/deleteCompanyAdmin/listCompanyAdmins)
  + SuperCompanyTab 카드 펼침 관리 UI(자기 자신 보호 3중)
- **ETA 진척률 안분 + delaySec raw 분리** (사용자 폰 실측 결함 보고 대응):
  segTravelMs plan offsetMin 델타 우선·next routePath 진척률 안분·useSmoothedEta jumpThreshold 300→180s
  + DriverApp/PassengerApp/EmployeeApp 진단 오버레이 [DIAG-ETA] (검증 후 제거 예정)
- **etaDiagnostics 자동 로깅**: runId 운행 1회 단위 + 30초 throttle + stopArrivals 즉시 트리거
  + fetchEtaDiagnostic onCall + 슈퍼관리자 EtaDiagnosticCard (사용자 폰 조작 0)
- **stopArrivals onCall 우회**: recordStopArrival CF — 클라 updateDoc silent fail 우회
  + 결과 로그 stopArrivalsLog 진단 페이로드에 자동 첨부 (다음 운행 정밀 진단)
- **운행 이력 정류장 반경 시각화** (검증 도구): Circle(100m) + 통과/미통과 색 분리 + 토글 + 범례
  + stops lat/lng coerce(string/GeoPoint/{location} 다중 형식)
- **LoginApp 합성이메일 mismatch fix**: 사번 필드에 @ 있으면 합성 X
  (createCompany admin 이메일 직접 사용과 통합)

## 신규 CF (총 13종 추가, 모두 assertSuperAdmin 또는 호출자 인증)
createCompany, listCompanies, toggleCompanyActive, updateCompanyInfo, resetCompanyAdminPassword,
updateCompanyAdminInfo, deleteCompany, createCompanyAdmin, updateCompanyAdminPermissions,
deleteCompanyAdmin, listCompanyAdmins, fetchEtaDiagnostic, recordStopArrival, sendPartnerNotice

## 신규 라이브러리
src/lib/companyResolver.js, src/lib/etaDiag.js, src/lib/useOnlineRecover.js

## prod 라이브
main.70e47fc7.js (2026-06-01 배포)

## 회귀 가드 (issues.md [패턴] 신규/보강 13건)
- LoginApp 합성이메일 분기 보존
- stops lat/lng coerce 헬퍼 일관 사용
- 슈퍼관리자 자기 회사/계정 삭제 차단 3중
- updateCompanyAdminPermissions 권한 상승 벡터 차단
- recordStopArrival 멱등 + 진단 로그 첨부
- ETA 진척률 안분 routePath 부재 시 chain fallback (회귀 0)

## 다음 (월요일 운행 후)
stopArrivalsLog ok 확인 → silent fail 해소 검증 → Phase B(AdminApp 협력사 필터 자동 적용)

🤖 Generated with Claude Code
