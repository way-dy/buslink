# Cloud Functions

`functions/index.js` — Firebase Functions **v2**, region `us-central1`, CommonJS. 독립 npm 패키지(`firebase-admin`, `firebase-functions`).

## 함수 목록
| 함수 | 트리거 | 역할 |
|---|---|---|
| `sendNoticeToCompany` | Firestore `onDocumentCreated("fcmQueue/{queueId}")` | 회사 `fcmTokens` 멀티캐스트(2026-05-21: `partnerCode` 있으면 `.where("partnerCode","==",X)` 협력사 한정, 없으면 전체). 500개 청크, 만료 토큰 자동 삭제. 결과 `fcmQueue.status`(`sent`/`no_tokens`/`error`) + `totalTokens`/`successCount`/`failureCount` 기록 → AdminApp이 onSnapshot으로 실시간 표시 |
| `notifyPreArrival` | Firestore `onDocumentUpdated("companies/{companyId}/dispatches/{date}/list/{dispatchId}")` | 도착 임박 푸시(2026-05-22). before/after `stopArrivals` diff로 새 도착 정류장 검출 → route stops를 `order` 오름차순 정렬한 배열로 구성, 도착 정류장 중 정렬 배열의 최전방 위치(index)=K → 정렬 배열에서 한 칸 뒤(K+1, 1정거장 전)·두 칸 뒤(K+2, 2정거장 전) 정류장의 직원에 FCM(raw order 산술 아님 — `order` 결번 안전, `computeStopEstimates`와 동일 위치 개념). `fcmTokens` where `routeId`+`stopId` 매칭(직원이 `/p`에서 선택한 내 정류장 denormalize). 멱등=`dispatch.preArrivalNotified[]` 마커(`{stopId}:pre1`/`pre2`) — 재발화·백필 다중기록에도 중복 0. 무효 토큰 자동 삭제. v1=routeId+stopId만 타겟(shift 미구분 — 후속 과제) |
| `createDriver` | `onCall` | Auth 계정 + `users/{uid}` + `companies/.../drivers` 동시 생성 |
| `deleteDriver` | `onCall` | Auth/users/drivers 정합 삭제 |
| `updateDriverPassword` | `onCall` | 기사 비밀번호 변경(≥6자) |
| `createDriverAuth` | `onCall` | 기존 기사에 Auth 계정 부여(이메일 중복 시 복구) |
| `expandDispatchSchedules` | `onSchedule("30 0 * * *", Asia/Seoul)` | 매일 새벽 00:30 향후 7일치 dispatches 자동 펼침. companies 전체 순회 + dispatchSchedules where active==true |
| `expandDispatchSchedulesNow` | `onCall` | AdminApp "지금 펼치기" 즉시 트리거. 같은 companyId 한정. 멱등 `${scheduleId}_${day}` dispatch ID — 기존 일별 수동 수정 보존 |
| `recordStopArrival` | `onCall` | **(2026-06-01)** 정류장 도착 기록 위임 채널. 입력 `{companyId,date,dispatchId,stopId,plannedAt?,delaySec?,viaBackfill?}`. 검증 = request.auth + users/{uid}.companyId 일치 + role∈{admin,superadmin,driver} + driver는 drivers/{dispatch.driverId}.uid===uid 추가 검증(rules 패턴 미러) + date `^\d{4}-\d{2}-\d{2}$` 형식. dispatch 존재 검증 + 멱등(stopArrivals[stopId] 이미 있으면 `{ok:true,alreadyExists:true}` 즉시 반환). Admin SDK 가 rules 우회 update — `stopArrivals.{stopId}={actualAt:serverTimestamp,plannedAt:plannedAt||null,delaySec:typeof delaySec==="number"?value:null,estimated:!!viaBackfill}` (dot notation). 반환 `{ok:true,stopId,actualAt:"serverTimestamp"}`. 배경 = 2026-06-01 진단 JSON 분석에서 `source:"stopArrivals"` 트리거 0개·stopArrivals 갱신 0건 확정 → 기존 클라이언트 updateDoc 경로가 silent fail(rules driver 검증 미스매치 의심). 본질 우회로 CF 위임. 호출처=DriverApp `onStopReached` 콜백. |
| `sendPartnerNotice` | `onCall` | **Phase 1.4 (2026-05-29)** 협력사 공지 발송. PartnerApp(익명 인증) 이 호출. `partnerCodes/{code}` 검증(exists+active+companyId 일치) + 서버측 rate-limit(`partnerCodes/{code}.recentNoticeTimestamps:number[]`, 1시간 5건, 옛 항목 정리 + now 추가) + title 50/body 500 trim 검증 + Admin SDK 로 `companies/{cid}/notices`(sender:"partner"/senderCode)·최상위 `fcmQueue` create(룰 우회). 기존 `sendNoticeToCompany` 트리거가 partnerCode 필터 멀티캐스트 인프라 재사용. 반환 `{ok, noticeId, queueId, remainingPerHour}`. 한도 초과시 `resource-exhausted` HttpsError 한국어("시간당 5건까지 발송 가능합니다. 남은 시간: M분"). |
| `fetchEtaDiagnostic` | `onCall` | **(2026-05-29)** ETA 진단 조회 임시 채널. assertSuperAdmin. `{date?,runId?}` 입력 — runId 없으면 그 날짜 runs 목록(`listDocuments()`), 있으면 points `orderBy tsMs asc` 전체 반환. AdminApp 회사관리 탭의 EtaDiagnosticCard 가 호출 → 텍스트 복사. |
| `createCompany` / `listCompanies` / `toggleCompanyActive` | `onCall` | **Phase 1.2 (2026-05-28)** 슈퍼관리자 회사 관리. assertSuperAdmin. createCompany=companies+Auth+users 3건 동시 생성·rollback. listCompanies=전체 회사 목록(활성 우선 정렬). toggleCompanyActive=active 토글. |
| `updateCompanyInfo` / `resetCompanyAdminPassword` / `updateCompanyAdminInfo` / `deleteCompany` | `onCall` | **Phase 1.2+ (2026-05-29)** 슈퍼관리자 회사 운영 메뉴 4종. assertSuperAdmin. `updateCompanyInfo({companyId,name})`=회사명만 update(1~50자). `resetCompanyAdminPassword({companyId,newPassword})`=그 회사 첫 admin uid의 Auth 비번 변경(≥6자). `updateCompanyAdminInfo({companyId,displayName?,email?})`=그 회사 admin Auth + users 도큐먼트 동기 변경(둘 중 1개 이상). `deleteCompany({companyId,confirmCompanyId})`=cascade 영구 삭제: 최상위 컬렉션(gps/partnerCodes/boardingTokens/passengerTokens/fcmQueue) where companyId==cid 500개 청크 + gpsHistory/{cid} recursiveDelete + companies/{cid} recursiveDelete(routes/stops/dispatches/drivers/vehicles/passengers/boardings/fcmTokens/notices) + users where companyId==cid 각 uid Auth+도큐먼트 삭제. **자기 회사 보호**=호출자 users/{uid}.companyId === companyId 면 `permission-denied`. confirmCompanyId 불일치는 `failed-precondition`. etaDiagnostics 는 cascade skip(companyId 인덱싱 불가). 부분 실패 시 throw — 사용자 재시도하면 멱등(이미 삭제된 건 자연 skip). |
| `updateCompanyAdminProfile` | `onCall` | **(2026-06-08)** 특정 uid 의 admin 로그인 정보(이름/이메일/비밀번호) 수정. assertSuperAdmin·Admin SDK 우회. 입력 `{uid, name?, email?, password?}`(최소 1개). 대상 `users/{uid}.role!=="admin"` 거부(superadmin/driver 보호·권한상승 차단). name 1~30자·email 형식·password ≥6자(제공 시). 변경 제공된 것만 Auth `updateUser(uid,{displayName?,email?,password?})` + `users/{uid}` `{name?,email?}` update. **role/companyId/empNo/allowedPartnerCodes 절대 미변경**. email 중복=`already-exists` "이미 사용 중인 이메일입니다". 자기 자신(uid===request.auth.uid)도 허용. `updateCompanyAdminInfo`(companyId 기준·회사 첫 admin)와 달리 uid 기준이라 특정 admin 수정에 적합. AdminApp `AdminListSection` 행 "✏️ 정보수정" 버튼→`EditAdminProfileModal` 호출. |
| `createCompanyAdmin` / `updateCompanyAdminPermissions` / `deleteCompanyAdmin` / `listCompanyAdmins` | `onCall` | **Phase A (2026-05-29)** admin별 협력사 권한 인프라(슈퍼관리자 전용). assertSuperAdmin·Admin SDK 우회(rules 변경 0). `createCompanyAdmin({companyId,empNo,email,name,password,allowedPartnerCodes})`=Auth+users 동시 생성(`role:"admin"` + `allowedPartnerCodes:string[]` 필드 포함)·rollback(Auth deleteUser)·email 중복=`already-exists`·`createCompany` 패턴 미러. `updateCompanyAdminPermissions({uid,allowedPartnerCodes})`=users 의 `allowedPartnerCodes` 필드만 update(role/companyId/empNo/email/name 보호)·role!=="admin" 거부. `deleteCompanyAdmin({uid})`=Auth+users 삭제·**자기 삭제 차단**(uid===request.auth.uid 면 `permission-denied`)·Auth 없으면 catch 멱등. `listCompanyAdmins({companyId})`=users where companyId+role=="admin" get → `{uid,empNo,email,name,allowedPartnerCodes}` 반환·allowedPartnerCodes 부재=`["*"]` 정규화·이름순 정렬. **공통 헬퍼** `normalizeAllowedPartnerCodes(input)`=배열 검증+`"*"` 포함 시 `["*"]` 로 정규화+중복 제거. **빈 배열 허용**(어떤 협력사도 접근 못 하는 admin — 유효 케이스). Phase B(AdminApp 협력사 필터 자동 적용) 는 별도 — 이 4종은 인프라+관리 UI 한정. |

| `resolveBusinCarId` | `onCall` | **(2026-07-08 RQ#2 유비칸)** 차량번호→busin carId 조회. assertAdmin. `{vehicleNo}` 입력 → busin `CarAlloc.aspx?drv=1`(dr.busin.co.kr:4431, 키불필요·서버 fetch·`rejectUnauthorized:false`) HTML 파싱 → 번호 정규화(공백/하이픈 제거) 매칭 → `{carId, name}` / 미매칭 `{carId:null}`. AdminApp VehicleTab "번호로 carId 조회" 버튼이 호출(생성 직후 수분 401 가능=Cloud Run IAM 전파). |
| `resolveStaticBoarding` | `onCall` | **(2026-07-13 정적 QR CF 위임)** 정적(고정) QR 확인화면 프리뷰용 읽기전용. `{companyId,vehicleId}` → 오늘(KST) `dispatches/{today}/list where vehicleId==X` 조회(비면 `failed-precondition` "오늘 이 차량은 운행 배차가 없습니다\n관리자에게 문의하세요") → `{today,routeId,routeName,driverId,vehicleNo}` 반환(vehicleNo 없으면 vehicles.plateNo 폴백). `request.auth` 존재만 확인(**익명 통과**·assertAdmin 없음 — 익명 직원이 호출). 근인=배차 읽기 rules admin/driver 잠금이라 익명 진입점(EmployeeApp 스캐너·BoardingApp) 쿼리 거부 → Admin SDK 우회. 공용 헬퍼 `resolveStaticDispatchAdmin`. |
| `boardStatic` | `onCall` | **(2026-07-13 정적 QR CF 위임)** 정적 QR 배차 재해석 + 멱등 boarding 생성. `{companyId,vehicleId,empNo,name}`(empNo trim·미입력 `invalid-argument` "사번을 입력해주세요") → resolveStaticDispatchAdmin 로 배차 확보 + passengers.partnerCode·gps 좌표 캡처(try/catch null) → 멱등 doc id `${empNo}__${vehicleId}` in `boardings/{today}/list`(exists 면 create 없이 `alreadyBoarded:true`) → setDoc(validateAndBoardStatic 동일 스키마 + `via:"static"`) → `{ok,alreadyBoarded,routeName,vehicleNo,dispatchDate}`. `request.auth` 존재만 확인(익명 통과). 클라 `boarding.js validateAndBoardStatic` 가 호출(반환 계약 보존·EmployeeApp/BoardingApp 무변경). |
| `boardNfc` | `onCall` | **(2026-07-22 NFC 사원증 탑승)** 기사앱 Web NFC 태깅 판정. `{companyId,vehicleId,uid,selectedRouteId?}` → ① `request.auth` + `users/{uid}.role∈{driver,admin,superadmin}` + companyId 일치(superadmin 예외) ② 공용 헬퍼 `resolveStaticDispatchAdmin` 로 오늘 배차 해석(정적 QR 과 로직 공유) ③ `normalizeNfcUidServer`(클라 `lib/nfc.js` 와 동일 계약) ④ `passengers where nfcUid==` limit 1(단일필드=자동 인덱스). **매칭**=멱등 doc id `${empNo}__${vehicleId}` 로 boardings 생성(`via:"nfc"`+`nfcUid`, 정적 QR 과 **같은 id 규칙이라 모드 간 중복 적재 0**) / **미매칭**=`nfcRejects/{today}/list` 적재. 반환 `{ok,registered,empNo?,name?,alreadyBoarded?,uid?,routeName,vehicleNo,dispatchDate,todayCount}`. ⚠ **미등록은 throw 가 아니라 `registered:false`** — 기사 화면이 빨간 UI 를 그려야 하고 throw 면 네트워크 오류와 구분 불가. `todayCount`=`boardings where vehicleId` count() 집계(admin 12.7+). |
| `registerNfcCard` | `onCall` | **(2026-07-22 카드 등록 대행)** 기사앱이 카드를 읽어 승객에게 매핑. `{companyId,empNo,uid}` → driver/admin + 같은 회사 검증 → UID 정규화 + 길이 검증(8자↑·짝수) → `passengers/{empNo}` 존재 확인 → **중복 카드 검사**(`where nfcUid==` limit 2 에서 본인 외 다른 문서 있으면 `already-exists` 로 거부 — 조용히 뺏으면 원 소유자가 어느 날 갑자기 미등록이 되고 추적 불가) → `nfcUid`+`nfcUidUpdatedAt` update. **같은 사람이 다른 카드로 재등록은 허용**(분실·교체. 레거시 버스인도 재태깅=재등록). 반환 `{ok,empNo,name,replaced}`. `passengers.write` 룰이 `isAuth()` 라 클라 직접 쓰기도 가능하지만 **중복 검사·정규화를 한 곳에서 강제**하려고 서버 경유. |
| `onImprovementRequestCreate` | Firestore `onDocumentCreated("improvement_requests/{id}")` | **(2026-07-13 개선 요청 게시판)** 새 요청 생성 시 구글챗 cardsV2 카드 발송(새 스레드 `thread.threadKey='imp-'+id`). 웹훅 URL 우선순위=**Functions 시크릿 `GCHAT_WEBHOOK_URL`**(`defineSecret`·`secrets:[]` 바인딩·`firebase functions:secrets:set` 로 설정·git 미포함) → **폴백** `config/improvementBoard.gchatWebhookUrl` getDoc. 둘 다 없으면 조용히 skip. **YDYOPS 와 같은 스페이스 공유 → 카드 타이틀 `🚌 BusLink · …` 구분자**(2026-07-13 사용자 요청). node `https` POST(serviceAccount 불요)·best-effort try/catch·throw 금지. 카드=제목/회사명(`companies/{companyId}.name`)/요청자/상태/본문 140자 + 게시판 버튼(`https://admin.buslink.co.kr`). doc write 0(재귀 없음). |
| `onImprovementRequestUpdate` | Firestore `onDocumentUpdated("improvement_requests/{id}")` | **(2026-07-13 개선 요청 게시판)** before/after `history` **길이 diff 로 신규 항목만** 추출(arrayUnion 말미 append) → 같은 threadKey 답글 POST(`messageReplyOption=REPLY_MESSAGE_FALLBACK_TO_NEW_THREAD`). 변동 없으면 skip(자기 doc write 0·재귀 0). **완료(statusTo==='done') 항목은 skip**·코멘트/기타 상태전이는 발송. 웹훅 미설정 시 no-op. |
| `pollDeviceVehicleGps` | `onSchedule("* * * * *", Asia/Seoul)` | **(2026-07-08 RQ#2 유비칸)** GPS 단말 차량 위치 서버 폴링(us-central1). companies 순회 → 각 회사 `vehicles where gpsSource=="device"`(+carId) → 오늘(KST) 배차 1회 조회로 `vehicleId→{routeId,routeName,driverId,driverName}` → busin `CarLocationAll.aspx?carid=X&date=Y` 최신 좌표 → **운행 시간 창 가드(2026-07-13 `computeGpsWindow`: 노선 `departTime`−30분 ~ (departTime+마지막 정류장 offsetMin)+60분, offsetMin 전무 시 기본 120분. 현재 KST 분이 창 밖이면 busin 조회·write 모두 skip — 상시신호 단말이 운행 전/후 이동해도 표시 안 함. departTime 미설정 노선=게이트 없음 기존 동작 보존)** + **신선도 가드(좌표시각 vs KST now 15분 초과 skip=주차차량 배제)** + 배차없음/좌표0건 skip → `gps/{cid}_{vid}` setDoc(**sendGPS 동일 필드** + `source:"device"`·speed 0) + gpsHistory append + 100m 정류장 서버측 도착감지(dot-notation `stopArrivals.{stopId}`·via:"device"). Admin SDK(rules 우회). 회사/차량별 try/catch. |

## 트리거 흐름 — 공지/FCM
AdminApp `sendNotice({companyId, title, body, type, partnerCode?})` → `companies/{cid}/notices`(partnerCode 포함) + 최상위 `fcmQueue` 문서 생성 → `sendNoticeToCompany`가 partnerCode 있으면 `where("partnerCode","==",X)` 협력사 토큰만 수집(없으면 전체 — partnerCode 필드 누락한 기존 토큰도 포함) → `sendEachForMulticast` → `fcmQueue` 문서에 status(`sent`/`no_tokens`/`error`) + totalTokens/successCount/failureCount 갱신. NoticeTab은 발송 직후 `fcmQueue/{queueId}` 단일 doc onSnapshot 구독 → 결과 카드 실시간 표시(pending→완료/0건/오류). 무효 토큰은 `fcmTokens`에서 자동 삭제.

**협력사 발송**(2026-05-29 Phase 1.4): PartnerApp `sendPartnerNotice({companyId, partnerCode, title, body, type})` 호출 → CF 가 검증·rate-limit 후 `notices`(sender="partner")·`fcmQueue` create(클라 룰 우회) → 동일 `sendNoticeToCompany` 트리거가 partnerCode 필터 발송. notices/fcmQueue 스키마 100% 호환(EmployeeApp 공지함·강제 모달·기존 NoticeTab 무영향). 서버 시각 기준 rate-limit 으로 클라이언트 시각 조작 차단.

## 주의사항
- onCall 함수는 모두 `request.auth` 필수(미인증 시 `unauthenticated`).
- 합성 이메일 규칙 `${empNo}@buslink.com` — 기사 생성/삭제 시 Auth·`users`·`drivers` 3곳 정합 유지가 핵심.
- `createDriverAuth`는 `auth/email-already-exists` 시 기존 계정 비밀번호 갱신으로 복구.
- `expandDispatchSchedules*`: 한국 공휴일 정적 데이터 `functions/holidays.js` (클라 사본 `src/lib/holidays.js`). **2028년까지 작성, 2028 말 전 갱신 필요**(issues.md). 멱등 dispatchId=`${scheduleId}_${day}` — exists() skip → 일별 수동 수정/기사교체 보존. Cloud Scheduler API 첫 배포 시 자동 활성화됨(`cloudscheduler.googleapis.com`).
- 런타임 불일치·구버전 스니펫 등 함정은 @.claude/issues.md.
