# Tasks Log (옛 완료 누적·시간역순)

> tasks.md의 최근 7일을 제외한 누적 로그. 상세는 issues.md 패턴·redesign-log.md·git log.

## 2026-08-04~05 일부 세션 (tasks.md 에서 이관, 2026-08-06)

> **2026-08-05 세션 G(하단 탭바 감각 개선·hosting 배포)**: way "아이콘도 색을 넣을 수 있는 감각적인 것으로". 색은 이미 먹고 있었고(픽셀 실측 확인) **선이 얇아 브랜드색이 안 읽히던 것**이 진짜 문제. 후보 5안을 렌더해 비교 후 **선택 탭만 채움 + 알약 배경** 채택 — 알약색은 기존 `--color-primary-soft`(브랜드색 자동 파생)라 **신규 필드 0**. 검증 도구 `scripts/headless_check_tabbar_branding.cjs` 신설(렌더된 색을 픽셀로 잼). prod `main.8fc6f967.js`. **거래처 9곳 중 4곳만 색 설정** — 나머지는 관리자 → 협력사 관리 → ⚙️ 포탈 설정에서 지정.

> **2026-08-05 세션 F(길안내 지도 중심 모드·hosting 배포)**: way "지도를 최대한 크게 · 방향안내 글씨 잘림 · 상단 메뉴 최소화". **글씨 잘림의 근인은 겹침**이었다 — `↑ 진행방향` 토글이 배너 오버레이 위에 얹혀 문구 오른쪽을 가림 → 오른쪽 가운데 ＋/－ 열로 옮기고 **지도 위쪽에는 버튼을 두지 않는다**. 길안내 탭에서 인사·배차 히어로·상태 스트립을 접고(`navFocus`) 지도 높이를 **실측**으로 화면 끝까지(`compactH` — 고정 px 빼기는 기기마다 어긋난다). prod `main.cb4b720e.js`. ⚠ 기사앱은 로그인이 필요해 헤드리스 캡처 불가 → **실주행 확인 필요**.

> **2026-08-05 세션 C(추가 미팅 반영 1차·hosting 배포)**: 자료=`RQ/20260805/{meeting.txt,summary.txt}`(git 추적본). **전사가 정본**(요약은 대조용). 이번에 낸 것 3건 — ① 🔴 **승객앱 홈 노선도 2단 → 1단 통합**(요약 진척바 + 전 정류장 스트립이 위아래로 쌓여 있어 노선도가 두 줄로 보이고, 위 요약바를 눌러도 반응이 없어 "탑승객이 누르고 탑승 못 하는" 일이 있었다). **모양은 요약바(역할 라벨·진척 색·상태줄), 기능은 스트립(전 정류장 + 눌러서 내 정류장 지정)** 으로 합침 — 4노드만 남기면 **내 정류장을 고를 수가 없어서** 전 정류장을 유지했다. 짝으로 **자동 가로 스크롤** 추가(합치면 정류장 10곳 노선은 버스가 화면 밖이라 "진입 즉시 도착정보"가 깨진다). ② **하단 탭바 아이콘을 이모지→벡터 `Icon`** — 이모지는 색을 못 바꿔 거래처 브랜드 컬러가 탭바에만 적용이 안 됐다(회의 #5). 아이콘 업로드는 미도입(way 결정). ③ **승객앱 노선 필터에 '셔틀' 칩**(회의 #7 "확인해 보고 없으면 알려줘" → 칩에 셔틀이 빠져 있었다). **prod 실측: type 분포 출근 37·퇴근 23·셔틀 0건** → 셔틀 노선이 있을 때만 칩 노출(항상 띄우면 249명에게 상시 0건 칩). 검증=격리 4스위트(13/34/26/23)·4화면 콘솔오류 0·노선변경 회귀·홈 화면 실캡처 2종·신규 경고 0(25→24, `isPassed` 제거). prod `main.62420bbc.js`. **미착수(별도)**: 노선 표시 시간창 · BYYC 문의하기 · 안내방송 · 온세미 패턴 배차 · 콜센터 신호 — 사유는 issues.md `[진행중]`.

> **2026-08-05 세션 B(게시판 1건·hosting 배포)**: 배시현 개선요청 `Tuw5QwWf9QLqGt2FDFGW` "노선변경 후에도 버스 GPS가 그자리 그대로". 첨부 2장이 근인을 짚어줬다 — 두 노선 다 `1대 운행중` 인데 마커는 **어느 노선에도 없는 판교**에 있었다. 구독 필터는 정상이었고, 근인은 `useAnimatedPositions` 가 rAF 루프 안에서만 갱신되는 `displayVehicles` 를 그대로 반환하는데 **그 루프가 빈 목록에선 다시 안 돈다**는 것 — 운행 차량 없는 노선으로 바꾸면 이전 노선 버스가 **영구 잔존**하고 배지·진척바·ETA·지도 센터까지 전부 그 값을 탄다. 반환의 구성원을 항상 `rawVehicles` 가 정하게 바꾸고, 빈 목록 clear + 사라진 차량의 보간 ref 정리를 추가. **공용 훅이라 승객앱·직원앱·관제·협력사 포털 4화면 동시 수정**. 검증=격리 13케이스(최소 React 훅 런타임+수동 rAF 큐로 소스를 그대로 태움)·**실화면 재현**(옛 번들에서 `19:50 판교역` 에 이전 노선 차량 `경기79사9849` 가 남는 것 확인 후 신 번들에서 `운행 없음` 전이 확인)·4화면 콘솔오류 0·신규 경고 0. prod `main.cc8fb4fc.js`. 상세 issues.md `[해결]`.

> **2026-08-04 세션(게시판 1건·hosting 배포)**: 배시현 개선요청 `8E6U4bwr5rSJyMRxvsEN` "버스위치 하이라이트가 아래쪽으로 가있다". 대상=승객앱 홈 **요약 진척바**의 현재 노드 펄스 링. 근인=**`buspulse` 키프레임이 `transform` 을 애니메이션해서 중앙 맞추기용 인라인 `transform` 이 통째로 무시**되고 링이 오른쪽·아래로 11px 씩 밀린 것 — **2026-07-13 의 "-4px 위로" 가 링에는 적용된 적이 없었다**(같은 커밋의 노선도 스트립은 래퍼/안쪽 분리라 정상이라 실패가 안 보였다). 오프셋을 `left/top` 으로 옮겨 해결. 검증=소스에서 style 을 뽑아 실제 keyframes 와 헤드리스 렌더 후 **두 중심을 픽셀로 측정**(옛 코드 어긋남 재현 포함)·전 소스 스윕 0건·18케이스. prod `main.44e9e679.js`. 부수=`board.cjs` 가 **본문 인라인 이미지**도 세고 `dump` 로 내려받게 보강(이번 요청의 유일한 스크린샷이 본문 안에 있어 "첨부 0장"으로 보였다). 상세 issues.md `[해결]`.


## 2026-07-29~31 세션 누적 (tasks.md 에서 이관, 2026-08-05)

> **2026-07-31 세션 C(길안내 현장 지적 4건·prod 배포)**: way 실주행 테스트 피드백 — ①사진이 지도를 밀어냄 ②진행 방향이 아니라 헷갈림 ③그린 노선대로 안내를 안 함 ④축척이 작음. **③이 실측으로 확인된 진짜 결함**: `getNaviRoute` 가 정류장만 경유지로 주고 도로는 카카오가 골라, prod 8개 노선에서 그린 경로 점의 **24~63%가 300m 넘게 이탈**(최대 5.9km). 2단계 호출(벗어난 구간에만 보정 경유지)로 **0~22%** 까지 낮춤. ⚠ 다만 **보정이 오히려 악화되는 노선이 있어**(`[압구정] 하교` 42%→52%) 전후를 같은 잣대로 재서 **나아질 때만 채택**하는 게이트를 넣었다(이게 없으면 일부 노선이 더 나빠진 채 나갔다). ①②④는 화면 재구성 — 지도를 `64vh` 본체로 올리고 안내는 오버레이, 사진·주소 제거, **지도 판 CSS 회전(heading-up)** + 카카오 로고·축척을 회전 밖으로 옮겨 세움(약관), 기본 `level 3` + 내 위치 자동 추적(끌면 해제). prod `main.42b8647a.js` + `functions:getNaviRoute`(2회). 격리 166케이스·헤드리스 회전 실증·실노선 8개 카카오 실호출·신규 경고 0. **way 실주행 재확인 필요**(회전 감각·안내 시점은 헤드리스로 못 잼). 상세 issues.md `[해결]`.

> **2026-07-31 세션 B(게시판 1건·hosting 배포)**: 배시현 개선요청 `sPnYpmChrcOqSIzwbQEn` "과거 공지가 앱에 다 보인다 · 관리자가 정리하게". prod 실측 = 공지 110건 중 **active 101건**(최고참 3-22·4개월치), **75건이 "전체" 발송**이라 채드윅 학부모 앱에 5월 테스트 공지가 그대로 노출. 근인 = 관리 화면 발송 이력이 `slice(0,10)` 이라 **95건이 앱엔 보이는데 관리 화면엔 없었다** + 제한 admin 은 "전체" 공지가 목록에서 필터돼 존재조차 몰랐다. way 결정대로 숨기기+영구삭제(2단 확인)·제한 admin 은 전체 공지 **읽기만**·자동 만료 미도입. 목록 상한 제거(더보기)·검색·상태 칩·되돌리기 추가. **`isShown = active === true`** 로 소비측 쿼리와 정합(느슨한 `!==false` 는 유령 건수). 격리 26케이스·신규 경고 0·prod `main.8c460627.js`. rules/indexes/CF 변경 0. **운영 후속**: 101건 실제 정리는 전체권한 계정(김희정·황광연)이 화면에서. 상세 issues.md `[해결]`.

> **2026-07-31 세션(게시판 1건·CF 배포)**: 배시현 개선요청 `BiSp9ujKqmPOGgAYzocX` "서울72바7026 / CARID 4964 오전 관제가 안된다". 등록·busin 연동은 전부 정상이었고(좌표 76건·최신 2분 전), 근인은 폴러가 **한 차량당 배차를 1건만** 남긴 것 — 등교·하교를 같이 뛰는 차량에서 하교 배차가 뽑히면 그 시간창(15:20~17:30)으로만 판정해 **오전 좌표가 통째로 차단**됐다. prod 실측 = device 27대 중 다중 회차 10대, 그중 4대가 오늘 오전 포인트 0~4건(등교가 우연히 뽑힌 3대는 93~134건) — **문서 ID 운**에 좌우되던 상태. `pickActiveDispatch` 신설(현재 시각이 든 시간창의 회차 선택·창 겹치면 출발시각 근접)·`functions:pollDeviceVehicleGps` 배포·prod 실호출 확인(7012 가 `08:20 판교역`→`09:20 판교역` 회차로 전환). 격리 29케이스·진단 도구 `scripts/inspect_device_gps.cjs` 신설. **오후 하교 회차는 15:20 이후 재확인 필요**. 상세 issues.md `[해결]`. ⚠ **별건 보고(게시판 미기재)**: 잔존 mobile gps 문서 2건(서울72바7026·경기79사3539 "테스트노선")이 device 차량에 남아 있다 — `cleanupDeviceGpsDoc` 은 `source==="device"` 만 지우므로 영구 잔존. 관제 "잔존 운행 신호"에서 정리 가능.

> **2026-07-29 세션(도착알림 시간표 폴백 + 길안내 카카오 도로경로 연동)**: ① 🔴 도착 임박 알림 76% 미발송 → 시간표 폴백 CF `notifyPreArrivalBySchedule` 배포(회사 옵트인·GPS 살아있으면 미발송·마커 공유로 중복 0). way 가 dy001 켬. 하루 상한 3통(시뮬레이션). ② way 질문에서 출발해 **카카오모빌리티 길찾기 API 연동** — 앱 임베드는 불가하지만 데이터를 받아 우리가 그리면 앱을 안 떠나므로 GPS 가 안 끊긴다. CF `getNaviRoute` 배포·시크릿 `KAKAO_REST_KEY`(buslink 전용 앱 REST 키). prod 실호출 검증=점간격 29m·회전안내 39개. 프리뷰 **`main.c65f3246.js`**·**prod 미배포**. ③ way 현장 확인 반영 2건 — 경로 밖 투영 결함(16km 밖에서 회전 안내가 떴다) 수정 · **차량 마커 진행방향 화살표**(카카오 지도는 회전 미지원 확인). 격리 104케이스. 상세 issues.md.


## 2026-07-22~28 세션 누적 (tasks.md 에서 이관, 2026-08-05)

> **2026-07-28 세션 B(기사 길안내 신설·프리뷰까지)**: way 요청 "초보 기사를 위한 길안내". 실측으로 **기사앱에만 지도가 없다**는 걸 확인(다른 3개 앱엔 다 있음). `src/lib/navGuide.js`(순수·78케이스) + DriverApp "길안내" 탭. 핵심=경로를 **지나온/가야 할/그 이후**로 잘라 가야 할 구간만 굵게 + 출발 전엔 노선 전체가 한 화면에. **프리뷰 `main.3e9079ca.js`**(https://buslink-prod--navguide-aq5atnnv.web.app) — **prod 미배포·way 검토 중**. 음성(정류장 접근 TTS)은 way 결정만 나오고 **미착수**. ⚠ **프리뷰 채널은 카카오 도메인 미등록이라 지도가 통째로 안 뜬다** — way 가 콘솔에 이 채널 주소를 등록해 해결(다음부터는 고정 채널명 재사용 권장). 부수 발견=**도착 임박 알림 76% 미발송**(별건·issues.md `[미해결]`). 상세 issues.md.

> **2026-07-28 세션(강제 운행 종료 신설·게시판 1건)**: 배시현 개선요청 `cv4XzFYLUdUfzqBEuDQw` "기기 로그아웃 후에도 계속 운행 중 — 강제 로그아웃 기능". prod 실측으로 **신고보다 넓은 문제** 확인: gps 문서 10건 중 4건 잔존(309~1102시간)·신고 대상은 **배정 기사가 없는 고아 신호**(로그아웃으로는 영원히 못 지움)·반대로 `status="운행중"` 만 18일째 남은 기사도 1명. mobile gps 문서는 청소 주체가 "기사앱 운행 종료 버튼" 하나뿐인 게 근본. 신규 `src/lib/runSignals.js`(순수 판정)·`src/lib/forceEndRun.js`(쓰기) + AdminApp 실시간 관제 좌측 레일에 "🧹 잔존 운행 신호" 섹션·강제 종료 버튼(인라인 확인·경과시간 노출) + DriverApp 로그아웃 경합 수정(fire-and-forget 쓰기가 signOut 으로 유실되던 것). 격리 34케이스·헤드리스 4화면·신규 경고 0. rules/indexes/CF 변경 0. prod `main.9026b68f.js`(`--only hosting`·curl 확인)·게시판 done + **사용자가이드 댓글**(2026-07-28 way "추후도 반영" → 완료 처리에 가이드 댓글 상시 첨부로 승격·`/board` 스킬 반영). 잔존 데이터도 정리 완료(신호 4건·기사 1명·재검증 0). 상세 issues.md `[해결]`. ⚠ **분류기 함정**: 허용 규칙이 `Bash(firebase deploy:*)` 라 `npx --no-install firebase deploy …` 로 부르면 접두사가 안 맞아 차단된다 — **`firebase` 를 그대로** 호출할 것(cwd 를 프로젝트로 먼저 옮기고 `cd … && firebase …` 결합도 피할 것).

> **2026-07-27 세션(승객 계정 발급·배부 3종·prod 배포·게시판 done)**: 게시판 요청(배시현 `eWh6y3K5NDqNwzkdlxhJ`) "승객 자가 가입" → way·요청자 협의로 **자가가입 미채택**(고정 명부 239~250명·담당자 셋팅이 효율적) 확정 후, 진짜 병목인 **계정 배부**를 자동화. ① 초기 PIN 개인별 발급(prod 실측 = 승객 12명 전원 `000000` 미변경 → 사번만 알면 남의 계정 진입) + 첫 로그인 시 본인 PIN 설정 강제 ② 계정 안내문 A4 인쇄(`src/lib/accountCards.js` 신설·QR=`/p?emp=` 프리필) ③ `passengers.lastLoginAt` + 협력사 포털 "미시작" 집계·배지·일괄 재발급＋인쇄. **구현 중 잡은 버그**: 미시작 판정을 `!lastLoginAt` 단독으로 두면 레거시 사용자(필드 부재·본인 PIN 사용 중) 비밀번호를 날림 → `&& pinInitial` AND 필수. prod `main.2b347671.js`(`--only hosting`)·rules/indexes/CF 변경 0·신규 경고 0. 검증=격리 38케이스+헤드리스 5화면+인쇄물 렌더+포털 실데이터 캡처. 상세 issues.md `[패턴]`.

> **2026-07-27 세션 C(관리자 가시범위 점검·코드 변경 0)**: 김희정 점검요청 "관리자 화면에 제가 test로 입력한 것만 보인다". 코드 결함 아님 — 그 계정 `allowedPartnerCodes:[]` 라 본인 생성 거래처만 노출(createdBy 격리 설계대로). dy001 admin 4명 전수 실측 → **황광연 계정은 0/58 로 화면이 통째로 비어 있었음**(미신고). 김희정·황광연 `["*"]` 부여·재실측 58/58 확인. 신규 진단 스크립트 2종(`inspect_admin_visibility.cjs` 읽기전용 · `set_admin_partner_access.cjs` dry-run 기본). **배포 없음**(users 문서 데이터 변경만). **사용자 안내 필요**: 권한은 `getDoc` 1회라 **새로고침 후** 반영 · 김희정 계정 비밀번호가 `000000` 이라 전체권한 부여와 함께 변경 권고. 상세 issues.md `[패턴]`.

> **2026-07-27 세션 B(게시판 2건·전부 prod 배포)**: ① 🔴 **배차 일정 하루 밀림**(배시현 `CmxQPRtRhUAnDq4kfVna`) — 서버(UTC)에서 `getDay()` 를 써 요일이 하루 전으로 계산됨. prod 실측 = 토요일에 평일 배차 29건 / **월요일 배차 0건**(신고 당일 오후에 그날 배차가 통째로 없었다). `dayOfWeekKST` 를 `getUTCDay()` 로 교정·CF 2종 배포 + `scripts/fix_schedule_weekday_shift.cjs` 로 데이터 정리(way `--apply` 실행: 삭제 36/생성 36, 재검증 0건). ② **노선 탭 "실시간 지도" 경로 직선**(배시현 `qYol841t9rt9yOOA9VUI`) — 데이터는 정상(50개 노선 routePath 보유·전부 number), 그 화면만 정류장을 곧장 잇고 있었음. `routeProgress.toLatLngPath` 신설 + 모달이 routePath 우선. prod `main.c87a40a9.js`. 상세 issues.md `[해결]` 2건.

> **2026-07-22 세션 B(NFC 사원증 탑승 신설·prod 배포)**: way 요청 — 레거시 버스인3 NFC 태깅(`file/20260722/*.pptx` 20슬라이드)과 `swsfirebase/swstagsys`(Web NFC 선례)를 참고해 buslink 에 이식. 탑승 모드 4번째. 신규 `src/lib/nfc.js`(순수·격리 테스트 36케이스) + CF `boardNfc` + DriverApp NFC 탭 + PartnerApp 카드번호 등록 + AdminApp "부정승차" 탭. prod `main.3d678744.js` + `firestore:rules` + `functions:boardNfc`(카나리아 401=우리 핸들러 확인). 신규 경고 0(35↔35). 이어서 **카드 등록 대행**(way 결정=기존 사원증 재사용 → UID 목록 없음 → 기사앱 대행): CF `registerNfcCard` + NFC 탭 모드 토글 + 미등록 결과에서 등록으로 잇는 버튼. prod `main.ac32f83b.js`. 헤드리스 4화면 콘솔오류 0. **실기기 검증 대기** — Web NFC 는 헤드리스 불가. 상세 issues.md `[패턴]`.

> **2026-07-22 세션 A(배차 복사 실행상태 복제 근절·prod 배포)**: 승인된 `clean_stale_arrivals.cjs --apply` 를 실행하려다 dry-run 수치 증가(69/122→120/215)와 **미래 날짜 배차의 도착기록**을 보고 발생원 재수색 → `handleCopyDispatches` 의 `{...data}` 스프레드가 `stopArrivals`·`preArrivalNotified` 까지 복제하던 것 확정(prod 필드 분포 실측). 명시 필드 화이트리스트로 교체·prod `main.3a183a76.js` 배포. 청소 스크립트는 미래일 `preArrivalNotified` 까지 처리하도록 확장. way 가 직접 `--apply` 실행 → **120배차 정리·재검증 0건/0건**. 상세 issues.md `[해결]`.


## 2026-06-08~17 세션 누적 (tasks.md 에서 이관, 2026-07-09)
> **2026-06-17 세션 추가**: EmployeeApp(`/p`) 노선 변경 모달이 전 거래처 노선 노출 → 직원 partnerCode 필터(모달+홈 폴백). 상세 issues.md `[패턴]`.
> **2026-06-17 세션(prod 배포)**: 배차관리·배차일정·대시보드 기사현황에 createdBy 격리 확장(누락분) — 제한 admin(dy04)이 배차 안 보이던 것·대시보드에 회사 전체 기사 보이던 것 해소. canSeeDispatch/canSeeSchedule(노선 partnerCode∈allowed OR 노선/배차 createdBy===uid) + 신규/복사 createdBy 기록 + 폼 드롭다운 본인것만(전체권한 전체 유지). 마스터키=partnerCodes.createdBy(거래처에 createdBy만 있으면 전 탭 partnerCode 체인 자동노출). 레거시(채드윅 등) 거래처/기사는 슈퍼관리자 권한체크 or createdBy 백필 필요·신규는 자동. rules/indexes/CF 변경 0. 상세 issues.md `[패턴]`.
> **2026-06-16 세션 누적(전부 prod 배포·master 커밋)**: ① **정류장 주소검색 무반응** = 카카오 키(`58bf34`) 신규 서브도메인(`admin.buslink.co.kr` 등) 미등록 → 지도+검색 동시 사망. **사용자가 카카오 콘솔 4개 서브도메인 등록 완료(해결)** + 코드측 `handleAddrSearch` 8초 워치독(콜백 미응답 시 "검색 중" 영구정지 차단). ② **본인 등록 거래처 배차가 배차관리에서 안 보임** = createdBy 가시성을 대시보드·협력사관리만 적용했던 누락 → AdminApp 최상위에서 `allowed = rawAllowed∪(createdBy==uid 거래처)` 합쳐 **전 탭 동일 적용**. 사용자 결정 "각자 생성 거래처는 권한 무관 항상 노출". rules/indexes/CF 변경 0. 상세 issues.md `[패턴]` 2건.
> **2026-06-15 세션 누적(전부 prod 배포·master 커밋)**: ① admin 협력사 권한 "전체 해제" 저장 안 됨 수정(`listCompanyAdmins` CF + `EditAdminPermissionsModal`: 빈배열 `[]`≠`["*"]`, 부재만 폴백) + 회사관리 관리자목록 자동펼침·게시판형 카드 ② 신규 admin 기본 전체권한→전체해제 ③ 대시보드 **거래처 관리 현황**(거래처별 노선/배차/운행/탑승 + 업체코드·포털URL 열람) + `partnerCodes.createdBy` 로 본인 발급분 즉시 열람(가시범위 `allowed∪createdBy`) ④ 대시보드 거래처 등록·거래처별 노선관리 버튼 ⑤ 노선 저장 후 필터 초기화(안보임 해소)·기사 사번/PIN 안내+autofill 차단·**SearchableSelect**(기사/차량/노선 검색, 400+ 대비). **rules/indexes/CF 시그니처 변경 0**(listCompanyAdmins 내부 로직만)·클라 UI 중심. 상세 issues.md `[패턴]` 3건.
> **이전 2026-06-08~09 세션 누적(전부 prod 배포)**:
> **2026-06-08~09 세션 누적(전부 prod 배포)**:
> - **앱별 서브도메인 분리(Phase 1)**: `admin/d/p/partner.buslink.co.kr` — App.js `HOST_APP` 호스트명 라우팅(`src/App.js`)·companyResolver/SW 호스트맵 동기. **DNS = 카페24 [서버호스팅 DNS 관리](cns1/cns2.simplexi.com zone)에 CNAME `→buslink-prod.web.app` 추가**(도메인-DNS관리 ns1.cafe24.com zone 아님!) → 반영 후 Firebase 인증·SSL → 동작. (사용자 작업 대기/진행)
> - **앱별 아이콘**: `src/lib/appIcons.js`(호스트/경로→favicon/apple/manifest/title/install) — index.js 조기 swap·InstallPrompt 설치팝업·partner.svg/manifest-partner.json 신설. CRA 기본 favicon.ico(React 로고) 폴백 제거.
> - **Phase B 협력사 게이팅**: 로그인 admin `allowedPartnerCodes`로 AdminApp 8지점 데이터 필터(`src/lib/partnerAccess.js`)·"전체"여도 한정·공지 "전체" 차단. 클라만(rules 별도 후속).
> - **관리자 행 정보수정**: CF `updateCompanyAdminProfile`(uid 기준·role 가드·이름/이메일/비번만).
> - **도착 기록/ETA**: computeStopEstimates **GPS 진척률 통과 안전망**(busProgress→arrived·현실 지연, "24분 균일" 아티팩트 소멸) + DriverApp **실시간 자기 GPS onSnapshot(`liveVehiclePos`)** → computeStopEstimates 주입(표시 지연 ~30초→~5초) + **busProgress 통과 정류장 recordStopArrival 우회**(detectStops 미발화 근인 미해결이나 우회로 충족). 진단 CF `fetchEtaDiagnostic`에 appVersion/stopArrivalsLog/allStopProgress 반환 추가.
> - **로딩 화면**: `src/components/LoadingScreen.js`(밝은 브랜드·버스 스피너) — App.js 3 게이트 교체(다크 "지도 로딩 중" 제거).
> - **정류장 진입시각 인라인 편집**: RoutesTab 정류장 목록 행에서 `🕒 진입` 직접 입력→즉시 저장(`saveStopTime`). 폼 왕복 불필요.
> **🟢 캐시 블로커 해소 확정**(appVersion 반환되며 폰 새 빌드 입증). **도착 표시·지연 정상 작동 입증**(전 정류장 계획시각 있는 노선).
> **다음 검증(다른 PC/내일)**: ① 기사 운행 — 배지 `v2026-06-09-livegps`·도착 즉각(~5초)·재조회 JSON `stopArrivalsLog` `via:"liveGps" ok:true`·estimates `actual` ② Phase B — 제한 담당자 로그인 시 자기 협력사만 ③ 서브도메인 SSL 발급 후 4개 도메인 접속. **잔존**: unplanned 정류장 위주 노선 지연 라벨 제한(offsetMin 입력 보강)·detectStops 근인 미해결(우회 충족)·Phase B rules 강제(별도).


- [x] **2026-05-22** 기사앱·승객앱 도착시간 정밀화(`main.69bb5377.js`) — computeStopEstimates 단조증가 재설계·gps.js GPS 복구 정류장 백필·DriverApp routePath 주입. 단조증가 강제로 동일값/역전/과거 표시 차단.
- [x] **2026-05-22** PWA 설치 충돌 해소 — 기사앱 `/driver` 경로 분리(`main.8a33eceb.js`/`968c8f9`). manifest 3종 고유 id + scope 분리(/, /driver, /p) — Chrome scope dedupe 회피.
- [x] **2026-05-22** 공지 도달 보장 — `/p` 인앱 공지함 탭·삼성 배터리 안내·iOS 설치 가이드 바텀시트(`main.25b9e8df.js`/`2c1164e`). PWA 푸시 OEM 절전 누락 보정.
- [x] **2026-05-22** 내 정류장 도착 임박 푸시 신규 CF `notifyPreArrival` + `/p` 내 정류장 영속화(`main.08c0818a.js`/`5a9839c`). fcmTokens routeId+stopId denormalize·diff+멱등 마커.
- [x] **2026-05-21~22** 모바일 공지 푸시 정상화 5건 배포 — VAPID 키 1글자 오타 fix·SW 이중알림 가드·InstallPrompt 자동회복+안드로이드 폴백·FCM webpush 아이콘. 상세 issues.md.
- [x] **2026-05-21** 공지 발송 진단·복구 인프라 강화 배포(`main.903dd602.js`) — NoticeTab 결과 가시화·snapshotError·EmployeeApp 🔔 알림 진단 카드. CF 정상·근인=토큰 invalid 자동삭제.
- [x] **2026-05-21** 4건 묶음 hotfix 배포(`main.47396703.js`) — `new window.Map()`(카카오 SDK shadow)·DriverApp 리스트 내부 스크롤·`useWakeTick`(백그라운드 stale)·formatDelayLabel ±2분.
- [x] **2026-05-21** ErrorBoundary 11탭 도입 — 흰 화면 차단 hotfix (`54bcdab`/`main.25ea7440.js`). NoticeTab render throw → AdminApp까지 unmount(전체 흰 화면) → 각 탭을 ErrorBoundary로 격리.
- [x] **2026-05-21** 공지 발송 결함 진단·복구 + 협력사 단위 운영 통일 (`main.6431ecf5.js` + functions + rules). fcmTokens.partnerCode 필드·NoticeTab 진단 패널·PartnerFilter 공통 컴포넌트.
- [x] **2026-05-21** 승객앱 ETA 안정화 + 정밀 표현 (`main.dbeedce7.js`). GPS 가중 50:50→70:30·useSmoothedEta(EMA)·formatPassengerEta 버킷 라벨·describeEtaSource.
- [x] **2026-05-21** 기사앱 현재 정류장 자동 스크롤 + 강조 (`main.d1013e16.js`). `currentStopRowRef` + scrollIntoView.
- [x] **2026-05-21** 기사앱 어르신 가독성 강화 (`main.ec085ae6.js`). 정류장·시각·지연 글씨 키움·지연 칩화·도착 정류장 강조.
- [x] **2026-05-20** 기사앱 설치 카드/팝업 미표시 fix + 운행 전 지연 라벨 게이팅 (`eb1c2e7`/`main.ff93d91f.js`).
- [x] **2026-05-20** 승객앱(/p·/bus) UI 강화 3건 (`main.728aa7e6.js`) — 지도 정류장 시각·노선도 폰트·버스 마커 펄스.
- [x] **2026-05-20** 배차 일정 자동 펼침 시스템 (`main.4d4a3650.js` + functions/rules) — dispatchSchedules + expandDispatchSchedules cron.
- [x] **2026-05-20** 정류장 진입시각 음수 자정 보정 버그 fix (`main.bc5eccda.js`) — offsetMinFromPlanTime 자정 보정 폐기.
- [x] **2026-05-20** 정류장 진입시각 입력 UX — 오프셋(분)→시각(HH:MM) 직접 입력 (`main.0a94e306.js`).
- [x] **2026-05-20** 정류장 계획·예상시각 시스템 (`b122941`/`main.1cef341f.js` + rules) — stops offsetMin·stopArrivals·stopSchedule.js.
- [x] **2026-05-20** 설치팝업 스누즈 14→3일 + DIAG-INSTALL 제거 (`da24224`/`main.f0dd8be3.js`).
- [x] **2026-05-20** 기사앱 배차 선택 칩→모달 + DIAG-INSTALL (`e690b41`/`main.cb747e1c.js`).
- [x] **2026-05-20** 기사앱 설치 BIP 글로벌 stash + 다중 배차 LS 영속 (`ac6fbf3`/`main.6c626f25.js`).
- [x] **2026-05-20** 노선 그리기 편집 UX — ⊕삽입·앞에추가·선택삭제·3색핀·번호라벨 (`1849cce`/`main.7d89705e.js`).
- [x] **2026-05-20** main→master 머지 + prod 재배포 (`d06bb7f`/`main.dc99419e.js`) — 노선 그리기 회귀 복구. autoload=false + onCreate 더블 relayout 공존.
- [x] **2026-05-19** 앱별 PWA 아이콘 Route Family 적용·배포 (`8b3e37e`/`0095e16`) — manifest 3종 + icons 6 + 동적 교체.
- [x] **2026-05-19** prod 첫 새 배포 — master HEAD `f2aa1a2`/`main.1614fa6b.js` (롤백본 `d85ec794` 대체).
- [x] **2026-05-19** PWA 설치형 + 팝업 / 문제 B `/p` 흰화면 근본수정(`autoload=false`+`maps.load(cb)`) / ETA "목적지 도착" (`55112aa`·`1597097`·`f2aa1a2`).
- [x] **2026-05-18** 노선 사전경로 + routeProgress + 권한·설치 게이트 + gps 콜드스타트(`5fa0737`); prod 장애→롤백→실원인(카카오 키 단일) 확정·문제 A 오진 정정(`c970c07`).
- [x] **2026-05-17** 정류장 사진+설명·클립보드·#14-B 주소검색·핀 미세조정·앱 노선변경·승객 노선선택·재활성화 — 다수 라이브.
- [x] **2026-05-16** 리디자인 0~6단계 + QA 안정화 4건 + PLANNING.md 정본화 + `.claude/` 분리 — 라이트 테마 전면 리스킨.

## 아카이브 (2026-07-21 wrapup 이관 — tasks.md 150줄 초과 정리)
> 2026-05~06 완료분 + 그 시기 검증/캐시 항목(전부 후속 배포로 superseded). 본문 변형 없이 이동만.

- [ ] **✅🟢 도착시각 표시 정상 작동 입증 + 실시간화(2026-06-09, `main.e4f19b04.js`·`DRIVER_BUILD=2026-06-09-livegps` 배포)**: 운행 JSON(runId `Mq3...1780955410666`, 과천라인 전 정류장 계획시각 有) — `allStopProgress` 정상 단조투영, busProgress 0→14091 완주, **모든 정류장 순차 `arrived`(source `gps`)·nextIdx 0→7 진행·지연 현실적(~3~6분, 24분 균일 아티팩트 소멸)**. GPS 안전망(computeStopEstimates) 작동 확정(기사앱·승객앱 공통). **단 사용자 보고 "도착시각 다소 늦음"** = computeStopEstimates 가 쓰던 vehiclePos 가 `lastVehiclePosRef`(flushDiag 30초 주기)라 ~30초 지연 + `stopArrivalsLog=null`(flushDiag 기록기 미발화·실 stopArrivals 미적재). **수정 배포**: ① DriverApp 이 본인 `gps/{cid}_{vehicleId}` 를 **실시간 onSnapshot**(~5초) 구독→`liveVehiclePos` state ② computeStopEstimates 에 liveVehiclePos 주입(지연 30초→~5초) ③ 도착 기록기를 **liveVehiclePos useEffect 로 이전**(flushDiag 30초 기록기 제거) → busProgress>stopProgress+40 통과 정류장 `recordStopArrival`(실시각·세션 중복가드·CF 멱등) → 실 stopArrivals 적재→ source `actual`·탑승통계/도착임박푸시 정상화. **검증(1회 운행)**: 배지 `v2026-06-09-livegps` → ① 도착 표시 즉각(~5초) ② JSON `stopArrivalsLog` 에 `via:"liveGps" ok:true` + estimates 가 `actual`. **잔존**: 계획시각 없는(unplanned) 정류장 위주 노선은 GPS 안전망 delay 라벨 제한(데이터 보강 필요)·detectStops 근인 미해결(우회로 충족).
- [ ] **✅ admin 행 "✏️ 정보수정" 신설(2026-06-08, `main.c158506e.js` 배포 완료(2026-06-08 functions:updateCompanyAdminProfile+hosting), +521B gzip, 신규 ESLint 경고 0)**: SuperCompanyTab 회사 관리 탭 `AdminListSection` 각 admin 행에 이름/이메일/비밀번호 수정. 신규 CF `updateCompanyAdminProfile({uid,name?,email?,password?})`(assertSuperAdmin·Admin SDK 우회·`role!=="admin"` 거부·role/companyId/empNo/allowedPartnerCodes 미변경·email 중복 한국어·self 허용)·UI `EditAdminProfileModal`(변경 필드만 전송·배경클릭 닫힘 없음·이메일=로그인ID 안내·1초 후 listCompanyAdmins 재로드). 행 버튼 `✏️ 정보수정`(self 허용)|`🛡 권한`(self disabled)|`🗑`(self disabled). 기존 권한변경/삭제/추가·다른 CF 22종·rules/indexes 변경 0. issues.md `[패턴]` 등록+회귀 가드 3건. **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:updateCompanyAdminProfile,hosting`. **배포 금지** — 부모(개발자) CF 핵심 로직 리뷰 후 배포.
  - **검증 절차**: 슈퍼관리자로 `/` 로그인 → 회사 관리 탭 → 회사 카드 "▾ 👥 관리자 목록" 펼침 → admin 행 "✏️ 정보수정" → 이름/이메일/비밀번호(+확인) 변경 → "변경 적용" → 성공 카드 → 1초 후 목록 재로드(이메일/이름 반영) → 이메일/비번 바꿨으면 그 계정으로 재로그인 확인(새 이메일=로그인 ID·새 비번). 중복 이메일 입력 시 "이미 사용 중인 이메일입니다". self 행도 정보수정 가능(권한/삭제는 self disabled 유지).
- [ ] **🔴 도착 기록 = busProgress 직접 판정 우회 배포(2026-06-08, `main.99fbb5e7.js`·`DRIVER_BUILD=2026-06-08-busprog-rec`)**: 운행 JSON(runId `Mq3...1780899399262`) 확정 — `allStopProgress=[0,288,707,1669,4554,5855,10284,11600,13932,14742]`(정류장 routePath 정상투영·**기하 OK**)·busProgress 0→14742(전 구간)인데 `stopArrivalsLog=null`(onStopReached 여전히 0회·근인 불명). **GPS 안전망(computeStopEstimates) 2버그**: ① plannedMs 있는 정류장만 주입→9/10 unplanned라 stop0만 ② gpsDelaySec 를 lastPassed.plannedMs 로 산출→lastPassed 가 unplanned면 null→주입 전체 꺼짐→stop0 가 stop1 통과 순간 "next" 복귀. **근본 우회 배포**: DriverApp `flushDiag` 가 `busProgress > allStopProgress[i]+40` 미기록 정류장을 직접 `recordStopArrival`(actualAt=serverTimestamp≈now·viaBackfill·세션 중복가드 `gpsRecordedRef`·CF 멱등·실패 재시도) — gps.js detectStops 우회. **실 stopArrivals 적재→기사앱·승객앱 모두 실 도착시각**(계획 유무 무관). flush ~30초 주기 일괄. **검증(1회 운행)**: 배지 `busprog-rec` → ① 기사앱 전 통과 정류장 도착시각 ② 승객앱 도착시각 갱신 ③ 재조회 JSON `stopArrivalsLog` 에 `via:"busProgress" ok:true` + estimates passed arrived. **잔존**: detectStops 근인 미해결(busProgress 우회로 기능 충족)·9/10 offsetMin 미설정→지연 라벨용 진입시각 입력 보강(데이터).
- [ ] **🔵 (구) GPS 진척률 안전망 배포(2026-06-08, `main.0ef08415.js`)**: 재조회 JSON(runId `Mq3...1780868895085`)에 appVersion·stopArrivalsLog 포함되어 **확정**: `appVersion="2026-06-05-badge"`(폰 새 빌드·캐시 해결 확정) + `stopArrivalsLog=null` 전 포인트(=recordStopArrival 0회 호출 = **onStopReached 14km 내내 0회 발화**). → 쓰기 문제 아님, **감지층 미발화**(이전 "쓰기 실패" 가설 틀림·정정). busProgress 0→14km 정상인데 detectStops 백필만 미작동(코드는 정상으로 보임 — 노선 routePath/정류장 기하 실측 필요). **근본 해법 배포**: `computeStopEstimates`에 **GPS 진척률 통과 안전망** — busProgress가 정류장 progress+40m 넘으면 통과 간주, 버스 실위치 계획시각 보간으로 현재지연 산출→통과 정류장에 합성도착(source `gps`) 주입→nextIdx·누적지연 재산출. **출발지 기준 24분 균일 carry 아티팩트 제거**(승객앱 "약 25분"도 동일 해소). 가드: routePath+vehiclePos 없으면 비활성·실측 arrivals 우선·source 'gps' 구분. DriverApp이 vehiclePos 주입(이전 null)·`allStopProgress`(전 정류장 투영) 진단 추가·`fetchEtaDiagnostic` 3필드(appVersion/stopArrivalsLog/allStopProgress) 반환. **검증(1회 운행)**: 배지 `v2026-06-08-gpspass` 확인 → 하류 정류장 지연이 24분 아닌 현재값(~수분)인지 + 재조회 JSON `allStopProgress`가 단조증가 분포(정류장 정상 투영)인지. **미해결 잔존**: onStopReached 자체 미발화 → stopArrivals Firestore 기록 0(탑승통계·도착임박푸시 영향). allStopProgress로 정류장 투영 정상 여부 확인 후 감지층 별도 수정 필요(GPS 안전망은 ETA 표시만 우회).
- [ ] **🌐 앱별 서브도메인 분리 (2026-06-05 진행 중)**: `buslink.co.kr` 서브도메인 4개를 앱별로 — `admin.`=관제 / `d.`=기사 / `p.`=직원 / `partner.`=협력사. **카페24에서 도메인 구입·관리**(네임서버 카페24).
  - **DNS(사용자 작업)**: Firebase 콘솔이 CNAME 방식 지정(A·TXT 아님) — `admin/d/p/partner → buslink-prod.web.app`. ⚠ **추가 위치 주의**: buslink 위임처가 카페24 서버호스팅 NS(cns1/cns2.simplexi.com)라 **[서버호스팅 DNS 관리]**에 넣어야 함(아래 🔴 근인 정정 참조). 반영 30분~1시간 → Firebase `확인` 재클릭 → SSL 자동 발급.
  - **⚠ Firebase 콘솔 등록 오타 정정 완료**: 처음 admin/d/p 가 `.com` 으로 잘못 등록됐다 `.co.kr` 로 재등록함. 기사앱은 `driver`→`d` 로 단축.
  - **Phase 1 코드 = 호스트명 라우팅 (완료·`main.c20ae665.js` prod 배포 완료 2026-06-05)**: `src/App.js` 상단에 `HOST_APP{ d/admin/p/partner → 앱 }` 추가 — 경로 미명시(루트)면 호스트로 앱 분기, `/bus`·`/board` QR 딥링크는 항상 경로 우선, `*.web.app`/localhost 는 HOST_APP 미포함이라 **기존 경로 분기 100% 보존(회귀 0)**. `companyResolver.HOSTNAME_TO_COMPANY` + SW `SW_HOSTNAME_TO_COMPANY` 4 서브도메인 → dy001 동기(현 단일테넌트, 폴백과 동일값이나 명시·향후 멀티테넌트 대비). 신규 경고 0.
  - **Phase 2a 앱별 아이콘 완료(2026-06-06, `main.faef8e65.js` 배포)**: 설치 가이드 팝업 아이콘이 `/logo192.png`(React 기본로고) 하드코딩이라 전 앱 동일하던 것 + 파비콘 admin 고정 → **앱별 전용 아이콘**. 신규 `src/lib/appIcons.js`(순수 — 호스트명/경로→{favicon,apple,manifest,title,install}, App.js HOST_APP 규칙 일치) → `index.js` 조기 swap(favicon svg+apple+manifest 전 앱), `InstallPrompt.js` 설치팝업 img를 appIcon.install로(logo192 제거·alt=앱명), PartnerApp에 InstallPrompt+applyAppManifest 추가(기존 없었음). 신규 `public/icons/partner.svg`(teal 형제 디자인)·`public/manifest-partner.json`. admin/driver(d)/employee(p) 전용 + partner 신규. 배포·라이브 검증(partner.svg·manifest-partner.json 200).
  - **Phase 2b 미완**: PWA manifest scope — manifest 의 scope/start_url 이 `/driver`·`/p`·`/partner` 라 서브도메인 루트(`/`) 설치 시 scope 불일치 가능(브라우저 사용·파비콘·팝업 아이콘은 정상). 실기기 설치 테스트 후 루트 scope 변형 검토. partner apple-touch는 raster 도구 부재로 passenger-1024.png 폴백(전용 partner-1024.png 추후).
  - **배포 완료(2026-06-05)**: 사용자 전권 위임 승인 → `firebase deploy --only hosting` → prod 라이브 `main.c20ae665.js` 확인·`/driver` no-cache 유지. `*.web.app` 동작 불변(회귀 0). **남은 것은 DNS 전파 + Firebase SSL 발급뿐** — 완료 시 4개 서브도메인 즉시 동작.
  - **🔴 근인 확정(2026-06-05, 정정): 카페24 내 DNS 영역 착오 — simplexi는 카페24 서버호스팅 NS**: `cns1/cns2.simplexi.com`은 **카페24의 (서버호스팅) 네임서버**(카페24 구입 도메인 elecin.kr·ministry.co.kr 동일 사용이 증거 — 원용지 말이 맞음). 가비아는 buslink.co.kr을 거기로 위임(정상)·**NS 변경 불필요**. 문제는 카페24 안에 DNS 영역이 둘: ① [도메인>DNS 관리] = `ns1.cafe24.com` zone(way@가 CNAME 4건 넣은 곳·새 레코드 O·**하지만 buslink 위임처 아님**·apex 116.126.143.242) ② [서버호스팅 DNS 관리] = `cns1/cns2.simplexi.com` zone(**buslink 실제 위임처·라이브**·옛 와일드카드·apex 58.121.85.54/175.126.62.52·serial 20260114). way@가 ①에 넣어 안 먹힘. **해결=②[서버호스팅 DNS 관리]에 CNAME 4건 추가**(way@ 직접 가능). 타기관 도메인이라 [도메인>DNS 관리] 목록의 "현재 네임서버"가 빈 건 정상(카페24 구입분만 자동표시). 검증=`nslookup -type=CNAME d.buslink.co.kr cns1.simplexi.com`가 buslink-prod.web.app 되는지 + SOA serial(20260114) 변화.
    - **NS 변경은 비권장**(simplexi=라이브 zone에 루트·www·와일드카드로 타 시스템 살아있음 — NS 통째 변경 시 카페24 도메인-DNS zone으로 이전돼 apex 116.x로 바뀌고 타 레코드 끊김). **그냥 [서버호스팅 DNS 관리]에 CNAME 4건 추가가 정답**(그 4개 이름만 와일드카드 덮어씀·나머지 0 영향·way@ 직접). way@가 [도메인>DNS 관리]에 넣은 4건은 효력없는 zone이라 무의미.
    - **✅ 해결됨(2026-06-05): [서버호스팅 DNS 관리](URL controller=myservice_hosting_server_dns, 좌측 호스팅관리>서버호스팅 DNS 관리)에 CNAME 4건 추가 → cns1/cns2.simplexi.com 에 반영 확인**(admin/d/p 양쪽 NS·공용 8.8.8.8 → buslink-prod.web.app, partner는 cns1 동기 지연 후 따라옴). **교훈**: 카페24 [도메인>DNS 관리](ns1.cafe24.com zone)≠[서버호스팅 DNS 관리](cns1.simplexi.com zone) — 위임 NS 일치하는 후자가 정답. SOA serial(20260114)은 안 바뀌어도 CNAME은 갱신됨(serial만으로 판단 금지).
    - **다음**: Firebase 콘솔 4개 도메인 `확인`(재인증) → SSL 자동 발급(몇분~몇시간) → `https://admin/d/p/partner.buslink.co.kr` 각 앱 접속 확인. 코드(호스트명 라우팅 main.c20ae665.js)·배포 완료라 SSL만 끝나면 동작. **보너스**: 기사앱 `d.buslink.co.kr`(새 origin) 접속 시 옛 캐시 번들 블로커 자동 해소.
- [ ] **🔥 기사앱 지연-중심 표시 빌드 검증(`main.0f4ab136.js` 빌드 완료·배포 대기)**: 사용자 결정(2026-06-04) — 기사앱은 "계획 대비 지연"이 본질, 예상시각은 작게 참고용. **DriverApp.js만 수정**(승객/직원앱·stopSchedule 무영향): ① `nowTick`(30초) 시계 ② 정류장 카드 `dispDelaySec` — 도착=실측지연 / 운행 중 미도착이라도 계획시각 지나면 (현재−계획) 누적 → "늦게 출발해도 정시" 버그 차단(도착 감지 무관) ③ 렌더 = 계획시각+지연칩 크게, 예상도착 작게 "(참고)". DRIVER_BUILD="2026-06-04-delay-display". **단 "각 정류장 실제도착 기반 지연"은 도착 감지(08da6948)가 폰에 실려야 정확** — 아직 폰이 옛 캐시라 미검증(아래 항목). **검증/배포 순서**: 시크릿 탭으로 새 빌드 받아 ① appVersion="2026-06-04-delay-display" 확인 ② 운행 늦으면 "지연 N분" 표시 ③ 도착 감지 시 "도착 HH:MM"+실측지연.
- [ ] **🔥🔥🔥 캐시/빌드 전달 — 폰이 계속 옛 번들(appVersion 부재)**: 2026-06-02 캐시 헤더 교정(클린URL no-cache) 후에도 06-04 운행 JSON 에 appVersion 없음 = 설치앱/기존 탭이 메모리에서 옛 코드 복원, 새 index.html 미패치. **해법 = 시크릿(Incognito) 탭**(`buslink-prod.web.app/driver`) — 캐시·SW·복원탭 0 → 무조건 새 번들. 검증되면 PWA 자동 업데이트+화면 빌드 뱃지 추가 검토(운영 기사 옛 화면 방지).
- [ ] **🔥🔥🔥 캐시 헤더 결함 교정 후 재운행 검증(2026-06-02, `firebase.json` 배포 완료)**: 3일간 새 빌드가 기사폰에 미반영된 진짜 근인 = 클린 URL(`/driver` 등) rewrite 가 `**/*.html` no-cache 규칙에 안 걸려 `max-age=3600` 으로 index.html 서빙(`curl -sI /driver` 로 확정). headers 3규칙 재구성·배포(클린URL/SW=no-cache, `/static/**`=immutable). 진단 JSON `appVersion` 부재 = 폰이 08da6948 미수신 증거였음. **기사폰 1회 escape 필요**(옛 max-age=3600 HTML 만료 ≤1h 또는 재설치/`?v=`/하드리로드) → 이후 영구 자동갱신. escape 후 재운행 JSON 에 `appVersion="2026-06-02-detect-v2"` 뜨면 새 빌드 정상 수신 → nextIdx 증가·stopArrivals ok·"도착 HH:MM"·지연 표시·출발지 기준 ETA(chain) 모두 검증. **사용자 설계 지적 확인**: "운행 도착예정시간은 출발지(실제 출발) 기준 재설정" = 이미 의도된 동작(chain plan-first, actualLast+offset 델타) — 단 stop0 도착 감지가 트리거라 감지 살아나야 발동. 9/10 정류장 offsetMin 미설정 → 지연/계획 라벨 위해 입력 보강 필요.
- [ ] **🔴 다음 관문 — 폰에 최신 빌드 싣기(캐시 정리)**: 기사폰 ① 설치된 BusLink 앱 제거 ② 크롬 사이트 데이터 삭제(`buslink-prod.web.app` 삭제 및 재설정) ③ `/driver` 재접속 → **화면 우하단 `v2026-06-05-badge` 확인**(보이면 새 빌드). 그 상태로 앞 정류장 1~2개 운행 → ① 첫 정류장 포함 도착 잡힘 ② 지연-중심 표시("계획·지연 N분"+예상 작게) ③ 진단 JSON `appVersion="2026-06-05-badge"`. **참고**: 2026-06-05 운행은 옛 캐시(70e47fc7)였어도 도착감지·실측지연은 작동 입증됨 — 최신 빌드는 백필로 첫 정류장까지 잡아 "지연 누적" 현상도 해소.
- [ ] **PWA 자동 업데이트 + (검증 후) 운영 안정화**: 캐시 정리로 1회 최신빌드 확인되면 — 기사앱이 켤 때마다 최신 자동수신(visibilitychange 시 신빌드 감지→reload)·화면 뱃지 유지. 운영 기사들이 옛 화면 도는 문제 영구 차단. (현 미해소 블로커: 시크릿탭조차 옛 번들 — 원인 불명, 데이터 통삭제가 확실)
- [ ] **첫 정류장(출발지) 견고화 검토**: 최신 빌드 백필이 첫 정류장도 잡는지 검증 후, 미흡하면 "운행 시작 시 출발 정류장 도착 기록"(GPS 무관 결정적) 추가. 9/10 정류장 offsetMin 미설정 → 정류장별 지연 라벨 위해 진입 오프셋 입력 보강 필요.
- [x] **Phase B — AdminApp 필터 자동 적용**(2026-06-08, `main.1f282c81.js` 빌드 미배포, +605B gzip, 신규 ESLint 경고 0): 8지점이 로그인 admin 의 `users/{uid}.allowedPartnerCodes` 자동 강제. 신규 `src/lib/partnerAccess.js`(순수: resolveAllowed/isAllAccess/partnerCodeAllowed). App.js 가 users data 에서 추출→AdminApp prop. PartnerFilter `allowedCodes` prop(옵션 제한+"내 협력사 전체" 라벨+빈권한 안내). 8지점(대시보드/실시간관제/배차관리/배차일정/노선관리/운행이력/탑승통계/협력사관리/공지발송) 각각 **"전체"여도 allowed 로 실데이터 한정**(옵션만 줄이면 우회되므로 `!isAllAccess` 일 때 교집합 추가). 공지발송은 제한 admin "전체"(=CF 회사전체) 차단·특정 협력사 강제(CF 변경 0). **isAllAccess(superadmin/["*"]/미설정 폴백)=현재 코드 100% 동일=회귀 0**. rules/CF/indexes 변경 0(클라 게이팅만). issues.md `[패턴]` 등록 + 회귀 가드 3건 + 한계 3건. **배포 명령**(사용자 명시 요청 시): `firebase deploy --only hosting`. **배포 금지** — 부모(개발자) 핵심 로직 리뷰 후 배포.
  - **사용자 검증 절차**: ① 슈퍼관리자로 신규 제한 admin 생성(협력사 2~3개만 권한, "+ 관리자 추가"에서 ⭐전체 해제 후 특정 협력사 선택) ② 그 admin 으로 로그인 → 8지점에서 자기 협력사 데이터만 보이는지(대시보드 카드 수·실시간관제 차량·배차/일정/노선 목록·운행이력 차량/배차·탑승통계 분포·협력사관리 목록·공지발송 대상자/이력) ③ 공지발송 "전체" 선택 시 발송 차단·특정 협력사 선택 시만 발송 ④ `["*"]`(또는 권한 미설정·기존) admin 로그인 시 전체 보이는지(회귀 0) ⑤ 빈 권한(`[]`) admin 은 "접근 가능한 협력사 없음" 안내. **한계**: 제한 admin 신규 발급 코드는 자동 권한 추가 안 됨(슈퍼관리자 부여 필요)·클라 필터 우회 가능(rules isAdmin 협력사 제약 후속)·차량/기사 탭 미게이팅.
- [ ] **사용자 폰 실측 검증(ETA 진척률 안분 + 도착 모달 + 통신 복구, prod 배포 완료)**: ① 정류장 사이 ETA 부드러운 변동 ② 터널 출렁임 0 ③ `/p`·`/bus` 직전 정류장 도착 시 풀스크린 모달 ④ 오프라인→온라인 자동 활성화 ⑤ 운행 시작/종료 버튼 즉시 반응. 검증 후 [DIAG-ETA] 4지점 grep 1줄로 제거.
- [ ] (선택, 옛 흔적) ~~사용자 검증 + 배포(2026-06-01 stopArrivals CF 위임, 빌드 `main.70e47fc7.js` 미배포)~~: ① 배포 즉시 prod 라이브 운행 → 정류장 100m 진입 시 DriverApp "현재" 표시 갱신 + Firebase Console 에서 `companies/{cid}/dispatches/{today}/list/{dispatchId}` 도큐먼트 `stopArrivals` 필드 채워지는지 확인 ② 운행 종료 후 슈퍼관리자 회사관리 탭 EtaDiagnosticCard → 그 runId points 조회 → 최근 point 의 `stopArrivalsLog` 배열에 각 정류장 `ok:true`(또는 `alreadyExists:true`) 표시 확인. error 가 있으면 code/message 로 정밀 진단(permission-denied = users 도큐먼트 누락·companyId 불일치 / not-found = dispatch 오타). ③ chain 알고리즘 정상 작동 검증 — 다음 정류장 ETA 가 actualLast + plan 델타 시각으로 표시(이전 결함: 누적 지연만 늘어남). ④ AdminApp 운행 이력 탭 → 그 운행 카드 "✅ N/M 통과" 표시(이전 결함: 0/M). ⑤ 도착 임박 푸시(notifyPreArrival) 자동 발화 — stopArrivals 갱신이 정상화되면 onDocumentUpdated 트리거가 발화해 직원 폰에 푸시 도착(부수 정상화). **배포 명령**: `firebase deploy --only functions:recordStopArrival,hosting`. firestore.rules 변경 0(향후 클라이언트 경로 제거 후 rules 좁힘은 별도). **배포 절대 금지** — 사용자 명시 결정. **알림 영향 명시**: recordStopArrival 자체는 push 발송 안 하나, 정상 작동 시 기존 `notifyPreArrival` 가 한동안 누적된 stopArrivals 변화에 발화될 수 있음(첫 1회 운행은 raw 신규 도착만이라 영향 거의 없음).
- [ ] **사용자 검증 + 배포(2026-05-29 Phase A admin별 협력사 권한 인프라, 빌드 `main.33e1a717.js` 미배포)**: ① 슈퍼관리자로 `/` 로그인 → 회사 관리 탭 → dy001 카드 "▾ 👥 관리자 목록" 펼침 → 본인(슈퍼관리자) admin 행 표시 + 권한 "전체" 배지 + (나) 라벨 + 권한/삭제 버튼 disabled ② "+ 관리자 추가" → 신규 admin 폼: 이름=`최우석`, empNo=`wschoi`, email=`wschoi@dybus.co.kr`, password=`123456`(2회), 권한=⭐ 전체 권한 체크 해제 후 특정 협력사 2~3개 선택 → "관리자 추가" → 성공 카드 → 1초 후 목록 자동 재로드 → 추가된 admin 행에 "협력사 N개" primary 배지 표시 ③ 그 admin 행 "🛡 권한" → PartnerPermissionPicker 모달 → 다른 협력사 추가 또는 "⭐ 전체 권한" 체크 → 저장 → "권한 변경 완료" → 재로드 시 배지 갱신 ④ "🗑" → "삭제 확인" → Auth Console (Firebase Console → Authentication) 에서 그 email 계정 삭제 확인 + users 컬렉션에서 해당 uid 도큐먼트 삭제 확인 ⑤ 신규 admin 으로 다른 창에서 `wschoi@dybus.co.kr / 123456` 로그인 시도 → AdminApp 11탭 정상 진입(Phase B 미도입이라 권한 게이팅은 아직 없음 — 모든 협력사 보임, 인프라만 누적) ⑥ 일반 admin(슈퍼관리자 아님) 로그인 시 회사 관리 탭 비표시 → 펼침 토글 불가 회귀 0 ⑦ AdminApp 11탭(대시보드~공지 발송) + 회사 관리 탭 기존(생성/전환/활성토글/✏️편집/🔑비번/🗑삭제/EtaDiagnosticCard) 100% 보존. **배포 명령**: `firebase deploy --only functions:createCompanyAdmin,functions:updateCompanyAdminPermissions,functions:deleteCompanyAdmin,functions:listCompanyAdmins,hosting`(rules/indexes 변경 0). 첫 배포 후 prod 슈퍼관리자로 동일 절차 1회 재검증.
- [ ] **Phase B 후보**(Phase A 사용용 사용 검증 후): AdminApp 11탭이 로그인 admin 의 `users/{uid}.allowedPartnerCodes` 를 읽어 `PartnerFilter` 기본값+선택지 제한·`["*"]` 이면 무제한·그 외엔 해당 코드만. 게이팅 지점=대시보드/실시간관제/배차관리/배차일정/노선관리/공지발송/협력사관리/탑승통계(8지점, `PartnerFilter` 사용처 전수). rules 도 동반(`isAdmin` 헬퍼 확장)할지 별도 검토(SaaS 보안 표면).
- [ ] **사용자 검증 + 배포(2026-05-29 회사 운영 메뉴 4종, 빌드 `main.fa48b1cb.js` 미배포)**: ① 슈퍼관리자로 `/` 로그인 → 회사 관리 탭 → 임시 테스트 회사 "test001" 생성(이미 있으면 skip) ② test001 카드 "✏️ 편집" → 회사명만 "테스트001" 변경 → "변경 적용" → reload 후 카드 회사명 갱신 확인 ③ "🔑 비번" → 새 비번 "abc123" + 확인 → 완료 알림(관리자 이메일 표시) → 다른 창에서 그 이메일/abc123 로 로그인 시도(성공이면 OK) ④ 편집 모달 다시 열어 관리자 이름/이메일 변경 → 회사명·관리자 정보·비밀번호 동시 변경도 1회 ⑤ "🗑 삭제" → 빨강 경고 + companyId 재입력 input → 잘못 입력 시 버튼 비활성 → "test001" 정확 입력 → "영구 삭제" → 1.5초 후 자동 닫기 + reload → 카드 사라짐 → Firebase Console 직접 확인: `companies/test001/*` 전부·`users` where companyId=test001 도큐먼트·Auth 계정 모두 삭제 + `gps/test001_*` / `partnerCodes` where companyId=test001 등 최상위도 비어있는지 ⑥ 자기 회사(dy001) 카드 "🗑 삭제" 버튼 회색·hover tooltip "자기 소속 회사는 삭제할 수 없습니다" 확인 ⑦ 일반 admin 로그인 시 회사 관리 탭 비표시·새 운영 메뉴 부재 ⑧ AdminApp 11탭(대시보드~공지 발송) + 회사 관리 탭의 신규 회사 등록·전환·비활성 토글·EtaDiagnosticCard 모두 회귀 0. **배포 명령**: `firebase deploy --only functions:updateCompanyInfo,functions:resetCompanyAdminPassword,functions:updateCompanyAdminInfo,functions:deleteCompany,hosting`(rules/indexes 변경 0). 첫 배포 후 prod 슈퍼관리자로 동일 절차 1회 재검증(실제 cascade 동작·Auth 계정 삭제). **데이터 손실 주의**: deleteCompany 는 되돌릴 수 없음 — prod 운영 회사로 실험 금지, 반드시 임시 test 회사로만.
- [ ] **사용자 폰 실측 + 진단 캡쳐(2026-05-29 ETA 진척률 안분, 빌드 `main.15820be4.js` 미배포)**: ① localhost `/driver` 운행 시작 → 정류장 리스트 하단 "🔍 진단 ▼" 토글 → 정류장별 plan/est/delay/source 표 노출 확인 ② localhost `/p?debug=1` 직원앱 진입 → 내 정류장 선택 → 헤더 다음 노란 [DIAG-ETA] 카드 표시(busProgress/myStopProgress/segProgress/speed 가시화) ③ localhost `/bus?debug=1` 승객앱 → 동일 진단 카드 ④ 사용자 본인 폰 운행 실측 — 정류장 사이 진행 중 ETA가 시간/거리 비례로 부드럽게 변동(점프 0)·정류장 직전 자연스러운 "곧 도착"·정시보다 빨리 도착한 정류장의 delaySec이 음수(조기) 표시되는지·승객앱 큰 분수 표시 사라졌는지 ⑤ 진단 캡쳐 후 다음 커밋에 `grep -rn "\[DIAG-ETA" src/` 1줄로 4지점 통째 제거. **배포 금지** — 사용자가 직접 폰 실측·캡쳐 후 별도 배포. 회귀 시 콘솔 Hosting 롤백 먼저.
- [ ] **사용자 검증 + 배포(2026-05-28 SaaS Phase 1.1+1.2, 빌드 `main.45c350a7.js` 미배포)**: ① localhost `/` 동영관광 admin 로그인 → 기존 11탭 정상 + companyId=dy001 그대로(회귀 0) ② `/p`·`/bus`·`/board`·`/partner`·`/driver` 모두 동영관광 흐름 그대로 ③ 슈퍼관리자 계정 있으면 로그인 → 헤더 드롭다운·"회사 관리" 12번째 탭 노출 → 신규 회사 "test001" 생성·삭제 토글·회사 전환 동작 ④ 일반 admin은 드롭다운·12탭 비표시. **슈퍼관리자 계정 부재 시 사용자 안내**: Firebase Console → users 컬렉션 → 본인 uid 도큐먼트 → role 필드를 "admin"→"superadmin"으로 1회 수정(혹은 본인 PC dev 계정 신설). 배포 명령: `firebase deploy --only firestore:rules,functions,hosting` (rules는 이번 변경 없으나 functions 신규 3종 동반).
- [ ] **사용자 검증 + 배포(2026-05-28 Phase 1.3 협력사 운영 포털, 빌드 `main.dd6a87da.js` 미배포)**: ① localhost `/partner` → 업체코드 입력 → STEPS.MAIN 진입 시 기본 탭 "register" 유지(회귀 0) ② mainTab "🚌 운영 포털" 4번째 탭 클릭 → 4섹션(자사 노선 카드·실시간 지도·오늘 탑승·공지함) 표시 ③ dy001 실 협력사 코드(또는 테스트 데이터) → 노선 카드 표시·클릭 시 필터 토글·지도 폴리라인+버스 마커·탑승 카운트·공지 카드 정상 ④ 기존 register/manage/stats 탭 회귀 0 ⑤ 폴더블/모바일 좁은 폭 진입 시 지도 onCreate relayout 동작. 배포 명령(rules/functions 무변경): `firebase deploy --only hosting`. **테스트용 partnerCode**: prod dy001 실 협력사 코드 또는 AdminApp "협력사 발급" 탭으로 신규 발급 후 PartnerApp으로 인증.
- [ ] **사용자 검증 + 배포(2026-05-29 Phase 1.4 협력사 공지 발송, 빌드 `main.7d984f96.js` 미배포)**: ① localhost `/partner` → 업체코드 인증 → 운영 포털 탭 → "📢 공지 발송" 섹션 표시(섹션 D 공지 수신함 위) ② 제목/본문/일반/긴급 입력 → "발송하기" → 인라인 컨펌 카드 → "확인 — 발송" → 성공 카드 "발송 완료 (이번 시간 남은 발송 N건)" ③ 5건 연속 발송 → 6번째 시도 시 한국어 rate-limit 에러("시간당 5건까지... 남은 시간: M분") ④ 발송 후 EmployeeApp `/p`(같은 partnerCode 직원) 공지함·강제 모달 즉시 표시 ⑤ AdminApp NoticeTab 발송 무회귀 ⑥ PartnerApp 기존 4탭(register/manage/stats/ops 4섹션) 100% 보존. **배포 명령**: `firebase deploy --only functions:sendPartnerNotice,hosting`(rules/indexes 변경 0). 협력사 인사 담당이 카카오 알림톡 없이도 자기 직원 공지 발송 가능 — SaaS-lite 마지막 핵심 기능.
- [ ] **Phase 1.5 후보** (사용자 검증 + 사용 피드백 후): ① 협력사 공지 발송 이력 가시화(notices where senderCode==code 별도 카드) ② 노선별 발송(routeId 필터 추가) ③ 예약 발송(cron) ④ 공지 안 읽음 추적(empNo 또는 partnerCode 단위 localStorage) ⑤ 안내문 자동 발송(특정 노선 지연·휴무 알림) ⑥ 운영 포털 공통 헬퍼 추출(노선 카드/지도/탑승통계 미니 — 인라인 V1 검증 후) ⑦ partnerCodes(companyId + active) 인덱스 활용한 admin 협력사 필터(이미 인덱스 있음).
- [ ] **사용자 폰 실측 검증(2026-05-28 ETA 변경, prod 배포 완료)**: ① 기사앱 운행 중 정류장 통과 시 다음 정류장 ETA가 (이전 actualAt + offsetMin 델타) 시각인지 ② 터널 진입·이탈 시 승객앱 ETA 출렁임 없는지 ③ 직원앱/`/bus`에서 직전 정류장 도착 시 풀스크린 모달 노출 ④ 통신 복구 후 자동 활성화 ⑤ 운행 시작/종료 버튼 즉시 반응. 회귀 시 콘솔 Hosting 롤백 먼저.
- [ ] **EmployeeApp 내 정류장 복원 권한 오류 진단**(prod 콘솔 발견 2026-05-27) — fcmTokens/{empNo} 또는 routes/{id}/stops 읽기 권한 확인. 비치명적이나 사용자 정류장 자동 복원 실패. issues.md `[미해결]` 참조.
- [ ] (선택) PPT 매뉴얼 06-notices·기사 03/04·승객 02/03 본인 폰 캡처해 `docs/manual-ppt/assets/<role>/`로 두고 `npm run all` 1회 — 매뉴얼 캡처 완전성.
- [ ] (선택) 운영 노선에 `routePath` 실제 그리기 — 안 그린 노선은 stops 직선 폴백(정상).
- [ ] (선택) gpsHistory TTL 정책 검토 — Firebase 콘솔에서 ts 필드 기준 90일 자동 삭제(저장 비용 작아도 운영 정리 측면).
- [x] **2026-06-04~05** 도착 감지 ref-getter 경합수정 + 클린URL 캐시헤더 교정 + 기사앱 지연-중심 표시 + 화면 빌드 뱃지 (배포: `08da6948`→`0f4ab136`→`6e2a8302`). ① `gps.js startGPS` getStops/getRoutePath getter — startGPS 1회 캡처가 빈 routePath여도 매 감지 시 ref 최신값 읽어 백필 항상 작동(once-captured 경합 제거). ② `firebase.json` headers 재구성 — **클린URL rewrite(`/driver` 등)가 `**/*.html` 규칙 미적용→`max-age=3600` stale**(며칠간 새빌드 폰 미반영 근인, `curl -sI`로 확인) → `**`=no-cache 캐치올 + `/static/**`=immutable(last-match-wins) + SW=no-cache. ③ `DriverApp.js` 정류장 카드 지연-중심(계획+지연칩 크게, 예상 작게 "참고", `nowTick` 30초로 운행 중 "현재−계획" 누적표시→"늦게 출발해도 정시" 버그 차단) — 승객/직원앱·stopSchedule 무영향. ④ 진단 `appVersion` 마커 + 화면 우하단 `v{DRIVER_BUILD}` 뱃지(옛 캐시 즉시 식별). **🟢 2026-06-05 시크릿 운행서 도착 감지·실측지연(도착−계획) 작동 입증**(7정류장 `source:actual`). **🔴 단 그 운행은 옛 캐시번들(70e47fc7, appVersion 부재)** — 폰이 최신 빌드 계속 미수신(시크릿도 실패), 데이터 통삭제가 다음 관문. 옛 빌드라 백필 꺼져 첫 정류장 누락+지연누적 현상. issues.md `[해결]`/`[패턴]` 등록.
- [x] **2026-06-02** 도착 감지 경합·좌표 coercion 수정 + prod 배포(`main.5539a881.js`, +189B gzip, 신규 경고 0) — 2026-06-01 라이브 운행 진단 JSON(70포인트) 분석으로 **onStopReached 0회 발화** 확정(nextIdx 0 고정·busProgress 14.7km·stopArrivalsLog 부재·recordStopArrival 호출 0건). 근인 정정: 쓰기층(updateDoc) 아니라 **감지층** — 고속 주행 시 100m 직접 감지 무력 + routePath 백필이 startGPS 빈-routePath 캡처(운행 시작 경합)로 비활성. **수정**: ① `DriverApp.js` `routeDataReady` 게이트(loadStops 완료 전 운행 시작 버튼 disabled, 응답성 가드 보존 — await 미추가) ② `gps.js` `toLatLng` coercion(stopProg·detectStops 양경로 — number/string/GeoPoint 수용). recordStopArrival/CF 22종/rules/indexes/onStopReached·startGPS 시그니처/stopSchedule 무변경. 검증: build 통과(DriverApp 신규 경고 0)·prod 라이브 해시 5539a881 + toLatLng 반영 확인. issues.md `[패턴]` 등록 + 회귀 가드 3건. **부수**: 9/10 정류장 offsetMin 미설정(데이터 보강 필요). 다음: 내일 아침 운행 검증(앱 완전 재실행 필수).
- [x] **2026-06-01** stopArrivals 기록 CF 위임 채널 신설(`main.70e47fc7.js` 빌드 미배포, +0KB gzip, 신규 ESLint 경고 0) — 사용자 진단 JSON 분석으로 stopArrivals 갱신 0건 결함 확정(소스 트리거 0개·chain 부재로 delaySec 1930s 누적·"지연 14분 고정" 호소). 근인 = `setCurrentStopIdx`는 갱신되는데(콜백 호출됨) `updateDoc(dispatches.../stopArrivals.{stopId})` 만 silent fail — rules driver 본인 검증(drivers.uid 매핑) 의심. **CF 신규** `recordStopArrival`(`functions/index.js` L321~426): 입력 `{companyId,date,dispatchId,stopId,plannedAt?,delaySec?,viaBackfill?}`. 검증 = request.auth + users/{uid}.companyId 일치 + role∈{admin,superadmin,driver} + driver 면 dispatch.driverId 매핑 추가 검증(rules 패턴 미러) + date `YYYY-MM-DD` 정규식. 멱등 가드(stopArrivals[stopId] 이미 있으면 `{ok:true,alreadyExists:true}` 즉시 반환). Admin SDK update — `stopArrivals.{stopId}={actualAt:serverTimestamp,plannedAt:plannedAt||null,delaySec:typeof number?value:null,estimated:!!viaBackfill}` (dot notation). 한국어 HttpsError. **DriverApp 변경**(`src/pages/DriverApp.js`): ① `import { getFunctions, httpsCallable } from "firebase/functions"` 추가(L5) + 모듈 레벨 `const functions = getFunctions(undefined, "us-central1")` 신설(L23, PartnerApp 패턴 일관) ② `serverTimestamp` import 제거(onStopReached 교체로 더 이상 미사용·신규 경고 0 위해) ③ `stopArrivalsLogRef = useRef([])` 신설(L88) — 각 호출 결과 누적(`{stopId,ok,alreadyExists?,error?,code?,viaBackfill,ts}`, 최근 40건 유지) ④ `handleStart` 진입 시 `stopArrivalsLogRef.current=[]` 초기화·`handleStop` 정리(L455·L540) ⑤ `onStopReached` 콜백 안 `getDoc+updateDoc` 통째 교체 → `httpsCallable(functions,"recordStopArrival")` 호출 + try/catch 로 로그 기록(silent fail 차단). 멱등 가드는 CF 측이 수행 → 클라가 사전 getDoc 불필요. ⑥ `flushDiag` 안 `recordEtaDiagnostic` payload 에 `stopArrivalsLog: log.slice(-20)` 옵셔널 첨부. **etaDiag.js 변경**: `recordEtaDiagnostic` 시그니처에 `stopArrivalsLog?: Array` 옵셔널 추가 — Array.isArray + length>0 일 때만 payload 에 포함(undefined 필드 Firestore 거부 회피). 미제공 시 미저장(하위호환 100%). **firestore.rules 변경 0** — Admin SDK 가 우회. 향후 클라이언트 경로 완전 제거 후 rules 좁힘은 별도 작업(현재는 양쪽 공존 가능). **보존 불변**: 다른 17 CF 시그니처·동작 불변·gps.js/stopSchedule.js/useSmoothedEta.js/companyResolver.js/useOnlineRecover.js 무영향·카카오 SDK·PWA·익명 인증·rules·indexes 변경 0·AdminApp/PartnerApp/EmployeeApp/PassengerApp 무영향·DriverApp 외 변경 없음·`onStopReached` 콜백 시그니처 `(stop, dist, viaBackfill)` 그대로(gps.js 호출부 무영향). 검증: `node --check functions/index.js` 통과·`CI=false npm run build` 통과(`main.70e47fc7.js`, 340.57 KB gzip 변동 0KB, 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:recordStopArrival,hosting`. **배포 절대 금지** — 사용자 명시 결정. issues.md `[패턴]` 등록 + 회귀 가드 4건. **알림 부수 정상화**: recordStopArrival 자체는 push 발송 안 하나, stopArrivals 갱신 정상화로 기존 `notifyPreArrival` 트리거가 한동안 누락됐던 도착 임박 푸시를 정상 발화 시작 — 배포 직후 운행 시 직원 폰 알림 도착 가능성 명시.
- [x] **2026-05-29** Phase A admin별 협력사 권한 인프라 신설(`main.33e1a717.js` 빌드 미배포, +2.52KB gzip, 신규 ESLint 경고 0) — 사용자 결정 "한 회사 안에서 여러 담당자 admin 이 각자 자기 협력사만 관리". **데이터 모델**: `users/{uid}.allowedPartnerCodes:string[]` 옵셔널 신규 필드(`["*"]`=전체, `["code1","code2"]`=한정, 부재=`["*"]` 폴백·마이그 불필요). **CF 4종 신설**(`functions/index.js` L846~1040, 모두 assertSuperAdmin + Admin SDK 우회): ① `createCompanyAdmin({companyId,empNo,email,name,password,allowedPartnerCodes})` Auth+users 동시 생성·rollback(`createCompany` 패턴 미러)·email 중복=`already-exists` 한국어 ② `updateCompanyAdminPermissions({uid,allowedPartnerCodes})` `allowedPartnerCodes` 필드만 update(role/companyId/empNo/email/name 보호)·role!=="admin" 거부 ③ `deleteCompanyAdmin({uid})` Auth+users 삭제·자기 삭제 차단(uid===request.auth.uid → `permission-denied`)·Auth catch 멱등 ④ `listCompanyAdmins({companyId})` users where companyId+role=="admin" → `{uid,empNo,email,name,allowedPartnerCodes}` 정규화·이름순 정렬. 공통 헬퍼 `normalizeAllowedPartnerCodes`(배열 검증+`"*"` 정규화+중복 제거+빈 배열 허용). **UI 신설**(`AdminApp.js SuperCompanyTab` L4326~ 확장 + 신규 모달/섹션 4종): ① 회사 카드 하단에 "▾ 👥 관리자 목록" 펼침 토글(단일 펼침 `expandedCompanyId`·`adminsByCompany` cache·`Promise.all` 로 listCompanyAdmins+partnerCodes 병렬 lazy 로드) ② `AdminListSection` 컴포넌트 신설(admin 행=이름·이메일·empNo + 권한 배지[전체=녹/한정=primary "협력사 N개"] + "🛡 권한"/"🗑" 버튼 + (나) 라벨·자기 자신 disabled) ③ `PartnerPermissionPicker(partnerCodes,value,onChange)` 공용 픽커(⭐ 전체 권한 별도 체크박스 + 다중 체크박스·전체 ON 시 disabled) ④ `AddAdminModal`(이름·empNo·email·password 2회+권한 picker·기본 `["*"]`) `EditAdminPermissionsModal`(allowedPartnerCodes 만 변경) `DeleteAdminModal`(빨강 강조·confirm). `currentUserUid={user?.uid}` prop 추가(자기 자신 비교용). `partnerCodes` read 는 rules 공개라 onCall 없이 클라 `getDocs(query+where companyId)` 직접. **회귀 가드**(복원 금지): ① CF 자기 삭제 차단 가드 제거 금지(lockout 방지) ② `updateCompanyAdminPermissions` 가 allowedPartnerCodes 외 필드를 update 하도록 확장 금지(권한 상승 벡터 차단) ③ allowedPartnerCodes 폴백(`부재=["*"]`) 변경 금지(기존 admin 무권한 회귀 위험). **보존 불변**: rules/indexes 변경 0(users write 는 이미 superadmin 만·CF 가 Admin SDK 우회)·기존 17 CF 시그니처·동작 불변·AdminApp 11탭+회사 관리 탭 기존(회사 생성/전환/활성토글/✏️편집/🔑비번/🗑삭제/EtaDiagnosticCard) 100% 보존·일반 admin/기사/직원/승객/협력사 화면 무영향(슈퍼관리자 분기 안에만)·companyResolver/useOnlineRecover/useWakeTick/gps.js/stopSchedule.js/etaDiag.js 무영향·LoginApp 합성/이메일 분기 보존·카카오 SDK/PWA/익명 인증/SW/manifest 변경 0. 검증: `node --check functions/index.js` 통과·`CI=false npm run build` 통과(`main.33e1a717.js`, +2.52KB gzip, AdminApp 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:createCompanyAdmin,functions:updateCompanyAdminPermissions,functions:deleteCompanyAdmin,functions:listCompanyAdmins,hosting`. **배포 절대 금지** — 사용자 명시 결정. issues.md `[패턴]` 등록 + 회귀 가드 3건 + Phase B 후보 1건 명시. **Phase B(미도입)**: AdminApp 8지점 PartnerFilter 가 admin 의 allowedPartnerCodes 를 자동 강제 — Phase A 사용 검증 후 별도.
- [x] **2026-05-29** 슈퍼관리자 회사 운영 메뉴 4종 신설(`main.fa48b1cb.js` 빌드 미배포, +1.93KB gzip, AdminApp 신규 ESLint 경고 0) — 사용자 결정 "비활성과 영구 삭제 둘 다 제공". **CF 4종 신설**(`functions/index.js`, 모두 assertSuperAdmin·Admin SDK 우회): ① `updateCompanyInfo({companyId,name})` companies.name 1~50자 update ② `resetCompanyAdminPassword({companyId,newPassword})` 그 회사 첫 admin uid Auth 비번 변경(≥6자) ③ `updateCompanyAdminInfo({companyId,displayName?,email?})` Auth + users 도큐먼트 동기 변경(둘 중 1개 이상·이메일 형식 검증) ④ `deleteCompany({companyId,confirmCompanyId})` cascade 영구 삭제 — 최상위(gps/partnerCodes/boardingTokens/passengerTokens/fcmQueue) where companyId 500개 청크 + gpsHistory/{cid} recursive + companies/{cid} recursive + users where companyId 각 uid Auth+도큐먼트 삭제. **자기 회사 보호 이중 안전망**: 카드 삭제 버튼 disabled + CF가 callerCompanyId === companyId 면 permission-denied. **etaDiagnostics는 cascade skip**(companyId 인덱싱 불가, 후속 과제). **UI**(`AdminApp.js SuperCompanyTab`): 카드에 운영 메뉴 3 버튼(✏️ 편집·🔑 비번·🗑 삭제, 기존 전환·비활성화 줄 아래) + 모달 3종 — `EditCompanyModal`(4필드 입력 → 변경된 것만 onCall 순차 호출·비밀번호 동시 변경 옵셔널) / `ResetPasswordModal`(새 비번 + 확인 일치 + ≥6자) / `DeleteCompanyModal`(빨강 borderTop·companyId 재입력 input·일치 시만 "영구 삭제" 활성·자기 회사면 안내 카드만 표시·form submit 차단). SuperCompanyTab 시그니처에 `currentUserCompanyId={companyId}` prop 추가(자기 회사 비교용). **회귀 가드**: ① rules/indexes 변경 0(전부 Admin SDK) ② AdminApp 11탭 + 회사 관리 탭 기존(회사 생성·비활성 토글·전환·EtaDiagnosticCard) 100% 보존 ③ 일반 admin/기사/직원/승객/협력사 화면 무영향 ④ companyResolver/useOnlineRecover/useWakeTick/gps.js/stopSchedule.js/etaDiag.js 무영향 ⑤ CF 기타 13종(sendNoticeToCompany/notifyPreArrival/sendPartnerNotice/createCompany/listCompanies/toggleCompanyActive/createDriver/deleteDriver/updateDriverPassword/createDriverAuth/expandDispatchSchedules*/fetchEtaDiagnostic) 시그니처·동작 불변 ⑥ 부분 실패 시 throw — 사용자 재시도하면 멱등(이미 삭제된 건 query 가 빈 결과·Auth deleteUser 는 catch 로 무시). 검증: `node --check functions/index.js` 통과 + `CI=false npm run build` 통과(`main.fa48b1cb.js`, +1.93KB gzip, AdminApp 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:updateCompanyInfo,functions:resetCompanyAdminPassword,functions:updateCompanyAdminInfo,functions:deleteCompany,hosting`(rules/indexes 변경 0). **배포 절대 금지** — 사용자 명시 결정. issues.md `[패턴]` 등록 + 회귀 가드(복원 금지) 3건 명시.
- [x] **2026-05-29** 운행 이력 정류장 반경 시각화 검증 도구(`main.c7fa8819.js` 빌드 미배포, +789B gzip, 신규 ESLint 경고 0) — AdminApp HistoryTab 단독. `react-kakao-maps-sdk` import 에 `Circle` 추가(L2). HistoryTab state 3 추가(`stopsByRoute` Map 캐시 / `showStopMarkers` 기본 ON / `showStopRadius` 기본 ON). dispatches → distinct routeId stops lazy `getDocs(orderBy order asc)` 1회 로드(BoardingStatsTab 패턴 차용). 카드 보조 표시 `✅ N/M 통과`(stops 미로드 시 "—"). 지도 안: ① `<Circle radius=100>` 통과(#0066FF/0.18)/미통과(#aaa·#666 stroke/0.12) ② `<CustomOverlayMap yAnchor=1.4>` 라벨(통과 녹배경+#fff/미통과 회색·maxWidth 140 ellipsis) ③ 좌상단 토글 컨트롤 바(🎯 반경/📍 마커) ④ 좌하단 범례. 회귀-0: GPS Polyline·출발/도착/selected 마커·infoBox·"왼쪽 선택" 안내·협력사 필터·차량 직접 선택·노선별 그룹·시간범위 필터·`new window.Map()` shadow 가드 보존. AdminApp 다른 11탭/Driver/Passenger/Employee/Partner/stopSchedule/useSmoothedEta/etaDiag/companyResolver/useOnlineRecover/gps.js/rules/CF/indexes 무영향. issues.md `[패턴]` 등록. **배포 금지** — 사용자 명시 결정. 검증 절차: localhost admin 로그인 → 운행 이력 탭 → 어제 운행 카드 클릭 → 지도에 회색 원(미통과)+파란 경로 표시 → 토글 OFF 시 숨김 → 카드 "✅ 0/N 통과" 확인.
- [x] **2026-05-29** ETA 진단 조회 임시 채널 신설(`main.040aaf7c.js` 빌드 미배포, +829B gzip, 신규 ESLint 경고 0) — 부모(개발자)가 gcloud/ADC 없이 prod `etaDiagnostics/{date}/runs/{runId}/points` 회수. 신규 onCall **`fetchEtaDiagnostic`**(`functions/index.js` L778~826): assertSuperAdmin 가드 + 입력 `{date?,runId?}` (date 미지정=오늘 KST `en-CA` 포맷, 형식 검증 정규식) + runId 미지정 시 `runsCol.listDocuments()` 로 ID 목록만(빈 문서 ID 도 회수) + runId 지정 시 `points` 컬렉션 `orderBy tsMs asc` 전체 read(V1 페이징 없음, ≤300 도큐먼트 전제). 반환 = `{date, runIds[]}` 또는 `{date, runId, count, points[]}`. Admin SDK 사용으로 firestore.rules 무관(read=admin/superadmin 룰과 별개). 한국어 로그(`[진단조회] date=... runs=... 호출자=...`). UI = AdminApp `SuperCompanyTab` 위 `EtaDiagnosticCard` 컴포넌트 신규(L4037~4156, 슈퍼관리자 분기 안이라 자동 가드): 날짜 input(`type="date"` 기본 오늘 KST) + "Runs 목록 조회" 버튼 → select 드롭다운(runId 목록 표시) + "Points 조회" → `<textarea readonly>` 고정폭 폰트 height 400px 가로 스크롤 + "📋 전체 복사"(`navigator.clipboard.writeText` + "복사 완료" 토스트 2초) + 로딩/에러 상태 표시(한국어 빨강 박스). **회귀 가드**: ① rules/indexes 변경 0 ② AdminApp 기존 11탭·회사 관리 탭의 회사 카드 그리드/신규 회사 등록/활성 토글/전환 100% 보존(panelHeader 다음에 카드 1개 추가만) ③ 일반 admin/기사/직원/승객/협력사 화면 무영향(슈퍼관리자 분기 안에만) ④ companyResolver/useOnlineRecover/useWakeTick/gps.js/stopSchedule.js/etaDiag.js 무영향 ⑤ CF 기타 11종(sendNoticeToCompany/notifyPreArrival/sendPartnerNotice/createCompany/listCompanies/toggleCompanyActive/createDriver/deleteDriver/updateDriverPassword/expandDispatchSchedules*/createDriverAuth) 시그니처·동작 불변. 검증: `node --check functions/index.js` 통과 + `CI=false npm run build` 통과(main 번들 `main.040aaf7c.js`, +829B gzip, 신규 경고 0). **사용자 사용 절차**: ① 슈퍼관리자 계정으로 `/` 로그인 → 회사 관리 탭 진입 ② 헤더 다음 "🔍 ETA 진단 조회" 카드 → 날짜 input 오늘 그대로 → "Runs 목록 조회" → 드롭다운에서 어제 운행 runId 선택 → "Points 조회" ③ "📋 전체 복사" → 채팅에 붙여넣기. **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:fetchEtaDiagnostic,hosting`(rules/indexes 변경 0). **배포 절대 금지** — 사용자 명시 결정.
- [x] **2026-05-29** ETA 진척률 안분 + delaySec raw 분리(`main.15820be4.js` 빌드 미배포, +1.41KB gzip, 신규 ESLint 경고 0) — 사용자 폰 실측 결함 3건 fix: ①조기 도착인데 지연 누적 ②큰 분수 표시 ③정류장 직전 "곧 도착" 점프. `src/lib/stopSchedule.js`: ARRIVING_BUFFER_SEC=45/ARRIVING_PROGRESS_THRESHOLD=0.95/PROGRESS_NOISE_FLOOR=0.05 상수 신설(L52-60). next 정류장 한정 routePath 진척률 안분 분기 추가(L317-365) — `actualProgress=(busProgress-stopProgress[i-1])/segDist` + `slowFactor=max(1, expectedByTime/actualProgress)`(빠른 방향 1 클램프) + `remainingMs=(1-actualProgress)*expectedTotalMs*slowFactor`, ≥0.95=45초 표시·≤0.05=skip, source='gps'. `rawForDelay` 변수 도입(L368) — `delaySec`을 chain→plan→est raw 후보 기준으로 산출(L388) → 단조증가/과거금지 가드가 음수(조기) 지연을 양수로 뒤집던 결함 차단. `src/lib/useSmoothedEta.js jumpThresholdSec 300→180`(L28-32) — 진척률 안분으로 자연 변동 → 정류장 통과 시점만 즉시 raw. **진단 오버레이 4지점**(`[DIAG-ETA 제거예정]`/`[/DIAG-ETA]` 경계 주석): ① stopSchedule.js 상수 주석 ② `DriverApp.js` 운행 탭 정류장 리스트 하단 "🔍 진단 ▼" 토글(L57·L843-883) — 정류장별 plan/est/delay/status/source 표 ③ `PassengerApp.js` `?debug=1` URL param 시 mapLayer 상단 floating 카드(L418·L534-578) — 내 정류장 한정·busProgress/myStopProgress/segProgress/speed ④ `EmployeeApp.js` `?debug=1` URL param 시 HomeTab 헤더 다음 카드(L702-704·L889-927) — 동일 양식. 사용자 폰 캡쳐 후 다음 커밋에 grep "\[DIAG-ETA" 1줄로 통째 제거. **회귀 가드(복원 금지)**: ① 진척률 안분을 GPS 가중 재도입 회피라며 제거 금지(점프 결함 재발) ② delaySec를 가드 후 estMs 기준으로 되돌리지 말 것(음수 지연 뒤집힘 재발) ③ slowFactor 빠른 방향 클램프 제거 금지. **보존 불변**: 출력 계약(stopId/plannedAt/estimatedAt/delaySec/status/source enum 4종) 절대 불변·source enum 추가 없음(안분도 'gps' 재사용)·companyResolver/useOnlineRecover/useWakeTick/gps.js/onStopReached/startGPS 시그니처·MIN_STOP_GAP_SEC=60/MIN_FUTURE_BUFFER_SEC=30/DWELL_SEC=30/MIN_EFFECTIVE_SPEED_KMH=20·next GPS 가중 0%·AdminApp/PartnerApp/슈퍼관리자 콘솔 무영향·카카오 SDK 컴포넌트 호출 0줄 변경·rules/indexes/CF/SW/manifest 무변경. 검증: `CI=false npm run build` 통과·신규 경고 0·번들 해시 `main.15820be4.js`. **배포 절대 금지** — 사용자 명시 결정. issues.md `[패턴]` 등록.
- [x] **2026-05-29** Phase 1.4 협력사 공지 발송 신설(`main.7d984f96.js` 빌드 미배포, +1.56KB gzip, 신규 ESLint 경고 0) — PartnerApp OperationsMode 섹션 C2(섹션 D 공지 수신함 위) 추가. 신규 onCall **`sendPartnerNotice`**(`functions/index.js`): partnerCodes 검증(exists+active+companyId 일치) + 서버측 rate-limit(`partnerCodes/{code}.recentNoticeTimestamps:number[]`, 1시간 5건, 옛 항목 정리 + now 추가로 무한 증가 방지) + title 1~50/body 1~500 trim 검증 + type ∈{normal,emergency} + Admin SDK 로 notices(sender:"partner"/senderCode)·fcmQueue create(룰 우회) → 기존 `sendNoticeToCompany` 트리거가 partnerCode 필터 멀티캐스트 인프라 재사용. 반환 `{ok, noticeId, queueId, remainingPerHour}`. 한도 초과시 `resource-exhausted` 한국어("시간당 5건까지 발송 가능합니다. 남은 시간: M분"). UI(`OperationsMode`): 제목 input(maxLength=50)·본문 textarea(maxLength=500, 4줄)·일반/긴급 라디오·글자수 카운터·인라인 컨펌 카드("자사 직원에게 공지가 발송됩니다. 발송하시겠습니까?")·성공/실패 결과 카드·`partnerCodes/{code}` onSnapshot 으로 "시간당 남은 발송 N/5건" 실시간 표시(다른 세션 발송도 즉시 반영). 신규 경고 0. **회귀 가드**: ① rules/indexes 변경 0(partnerCodes update 는 CF Admin SDK·notices/fcmQueue create 도 Admin SDK 우회) ② AdminApp NoticeTab/EmployeeApp 공지함·NoticeForceModal·승객앱 공지배너 무영향(notices read 패턴 동일·sender/senderCode 옵셔널) ③ PartnerApp register/manage/stats/ops 기존 4섹션(자사 노선·실시간 지도·오늘 탑승·공지 수신함) 100% 보존 ④ functions 기타 7종(createDriver/sendNoticeToCompany/notifyPreArrival/createCompany 등) 시그니처·동작 불변 ⑤ partnerCodes 신규 필드 `recentNoticeTimestamps` 옵셔널(미존재=빈배열). 검증: `node --check functions/index.js` 통과 + `CI=false npm run build` 통과. **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:sendPartnerNotice,hosting`(rules/indexes 변경 0). 첫 배포 후 모바일/PC에서 `/partner` 코드 인증 → 운영 포털 탭 → 📢 공지 발송 → 5건 연속 후 6번째 rate-limit 검증.
- [x] **2026-05-28** Phase 1.3 협력사 운영 포털 신설(`main.dd6a87da.js` 빌드 미배포) — PartnerApp `mainTab="ops"` 4번째 탭 + 인라인 `OperationsMode`. 4섹션: ① 자사 노선 요약 카드(passengers where partnerCode·active로 routeId 집합·노선별 직원수·미배정 카운트·카드 클릭 시 필터 토글) ② 실시간 버스 위치(KakaoMap 40vh·`import { Map as KakaoMap }` shadow 회피·`onCreate` relayout 더블·routePath 우선 stops 폴백·useAnimatedPositions rAF 보간) ③ 오늘 탑승 현황(boardings where partnerCode==code onSnapshot·3 카드+노선별 상위 5 미니 차트·상세는 stats 탭 안내) ④ 공지 수신함(notices where active+limit 20·클라이언트 partnerCode null||==code 필터·EmployeeApp NoticesTab 패턴). App.js `needsKakao`에 `isPartnerRoute` 추가(카카오 5초 대기). onSnapshot 4종 deps에 wakeTick+recoverTick. 카드 너비 ops 탭만 760. **불변 보존**: register/manage/stats 탭 100%·BoardingStatsMode/EmployeeManageMode/AdminApp/Driver/Passenger/Employee 코드 변경 0·firestore.rules/indexes/functions 변경 0·STEPS.CODE→STEPS.MAIN 흐름·익명 인증·partnerCode/companyId/routes preload·hashPin·importEmployees·boardings/notices 스키마. 빌드 +2.55KB gzip(목표 +5KB 이하). 신규 ESLint 경고 0. issues.md `[패턴]` 등록 + 회귀 가드 3건.
- [x] **2026-05-28** ETA chain plan-first + ArrivalProximityModal + useOnlineRecover + 운행 버튼 응답성(`main.f642a4f5.js` 배포 완료) — 사용자 호소 6건 일괄. ① `stopSchedule.js segTravelMs`를 plan offsetMin 델타 우선(둘 다 offsetMin 있으면 `(offsetB-offsetA)*60*1000`·DWELL 가산 X — offsetMin 자체가 정차 포함 진입시각 차이) → chain = `actualLast + Σ(plan 델타)` = 사용자 의도 "각 정류장 실제 도착시간 기준 + 미리 설정한 offsetMin" 수학적 등가. ② next 정류장 GPS 가중 **완전 제거(0.7:0.3→0%)** — 터널·약전계 GPS 노이즈 직접 노출 차단. source enum 4종 호환. ③ `src/lib/useOnlineRecover.js` 신규(30줄) — `window.online` 이벤트 + 옵셔널 `disableNetwork→enableNetwork` 1회 강제. DriverApp/PassengerApp/EmployeeApp HomeTab 셋 다 적용·onSnapshot deps에 recoverTick 추가. DriverApp은 추가로 driving=true 시 online 복귀 즉시 `triggerHeartbeat(watchId)` 1회 호출(`gps.js` heartbeatRegistry로 lastPos 캡쳐·외부 노출). ④ DriverApp `handleStart`/`handleStop` 응답성 수정 — 로컬 state·startGPS/stopGPS 즉시, `updateDoc`/`clearGPS`/`refreshToken`/WakeLock은 fire-and-forget(`.catch` graceful) → 통신 불량 시 버튼 무반응 결함 차단. ⑤ PassengerApp/EmployeeApp HomeTab `ArrivalProximityModal`(풀스크린, 1회 dismissed·routeId 변경 시 reset·myStopIdx>0+직전 stopArrivals 최근 5분 트리거·notifyPreArrival CF FCM과 별도 인-앱 보장 통로). 출력 계약·useSmoothedEta/formatPassengerEta/describeEtaSource/formatDelayLabel/onStopReached/startGPS 시그니처 절대 불변·useWakeTick 적용지점 보존·rules/indexes/CF 무변경·카카오 SDK 컴포넌트 호출 0줄 변경. issues.md `[패턴]` 등록 + 회귀 가드(복원 금지) 3건 명시.
- [x] **2026-05-28** 양방향 탑승 QR — 역방향(직원 발행 → 기사 스캔) 신설(`main.6fa4ef2d.js` + rules) — 카메라 권한 없는 협력사용. **신규 컬렉션 `passengerTokens/{tokenId}`**(2분 만료·1회 소각·companyId 일치 검증, boardingTokens와 분리 = rules·정책 독립). 우선순위 = **`routes.boardingMode` > `partnerCodes.boardingMode` > `'driver-qr'`**(혼승 노선 대응 — 노선 단위 override 우선). QR 페이로드는 URL 대신 JSON(`{v:1,t:"passenger",tokenId,companyId,empNo}`) — 기사앱이 외부 브라우저 튀는 사고 방지. EmployeeApp ScanTab 改名+래퍼(`ScanTabDriverQR` 100% 보존 + `ScanTabPassengerQR` 신규) — 호출부 변경 0. DriverApp 운행 시작 시 mode 조회 후 발행 카드 ↔ 카메라 스캔 UI 분기. AdminApp 협력사 발급/편집 + 노선 편집 모달 각각에 셀렉터·배지. boardings 스키마 100% 동일 → 통계 다운스트림 무영향. issues.md `[패턴]` 등록.
- [x] **2026-05-27** 관제 AdminApp PWA 설치 안내(`main.<재배포>.js`) — `<InstallPrompt/>` 마운트 + `applyAppManifest({manifestHref:"/manifest.json", appleTouchHref:"/icons/admin-1024.png", title:"BusLink 관제"})` 호출. PC Chrome 자동 바텀시트·주소창 설치 아이콘 활성화. 모바일 관제 시 직원앱 `/p`와 scope `/` 중첩 잠재 충돌(issues.md). manifest.json은 이미 admin 아이콘 정본. issues.md `[패턴]` 등록.
- [x] **2026-05-27** 강제 공지 모달(`NoticeForceModal`, `main.00a13b12.js`) — PWA 푸시 OS 누락 대비 도달성 보장. EmployeeApp 마운트 시 `unreadCount > 0`이면 가장 최신 안 읽음 공지 풀스크린 자동 노출. 일반=즉시 닫기·긴급(type=emergency)=5초 카운트다운+진동. `notices`/`noticeReadAt`/`markNoticesRead` 인프라 재사용·신규 Firestore 구조 0. 사용자 "안 본 공지 못 지나가게" 명시 결정. issues.md `[패턴]` 등록.
- [x] **2026-05-27** 알림 아이콘 결함 fix(`main.7c7dff9b.js` + functions 재배포) — `public/logo192.png`가 CRA 기본 React 로고였음. `public/icons/notification-employee.png`(passenger-1024.png 사본) 신설 + CF `sendNoticeToCompany`·`buildPreArrivalMessage` `webpush.notification.icon`+`badge` 교체 + SW `firebase-messaging-sw.js` `showNotification` 교체 + `manifest-employee.json icons`에 192~512 사이즈 등록. 부수 발견: 안드로이드 알림 발신자 "Chrome <URL>" 형태 = WebAPK 미설치 진단 단서. issues.md `[패턴]` 등록.
- [x] **2026-05-27** ETA 산출 chain 우선 정책 전환(`src/lib/stopSchedule.js`, 빌드 `main.7c7dff9b.js` 미배포) — `computeStopEstimates` 후보 결합 max→chain 우선. upcoming: chainCandidate 채택·없으면 plan+delay fallback. next: chain·gps 0.7:0.3 가중. 출발 지연 시 누적지연이 모든 미래 정류장에 carry되던 결함 차단(사용자 의도=actual 기반 재추정). 단조증가/과거금지 보호장치·source enum 4종 호환·출력 계약 불변. 다운스트림 무영향. issues.md `[패턴]` 등록. 사용자 localhost 검증 후 `firebase deploy --only hosting`.
- [x] **2026-05-27** PPT 매뉴얼 3종 신설(`docs/manual-ppt/`) — callcenter 정본 복제, 관리자(15장)·기사(12장)·승객(14장). 관리자 10장 자동 캡처 성공, 모바일 2종은 일부 placeholder. 캡처 자동화 4단 트랩(텍스트→viewport→span→locator) 후 06-notices만 실패 → placeholder + 본인 폰 폴백. 자동화 교훈은 `.claude/agents/refs/manual-playbook.md` §PPT 보강. prod 코드/규칙 무변경. **별건 발견**: prod EmployeeApp `[내 정류장 복원 실패] Missing or insufficient permissions`(issues.md `[미해결]`).
- [x] **2026-05-26** BoardingApp 익명 인증 hotfix(`main.3c4d91c8.js`) — QR 탑승이 통계 안 잡힌 결함. `signInAnonymously` 마운트 호출 추가(EmployeeApp 패턴 일관). 이전 QR 탑승은 복구 불가(토큰 만료). issues.md `[해결]`.
- [x] **2026-05-26** 탑승 통계 — GPS 기반 정류장별 정밀 집계(`main.75f79871.js`) — 신규 `lib/stopMapping.js`(haversine 매핑·반경 300m), boarding.js 차량 GPS 캡처, AdminApp/PartnerApp 정류장별 패널.
- [x] **2026-05-26** QR 탑승 통계 — AdminApp 신규 탭 + PartnerApp 일자별 누적(`main.44de1aa0.js`) — boarding.js partnerCode denormalize·BoardingStatsTab(탭 9)·BoardingStatsMode(메인탭3)·PartnerApp 익명 인증·rules boardings read = isAuth().
- [x] **2026-05-26** 운행이력 — 배차별 GPS 시간범위 필터(`main.797cd3d4.js`) — `dispatchTimeRange(d, all)` 헬퍼, 같은 차량 여러 배차 시 GPS 동일 표시 결함 차단.
- [x] **2026-05-26** `Map` shadow hotfix + 실시간 관제 일자별 조회(`main.c2b238ce.js`) — HistoryTab `new window.Map()` 가드, 날짜 picker + 자동 노선도 전환 + 과거 데이터 배너.
- [x] **2026-05-26** AdminApp 실시간 관제 노선도 뷰 + 운행이력 노선별 그룹화(`main.60c54a0d.js`) — MapTab 지도/노선도 토글·RouteTimelineCard·dispatchGroups·차량 직접선택 보조.
- [x] **2026-05-26** 도착예정·지연 표시 4건 일괄 수정(`main.b5108380.js`) — MIN_STOP_GAP_SEC=60·DWELL_SEC=30·unplanned chain 전파+prevDelaySec carry·GPS 가중 0.85:0.15.
- [x] **2026-05-26** PartnerApp 라이트 리스킨 7단계(`main.371e3b15.js`) — tokens.css 전면 적용·BusLinkLogo+Pill 채택·로직 100% 보존.
- [x] **2026-05-26** 협력사 영구 삭제 + 기사 삭제 성공 팝업(`main.346b6b07.js` + rules) — 비활성만 삭제 허용·직원수 confirm·`partnerCodes.delete = isAdmin(resource.data.companyId)`.

## 2026-06~07 세션 로그 (2026-07-31 tasks.md 에서 이관)
> **2026-07-21 세션 C(뒤로가기 종료 팝업 복구·prod 배포·게시판 done)**: 배시현 개선요청 `jJsU8M6QDoNvCPVPRWkL` "뒤로가기 종료 확인 팝업이 없어졌다". 배포 유실·코드 삭제 아님(prod 번들에 문자열 실재·데스크톱은 정상) → 근인=마운트 시점 `pushState` 발판을 **안드로이드 크롬이 제스처 없는 항목으로 보고 뒤로가기 때 건너뜀**. `lib/useExitConfirm.js` 단독 수정 — 첫 제스처 후 발판 push·발판 1개 유지·취소 시 재무장·"확인" 시 `history.back()` 1회 추가(예전엔 확인해도 안 나가던 결함). 헤드리스 4시나리오 통과·prod `main.1bfc9e62.js`. 3앱(승객/기사/직원) 동시 적용. 상세 issues.md `[해결]`.

> **2026-07-21 세션 B(알림 딥링크 + 공용계정 PIN 잠금·전부 prod 배포·게시판 done)**: ① way 요청 — 개선요청 구글챗 알림 링크가 해당 요청으로 바로 열리게. CF 2종 카드 버튼 URL `?imp=<id>` + AdminApp 딥링크 소비(탭 자동선택·상세 자동오픈·주소창 정리·미발견 배너). prod `main.4fc23452.js` + `functions:onImprovementRequestCreate,onImprovementRequestUpdate`. ② 배시현 개선요청 `KqeN2wUNat2QnlWbkr0l` "통합관제 계정 PIN 변경 잠금" — prod 실측으로 공용 계정 다수 확인(chadwick/chad2542/yuhan) + 전부 `pinInitial:true` 라 승객앱이 PIN 변경을 독촉하던 구조 확인. `passengers.pinLocked`(옵셔널·부재=기존) 신설 → 승객앱 설정 탭 실측 게이팅 + 협력사 포털 등록/수정 체크박스·목록 배지 + importEmployees 조건부 기록(대량등록 보존). prod `main.c1cc69af.js`(`--only hosting`). rules/indexes 변경 0·신규 경고 0·헤드리스 실로드 검증. **운영 후속**: 실제 잠금은 협력사 포털에서 해당 공용 계정 체크 필요. 상세 issues.md `[패턴]` 2건.

> **2026-07-21 세션 A(게시판 1건·prod 배포·게시판 done)**: 배시현 개선요청 `F6Y2FrYum6A6T2R6qztF` "노선복사 후 노선순서 ▲▼ 안 바뀜". 근인=`handleCopy` 가 원본 `order` 그대로 복제→중복, `moveRoute` 는 인접 값 교환(swap)이라 같은 값끼리=무효. 백필은 order 없을 때만 실행돼 전부 숫자면 중복 영구 잔존. 수정=① moveRoute 백필 트리거에 중복 감지(`new Set(numericOrders).size!==length`)→기존 중복도 첫 ▲▼ 시 0..n-1 정규화 치유 ② handleCopy 복사본 `order:null`(맨 뒤·신규 중복 차단). AdminApp RoutesTab 단독·rules/indexes/CF 변경 0·격리 node 7케이스 통과·신규 경고 0·prod `main.32bec07a.js`(`--only hosting`·curl 확인). 상세 issues.md `[해결]`. **git 커밋 미실행**(세션 끝 커밋 대기).

> **2026-07-16 회의 전사 환각 — 진단 확정·정본 교체 완료**: dev_recording 녹음이 **무음**(295KB/20분=250B/s, Gemini 검청 "말소리 없음") → 요약·전문 둘 다 환각(운수사 5건/지불 109건, 실안건과 겹침 0). 환각 md 폐기, **정본 = 사용자 제공 전사(`RQ/20260716/`) 기반 `meetings/2026-07-16_추가개선사항미팅.md` 재작성**. Firestore doc status:error 마킹·sync.js 무음 게이트(<1,000B/s 전사 차단) 추가(dev_recording repo). 실안건 6건은 "다음 할 일" 최상단.

> **2026-07-16 세션 3건(전부 prod 배포·커밋)**: ① 개선요청 웹훅 카드 base64 원문 노출 fix(CF `onImprovementRequestCreate`·`improvementPreviewText` 헬퍼·`8dfc3d3`) ② 배시현 게시판 요청 — 직원앱 내 정류장 카드 "운행 종료" 표기(`lib/runStatus.js` 순수 판정·`fb28514`·prod `main.7fd830f5.js`·게시판 done) ③ 개선요청 게시판 ↻ 새로고침 버튼(ImprovementTab — MapTab #2 `forceReconnect`+refreshTick 패턴 미러·stale 리스너 수동 재구독·prod `main.cbbabc98.js`). rules/indexes 변경 0.

> **2026-07-10 세션(prod 배포 `--only hosting`)**: 최우석 문의·점검·요청 3건. ① **문의(고정 QR)** — 유비칸 차량은 차량관리 고정 QR 사용이 맞음(2026-07-09 신설 기능의 정확한 용도). 답변만. ② **🔴 점검(스캔 오류)** — 고정 QR 을 직원앱 인앱 스캐너로 찍으면 `Paths must not contain //`. 근인=인앱 스캐너가 `?t=` 동적 토큰만 파싱(폰 기본 카메라는 원래 정상). `resolveStaticDispatch` 추출 + 3분기 파싱 + `includes("/")` 가드. ③ **요청(노선 순서)** — `routes` 조회에 정렬 부재(문서ID 순=무작위). `src/lib/routeOrder.js` 신설(order→departTime→name) + 관리자 "표시 순서" 입력·목록 ▲▼(레거시 0..n-1 백필). rules/indexes/CF 변경 0·신규 경고 0(HEAD 대조)·격리 node 테스트 통과. 상세 issues.md.

> **2026-07-09 세션(prod 배포·앱 코드 무변경)**: 카카오T 마케팅 콜라보 문의 대응. 카카오측 "개인정보(사번/전화번호) 연동 전달로 무로그인 유지 가능?" → **연동 불필요**(자체 세션 무기한 유지)·평문 사번 파라미터 수용 불가로 답변. 인앱 웹뷰 제약을 코드로 실측 = 푸시 사망 + **QR 탑승확인 `getUserMedia` 가 더 치명적**(boardings→탑승통계·정류장매핑 연쇄). 진단 페이지 `public/webview-check.html` 신설·`--only hosting` 배포(정적 단일파일, **재빌드 없이 `build/` 에 cp** → 번들 해시 불변·회귀 0). 미팅 다음주 화/수 — 확답 3가지는 issues.md `[미해결]`. 문서: issues.md(171줄) → `issues-patterns.md` 토픽분할, tasks.md 옛 로그 tasks-log.md 아카이브.

> **2026-06-26 세션(prod 배포)**: 배시현(G1학부모) 점검 5건 중 4건 코드·1건 진단. #2 노선 새로고침 버튼(`forceReconnect.js`+manualTick/refreshTick 재구독·3앱)·#3 정류장 탭 지도중앙(EmployeeApp 자동재센터 억제 `userCenteredRef`·/bus 이미 동작)·#4 버스 yAnchor 1.5→0.5(라인 안착)·#5 도착라벨 선택정류장만(`isMyStop`). #1 LG마그나 잠실 GPS 인식오류=진단완료(ETA버그 아님: 운행중 GPS 65분 공백 + 같은날 재운행 stopArrivals 잔존)·코드조치 대기. rules/indexes/CF·tokens.css 무변경·신규경고 0. 상세 issues.md.

> **2026-06-25 세션(prod 배포)**: 배시현 점검 2건 — ① 직원앱(`/p`) 로그인 사번칸이 숫자 키보드만 떠 영문 입력 불가(`type="tel" inputMode="numeric"`) → `type="text"` 일반 키보드로 전환(autoCapitalize/Correct off). PIN칸은 숫자 유지. ② 운행시간 아닐 때 첫 정류장 "예상: 현재시간" 표시 → `stopSchedule.computeStopEstimates` 에 `inOperation`(차량위치 OR 실측도착 유무) 판정 추가, 운행 중 아니면 과거금지 클램프 미적용·계획시각 그대로 노출. 운행 중(inOperation=true)=기존 동작 100% 보존(회귀 0·테스트로 입증). 상세 issues.md `[패턴]`.

> **2026-07-16 세션(빌드 `main.7fd830f5.js` 미배포·커밋 대기)**: 개선요청(배시현 kgyvTKo0GF5N1KyySlFO) — 직원앱 내 정류장 카드 **운행 종료 표기**. 신규 `src/lib/runStatus.js` `computeRunEnded`(순수·오늘배차+종점 stopArrivals 기록+신선 GPS 없음) + EmployeeApp HomeTab 카드 좌측 블록만 조건 교체("운행 종료"+"오늘 운행이 종료되었습니다"·회색). stopSchedule/useSmoothedEta/gps.js 무변경·신규 구독 0·격리 테스트 14케이스 통과·신규 경고 0(HEAD 대조). 상세 issues.md `[패턴]`. 배포는 부모 검증 후(`firebase deploy --only hosting`).

> **2026-07-14 세션**: ⓪ **게시판 코멘트 규칙 강제**(way 재지적) — 완료 코멘트는 `반영완료되었습니다` 한 줄·처리자는 **"시스템 관리자"**·시스템 용어 금지. `scripts/board.cjs` 에서 자유 메모 인자 제거(상태별 고정 문구만 write) + prod 기존 doc resultNote/history/byName 소급 교정. 앱 코드·배포 무관(CLI+Firestore 데이터). ① 개선요청 게시판 사용자 점검(배시현) — 작성/상세 모달 **배경 클릭 시 닫힘→작성 내용 소실** 수정(오버레이 `onClick={onClose}` 제거·닫기는 버튼만, prod `main.8bf320a8.js`·커밋 `0cb7531`). ② **개선요청 게시판 CLI** `scripts/board.cjs`(Admin SDK·`list`/`done`/`status`/`comment`) 신설 — buslink 는 로컬 prod 접근 수단 없어 `/board` 읽기·완료처리를 이 CLI 로. **`app/buslink/key/*.json` 서비스 계정 키 필요**(콘솔 다운로드·`.gitignore` 로 커밋 차단·`functions/node_modules/firebase-admin` 재사용·project_id 검증). 키 있으면 `/board` 가 이 채널로 자동 읽고 완료버튼도 CLI 로 누름.
