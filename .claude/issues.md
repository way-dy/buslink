# Issues & 주의할 패턴

> 새 발견사항은 이 파일에만 추가. 형식: `[미해결]` / `[해결]` / `[보류]` / `[패턴]`.

## 미해결 / 함정

- `[패턴]` **사용자 점검 4건 UI 개선 — 새로고침/정류장탭센터/버스아이콘/도착라벨(2026-06-26 배시현, prod `main.f4f4c8b5.js`, `--only hosting`)**: 학부모 점검 5건 중 4건 코드(#1은 진단). ① **#2 노선 새로고침 버튼** — GPS 껐다 켜진 뒤 화면 위치 미반영 시 수동 해소. 신규 `src/lib/forceReconnect.js`(순수: `disableNetwork→enableNetwork` 1회, useOnlineRecover 의 online-전이 미발화 케이스 보완) + EmployeeApp/PassengerApp(`manualTick` state→GPS·dispatch onSnapshot deps 재구독) 헤더 "↻ 새로고침" + AdminApp MapTab(`refreshTick`→gps deps) 탑바 버튼(과거날짜 비표시). ② **#3 정류장 터치→지도 중앙** — EmployeeApp `<Map center>` 가 GPS 갱신마다 버스↔내정류장 **중점으로 자동 재센터하는 useEffect** 가 탭 센터링을 즉시 덮어쓰던 결함(MapTab "클릭차량 첫차량 복귀"와 동일 계열). `userCenteredRef`(탭 시 true→자동 재센터 억제, `activeRouteId` 변경 시 리셋) + 노선도 스트립/지도 마커·라벨(`openStopInfo`) 탭에 `setCenter`. **PassengerApp(`/bus`)은 자동 재센터 effect 없어 이미 동작**(무변경 확인). ③ **#4 버스 아이콘** — 버스 `CustomOverlayMap yAnchor 1.5→0.5`(중심 정렬, EmployeeApp 본맵·모달 1.7→0.5·PassengerApp pill). yAnchor 0=top·1=bottom·>1=위로 뜸 → 0.5=라인 중심 안착. ④ **#5 도착시간 라벨 클러터** — 전 정류장에 timeLabel 표시하던 것을 `timeLabel && isMyStop` 으로 선택 정류장에만(Employee 본맵 라벨·Passenger 번호 오버레이). 출/도 라벨 이름은 유지. **회귀 가드(복원 금지)**: userCenteredRef 억제를 제거하면 탭 센터링 재무효화·yAnchor 1.5 복원 시 버스 라인 위로 재부상·#5 isMyStop 게이트 제거 시 클러터 재발. **보존 불변**: rules/indexes/CF 변경 0(프론트만)·tokens.css 무변경(`blspin` 기존 키프레임 재사용·css 해시 `main.02202394.css` 불변)·stopSchedule/gps/ETA 핫경로 무변경·DriverApp 무변경. 빌드 신규 경고 0(`main.f4f4c8b5.js` +659B). **#1 LG마그나 잠실 GPS 인식 오류 = 진단완료**(아래 `[진단완료]`).
- `[진단완료]` **#1 LG마그나 잠실 GPS/운행 인식 오류 = 운행 중 GPS 65분 공백 + 잔존 stopArrivals(2026-06-26 배시현, 진단 JSON 14포인트 분석, 코드 조치 대기)**: runId `6BjjFzj7…_ZXewfBBO…_1782422789109`(06:26 운행) 분석. **ETA 핫경로는 정상**(추측 수정 금지) — 표시 이상은 두 데이터 문제였음. ① **근인 = GPS 65분 완전 공백**: 06:38 busProgress 3,056m(서울) → **다음 포인트가 65분 뒤 07:43 인천 50,321m**(tsMs diff 3,899,675ms), 그 사이 진단/GPS 0건. 화면 켜고 거치해도 OEM 절전/백그라운드로 watchPosition 사망(웹앱 한계) → 관제·승객앱에 3km 지점 65분 고정 = "GPS·운행 인식 안 됨" 호소의 정체. ② **출발지 "도착 05:39"(−50분) = 같은 날 05:39 운행(runId `…_1782419967046`·동일 기사/차량/노선)의 stopArrivals 잔존**: dispatch 1건당 stopArrivals 공유 + 첫도착 멱등이라 재운행 시 이전 회차 시각 carry(로그 `alreadyExists:true`). ③ **종점 "−21분"·중간 "+50분" = 공백 후 GPS 복귀(07:43) 시 중간 정류장 일괄 백필**(via liveGps/viaBackfill 동시)·종점 07:44 기록(계획 08:05) — 실 통과시각 아닌 아티팩트. **권장 조치**: ⓐ 기사앱 GPS 끊김 강한 경고(마지막 fix N분↑ 시 배너+진동 — 현 `isGpsFresh` 60초 경고는 관제/승객용이라 기사 본인 미인지) ⓑ 재테스트 전 그 dispatch `stopArrivals` 비우기(운영) ⓒ 근본=callcenter Capacitor 네이티브 백그라운드 GPS 래퍼 이식. **진단 식별 팁**: runId=`{driverId}_{vehicleId}_{시작ms}`(`etaDiag.js`), 시작ms KST 변환은 **node Intl**(Git Bash `date -d @epoch` 는 Windows서 9시간 오차), driverId=`companies/{cid}/drivers/{ID}` 문서(name 대조). **#2 새로고침 버튼이 "위치 즉시 미반영" 운영 증상은 완화**(배포됨). 참고 재디자인(image 13/14 다크 직관형)은 별건(앱 라이트테마 의도).

- `[패턴]` **운행시간 아닐 때 "예상: 현재시간" → 계획시각 노출 + 사번칸 영문 키보드(2026-06-25 배시현, prod `main.d60b67d6.js`)**: 점검 2건. ① **사번 키보드** — EmployeeApp(`/p`) 로그인 사번 input 이 `type="tel" inputMode="numeric"` 라 숫자 키보드만 떠 영문(사번 영문혼용 가능) 입력 불가 → `type="text" inputMode="text" autoCapitalize="none" autoCorrect="off" spellCheck=false`. PIN 칸은 숫자 유지(`inputMode="numeric"` 정상). ② **"예상: 현재시간"** — 운행 중 아닐 때(차량 GPS 없음·실측 도착 0) 접속하면 첫 정류장이 `예상 [현재시각]` 으로 보임. 근인=`computeStopEstimates` 의 과거금지 클램프(`estMs = max(estMs, T_NOW + MIN_FUTURE_BUFFER)`)가 과거 계획시각(08:00)을 현재(14:20)로 끌어올림. **수정**: `inOperation = (vehiclePos!=null) || (실측 도착 1건+)` 판정 신설 → **운행 중 아니면**(`!inOperation`) 계획 정류장은 `estMs=plannedMs`(계획시각 그대로·delaySec=null), unplanned 정류장은 과거금지 클램프 미적용(일정 체인값 그대로). **운행 중이면 inOperation=true 라 분기 미진입 = 기존 동작 100% 보존**(node 테스트로 운행중 est=현재시각~ 클램프 유지 입증). vehiclePos 는 `mainBus ? {...} : null` 이라 무운행 시 자동 null·myStop 큰 카운트다운은 `!mainBus` 게이트라 무영향. 출력 계약(stopId/plannedAt/estimatedAt/delaySec/status/source) 불변. rules/indexes/CF 변경 0(프론트만). **회귀 가드(복원 금지)**: inOperation 게이트 제거하면 "예상 현재시간" 재발.

- `[패턴]` **노선도 스트립 "버스 2대처럼 보임" → 단일 버스 + 점멸 화살표(2026-06-24 채드윅, prod `main.8dd2d627.js`)**: 고객 — 정류장 사이 이동 중 버스가 2대 움직이는 듯. 근인=EmployeeApp(`/p`) 노선도 스트립이 `busStopIdx===i`일 때 버스 🚌 를 **노드 위 슬롯(isBusHere)+커넥터 선 위 동시 렌더** → 2대로 보임. **수정**: 노드 버스=단일 마커 유지, 커넥터의 두 번째 버스를 **점멸 우향 화살표 `›`**(`@keyframes busblink` tokens.css 신설·`busStopIdx===i && mainBus` 이동 중만)로 교체. 죽은 변수 `showBusBetween` 제거. **PassengerApp(`/bus`)은 가로 스트립 패턴 없음**(전체화면 맵+CustomOverlayMap 핀)→무변경. rules/indexes/CF 변경 0(프론트 표시만).
- `[패턴]` **`formatDelayLabel` 조기 라벨 "조기 N분"→"조기도착 N분"(2026-06-24 채드윅, prod `main.8dd2d627.js`)**: 고객 — 정류장에 일찍 도착 후 정시 출발했는데 기사앱이 "조기 2분"=조기출발로 오인. `stopSchedule.js formatDelayLabel` 조기(≤-2분) 분기 라벨만 변경. delaySec=실측/예상−계획 도착시각이라 전 사용처(Driver/Admin/Passenger/Employee) 의미상 정확=전역 변경 정본. "지연 N분"·"정시" 불변.
- `[미해결]` **계획≠예상·도착 전 "목적지 도착"/"이미 지나침"(2026-06-24 채드윅 진단, 코드 미수정)**: 승객앱(`/p`)이 도착 전인데 "목적지 도착"·"이미 지나침", 계획06:46인데 예상05:42(현재보다 과거) 표시. **08:00 종점(LG사이언스파크)은 의도값 확정**(강서지역 실 소요 김)→ 다른 정류장(06:5x) 대비 70분 plan 갭은 정상. 그 갭을 시뮬/실운행이 빠르게 주파하면 GPS 진척률 안전망(stopSchedule L255~299)의 음수 `gpsDelaySec`가 **과거 합성 도착시각**을 만들어 "예상 05:42"·과조기 표시(arrived 분기엔 MIN_FUTURE_BUFFER 미적용). + `projectToPolyline`(routeProgress L77 전역 min perpDist)이 **자기교차/왕복 노선**에서 오투영→busProgress 부풀려져 멀리서도 'passed'/'arriving' 오판 가능(확신도 중~높음, 실 routePath 자기교차 여부 데이터 확인 필요). **회귀 가드 많은 핫경로**라 추측 수정 금지. 저위험 후보=합성 도착시각 T_NOW 이전 클램프(단 "조기 운행 시 통과 정류장 미표시" trade-off)·근본=projectToPolyline 단조진행 우선(고위험·E2E 필요). **실 traffic에선 종점 08:00 정상 소요라 과조기 자연 완화**.

- `[해결]` **실시간 관제 클릭한 차량이 자동으로 첫 차량/기본 위치로 되돌아감 → 최초1회 센터 + 선택 차량 추적(2026-06-23, prod `main.83d1e753.js`)**: MapTab gps onSnapshot 이 **매 스냅샷마다 `setCenter(list[0])`** → 차량 클릭/패닝해도 다음 GPS 갱신(~5초)에 첫 차량 위치로 점프(controlled `<Map center>` 이라 강제 재센터). **수정**: ① `didInitCenter` ref 로 **최초 1회만** 첫 차량 센터(이후 스냅샷 재센터 안 함·패닝/선택 보존) ② 선택 차량 추적 effect — `selected` 있으면 rawVehicles 갱신마다 그 차량 위치로 센터(이동 따라감) ③ **선택 시 그 차량 마커만 렌더**(`selected ? filter(id) : 전체`) ④ 레일 헤더에 "✕ 전체 보기" 버튼(선택 해제). 좌측 레일 목록은 전체 유지(선택기). MapTab 단독·rules/CF/indexes 변경 0. **교훈**: 라이브 데이터 구독이 controlled map center 를 매 갱신 setCenter 하면 사용자 패닝/선택과 싸움 → init-once + follow-selected 분리.
- `[패턴]` **운행중인데 GPS 미수신 표기 + 마커 연속 이동(2026-06-23, prod `main.03ee32c8.js`)**: ① **GPS 미수신** — 기사 "운행 시작"은 `drivers.status="운행중"` 을 GPS 첫 수신 *전*에 찍음(handleStart 권한통과 후 즉시) → 폰 GPS 미확보/신호끊김이면 `gps/{cid}_{vehicleId}` 문서가 없거나 stale 인데 상태만 운행중 → 관제/대시보드에 그 차량 안 보임("기사 운행중 N명" vs "GPS 수신 M대" 불일치가 근거). **표기 추가**: 공용 헬퍼 `isGpsFresh(updatedAt)`(gps.js:77 serverTimestamp, ≤60초·**null/pending=방금=fresh**로 깜빡임 차단). 미수신 기사=`status==="운행중" && 그 vehicleId 의 fresh gps 없음`. 대시보드=기사현황 amber `⚠ GPS 미수신` 칩 + 경고 카드 + KPI sub. 실시간관제=`drivers` prop 추가(AdminApp:280) + 좌측레일 amber 경고 섹션(좌표 없어 마커 못 찍음). 제한 admin 은 visibleDispatches driverId 로 게이팅. ② **마커 들쑥날쑥 → 연속 이동** — `useAnimatedPositions` 가 1.5초 easeInOutCubic 인데 GPS ~5초 간격(5m/5초 스로틀)이라 "1.5초 이동→3.5초 정지→점프". **선형(등속) + per-vehicle 동적 duration=실제 도착간격 clamp(1200~8000)** 로 "받은 시간만큼" 이동→정지구간 소멸. **공용 훅이라 승객·직원앱도 동시 개선**(시그니처/폴백 불변). rules/indexes/CF 변경 0.
- `[해결]` **셔틀 여러 대가 관제에 1대만 = GPS 미수신(코드 정상)**(2026-06-23): "운행중 2대인데 관제 1대". 근인=한 기사 폰이 GPS 미발신(상태만 운행중). GPS 문서 키=`gps/{cid}_{vehicleId}` 차량별·MapTab/PassengerApp 은 gps 문서마다 마커(routeId dedup·캡 없음) → **코드는 차량 수만큼 표시**, 1대만 보이면 발신 vehicleId 1개뿐. 차량번호 다르면 공유충돌 아님→그 기사 폰 GPS 미확보/구버전캐시. 위 GPS 미수신 표기로 운영자가 즉시 인지 가능. **셔틀=노선 분리 운영**(차량/기사/vehicleId 고유 필수, 같은 vehicleId 공유 시 gps 덮어쓰기).
- `[해결]` **🔴 기사앱 로그인 "데이터 로드 중 오류" 화면(로그아웃만)에 갇힘 → 복구형 전환(2026-06-22, prod `main.926e66b8.js`)**: 고객 "기사/직원앱 로그인 시 로그아웃 처리 페이지 뜨고 재시도 반복하면 들어가짐". 화면 정체=`DriverApp.js:196` init() catch 의 종료성 에러("데이터 로드 중 오류"+로그아웃 버튼만). 근인=로그인 직후 일시적 Firestore/토큰 경합으로 `getDocs(drivers where uid==)` 가 가끔 throw(App.js 가 이미 onAuthStateChanged 게이트라 인증 자체 문제 아님·transient). **수정(DriverApp 전용)**: ① init 을 `useCallback`으로 추출(재호출 가능) ② transient catch 1회 한정 ~1.5초 자동 재시도(`autoRetriedRef` 가드·무한루프 차단) ③ 에러 화면에 "다시 시도" 버튼 추가(재로그인 불필요)+기존 로그아웃 유지 ④ init 트리거를 one-shot `auth.currentUser`→`onAuthStateChanged` 구독(currentUser 일시 null 엣지 방어) ⑤ catch 에서 실제 에러 console.warn. **"기사 정보를 찾을 수 없습니다"(데이터 부재)는 자동 재시도 제외**(무의미). **회귀 가드**: 자동 재시도 가드 제거 금지(무한루프)·데이터부재 분기를 재시도 대상에 넣지 말 것. rules/CF/indexes 변경 0. **표준 패턴**=로그인 직후 init 경합으로 종료성 에러화면 갇힘은 useCallback추출+1회자동재시도+복구버튼+onAuthStateChanged 4종으로 해소(아키타입 C CRA 결합형).
- `[패턴]` **"직원"→"승객" 전역 리네임(2026-06-22, prod `main.926e66b8.js`)**: 학교(채드윅) 고객 대상 — 설치 PWA 앱명 포함 전역. **변경=사용자 가시 한국어 라벨만**: `manifest-employee.json` name/short_name("BusLink 승객")·`appIcons.js` employee title·`EmployeeApp` PWA title+로그인 sub·`partner.js` 엑셀 시트/파일명(승객명부)·AdminApp 13곳+PartnerApp 11곳 화면 라벨. **불변(식별자·비가시)**: `employee`/`EmployeeApp`/`empNo`/`buslink_employee` LS키/`/p` 경로/`passengers`·`fcmTokens` 컬렉션/manifest **파일명**/`driver-qr`·`passenger-qr` boardingMode 값/주석/`firestore.rules`/`functions/`/`docs/manual*`/`.claude/*.md`. **운영 주의(단일테넌트)**: dy001=동영관광도 관리자/협력사 콘솔이 "승객"으로 보임("승객"=통근/학교 공통 중립어라 무방하나 인지 필요·콘솔만 "직원" 환원 가능). rules/CF/indexes 변경 0.
- `[패턴]` **셔틀=노선 type 옵션 "셔틀" 추가(2026-06-22)**: 사용자 결정 — 셔틀은 노선 분리 운영(동일 노선 내 정류장 반복 = stopArrivals 맵 키 구조라 첫 도착만 기록·구조변경 X). `AdminApp.js:2686` 노선 폼 type `["출근","퇴근"]`→`["출근","퇴근","셔틀"]`(form.type 저장·라벨/분류용). `r.type` 전 사용처 표시 전용(`==="출근"?A:B` 셔틀은 else 폴백·필터 `["전체","출근","퇴근"]`서 셔틀은 "전체" 노출)·깨지는 분기 0. 도착기록 구조 무변경.
- `[해결]` **🔴 차량 등록 "1대만" = 배열 길이 기반 ID 덮어쓰기(2026-06-22, prod `main.169c4f4e.js`)**: 고객 "차량 등록이 1대만 됨". 근인=VehicleTab `handleSave` 신규 차량 ID 를 `vehicle_${String(vehicles.length+1).padStart(3,"0")}`(AdminApp.js:2887)로 **배열 길이 기반** 생성 → 차량 삭제 후 재등록·다중 admin 동시 등록 시 **같은 ID 재생성→기존 차량 setDoc 덮어쓰기(데이터 손실)**. (부차 요인=createdBy 격리 visibleVehicles 가 타인 차량 숨김도 "1대"로 보이게 함·의도된 동작이라 미변경). **수정**: `setDoc(doc(...,vehicle_NNN),data)` → `addDoc(collection(db,"companies",companyId,"vehicles"),data)`(자동 ID). 기존 `vehicle_NNN` 차량은 ID 유지(배차/gps 가 vehicleId 값 참조)·신규만 자동 ID라 무영향. `setDoc` import 제거(유일 사용처였음→unused 경고 방지). **회귀 가드**: 배열 길이 기반 결정적 ID 로 되돌리지 말 것(덮어쓰기 재발). rules/indexes/CF 변경 0.
- `[패턴]` **뒤로가기 종료 확인 = `useExitConfirm` 훅(1회 back 가로채기)(2026-06-22, prod `main.169c4f4e.js`)**: 고객 "뒤로가기 시 확인 없이 바로 종료". `src/lib/useExitConfirm.js`(순수 React·Firebase import 0) — 마운트 시 `history.pushState` 더미 발판 1회 + `popstate`→`confirm("정말 종료하시겠습니까?")`, 취소 시 재push 로 위치 고정·cleanup·idempotent. EmployeeApp/DriverApp/PassengerApp 최상위 1회 호출(탭 전환은 state 기반이라 무관·로그인 리다이렉트와 충돌 없게 마운트 후 동작). **한계**: 설치형 PWA standalone OS back 100% 강제 불가(브라우저 한계·대부분은 잡힘).
- `[패턴]` **지도 밝기 = `color-scheme: light` 고정(2026-06-22, prod `main.169c4f4e.js`)**: 고객 "절전/시력보호 모드에서 카카오지도 너무 어두움". `public/index.html` `<meta name="color-scheme" content="light">` + `src/index.css` `:root{color-scheme:light}` → 브라우저/안드로이드 **auto-dark** 가 라이트 페이지를 강제로 어둡게 칠하는 것 차단. **filter(brightness/contrast) 미사용**(지도 색 왜곡·washout 위험). **한계**: 단말 자체 절전모드가 화면 전체에 씌우는 필터는 웹이 못 푸는 영역(코드로 해결 X·정직 안내).
- `[패턴]` **학교(채드윅) 근무조 = shift 옵션에 "등교/하교" 추가(2026-06-22)**: `AdminApp.js:2197` `shifts` 배열에 `"등교","하교"` 추가. shift 는 routes 필드이고 4개 앱이 `r.shift` 그대로 표시 → 옵션만 추가하면 관리자가 학교 노선에 직접 선택(거래처 결합/조건부 렌더 불필요·가장 견고). 표시 로직·다른 파일 변경 0.
- `[패턴]` **EmployeeApp 노선 변경 모달이 전 거래처 노선 노출 → 직원 partnerCode 필터(2026-06-17, prod `main.948fda8e.js`)**: 고객 호소="직원앱(`/p`) 노선 확인 중 노선 변경 모달에 타 거래처 노선(동영파트너 등)까지 다 나옴"(채드윅 학부모인데 전체 노선). 근인=EmployeeApp `filteredAllRoutes`(노선 변경 모달용)가 검색어만 필터하고 거래처 격리 없음. **수정(모달 표시 전용·회귀 0)**: ① `filteredAllRoutes` 에 `myPartner=session.partnerCode` 일치 노선만(미설정 직원=전체 하위호환) ② 검색창 표시 임계(`allRoutes.length>6`)도 partner 필터 카운트 기준 ③ 빈 메시지 `routeQuery` 유무로 분기 ④ 홈 노선 폴백(`all.slice(0,3)`)도 partner 필터(미배정 직원 타 거래처 노출 차단). **현재 노선 해석·activeRoute·홈 배정노선 로직은 full `allRoutes` 유지**(필터는 모달 표시·미배정 폴백 한정). session.partnerCode = passengers/{empNo}.partnerCode(로그인 시 주입). **한계**: 직원 partnerCode 미설정이면 전체 노출(데이터 부여 필요·`passenger-sync 정공법` 패턴)·노선에 partnerCode 미지정이면 그 거래처 직원에게도 안 보임(노선에 거래처 지정 필요). rules/indexes/CF 변경 0(클라 표시 필터). PassengerApp(`/bus` 익명)은 partnerCode 없어 미적용.
- `[패턴]` **배차·배차일정·대시보드기사로 createdBy 격리 확장 — 제한 admin "본인 등록한 거래처/배차/노선/기사만"(2026-06-17, prod `main.2c99171c.js`)**: 사용자 결정="dy04(dy001 소속 제한 admin)는 본인이 등록한 거래처·배차·노선·기사만 본다". 거래처/노선/기사/차량 createdBy 격리(2026-06-15~16)는 적용됐으나 **① 배차관리(DispatchTab) ② 배차일정(DispatchScheduleTab) ③ 대시보드 기사현황·전체기사 카운트(DashboardTab)** 가 누락 → dy04 가 배차 안 보이거나(접근 가능한 협력사 없음+"이 협력사 배차 없음"=DB엔 있는데 가림) 대시보드에 회사 전체 기사 6명 다 보임. **수정**: ① DispatchTab/DispatchScheduleTab 에 `currentUserUid` prop + `canSeeDispatch`/`canSeeSchedule` 술어(**노선 partnerCode∈allowed OR 노선 createdBy===uid OR 배차/일정 createdBy===uid**) → filteredDispatches/Schedules 에 적용 + 신규 addDoc·복사본에 createdBy 기록(복사는 본인 가시분만=타 담당자 오귀속 차단) + 폼 드롭다운(기사/노선/차량)을 `canSeeAll ? 전체 : 본인것`(handleX 의 find 선택값 해석은 전체 유지) ② DashboardTab `visibleDrivers = canSeeAll ? drivers : createdBy===uid` 로 기사현황 목록·전체기사 카운트·driving/waiting 산출(driverName find 는 전체 유지). **마스터 키 = `partnerCodes.createdBy`**: AdminApp:140 이 `createdBy==uid` 거래처를 구독→allowed 합산(148)→**거래처에 createdBy 만 있으면 배차/노선/이력/통계/관제/일정 전 탭이 partnerCode 체인으로 자동 노출**. 거래처 미지정 노선·routeId 없는 수기 배차만 createdBy 직접 격리로 보강. **회귀 가드(복원 금지)**: ① canSeeAll(전체권한/슈퍼관리자/미설정 폴백) 경로 현재 동작 100% 동일(전부 visible) ② DispatchTab/ScheduleTab 폼 드롭다운 필터는 **제한 admin 한정** — issues 의 기존 가드("배차·시뮬은 전체 필요")를 *전체권한 admin엔 유지·제한 admin엔 본인것*으로 정제(SimulatorTab 은 미변경=전체 유지) ③ handleDriverSelect/RouteSelect/VehicleSelect 의 `find` 는 전체 배열 유지(visible 로 바꾸면 선택값 해석 실패). **레거시 한계**: createdBy 부재 거래처(예 채드윅송도국제학교)·기사는 제한 admin 비노출 → 슈퍼관리자 회사관리>관리자목록>🛡권한 체크(allowedPartnerCodes 추가)로 노출 or createdBy 백필. **신규는 자동**(createPartnerCode·createDriver·신규 배차 모두 createdBy 기록). **보존 불변**: rules/indexes/CF 변경 0(클라 게이팅만).
- `[해결]` **노선 createdBy 소유 모델 — 제한 admin "노선 추가해도 리스트에 안 보임"(2026-06-16, prod `main.8896e8f8.js`)**: 사용자 호소="노선관리에서 노선 추가해도 리스트에 안 나옴". 근인=RoutesTab `filtered`(2153행) Phase B 게이팅 `else if (!isAllAccess(allowed) && !partnerCodeAllowed(allowed, r.partnerCode)) return false` 가 **제한 admin 의 거래처 미지정 노선을 숨김**(2026-06-15·06-16 issues 에 "잔존: 거래처 미지정 노선 제한 admin 비노출"로 이미 명시됐던 설계 한계가 사용자 불만으로 표면화). 거래처(2026-06-15)·기사/차량(2026-06-16) createdBy 격리와 **동일 소유 모델을 노선에 적용**이 일관 해법. **수정**: ① RoutesTab 에 `currentUserUid` prop ② handleSave 신규 addDoc + handleCopy 복사본 data 에 `createdBy:currentUserUid||null` ③ filtered 게이팅에 `&& !(currentUserUid && r.createdBy===currentUserUid)` 추가 → **allowed 협력사 노선 OR 본인 생성 노선** 노출(거래처 미지정 자기 노선도 보임). **회귀 가드(복원 금지)**: ① isAllAccess(전체권한/슈퍼관리자) 경로 동일·기존 partnerCode 허용 노선 그대로 ② createdBy 조건 제거 시 동일 결함 재발. **한계**: 레거시(createdBy 부재) 노선은 제한 admin 비노출(전체권한 admin 엔 보임)·신규 등록/복사부터 적용. **보존 불변**: rules/indexes/CF 변경 0(routes read=isAuth·표시 게이팅만)·다른 탭 무영향.
- `[패턴]` **createdBy 격리를 partnerCode 매핑 없는 컬렉션(drivers/vehicles)으로 확장 — 기사·차량 ID별 격리(2026-06-16, prod `main.91fa0fdd.js` + functions:createDriver)**: 사용자 결정="기사/차량도 등록한 admin ID별 완전 별도 관리·전체권한(["*"])/슈퍼관리자는 전부 조회". 거래처 createdBy 모델(2026-06-15)을 partnerCode 직접매핑 없는 컬렉션으로 확장 — **격리 키 = `createdBy===uid`**. ① **서버측 기록**: `createDriver` CF 가 drivers.add 에 `createdBy:request.auth.uid`(assertAdmin 통과 후라 auth 보장). 클라가 createDriver 에 createdBy 넘기지 않음(서버 권위). ② **클라 폴백 기록**: DriverTab 신규등록 catch 폴백 addDoc·VehicleTab setDoc 은 `createdBy:currentUserUid||null`. ③ **표시 게이팅**: DriverTab/VehicleTab 에 `allowed`+`currentUserUid` prop → `const canSeeAll=isAllAccess(allowed)` → `visibleX = canSeeAll ? all : all.filter(x=>x.createdBy && x.createdBy===uid)`. 목록·"총 N대" 카운트·기사폼 **배정차량 드롭다운 옵션**까지 visible 로(`handleVehicleSelect` 의 `find` 선택값 해석은 전체 유지·옵션 렌더만 visible). **회귀 가드(복원 금지)**: ① 공유 `vehicles`/`drivers` 배열을 다른 탭(DispatchTab/DispatchScheduleTab/SimulatorTab/HistoryTab/MapTab/DashboardTab)에서 필터하지 말 것 — 배차·시뮬은 전체 필요, 격리는 **DriverTab·VehicleTab 표시 한정** ② 차량 id 생성 `vehicle_${vehicles.length+1}` 는 전체 prop 유지(visible 로 바꾸면 id 충돌) ③ isAllAccess(전체권한/슈퍼관리자/미설정 폴백) 경로=현재 동작 100% 동일(canSeeAll=true). **한계**: 레거시(createdBy 부재) 기사/차량은 제한 admin 비노출(전체권한 admin 엔 보임)·의도된 동작(거래처 createdBy 와 동일 폴백 정책)·클라 게이팅이라 우회 가능(rules 강제는 Phase B 후속과 동일 별건). **보존 불변**: rules/indexes 변경 0(drivers/vehicles read=isAdmin·회사 내 전부 읽되 화면만 필터)·createDriver 외 CF 무변경.
- `[해결]` **🔴 drivers 룰 admin update 누락 → 기사 정보 수정 "Missing or insufficient permissions" + 그 throw 가 PIN/Auth 생성 차단 → 기사 로그인 불가(2026-06-16, 루트 firestore.rules 배포)**: 고객 점검 2건(기사앱 `d.buslink.co.kr` 로그인 "사번 또는 PIN이 올바르지 않습니다" + 관리자 "기사 정보 수정" 저장 시 빨간 `Missing or insufficient permissions`)이 **동일 근인**. `companies/{cid}/drivers/{driverId}` 룰이 `create,delete=isAdmin` 인데 **`update` 는 `isDriverOf`(기사 본인 status/uid/startedAt/endedAt) 한정뿐 — admin update 경로 부재**(원래부터 빠진 구멍·회귀 아님). 신규 기사 등록은 `createDriver` CF(Admin SDK 룰 우회)라 정상이라 그동안 표면화 안 됨. **기존 기사 수정 시**: DriverTab `handleSave` editItem 경로가 `updateDoc(drivers/{id})`(name/empNo/vehicle/phone) 를 **먼저** 호출 → 룰 거부 throw → catch 가 setError → **뒤의 PIN 변경/Auth 생성 블록(`updateDriverPassword`/`createDriverAuth`)이 실행조차 안 됨** → 기사 Auth 계정/비번 미갱신 → 로그인 실패. **해법(룰만, 순수 추가)**: dispatches 룰(line 64) 패턴 그대로 `allow update: if isAdmin(companyId) || (isDriverOf(companyId) && ...hasOnly([status,uid,startedAt,endedAt]))`. 루트 `firestore.rules` + stale `src/firestore.rules` 둘 다 동기(정본=루트). **회귀 0**: admin 신규 획득·기사 본인 경로 불변. **운영자 후속조치(필수)**: 룰 배포만으론 기존 기사 Auth 가 안 생김 — 관리자가 "기사 관리 → 해당 기사 수정 → 비밀번호 6자+ 입력 → 저장" 1회 재실행해야 `createDriverAuth`(uid 없는 기사) 또는 `updateDriverPassword`(uid 있는 기사) 가 실제 발화 → 그 후 기사앱 로그인 가능. **회귀 가드(복원 금지)**: drivers update 에서 `isAdmin(companyId)` 제거 금지(동일 결함 재발).
- `[패턴]` **노선 복사(정류장 일괄 복제) — RoutesTab 순수 클라 addDoc + 명시 필드 화이트리스트(2026-06-16, prod `main.718c2ded.js`)**: 고객 요청="정류장 정보 많이 입력된 노선을 통째 복사". RoutesTab `handleCopy(item)`(handleDelete 다음) — ① 노선 문서 `addDoc(companies/{cid}/routes)` **명시 필드만 나열**(`...item` 스프레드 금지=`item.id`/메타 누출 차단)·`name="원본 (복사본)"`·partnerCode/partnerName/routePath 보존·createdAt/updatedAt 신규 ② 하위 `stops` 를 `getDocs(orderBy order asc)` 로 읽어 새 노선 하위에 직렬 `addDoc`(name/address/lat/lng/**order 보존**/photo/description/offsetMin·id 제외) ③ 성공 시 `setFilter/setPartnerFilter/setSearch("전체")` 필터 초기화(handleSave 패턴)로 즉시 노출 + "정류장 N개와 함께 복사" alert. 관리 칸 수정↔삭제 사이 `📋 복사` 버튼. **회귀 가드**: ① `...item`/`...sd` 스프레드로 되돌려 id·메타 복사하지 말 것(addDoc 새 id 와 충돌·order 외 잔여 필드 오염) ② lat/lng 는 `toLatLng` coercion 없이 raw 복사가 정합(복제는 원본 형식 보존·표시/감지 계층과 별개). **보존 불변**: rules/indexes/CF 변경 0(companies/** write=admin 룰로 stops 하위까지 허용)·다른 탭/앱 무영향·Phase B 는 partnerCode 보존이라 제한 admin 복사본 자동 노출(추가 처리 0).
- `[패턴]` **createdBy 거래처 가시성을 전 게이팅 지점으로 확장 — 배차/노선 등 본인 생성분이 안 보이던 누락(2026-06-15, `main.9a5310da.js` 배포)**: 사용자 결정="아이디별로 각자가 생성한 거래처(createdBy===uid)는 권한과 무관하게 항상 본다·전체는 회사관리에서 추가체크/전체권한". 증상=제한 admin(`allowedPartnerCodes:[]`)이 본인 등록 거래처 배차를 배차관리에서 못 봄(스샷 "이 협력사에 해당하는 배차가 없습니다"=DB엔 있는데 필터로 가림). 근인=Phase B 8지점 중 **대시보드·협력사관리만 `allowed∪createdBy`**(visiblePartners/visibleCodes) 였고 **배차관리(DispatchTab 1245)·배차일정·노선관리·운행이력·탑승통계·실시간관제·공지발송은 raw `allowed`만** 봄. **해법(최상위 1지점)**: AdminApp 이 `partnerCodes where companyId==cid AND createdBy==uid` 구독(`createdPartnerCodes`)→`allowed = allAccess ? rawAllowed : rawAllowed∪createdPartnerCodes` 로 합쳐 **모든 탭에 그 값을 `allowed` prop 으로 내림** → 탭별 코드 변경 0·기존 createdBy 처리(대시보드/협력사관리)와 멱등. PartnerFilter 도 `allowedCodes=allowed` 라 드롭다운에 본인 생성 거래처 자동 노출. **잔존**: 거래처(partnerCode) **미지정** 노선은 여전히 제한 admin 비노출(거래처 ownership 모델이라 의도) — 노선 등록 시 거래처 지정 필요. **회귀 가드**: ① allAccess(superadmin/["*"]/미설정) 경로는 rawAllowed 그대로=현재 동작 100%(merge 스킵) ② createdBy==uid 2동등필터는 복합 인덱스 불요(자동 단일필드)·partnerCodes read 공개라 rules/indexes 변경 0. 검증: build `main.9a5310da.js` +94B 신규 경고 0. **운영 즉시조치(병행)**: 본사 운영 담당 계정은 회사관리>관리자목록>🛡권한>⭐전체권한 부여(거래처 미지정 노선까지 보려면 필수).
- `[패턴]` **신규 서브도메인에서 지도·정류장 주소검색 동시 사망 = 카카오 키 도메인 미등록(2026-06-15, `main.9c9e45b4.js` 배포·해결)**: 사용자 "정류장 관리 주소검색 무반응(구주소/신주소/명칭 다)". 진단=같은 폼 "지도에서 위치 선택"도 **흰 화면**·접속 도메인 `admin.buslink.co.kr`. 지도(맵 엔진)+검색(services Geocoder/Places)이 **동일 키 `58bf34…`(callcenter 공유키)** 사용 → 둘 다 죽음 = SDK가 그 도메인에서 거부됨 = **카카오 개발자 콘솔 플랫폼 Web 사이트 도메인에 새 서브도메인 미등록**(2026-06-05 서브도메인 띄울 때 콘솔 등록 누락·코드 문제 아님). `buslink-prod.web.app`은 등록돼 정상. **해결(운영자 1회·카카오 콘솔)**: developers.kakao.com → `58bf34…` JS키 앱 → 앱설정>플랫폼>Web>사이트 도메인에 `https://admin/d/p/partner.buslink.co.kr`(+apex `buslink.co.kr`) **각각** 추가(와일드카드 미지원)·`buslink-prod.web.app`은 유지. 수분 내 반영. **코드 부수 개선**: `handleAddrSearch`에 8초 워치독 — services 콜백 미응답 시 "검색 중" 영구정지 대신 수동경로 안내(도메인 등록과 별개·한도초과/네트워크 장애도 정직하게). **근본 백로그**: 비즈앱 심사 후 buslink 전용 키 분리(callcenter 공유 한도·도메인 동기 부담 해소). **회귀 0**: rules/CF/indexes 변경 0·프론트 단독.
- `[패턴]` **사용자 점검 3건 — 노선 등록 후 안 보임(필터)·기사 사번/PIN 안내·검색형 선택(2026-06-15, `main.a987fde5.js`)**: ① **노선 등록 후 안 보임** 근인 = RoutesTab `filtered`(거래처 `partnerFilter`·구분 `filter`·검색)가 신규 노선을 가림. 특히 대시보드 '🚌 노선 관리' 버튼이 `partnerFilter`를 특정 거래처로 고정 → 다른/미지정 거래처 노선 등록 시 숨음. **해법**: `handleSave` 성공(add/edit) 후 `setFilter/setPartnerFilter("전체")+setSearch("")` 로 필터 초기화 → 저장 노선 즉시 노출. **잔존**: 제한 admin(`!isAllAccess`)은 Phase B 가 거래처 미지정 노선을 숨기므로(2020행) 노선 등록 시 자기 담당 거래처를 지정해야 보임(설계대로). ② **기사 등록 사번/PIN** — 브라우저가 사번칸에 이메일/저장비번 autofill 하던 것 → `autoComplete="off"`(사번)/`"new-password"`(PIN) + 비표준 name + 안내문(사번=기사앱 로그인 ID·PIN=비번 6자+). ③ **검색형 선택** — 기사·차량 400+ 대비 native `<select>` 스크롤 → 재사용 `SearchableSelect`(타이핑 필터·click-outside·onChange(value) 시그니처 호환) 신설, 배차 등록(기사/노선/차량)·배차 일정(기사/차량)·기사 등록(배정차량)에 적용. **보존**: rules/CF/indexes 변경 0(전부 클라 UI)·핸들러 시그니처 동일·Phase B/거래처 게이팅 무영향.
- `[패턴]` **거래처 중심 관리 3종 — 관리자 게시판형·대시보드 거래처 현황·본인생성 협력사 열람(createdBy)(2026-06-15, prod 배포 `main.5a021d11.js`)**: 영업담당자 id별 거래처 관리 모델 강화. ① **회사관리 관리자 목록 게시판형**(`AdminListSection`) — 좁은 카드폭에 이름+이메일+배지+버튼3개를 한 줄로 욱여넣어 이름이 짤리던 것 → 세로 카드(이름 15px 단독행·전체 라벨 버튼)로 재디자인. ② **대시보드 거래처 관리 현황**(`DashboardTab`) — `partnerCodes` 구독 추가 + 거래처별 노선/오늘배차/운행중/오늘탑승 집계 테이블 + 업체코드 복사. 가시범위=`isAllAccess?전체:allowed∪createdBy`. ③ **본인 생성 협력사 열람**(`PartnerTab.visibleCodes` + 대시보드) — `partnerCodes.createdBy:uid` 신규 필드(`lib/partner.createPartnerCode`가 기록)로 제한 admin 이 **발급 직후(권한 부여 전)에도** 자기 생성분을 열람·관리. users self-update 가 rules 상 superadmin 전용이라 자동 권한추가 안 되던 한계(Phase B issues) 우회 — **클라 게이팅 모델 일관·rules 무변경**. `currentUserUid`(=user.uid)를 DashboardTab/PartnerTab 에 prop 전달(line 248·258). **회귀 가드**: createdBy 부재 레거시 코드는 종전대로 allowed 로만 노출(폴백 변경 금지). **보존**: rules/CF/indexes 변경 0(partnerCodes create=isAuth, createdBy는 신규 필드)·Phase B 8지점 게이팅·partnerAccess.resolveAllowed 무변경. 검증: build +631B `main.5a021d11.js` 신규 경고 0(`routes:3614` 등은 기존). **2026-06-15 추가(`main.59a832c0.js`)**: 대시보드 거래처 현황에 ⓐ '+ 거래처 등록'(인라인 발급 모달·`createPartnerCode` 직접 호출·createdBy 기록) ⓑ 행별 '🚌 노선 관리' 버튼 — AdminApp `routesFocusPartner` state → `setTab(4)` + RoutesTab `focusPartnerCode` prop → mount useEffect 가 `partnerFilter` 1회 고정 후 `onFocusConsumed` 로 신호 소비(재진입 시 강제필터 방지). 거래처 발급은 partnerCodes create=isAuth 라 rules 변경 0.
- `[해결]` **🟢 admin 협력사 권한 "전체 해제"가 저장 안 됨 + 회사관리 UX(2026-06-15, prod 배포 `main.9ee8690d.js` + `listCompanyAdmins` CF)**: 슈퍼관리자 회사관리 → 관리자 목록 → 권한 변경에서 **전체권한(*) 해제 후 저장해도 재오픈/재조회 시 다시 전체로 체크**되던 버그. 근인 = "필드 부재(레거시 admin)=전체"와 "빈 배열 `[]`(전체 해제·특정 0개)=전체 아님"을 **관리 UI 2곳이 `length>0` 검사로 혼동** → `[]`를 `["*"]`로 둔갑. ① CF `listCompanyAdmins`(functions/index.js) ② 클라 `EditAdminPermissionsModal` 초기값(AdminApp.js) 둘 다 `Array.isArray(v) ? v : ["*"]`(부재만 폴백·빈 배열 보존)로 교정 — `src/lib/partnerAccess.js resolveAllowed` 와 동일 규칙으로 통일(런타임 게이팅은 원래 정상이라 데이터는 잘 저장됐고 **표시/재조회만 깨졌던 것**). **둘 다 배포 필수**(CF가 readback 정규화하면 클라 수정만으론 무효). **회귀 가드(복원 금지)**: 두 지점에 `&& length>0` 재추가 금지 — 전체 해제가 전체로 되돌아가는 회귀. **UX**: ③ 회사관리 진입 시 자기 소속 회사 관리자 목록 **자동 펼침+선로드**(ref 가드 1회) — "매번 클릭해서 활성화+로드 대기" 제거 ④ PartnerPermissionPicker 에 "전체 해제+0개 선택 = 아무 협력사도 못 봄" 경고. 후속 후보: 행별 인라인 권한 편집(모달 왕복 제거).
- `[해결]` **🟢 폰 최신빌드 미수신 블로커(2026-06-05 미해결 → 2026-06-09 해소)**: 진단 `appVersion`·화면 빌드뱃지 도입으로 옛/새 즉시 판별 가능해졌고, 이후 운행 진단 JSON 에 `appVersion` 정상 반환 = 폰이 신빌드 실행 입증. 캐시는 더 이상 블로커 아님. **교훈**: 진단으로 "데이터 없음"이 안 갈리면 진단 채널이 필드를 안 보여주는 것일 수 있다(아래 `[패턴] 도착 감지·표시 최종 구조` ① 참조).
- `[패턴]` **도착 감지·표시 최종 구조(2026-06-08~09, prod 배포 `main.3b35b9b2.js`/`DRIVER_BUILD v2026-06-09-livegps`)**: ① **진단 채널 필드 완전성** — `fetchEtaDiagnostic`(진단 조회 CF)가 반환에서 `appVersion`/`stopArrivalsLog`/`allStopProgress` 를 빠뜨려 "옛캐시 vs 신빌드"·"감지미발화 vs 쓰기실패" 두 가설을 며칠 못 갈랐다. 세 필드 반환만 추가하니 즉시 "신빌드+감지 0회"로 확정. **진단으로 가설이 안 갈리면 코드 더 파기 전 진단 조회함수의 반환 매핑부터 대조**(기록은 하는데 반환 안 하는 필드). ② **감지 우회(busProgress)** — `detectStops`/`onStopReached` 100m 직접감지 미발화 근인은 미해결이나, `computeStopEstimates`의 **busProgress(routePath 진척률) 통과 안전망**(`stopSchedule.js` GPS_PASS_MARGIN_M=40 — busProgress가 정류장 투영지점+40m 넘으면 도착 주입·source "gps")으로 도착 판정 + DriverApp 이 통과 정류장을 `recordStopArrival(viaBackfill)` 로 우회 기록하여 충족. "24분 균일 지연" 아티팩트도 이 안전망으로 소멸. ③ **표시 지연(~30초→~5초)** — 기사앱 도착 표시 지연 근인 = throttle ref 의 stale 위치. DriverApp 에 자기 차량 `gps/{companyId}_{vehicleId}` **onSnapshot 실시간 구독(`liveVehiclePos`)** 추가 → computeStopEstimates `vehiclePos`/`speed` 주입 + recorder useEffect 가 `liveVehiclePos` 키로 발화. ④ **잔존** — 계획시각(`offsetMin`) 없는 unplanned 정류장 위주 노선은 지연 라벨 제한(각 정류장 진입 오프셋 데이터 입력 보강 필요)·detectStops 직접감지 근인 미해결(우회로 충족)·Phase B 협력사 게이팅의 rules 강제는 별도 후속(현재 클라 필터만). **회귀 가드(복원 금지)**: busProgress 안전망 제거 금지(도착기록 0건 회귀)·liveVehiclePos onSnapshot 를 throttle ref 로 되돌리지 말 것(표시지연 재발)·진단 CF 반환 필드 축소 금지.
- `[해결]` **🟢 도착 감지 작동 입증(2026-06-05 시크릿 운행)** + **첫 정류장 누락 근인**: 7정류장 `source:"actual"` 도착·실측지연(도착−계획, 예: +7분/+9분) 정확 기록·recordStopArrival 호출 확인 → 5일 추적한 "도착시간 안 찍힘" 해소(감지 로직 정상). **단 그 운행은 옛 캐시 번들(70e47fc7)** 이라 routePath 백필 OFF(경합) → 100m 직접감지만 작동 → **첫 정류장(출발지)만 누락**(출발지 좌표가 routePath에서 벗어나 100m 반경 밖). 그래서 "다음" 포인터가 stop0에 갇혀 지연이 누적되어 보임(사용자 호소). **downstream은 정상**(마지막 실제도착+계획 인터벌로 재계산: 데이터상 hUvsP 07:13 도착→다음 07:18=+5분 인터벌). **최신 빌드(ref-getter 백필)는 첫 정류장도 progress로 잡아** 누락·누적 둘 다 해소 — 폰이 최신 빌드 수신해야 발현(위 `[미해결]`).

- `[해결]` **🔴 클린 URL rewrite 가 no-cache 헤더 미적용 → 기사폰이 며칠간 옛 번들 서빙(새 배포 전부 미반영)**(2026-06-02, `firebase.json` 헤더 교정): 도착 감지 fix(5539a881·08da6948)를 여러 번 배포했는데도 기사폰이 계속 옛 동작 → 진단 JSON 에 빌드 마커 `appVersion` 부재로 **폰이 새 번들을 한 번도 안 받음** 확정. 근인 = `firebase.json` headers 가 `**/*.html` 패턴에만 no-cache 인데, **클린 URL(`/driver`·`/`·`/p`·`/bus`)은 rewrite 라 `.html` 확장자가 없어 패턴에 안 걸림 → Firebase 기본 `Cache-Control: max-age=3600`(1시간)** 으로 index.html 서빙 → 폰이 옛 HTML(→옛 JS 해시) 최대 1시간+ 재사용. `curl -sI https://buslink-prod.web.app/driver` 로 `max-age=3600` 실측 확인이 결정타. **해법** = headers 를 3규칙으로 재구성(**last-match-wins** — `curl` 로 검증된 Firebase 우선순위): ① `**` → no-cache(캐치올·클린 URL HTML 포함) ② `/static/**` → `max-age=31536000, immutable`(CRA 내용해시 자산, last 라 ①을 덮어 장기캐시) ③ `/firebase-messaging-sw.js` → no-cache(last). 검증: `/driver`·`/`·SW = no-cache, `/static/*.js` = immutable. **기사폰 escape**: 배포 후 폰에 캐시된 옛 index.html(max-age=3600)은 만료(≤1시간) 또는 강제(재설치·`?v=` 쿼리·하드리로드) 1회 후 영구 자동갱신. **교훈(메모리 `verify-build-freshness` 적용 사례)**: SPA rewrite 클린 URL 은 `**/*.html` 헤더 규칙에 안 걸린다 — 캐시 진단은 항상 `curl -sI <클린URL>` 로 실제 Cache-Control 실측. 빌드 마커(`appVersion`)가 "코드결함 vs 폰 미수신"을 가른 결정 도구. **이 헤더 결함이 6/01~6/02 '도착 감지 안 됨' 재현의 숨은 증폭기** — 감지 fix 자체는 폰에서 실행된 적이 없었음(08da6948 가 폰에 실린 뒤 재검증 필요).

- `[패턴]` **LoginApp 합성이메일 vs 슈퍼관리자 신규 admin 이메일 mismatch — 사번 필드 입력값에 @ 있으면 합성 X**(2026-05-29): `LoginApp.js:28` 본래 `email = ${empNo}@buslink.com` 단일 패턴이라 슈퍼관리자 콘솔 `createCompany`/`createCompanyAdmin`이 직접 입력받은 admin 이메일(예: `wschoi@dybus.co.kr`)을 그대로 Auth에 등록한 사용자가 사번 필드에 같은 이메일 입력 시 `wschoi@dybus.co.kr@buslink.com`(이중 @) 합성 → Firebase Auth 거부 → 로그인 영구 실패. **해법** = `empNo.includes("@") ? empNo : 합성` 분기. 기존 기사 사번(empNo) 입력은 그대로 합성, 신규 admin 이메일은 그대로 통과. **회귀 가드**: 향후 LoginApp 입력 필드명 변경 또는 합성 패턴 단순화 시 같은 분기 유지 필수.
- `[패턴]` **Firestore stops 의 lat/lng 다중 형식(string/GeoPoint/{location:GeoPoint}) — typeof === "number" 엄격 필터 금지**(2026-05-29 HistoryTab 정류장 반경 시각화 결함): `routes/{id}/stops`의 lat/lng가 number 외에 string("37.5..."), GeoPoint({latitude,longitude}), nested({location:{latitude,longitude}}) 형식으로 저장될 수 있음(엑셀 import·Console 수기·SDK 차이). `typeof s.lat === "number"` 단일 검사로 필터하면 모든 stops 가 제외돼 마커·반경 0개 표시 결함. **해법 헬퍼** = `toLatLng(s)` 3분기 coerce(`s.lat`/`s.latitude`/`s.location.latitude`) + `Number(...)` 후 `isFinite()` 필터. **회귀 가드**: 신규 stops 표시 로직(지도·검증 도구)에는 항상 같은 헬퍼 사용. 단순 `typeof === "number"` 필터로 회귀 시 모든 stops 표시 0건으로 silent fail.
- `[미해결]` **EmployeeApp 내 정류장 복원 권한 오류(2026-05-26)**: prod 콘솔 `[내 정류장 복원 실패] Missing or insufficient permissions` (EmployeeApp.js:485, useEffect deps `[activeRouteId, companyId, session?.empNo]`). `fcmTokens/{empNo}` 또는 `routes/{id}/stops` 읽기 거부. 비치명적(try/catch 폴백 — 사용자가 정류장 재선택). 익명 인증 직후 onSnapshot 타이밍 또는 fcmTokens 미발급 직원 한정 가능성. 매뉴얼 캡처 세션에서 발견, 별건 진단 필요.
- `[패턴]` **모노디자인 PDF 매뉴얼 신설(2026-06-12, `docs/manual/`)**: 기존 PPT 매뉴얼이 "직관 활용 어렵다" 피드백 → 사용법 위주·모노디자인(무채색+단일 포인트색 `#0066FF`=tokens.css `--color-primary`) A4 PDF 3종(관리자/기사/직원). **스택**=marked(MD→HTML) + playwright-core 시스템 Chrome(`C:\Program Files\Google\Chrome\Application\chrome.exe`, 브라우저 다운로드 0)으로 `page.pdf({format:"A4"})`. 빌드=`cd docs/manual && node _to_pdf.mjs`. **스크린샷 재캡처 0** — `../manual-ppt/assets/{admin,driver,employee}/*.png` 재사용(`rewriteImgSrc`가 `assets/...`→`file://` 치환). **PPT assets 라벨↔내용 불일치 주의**: admin/02-sidebar=실제 대시보드·04-dispatch=배차 일정(배차 관리 아님)·driver/01-login=관제 로그인 배지·employee/04~06-home=홈(공지 강제모달 오버레이)·07-scan=공지목록·08-settings=설정. **placeholder**(`<div class="ph">` 점선박스)=운영 중에만 뜨는 화면: 관리자 배차/차량 관리·기사 운행중/QR발급/운행종료·직원 QR스캔 카메라. **납품물 PII(외부 공개·모자이크 후보)**: admin/02·04·05·06·08 의 기사 실명(동영스)·사번·연락처(01066772542)·차량번호(동영12테1234)·직원명(용동영)·협력사 업체코드(DY001-…-NDW0 등)·07 공지 발송이력 테스트문구. driver/02·employee 05~08 동일 PII. **차단 아님·보고 사항**(playbook 원칙). PDF 라이브 검증=시스템 Chrome 으로 HTML 풀페이지 PNG 스크린샷 후 Read(pdftoppm 부재).
- `[패턴]` **PPT 매뉴얼 도구 신설(2026-05-26~27, `docs/manual-ppt/`)**: callcenter 정본 복제 → 3 PPT(관리자·기사·승객). 관리자 10장 자동 캡처. 기사 03/04(운행 중 한정)·승객 02/03(설치 안내)·06(공지 탭, 자동화 실패)은 placeholder — 본인 폰 캡처해 `assets/<role>/NN.png`로 두면 다음 빌드 자동 반영. 자세한 자동화 트랩은 `.claude/agents/refs/manual-playbook.md` §PPT.
- `[미해결]` **firestore.rules 이중화**: 배포 정본은 루트 `firestore.rules`(`firebase.json` 지목, fcmTokens/notices/fcmQueue 포함). `src/firestore.rules`는 오래된 사본 — 수정해도 효과 없음. 규칙 변경은 반드시 루트 파일에.
- `[해결]` 런타임 정렬: `firebase.json` `nodejs22` → `nodejs20`(`package.json` `engines.node:"20"`, 2026-05-16). 데드라인 2026-10-30 전 22 재상향 필요(tasks.md 백로그).
- `[미해결]` `public/firebase-messaging-sw.js` Firebase 설정 **하드코딩**(env 아님). 프로젝트 변경 시 수동 동기화. `notificationclick`에 `buslink-prod.web.app`·fallback `dy001` 하드코딩.
- `[해결]` `companyId` 하드코딩 5지점 → **`src/lib/companyResolver.js` 통합(2026-05-28 SaaS Phase 1.1)**: `HOSTNAME_TO_COMPANY` 맵 + `resolveCompanyIdForAuth/Anon/resolveByHostname` 헬퍼. App.js Auth users 분기·DriverApp(2지점)·EmployeeApp·PassengerApp 모두 동적. SW(firebase-messaging-sw.js)는 build-time import 불가로 동일 맵 인라인 복제(동기화 필요 주석). dy001 폴백 유지 — 동영관광 운영 보호(명시 결정). 신규 회사 추가 시 HOSTNAME_TO_COMPANY 양쪽(js/SW) + companies/{cid} 도큐먼트(CF createCompany)만 갱신.
- `[해결]` `.env.example` 추가됨 (`5fa0737`, 2026-05-18) — 키 목록+가이드, 값은 .env.local에. 어느 PC든 부트스트랩 가능.
- `[해결]` **partnerCodes 복합 인덱스 누락**: `firestore.indexes.json`에 `partnerCodes (companyId ASC + active ASC)` 추가·배포(2026-05-16).
- `[해결]` **기사 onCall 4종 admin 역할 미검증**: `functions/index.js` `assertAdmin(request)` 헬퍼로 `users/{uid}.role∈{admin,superadmin}` 검증, 4종 진입부 적용·배포(2026-05-16). 익명 토큰만으로는 차단.
- `[보류]` **firestore.rules `passengers` write 개방** (`allow write: if isAuth()`): PartnerApp이 `signInAnonymously` 호출 안 함(App.js L48-49 분기 누락) → `request.auth==null` 이라 `isAuth()` 좁히기가 직원 등록·PIN초기화를 100% 차단. 안전한 제약엔 PartnerApp 익명화+규칙 재설계 동반 필요. ※ 현 규칙대로면 PartnerApp write 거부되어야 하는데 운영 정본이라는 점에서 **배포 실제 규칙 ≠ 저장소 정본** 가능성. 재설계 전 규칙 변경 금지.
- `[미해결]` **Kakao 키 임시 공유**: callcenter 앱 키 `58bf34…` 공유 사용(원 buslink 키 `d464b4…`는 `.env.local` 주석 보존). Geocoder/Places 일일 한도 callcenter와 공유(한쪽 급증=양쪽 장애). 비즈앱 심사 통과 후 전용 키 분리 → tasks.md 백로그. ⚠ 빌드 PC `.env.local` 활성 키가 `d464b4`면 prod 지도 전체 사망 — 빌드 전 앵커 grep 확인 필수(아래 `[패턴]`).
- `[해결]` **EmployeeApp `/p` 지도 흰화면 — 2026-05-18~19 2단계 해결**: 
  - ① **SDK 게이팅(2026-05-19, `1597097`)**: `public/index.html` `&autoload=false` 추가 + `App.js`를 `window.kakao.maps.load(cb)` 콜백 only로 게이팅(초기 state `false`). `autoload=false` 없으면 `window.kakao.maps` 가 엔진/services 초기화 **전** truthy → `<Map>` 미완성 엔진에 생성→영구 흰화면. 사용자 localhost 육안확인(`Map=true mapBox=1920x436`).
  - ② **컨테이너 0px init 방어(2026-05-18, `5fa0737`)**: `EmployeeApp.js` `<Map onCreate={m=>{m.relayout();setTimeout(()=>m.relayout(),300)}}>` — `appWrap 100dvh→content flex:1→HomeTab flex:1→map flex:'0 0 55%'` 체인이 init 시 0px일 수 있고 카카오는 CSS와 달리 자동 relayout 안 함. `/bus`는 고정 `height:100vh`라 무관. 즉시+300ms 더블 relayout = idempotent·무위험.
  - 두 방어선 공존 = 더 견고. **플렉스/동적높이 카카오 맵엔 항상 onCreate relayout + `autoload=false`+`maps.load(cb)` 부착.**

## 주의할 패턴

- `[패턴]` **admin 행 "✏️ 정보수정" — uid 기준 로그인 정보 수정 CF(`updateCompanyAdminProfile`) + role 가드 + 필드 보호**(2026-06-08, `main.c158506e.js` 빌드 미배포, +521B gzip, 신규 ESLint 경고 0): SuperCompanyTab "회사 관리" 탭 `AdminListSection` 행에 이름/이메일/비밀번호 수정 기능 추가. 기존 행 버튼은 `🛡 권한`/`🗑` 만 있어 로그인 정보 수정 불가였음. **신규 CF `updateCompanyAdminProfile`**(`functions/index.js`, `updateCompanyAdminPermissions` 다음): assertSuperAdmin + Admin SDK 우회. 입력 `{uid, name?, email?, password?}`(최소 1개). ① 대상 `users/{uid}` 읽어 **`role!=="admin"` 거부**(superadmin/driver 보호·권한상승 차단) ② name 1~30자·email 형식·password ≥6자(제공 시) ③ 변경 제공된 것만 Auth `updateUser(uid,{displayName?,email?,password?})` + `users/{uid}` `{name?,email?}` update — **role/companyId/empNo/allowedPartnerCodes 절대 미변경**(권한상승 벡터 차단·`updateCompanyAdminInfo`/`updateCompanyAdminPermissions` 패턴 미러) ④ email 중복=`already-exists` "이미 사용 중인 이메일입니다"·기타=`internal` ⑤ 자기 자신(uid===request.auth.uid)도 허용(role 가드는 유지). `updateCompanyAdminInfo`(companyId 기준=회사 첫 admin)와 달리 **uid 기준**이라 특정 admin 행 수정에 적합. **UI `EditAdminProfileModal`**(`AdminApp.js`, `EditAdminPermissionsModal` 앞): 이름(기본=현재)·이메일(기본=현재)·새 비밀번호(빈칸=변경 안 함)+확인. 변경된 필드만 payload 구성→호출→1초 후 onDone(목록 재로드). `S.overlay`/`S.modal` 기존 스타일·**배경 클릭 닫힘 없음**(overlay onClick 핸들러 없음, 다른 모달 일관·입력 중 실수 방지). 이메일 변경=로그인 ID 변경 한 줄 안내. 행 버튼 순서 `✏️ 정보수정`(self 도 허용·disabled 안 함) | `🛡 권한`(self disabled) | `🗑`(self disabled). **회귀 가드(복원 금지)**: ① CF role==="admin" 가드 제거 금지(superadmin/driver 정보를 슈퍼관리자가 우회 변경·권한상승 벡터) ② role/companyId/empNo/allowedPartnerCodes 를 update 패치에 추가 금지(권한 범위/소속/역할 변조 차단) ③ "✏️ 정보수정"을 self disabled 로 막지 말 것(슈퍼관리자 본인 admin 정보 수정은 의도된 허용). **보존 불변**: rules/indexes 변경 0(users write 는 이미 superadmin·CF Admin SDK 우회)·기존 권한변경(updateCompanyAdminPermissions)/삭제(deleteCompanyAdmin)/추가(createCompanyAdmin) 100% 보존·다른 CF 22종 시그니처·동작 불변·companyResolver/gps/stopSchedule/etaDiag/partnerAccess(Phase B) 무영향·EmployeeApp/PassengerApp/PartnerApp/DriverApp·SuperCompanyTab 회사 운영 메뉴(✏️편집/🔑비번/🗑삭제)·EtaDiagnosticCard 무영향·SuperCompanyTab(role==='superadmin') 분기 안에만. 검증: `node --check functions/index.js` 통과·`CI=false npm run build` 통과(`main.c158506e.js`, +521B gzip, AdminApp 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:updateCompanyAdminProfile,hosting`. **배포 금지** — 부모가 CF 핵심 로직 리뷰 후 배포.

- `[패턴]` **도착시간 0건의 진짜 근인 = 감지층(onStopReached) 미발화, 쓰기층 아님 — routePath 캡처 경합 + 좌표 포맷**(2026-06-02 `main.5539a881.js` 배포, +189B gzip, 신규 경고 0): 2026-06-01 라이브 운행 진단 JSON(runId `Mq3...`, 70포인트) 분석 — `nextIdx` 70포인트 내내 **0 고정**, `busProgress` 0→**14,742m**(전 구간 주행·GPS 정상), `stopArrivalsLog` **전 포인트 부재**, `recordStopArrival` 함수 로그 **호출 0건**, `vehicleSpeed` 45~62km/h 다수. → **onStopReached 가 운행 내내 0회 발화**(setCurrentStopIdx 도 안 됨) = recordStopArrival(2026-06-01 위임)은 트리거 하류라 작동 기회조차 없었음. **이전 진단 "근인=updateDoc silent fail" 은 부분 오진** — 쓰기 실패가 아니라 쓰기 시도 단계 도달 실패(감지가 안 됨). **왜 14.7km 주행에 감지 0**: ① 100m 직접 감지는 고속(45~62km/h)+GPS 30초 간격 = 샘플 간 ~400~500m > 정류장 반경 200m → 샘플 사이 통과로 거의 못 잡음(애초 백필을 만든 이유) ② routePath 진행률 백필(고속 주력 감지기)이 안 터짐 — busProgress 는 routePath 로 산출됨(routePath 존재 확정)·정류장 좌표는 AdminApp 이 `parseFloat` 로 number 저장(`AdminApp.js:1721`) → 두 조건 다 참이면 백필 무조건 작동해야 하는데 안 됨 → **남는 결론: `startGPS` 호출(운행 시작) 시점에 `routePath`(또는 `stops`) state 가 아직 로드 전이라 빈 배열 캡처 → usePath=false 세션 고정.** 진단의 busProgress 정상은 진단 코드가 *나중에 로드된* state 를 읽기 때문(divergence 가 결정적 증거). **수정 2종**: (1) `DriverApp.js` `routeDataReady` state — `loadStops` 진입 시 false·완료(stops+routePath fetch) 시 true, 운행 시작 버튼을 완료 전 disabled("노선 불러오는 중…") → startGPS 가 빈 routePath 캡처하는 경합 차단. **응답성 가드 보존**(handleStart 에 await 추가 안 함 — 게이트는 데이터가 이미 state 에 있을 때만 활성화). (2) `gps.js` `toLatLng(s)` coercion 헬퍼 신설 — `s.lat ?? s.latitude ?? s.location?.latitude ?? s._latitude`+`Number`+`isFinite`, stopProg 산출·detectStops 100m 직접 감지·백필 approxDist 의 `typeof==="number"` 엄격 비교/raw `s.lat` 를 전부 교체(number/string/GeoPoint 모두 수용) → 정류장이 AdminApp 외 경로(엑셀/콘솔)로 string/GeoPoint 저장됐어도 백필 작동(issues.md `stops lat/lng 다중 형식` 함정의 감지층 적용). **회귀 가드(복원 금지)**: ① routeDataReady 게이트를 routePath *non-empty* 가 아니라 *fetch 완료* 기준으로 유지(routePath 없는 노선도 정상 시작) ② handleStart 에 route 데이터 await 직렬 추가 금지(통신 불량 시 버튼 무반응 재발 — 2026-05-28 가드) ③ toLatLng 를 strict `typeof` 로 되돌리지 말 것(string/GeoPoint 좌표 백필 재사망). **보존 불변**: recordStopArrival 등 CF 22종·rules·indexes 변경 0·onStopReached/startGPS 시그니처·stopSchedule/useSmoothedEta/companyResolver/useOnlineRecover 무영향·gps.js getNearestStop/calcETA 미변경(EmployeeApp/PassengerApp myStop ETA 표면 보존 — 향후 필요 시 동일 coercion 확대 검토). **부수 발견**: 진단상 10개 정류장 중 **9개 offsetMin 미설정**(unplanned) — 감지 고쳐도 정류장별 계획·지연 시각 의미 있으려면 각 정류장 진입 오프셋 입력 필요(데이터 보강). 검증: `node --check gps.js` 통과·`CI=false npm run build` 통과(`main.5539a881.js`, DriverApp 신규 경고 0)·prod 라이브 해시 교체 확인. **내일 검증**: 기사폰 앱 **완전 종료 후 재실행**(설치형 PWA 는 재실행 전 옛 캐시 번들 70e47fc7 유지 — 동일 함정)→ "노선 불러오는 중…" 이 "운행 시작" 으로 바뀐 뒤 시작 → 운행 → 진단 JSON 에 stopArrivalsLog 항목 ok=true + 카드 "✅ N/M 통과" + recordStopArrival 함수 로그 호출 발생 확인.
  - **2026-06-02 06:56 KST 2차 운행 결과 = 동일 결함 지속**(runId `Mq3...1780350989956`, 52포인트): nextIdx 0 고정·recordStopArrival 호출 0건. 단 이 노선은 8정류장 전부 plannedAt 有, `nextStopProgress=4.6m`(정류장 routePath 투영 정상·좌표 number 확정). 그런데 `busProgress`가 첫 샘플부터 2742→14091m 인데도 백필 미발화 = `busProgress(2742) > stopProg(4.6)+40` 첫 샘플부터 참인데 안 잡힘 → **usePath=false(startGPS 가 빈 routePath 캡처) 확정**. 게이트(5539a881)로도 못 막았거나 **폰이 옛 캐시 번들을 돌린 것** — 진단 출력만으론 빌드 구분 불가가 한계. **2차 수정(`main.08da6948.js`, +173B, 신규 경고 0)**: ① **ref 기반 감지** — `gps.js startGPS` 에 `getStops`/`getRoutePath` getter 추가, `detectStops` 가 매 호출 시 `resolveStops()/resolveRoutePath()` 로 최신값을 읽고 routePath/stops 레퍼런스 변화 시 pathCum·stopProg 재계산(메모). DriverApp 은 `stopsRef`/`routePathRef`(렌더마다 state 동기) 의 getter 주입 → **startGPS 1회 캡처가 빈값이어도, 로드 타이밍·게이트 경합·재실행 무관하게 백필이 routePath 로드 즉시 작동**(once-captured 클로저 제거). ② **빌드 마커** — `DRIVER_BUILD="2026-06-02-detect-v2"` 를 진단 payload `appVersion` 에 첨부(`etaDiag.js` 옵셔널 필드) → 다음 운행 JSON 에 appVersion 있으면 새 빌드 확정, 없으면 옛 캐시 번들(reload 문제·코드결함 아님)로 즉시 판별. 게이트(routeDataReady)는 UX 로 유지. **보존 불변**: startGPS 미getter 호출처는 인자값 폴백(하위호환)·onStopReached/stopGPS/triggerHeartbeat 시그니처·sendGPS·CF·rules·indexes 무변경. **prod 배포 완료**(2026-06-02, `main.08da6948.js`, 사용자 명시 승인). **검증**: 재운행 JSON 의 `appVersion="2026-06-02-detect-v2"` 확인 → 있으면서 nextIdx 증가+stopArrivalsLog ok=true 면 해결 / appVersion 없으면 폰이 새 번들 미수신(완전 재설치 필요).

- `[패턴]` **클라이언트 update silent fail 우회 = onCall 위임 + 진단 로그 첨부**(2026-06-01 stopArrivals 결함, `main.70e47fc7.js` 빌드 미배포, +0KB gzip, 신규 ESLint 경고 0): 사용자 진단 JSON 분석 결과 stopArrivals 갱신 0건·`source:"stopArrivals"` 트리거 0개 확정 — `delaySec` 시간 흐름 따라 55→288→843→1098→1930s 누적(chain 미동작·plan + T_NOW gate fallback)이 "지연 14분 고정" 호소의 정확한 근인. `setCurrentStopIdx` 갱신은 정상(선바위역 "현재" 표시됨) = `onStopReached` 콜백 자체는 호출됨 → **`updateDoc(dispatches.../stopArrivals.{stopId})` Firestore write만 silent fail**(catch 가 console.warn 만 — 사용자 진단 불가). 근인 후보 = rules `dispatches update` 의 `drivers/{dispatch.driverId}.uid === request.auth.uid` 매핑 실패(drivers.uid 누락) 또는 affectedKeys 가드. **본질 우회 fix = onCall CF로 위임**(`recordStopArrival`, Admin SDK 가 rules 우회). 검증 = request.auth 필수 + users/{uid}.companyId 일치(타 회사 차단) + role∈{admin,superadmin,driver} + driver 면 dispatch.driverId 매핑 추가 검증(rules 패턴 미러). 멱등 = stopArrivals[stopId] 이미 있으면 `{ok:true,alreadyExists:true}` 즉시 반환(첫 도착만 기록 정책 보존, 백필 다중 호출에도 중복 0). **진단 로그 첨부**: DriverApp `stopArrivalsLogRef.current` 에 각 호출 결과 누적(`{stopId,ok,alreadyExists?,error?,code?,viaBackfill,ts}`, 최근 40건 유지) → `recordEtaDiagnostic` payload 에 `stopArrivalsLog: log.slice(-20)` 옵셔널 첨부(없으면 미저장 — undefined 필드 Firestore 거부 회피) → 다음 진단 JSON 으로 어느 정류장이 어떻게 기록됐는지 추적 가능(권한 거부·not-found·alreadyExists 등). 운행 시작 시 ref 초기화·종료 시 정리. **보존 불변**: firestore.rules 변경 0(향후 클라이언트 경로 제거 후 rules 좁힘은 별도)·다른 CF 시그니처·동작·gps.js·stopSchedule.js/useSmoothedEta.js/companyResolver.js/useOnlineRecover.js 무영향·카카오 SDK·PWA·익명 인증·rules·indexes 변경 0·AdminApp/PartnerApp/EmployeeApp/PassengerApp 무영향·DriverApp 외 변경 없음·`onStopReached` 콜백 시그니처 `(stop, dist, viaBackfill)` 그대로(gps.js 호출부 무영향). 검증: `node --check functions/index.js` 통과·`CI=false npm run build` 통과(`main.70e47fc7.js`, 340.57 KB gzip 변동 0, 신규 ESLint 경고 0). **회귀 가드(복원 금지)**: ① CF 의 driver role 추가 검증(drivers/{driverId}.uid 매핑) 제거 금지 — 타 기사가 본인 dispatch 위장 차단 ② users.companyId 일치 검증 제거 금지 — 타 회사 dispatch 침입 차단 ③ 멱등 가드(`existingArrivals[stopId]` 이미 있으면 즉시 반환) 제거 금지 — 백필 다중 호출 중복 기록 차단 ④ stopArrivalsLog 첨부가 undefined 일 때 payload 에 포함 금지(Firestore 가 undefined 거부). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:recordStopArrival,hosting`. **알림 영향 없음** — recordStopArrival 자체는 알림 발송 안 함(기존 `notifyPreArrival` 트리거가 stopArrivals 변경을 감지해 발송 — 즉 이제부터 정상 동작하기 시작하면 도착 임박 푸시도 정상 발화 시작). **사용자 검증**: 첫 배포 후 라이브 운행 시 진단 JSON 에 `stopArrivalsLog` 배열 첨부 확인 → 각 정류장 ok=true 이면 결함 해결. 일부 error 면 code/message 로 정밀 진단(예: permission-denied = users 도큐먼트 자체 누락).

- `[패턴]` **admin별 협력사 권한 인프라 — `users.allowedPartnerCodes` + 카드 펼침 관리 UI**(2026-05-29 Phase A, `main.33e1a717.js` 빌드 미배포, +2.52KB gzip, 신규 ESLint 경고 0): 한 회사 안 여러 담당자 admin 이 각자 자기 협력사만 관리하는 모델. **데이터 모델**: `users/{uid}.allowedPartnerCodes:string[]` 신규 옵셔널 필드 — `["*"]`=회사 본부(전체), `["code1","code2"]`=특정 협력사 한정, 필드 부재=`["*"]` 폴백(기존 admin 호환·마이그 불필요). **CF 4종 신설**(`functions/index.js`, 모두 assertSuperAdmin + Admin SDK 우회): ① `createCompanyAdmin({companyId,empNo,email,name,password,allowedPartnerCodes})` Auth+users 동시 생성·rollback(createCompany 패턴 미러)·email 중복=`already-exists` ② `updateCompanyAdminPermissions({uid,allowedPartnerCodes})` `allowedPartnerCodes` 필드만 update(role/companyId/empNo/email/name 보호)·role!=="admin" 거부 ③ `deleteCompanyAdmin({uid})` Auth+users 삭제·**자기 삭제 차단**(uid===request.auth.uid → `permission-denied`)·Auth catch 멱등 ④ `listCompanyAdmins({companyId})` users where companyId+role=="admin" → `{uid,empNo,email,name,allowedPartnerCodes}` 반환·`["*"]` 정규화·이름순. 공통 헬퍼 `normalizeAllowedPartnerCodes`(배열 검증+`"*"` 정규화+중복 제거+빈 배열 허용). **UI**(`AdminApp.js SuperCompanyTab`): 회사 카드 하단에 "▾ 👥 관리자 목록" 펼침 토글 신설 — 클릭 시 `listCompanyAdmins`+`partnerCodes(where companyId)` 병렬 lazy 로드(`Promise.all`)·`adminsByCompany` cache·재펼침 시 캐시 즉시 표시. 단일 펼침(`expandedCompanyId` 1건만)로 스크롤 컨텍스트 단순화. `AdminListSection` 컴포넌트=admin 행마다 이름·이메일·empNo·권한 배지(`"*"`=녹 "전체" / 그 외=primary "협력사 N개") + "🛡 권한"/"🗑" 버튼 + "+ 관리자 추가" 하단. **자기 자신 행 이중 안전망**: 카드에서 `isSelf=a.uid===currentUserUid` 면 권한/삭제 disabled + CF 도 `request.auth.uid===uid` 거부. **공용 헬퍼** `PartnerPermissionPicker(partnerCodes, value, onChange)` = "⭐ 전체 권한(*)" 별도 체크박스 + 그 외 다중 체크박스(전체 ON 시 disabled)·`AddAdminModal` 과 `EditAdminPermissionsModal` 공유. 기본값=신규 admin 추가 시 `["*"]`(가장 흔한 본부 케이스). `partnerCodes` read 는 rules 공개(`read:true`)라 onCall 없이 클라 `getDocs` 직접. **회귀 가드(복원 금지)**: ① CF 의 자기 삭제 차단 가드 제거 금지(슈퍼관리자 자신 admin 동시 superadmin 일 때 의도치 않은 lockout) ② `updateCompanyAdminPermissions` 가 `allowedPartnerCodes` 외 필드를 update 하도록 확장 금지(role/companyId 보호 — 권한 상승 벡터 차단) ③ allowedPartnerCodes 폴백 정책(부재=`["*"]`) 변경 금지(기존 admin 무권한 회귀 위험). **보존 불변**: rules/indexes 변경 0(`users` write 는 이미 superadmin 만·CF 가 Admin SDK 우회)·기존 17 CF(*/fetchEtaDiagnostic) 시그니처·동작 불변·AdminApp 11탭+회사 관리 탭 기존(회사 그리드/전환/활성토글/✏️편집/🔑비번/🗑삭제/EtaDiagnosticCard) 100% 보존·일반 admin/기사/직원/승객/협력사 화면 무영향(슈퍼관리자 분기 안에만)·companyResolver/useOnlineRecover/useWakeTick/gps.js/stopSchedule.js/etaDiag.js 무영향·LoginApp 합성/이메일 분기·카카오 SDK·PWA·익명 인증·SW·manifest 변경 0. **Phase B(미도입, tasks.md 후보)**: AdminApp 11탭이 로그인 admin 의 `allowedPartnerCodes` 를 읽어 협력사 필터를 자동 강제(`PartnerFilter` 기본값+선택지 제한). 현재는 인프라+관리 UI 만 구축·실제 권한 게이팅은 Phase B. 배포 명령(사용자 명시 요청 시): `firebase deploy --only functions:createCompanyAdmin,functions:updateCompanyAdminPermissions,functions:deleteCompanyAdmin,functions:listCompanyAdmins,hosting`.

- `[패턴]` **Phase B — AdminApp admin별 협력사 게이팅(클라 필터, 옵션+실데이터 양쪽)**(2026-06-08, `main.1f282c81.js` 빌드 미배포, +605B gzip, 신규 ESLint 경고 0): Phase A 가 누적만 하던 `users/{uid}.allowedPartnerCodes` 를 AdminApp 이 **실제 화면/데이터에 강제**. App.js 가 users data 에서 추출→`<AdminApp allowedPartnerCodes={...}>`(부재=undefined 그대로 전달)→`src/lib/partnerAccess.js`(순수, Firebase import 0) `resolveAllowed(role,codes)` 정규화: superadmin=`["*"]`·`"*"` 포함=`["*"]`·비배열/부재/null=`["*"]`(**핵심 폴백·회귀 0**)·빈 배열=`[]`(접근 0). `isAllAccess(allowed)`=`["*"]` 포함 → true 면 **현재 동작 100% 동일**로 분기. `partnerCodeAllowed(allowed,code)`=무제한 true·미지정(null/빈) code 는 제한 admin 비노출. **PartnerFilter** `allowedCodes` prop 추가: 무제한/미지정=기존 전체 옵션, 제한=allowedCodes 교집합 옵션만+"내 협력사 전체" 라벨, `[]`=드롭다운 대신 "접근 가능한 협력사 없음" 안내. **8지점 게이팅**(각 탭이 `allowed` prop 수신): 대시보드(matchPartner)·실시간관제 MapTab(routeId→partnerCode 로 라이브 vehicles+노선도 dispatches 필터, PartnerFilter 없는 탭이라 데이터만)·배차관리·배차일정(routeOf→partnerCode)·노선관리 RoutesTab(raw select 옵션 제한+r.partnerCode 필터)·운행이력(차량 vehiclePartnerOf + dispatch routeId 양쪽)·탑승통계(b.partnerCode + byPartner 집계)·협력사관리 PartnerTab(visibleCodes)·공지발송 NoticeTab. **핵심 = "전체" 선택 시에도 실데이터를 allowed 로 한정**(옵션만 줄이면 "전체"로 전체조회 우회 — 각 탭 기존 `partnerCode==="전체"` 분기에 `isAllAccess(allowed)||partnerCodeAllowed(...)` 교집합 추가). 특정 협력사 선택은 이미 드롭다운이 allowed 로 제한하므로 그대로. **공지 발송 특례**: `sendNotice` 는 단일 partnerCode 만 지원·"전체"=CF 가 null→회사 전체로 해석 → 제한 admin 이 "전체" 발송 시도하면 alert 차단("담당 협력사를 선택해 발송")·특정 협력사 선택 강제(CF 변경 0 으로 회사 전체 누수 차단). targetCount/tokensByPartner/발송이력도 allowed 로 게이팅. **회귀 가드(복원 금지)**: ① `isAllAccess`(superadmin/["*"]/미설정 폴백) 경로는 현재 코드 100% 동일 — 모든 신규 필터는 `!isAllAccess` 일 때만, isAllAccess 분기 제거/변형 금지 ② `resolveAllowed` 의 비배열/부재=`["*"]` 폴백 변경 금지(기존 admin 전원 무권한 회귀) ③ NoticeTab 제한 admin "전체" 차단 제거 금지(회사 전체 발송 누수). **보존 불변**: rules/indexes/CF/functions 변경 0(클라 게이팅만·Phase A 인프라 재사용)·회사 분리(companyId) 로직 무변경·EmployeeApp/PassengerApp/PartnerApp/DriverApp·stopSchedule/gps/companyResolver/etaDiag 무영향·SuperCompanyTab 기존(회사·admin 관리/EtaDiagnosticCard) 보존·DriverTab/VehicleTab/SimulatorTab 미변경. 검증: `CI=false npm run build` 통과(`main.1f282c81.js`, +605B gzip, 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only hosting`(프론트만·rules/CF/indexes 변경 0). **한계 2건(후속)**: ① **제한 admin 이 협력사관리 탭에서 신규 발급한 코드는 자기 권한에 자동 추가 안 됨** — `users` self-update 는 rules 상 superadmin 만 가능 → 슈퍼관리자/팀장이 `updateCompanyAdminPermissions` 로 권한 부여해야 본인 목록에 나타남(자동 부여는 복잡·이번 범위 외). ② **클라 필터는 우회 가능**(F12·직접 SDK 호출) → SaaS 보안상 rules 의 `isAdmin` 에 협력사 제약(예 dispatches/boardings read 를 allowedPartnerCodes 교집합으로) 추가는 별도 후속 — 현재 회사 분리(companyId)만 rules 강제. ③ 차량/기사 탭은 협력사 직접 매핑 약해 게이팅 미적용(전체 표시 유지 — 과도 제한으로 기존 기능 깨지 않기). dashboard "전체 기사" 카운트도 회사 전체(노선 무관).

- `[패턴]` **슈퍼관리자 회사 운영 메뉴 4종 — 편집/비번 초기화/관리자 정보/영구 삭제 cascade**(2026-05-29 SaaS Phase 1.2+, `main.fa48b1cb.js` 빌드 미배포, +1.93KB gzip, AdminApp 신규 ESLint 경고 0): SuperCompanyTab 카드에 운영 메뉴 3 버튼(✏️ 편집/🔑 비번/🗑 삭제) + 3 모달(`EditCompanyModal`/`ResetPasswordModal`/`DeleteCompanyModal`). CF 4종 신설 — 모두 `assertSuperAdmin` 게이트, Admin SDK 로 rules 우회(rules 변경 0). **사용자 결정**=비활성(`toggleCompanyActive`, 기존)과 영구 삭제(`deleteCompany`, 신규) 양쪽 제공. **cascade 범위**: ① 최상위(`gps`/`partnerCodes`/`boardingTokens`/`passengerTokens`/`fcmQueue`) where companyId==cid → 500개 청크 batch delete ② `gpsHistory/{cid}` recursiveDelete ③ `companies/{cid}` recursiveDelete(routes/stops/dispatches/drivers/vehicles/passengers/boardings/fcmTokens/notices 일괄) ④ `users` where companyId==cid → 각 uid `admin.auth().deleteUser` + 도큐먼트 delete. **한계 명시(주석)**: `etaDiagnostics`는 runId 가 driverId 만 포함 → companyId 직접 매칭 불가 → cascade skip(진단 데이터·일자별 자연 정리). 후속에 runId 에 companyId 포함하면 cascade 가능. **자기 회사 보호**(이중 안전망): ① 카드 삭제 버튼 `disabled={c.id === currentUserCompanyId}` + 안내 ② CF가 호출자 `users/{request.auth.uid}.companyId === companyId` 면 `permission-denied`("자기 소속 회사는 삭제할 수 없습니다"). **편집 모달 멀티 onCall**: 회사명·관리자 이름·이메일·비밀번호 4필드 입력 → 변경된 필드에 대해서만 `updateCompanyInfo`/`updateCompanyAdminInfo`/`resetCompanyAdminPassword` 순차 호출(비어둔 필드는 변경 안 함). **영구 삭제 모달**: 빨간 강조(`borderTop` destructive)·companyId 재입력 input·일치 시에만 "영구 삭제" 버튼 활성·자기 회사면 처음부터 안내 카드만(form submit 차단). **회귀 가드**: ① rules/indexes 변경 0(전부 Admin SDK 우회) ② AdminApp 11탭 + 회사 관리 탭 기존 기능(회사 생성·비활성 토글·전환·EtaDiagnosticCard) 100% 보존 ③ 일반 admin/기사/직원/승객/협력사 화면 무영향(슈퍼관리자 분기 안에만) ④ companyResolver/useOnlineRecover/useWakeTick/gps.js/stopSchedule.js/etaDiag.js 무영향 ⑤ CF 기타 13종 시그니처·동작 불변. **부분 실패 시 throw — 멱등성**: deleteCompany 가 중간 실패해도 사용자 재시도하면 이미 삭제된 docs 는 자연 skip(query 가 빈 결과)·Auth 계정도 `.catch(()=>{})` 로 멱등. 검증: `node --check functions/index.js` 통과·`CI=false npm run build` 통과(`main.fa48b1cb.js`, +1.93KB gzip, AdminApp 신규 경고 0). **배포 명령**(사용자 명시 요청 시): `firebase deploy --only functions:updateCompanyInfo,functions:resetCompanyAdminPassword,functions:updateCompanyAdminInfo,functions:deleteCompany,hosting`. **회귀 가드(복원 금지)**: ① deleteCompany 자기 회사 보호 가드 제거 금지(슈퍼관리자 lockout 위험) ② confirmCompanyId 재입력 검증 제거 금지(오삭제 위험) ③ 최상위 컬렉션 cascade에서 한 컬렉션 누락 시 정합 깨짐 — 새 최상위 컬렉션 추가 시 `topLevelTargets` 배열 동기 갱신 필수.

- `[패턴]` **운행 이력 정류장 반경 시각화 검증 도구**(2026-05-29 AdminApp HistoryTab, `main.c7fa8819.js` 빌드 미배포): 정류장 도착 감지 실패(0회 통과 등) 진단 시 GPS 경로와 정류장 위치/100m 반경의 공간적 관계를 한눈에 보는 도구. 카카오 SDK `Circle` 컴포넌트 import 추가(import line L2) → HistoryTab 지도 안에 ① `<Circle radius={100}>` 정류장 100m 반경(통과=#0066FF/fillOpacity 0.18 / 미통과=#aaa·#666 stroke/fillOpacity 0.12) ② `<CustomOverlayMap yAnchor=1.4>` 정류장 이름 라벨(통과=`var(--color-positive)` 녹배경+#fff / 미통과=`var(--color-bg)`+회색·라벨 maxWidth 140·ellipsis) ③ 좌상단 토글 컨트롤 바(🎯 반경 / 📍 마커 체크박스 2개, 기본 둘 다 ON) ④ 좌하단 범례(통과/미통과/100m 반경). 카드 보조 표시 `✅ N/M 통과`(stops 미로드 시 "—"). **stops lazy fetch**: BoardingStatsTab 패턴 차용 — `dispatches` 의 distinct routeId 집합을 `stopsByRoute` Map 캐시에 `getDocs(orderBy order asc)` 1회 로드(onSnapshot 아님 — routes 변경 빈도 낮고 라이브 갱신 불필요). **회귀 가드**: ① 기존 GPS 경로 Polyline·출발/도착 MapMarker·selected 포인트 마커·infoBox·"왼쪽에서 선택" 안내 100% 보존 ② 협력사 필터·차량 직접 선택·노선별 배차 그룹·시간범위 필터·`new window.Map()` Map shadow 가드·`dispatchTimeRange`·`dispatchSummary` 시그니처 보존(arrivedCount 그대로 + totalStops 추가만) ③ AdminApp 다른 11탭/DriverApp/PassengerApp/EmployeeApp/PartnerApp 무영향 ④ stopSchedule.js/useSmoothedEta.js/etaDiag.js/companyResolver.js/useOnlineRecover.js/gps.js 시그니처·동작 불변 ⑤ rules/CF/indexes 변경 0 ⑥ stops 미로드(routes/stops 권한 거부 등) 시 try/catch 폴백 — 카드는 "—" 표시, 지도는 토글 ON이어도 validStops 0건이라 자연스럽게 비표시. **운영 사용**: 정류장 통과 0회 운행 카드 클릭 → 지도에 회색 원 N개 + 파란 경로 → 경로가 어느 회색 원에도 진입 안 했으면 routePath/실 경로/정류장 좌표 불일치 → 정류장 좌표 재검수. 진입했는데도 통과 미인식이면 GPS 정확도 노이즈 의심.

- `[패턴]` **routePath 진척률 안분 + delaySec raw 분리**(2026-05-29 `stopSchedule.js` 보강): 사용자 폰 실측 결함 3건 — ① "직전 정류장 정시보다 빨리 와도 지연이 계속 누적" ② "승객앱에 말도 안되는 분수가 나옴" ③ "정류장 직전에 갑자기 곧 도착으로 점프". 근인 = chain plan-first(2026-05-28) 도입 후 next 정류장 ETA가 정류장 통과 시점에만 갱신되어 정류장 사이는 정적(actualLast+plan 델타) → T_NOW만 흐르며 자연 감소 → 정류장 도착 직전 큰 점프 → useSmoothedEta jumpThresholdSec 300 통과 시 raw 즉시 채택 → "갑자기 곧 도착" 도약. 부수 결함: 단조증가(MIN_STOP_GAP_SEC=60)·과거금지(MIN_FUTURE_BUFFER_SEC=30) 가드가 가드 적용 후 estMs로 delaySec 산출하면서 음수(조기) 지연을 양수로 뒤집을 수 있음(사용자 호소 #1). **해법**: (a) `computeStopEstimates` next 정류장 한정 routePath 진척률 안분 — `actualProgress=(busProgress-stopProgress[i-1])/segDist`, `expectedByTime=elapsed/expectedTotalMs`, `slowFactor=max(1, expectedByTime/actualProgress)`(빠른 방향 1 클램프 — 갑작스러운 "곧 도착" 점프 차단), `remainingMs=(1-actualProgress)*expectedTotalMs*slowFactor`, `estMs=T_NOW+remainingMs`. actualProgress≥0.95=ARRIVING_BUFFER_SEC(45초) 단순 표시·≤0.05=노이즈 skip. source='gps'(라벨 'GPS 추정'). chain 채택 *후* 진척률 덮어쓰는 구조 — 진척률 입력 부재(routePath 없거나 startStop도 미통과) 시 chain 그대로(회귀 0). (b) `delaySec` 계산은 가드 적용 *전* raw 후보(chain→plan→est 순) 기준 — estMs(표시 시각)는 가드 적용 그대로(단조증가/과거금지 정합), delaySec(지연 라벨)은 raw 의도 반영. arrived는 (actualMs-plannedMs) 그대로(불변). (c) `useSmoothedEta jumpThresholdSec 300→180` — 진척률 안분으로 자연 변동 → 정류장 통과 시점의 의미 있는 변경만 즉시 raw, 자연 감소·노이즈는 EMA로 흡수. **회귀 가드(복원 금지)**: ① routePath 진척률 안분을 GPS 가중 재도입 회피라 해서 제거 금지(점프 결함 재발) ② delaySec를 가드 후 estMs 기준으로 되돌리지 말 것(음수 지연 뒤집힘 재발) ③ slowFactor 빠른 방향 클램프 제거 금지(actualProgress가 일시적으로 큰 값일 때 ETA가 0으로 도약). **출력 계약**(stopId/plannedAt/estimatedAt/delaySec/status/source enum 4종) 절대 불변·next GPS 가중 0%·MIN_STOP_GAP_SEC=60·MIN_FUTURE_BUFFER_SEC=30·DWELL_SEC=30·MIN_EFFECTIVE_SPEED_KMH=20 보존. useSmoothedEta/formatPassengerEta/describeEtaSource/formatDelayLabel/onStopReached/startGPS 시그니처·useWakeTick·rules/indexes/CF 무변경. 진단 오버레이 `[DIAG-ETA 제거예정]` 4지점(stopSchedule 주석/DriverApp 운행 토글/PassengerApp `?debug=1`/EmployeeApp `?debug=1`) — 사용자 폰 캡쳐 후 grep 1줄로 통째 제거.

- `[패턴]` **협력사 공지 발송 — onCall + partnerCodes rate-limit 인라인 + 기존 sendNoticeToCompany 트리거 재사용**(2026-05-29 Phase 1.4 `sendPartnerNotice`): 익명 인증 + 코드 인증 흐름의 PartnerApp 이 자기 직원 공지 발송 — 클라가 notices/fcmQueue 직접 create 는 admin-only 룰에서 차단됨 → 신규 onCall 이 ① partnerCodes 검증(exists+active+companyId 일치) ② 서버측 rate-limit(`partnerCodes/{code}.recentNoticeTimestamps:number[]` 배열, now-1h 이내 ≥5면 `resource-exhausted`, 옛 항목 정리 + now 추가로 무한증가 방지) ③ title/body trim+범위 검증 ④ Admin SDK 로 notices(sender:"partner"/senderCode)·fcmQueue create(룰 우회) — 그 후는 기존 `sendNoticeToCompany` 트리거가 partnerCode 필터 멀티캐스트(인프라 재사용·신규 발송 코드 0). **회귀-0 보장**: ① rules 변경 0(partnerCodes update·notices/fcmQueue create 모두 CF Admin SDK) ② notices/fcmQueue 스키마 100% 호환(sender/senderCode 옵셔널 — admin 발송분은 미존재, 다운스트림 read 코드 무영향) ③ rate-limit 저장처는 별도 컬렉션 신설보다 인라인 배열(`partnerCodes.recentNoticeTimestamps`)이 V1 운영 단순(별도 컬렉션 가비지 콜렉션 부담 0) ④ 서버 시각 기준(`Date.now()` server side) — 클라 시각 조작 차단. **클라 가시화**: `partnerCodes/{code}` onSnapshot 으로 "시간당 남은 발송 N/5건" 실시간 표시 → 다른 세션 발송도 즉시 반영·발송 후 자동 업데이트. **회귀 가드(복원 금지)**: 같은 컬렉션 분리(rateLimit/{code} 별도 컬렉션 이전) 금지 — V1 운영 단순 우선, 부하 증가 시에만 분리 검토. CF·UI 검증 한도(50/500/5건) 동기화 필수 — CF 가 권위 게이트, UI 는 placeholder/카운터.

- `[패턴]` **PartnerApp 운영 포털 구조 — STEPS.MAIN 4번째 mainTab + 인라인 OperationsMode**(2026-05-28 Phase 1.3, `main.dd6a87da.js` 빌드 미배포): 협력사 담당자가 자사 노선만 실시간 조회. **별도 경로 신설(`/partner-ops` 등) 비채택** — 진입 흐름(업체코드 인증 STEPS.CODE → STEPS.MAIN)·익명 인증·partnerCode 필터·routes preload 모두 재사용 → mainTab "ops" 4번째 탭이 가장 적합. 4구성: ① 자사 노선 요약 카드(passengers where partnerCode·active로 routeId 집합·노선별 직원수, 카드 클릭 시 노선 필터 토글 — useState routeFilter null=전체) ② 실시간 버스 위치(KakaoMap 40vh — `import { Map as KakaoMap }`로 native Map shadow 회피, `onCreate={m=>{m.relayout();setTimeout(()=>m.relayout(),300)}}` 컨테이너 0px init 방어 필수, key={routeFilter||"all"}로 노선 전환 시 재마운트, gps onSnapshot은 companyId 동등 + 클라이언트 vehicleId 매칭, 노선 폴리라인 routePath 우선 stops 직선 폴백, useAnimatedPositions rAF 보간) ③ 오늘 탑승 현황(boardings/{today}/list where partnerCode==code onSnapshot — 단일 컬렉션 동등매칭 자동 단일필드 인덱스로 처리·복합 인덱스 신규 0, 카드 3개+노선별 상위 5 미니 차트, 상세는 stats 탭 안내) ④ 공지 수신함(notices where active+orderBy createdAt desc limit 20 + 클라이언트 partnerCode null||== code 필터, 기존 `notices(active+createdAt)` 인덱스 재사용·EmployeeApp NoticesTab 패턴 일관). **App.js needsKakao에 isPartnerRoute 추가**(이전 미포함 → PartnerApp 진입 시 kakao 로드 안 함, 최대 5초 대기 추가·기존 EmployeeApp/PassengerApp 동일 패턴 회귀 표면 0). **onSnapshot 4개(passengers·dispatches·gps·boardings·notices) deps에 wakeTick + recoverTick 필수**(백그라운드/오프라인 복귀 자동 재구독). 공지 발송(write) V1 미포함 — Phase 1.4 분리(보안 표면 신규 검토). rules/indexes/functions 변경 0. BoardingStatsMode/EmployeeManageMode/AdminApp/Driver/Passenger/Employee 코드 변경 0. 카드 너비는 ops 탭 한정 760(기존 multi 720·default 480 보존). 회귀 가드: ① register/manage/stats 탭 100% 보존(STEPS.MAIN 기본 mainTab="register" 유지) ② notices 클라이언트 partnerCode 필터를 서버 인덱스 신설로 옮기지 말 것(EmployeeApp NoticeTab과 패턴 분리 회귀 위험) ③ 공통 헬퍼 추출(노선 카드/지도/탑승통계 미니) 비채택 — V1은 OperationsMode 내부 인라인, Phase 1.4 사용자 검증 후 추출 검토. **신규 위임 작업 시**: 사용자 호소("탑승률 안 보임" 등) 시 partnerCode denormalize 결손(`passenger-sync 정공법 = passengers/{empNo}.partnerCode 데이터 부여` 패턴 우선 진단).

- `[패턴]` **ETA chain plan-first(2026-05-28) — segTravelMs를 계획 offsetMin 델타 우선·next GPS 0% + ArrivalProximityModal + useOnlineRecover 패턴**: 사용자 호소 = "터널에 들어가면 ETA가 갑자기 늘었다 줄어듬"·"각 정류장 실제 도착시간 기준으로 다음 정류장까지 계획시간으로 정밀 예측해야". 근인 = ① `segTravelMs`가 routePath 거리/유효속도 + DWELL_SEC만 사용해 `offsetMin`(계획 정류장 간 간격)을 무시 ② next 정류장이 chain·gps 0.7:0.3 가중이라 GPS 노이즈(터널 속도 0/30/70km/h 출렁임)가 직접 노출. **해법** = (a) `segTravelMs(a,b)`를 plan-first로: 둘 다 offsetMin 있으면 `(offsetB-offsetA)*60*1000`(DWELL 가산 X — offsetMin 자체가 정차 포함된 진입시각 차이) — 한쪽 누락이면 geometric 폴백 유지. chain 전파 결과 = `actualLast + Σ(plan 델타) = plannedMs[i] + (actualLast-plannedLast)` 수학적 등가. (b) next 정류장 GPS 가중 **완전 제거(0%)**: chain 우선·없으면 plan+delay·없으면 gps(상수 보존). gpsCandidate 계산 자체는 보존(나중에 재도입 위해). source enum 4종(actual/plan+delay/gps/fallback) 호환 + 단조증가/과거금지/MIN_EFFECTIVE_SPEED_KMH 보호장치 전부 유지. (c) **ArrivalProximityModal**: 내 정류장(myStopIdx) 직전(myStopIdx-1)이 stopArrivals에 새로 등장(최근 5분) 시 PassengerApp(`/bus`)·EmployeeApp(`/p` HomeTab) 풀스크린 모달("○○역에 곧 도착, 다음이 내 정류장"). dismissed ref·routeId 변경 시 reset·myStopIdx==0이면 비표시. notifyPreArrival CF FCM과 별도 인-앱 보장 통로(중복 무해 — NoticeForceModal 패턴 준용). (d) **useOnlineRecover 훅 신규**(`src/lib/useOnlineRecover.js`, 30줄): `window.online` 이벤트 + 첫 마운트 skip + 옵션 `forceFirestoreReconnect`로 `disableNetwork(db)→enableNetwork(db)` 1회 강제. DriverApp/PassengerApp/EmployeeApp 셋 다 적용해 통신 끊김→복구 시 onSnapshot 재구독·Firestore reconnect 강제 → 노선 변경/재시작 없이 자동 활성화. DriverApp은 추가로 driving=true 시 online 복귀 즉시 1회 `triggerHeartbeat(watchId)` 호출(gps.js heartbeatRegistry로 lastPos 캡쳐·외부 노출) — 승객앱 마커 즉시 신선화. (e) **운행 시작/종료 버튼 응답성**: `handleStart`/`handleStop`의 `await updateDoc`/`await clearGPS`가 통신 불량 시 hang하여 `setDriving(true/false)`가 영영 실행 안 되던 결함 차단 — 로컬 state·startGPS/stopGPS 즉시 실행, Firestore 쓰기·refreshToken·WakeLock은 fire-and-forget(`.catch` 로 graceful). **회귀 가드(복원 금지)**: ① segTravelMs를 distance/speed-only로 되돌림 금지(누적지연 carry 결함 재발) ② next GPS 가중 재도입 금지(터널 노이즈 직접 노출) ③ handleStart/handleStop을 await 직렬로 되돌림 금지(통신 불량 시 무반응 재발). useSmoothedEta/formatPassengerEta/describeEtaSource/formatDelayLabel/onStopReached/startGPS 시그니처·useWakeTick 적용지점·rules/indexes/CF 절대 불변. issues.md 패턴 등록 + tasks.md 사용자 검증 항목.

- `[패턴]` **양방향 탑승 QR — 별도 컬렉션 + JSON 페이로드 + 노선/협력사 2단 fallback**(2026-05-28): 카메라 권한 없는 협력사 직원용 역방향(직원 발행 → 기사 스캔). 신규 컬렉션 `passengerTokens/{tokenId}` 분리(boardingTokens와 ① rules 영향 좁고 ② 만료/소각 정책 독립 = 2분 vs 5분 ③ 통계 boardings 스키마는 동일 함수로 호출해 다운스트림 무영향). QR은 URL(`/board?t=`) 대신 **JSON 페이로드**(`{v:1,t:"passenger",tokenId,companyId,empNo}`) — 기사앱이 외부 브라우저 튀는 사고 방지·딥링크 라우팅 불필요. **모드 우선순위 = `routes.boardingMode` > `partnerCodes.boardingMode` > `'driver-qr'`**(노선 단위 override 우선·혼승 노선 대응 = 여러 협력사 직원이 같은 버스 공유 시 노선 명시가 일관성 보장). 보안 가드: ① 2분 만료·1회 소각 ② `token.companyId === driver.companyId` 일치 검증(다른 회사 토큰 차단) ③ 기사 스캔은 운행 중(`driving=true`)일 때만. **改名+래퍼 패턴**: 기존 `ScanTab` 본체를 `ScanTabDriverQR`로 改名 + 동명 `ScanTab` 래퍼가 mode 조회·분기 → 호출부 변경 0·partnerCode 없는 케이스/조회 실패 모두 즉시 기본 모드 폴백 → 회귀 0 자동 입증. **boardings 스키마 100% 동일**(`validateAndBoardByDriver`가 기존 `validateAndBoard`와 동일 필드 셋: empNo/name/tokenId/companyId/routeId·Name/vehicleId·No/driverId/stopId·Name/partnerCode/vehicleLat·Lng·Speed/boardedAt) → BoardingStatsTab·BoardingStatsMode·stopMapping.js 모두 무영향. partnerCode/vehicleLat·Lng 캡처도 동일 패턴 복제.

- `[패턴]` **도착 임박 푸시 = 내 정류장 denormalize + onDocumentUpdated diff 트리거 + 마커 멱등**(2026-05-22): 직원 '내 정류장'에 버스가 2/1 정거장 전 도달 시 서버(CF) FCM 발송(앱 꺼져도 도달). **내 정류장은 원래 EmployeeApp `myStopIdx` 메모리 state 뿐 → Firestore 미저장 → 서버가 직원별 타겟 모름**. 해법 ① `fcmTokens/{empNo}`에 `routeId`+`stopId` denormalize(`setDoc` merge, `initNotifications` 토큰 upsert와 충돌 0) → 푸시 CF가 fcmTokens 한 컬렉션 where로 대상 검색. 부수 효과로 새로고침 시 내 정류장 복원(저장 routeId==activeRouteId 일 때만)도 동시 해결. 선택/해제/노선변경 모두 영속(해제·노선변경=null). ② 신규 CF `notifyPreArrival`=`onDocumentUpdated(dispatches/.../list/{id})` — 기사앱이 `stopArrivals` 갱신하는 걸 트리거 신호로 씀. before/after `stopArrivals` diff로 새 도착 stopId, route stops를 `order` 오름차순 정렬한 배열에서 도착 정류장 중 **최전방 위치(index)=K**(GPS 복구 백필이 한 콜백에 여러 정류장 기록 가능 → 버스 실 최전방), 정렬 배열의 K+1/K+2 위치 stopId가 대상(raw order 산술 아님 — order 결번 안전, `computeStopEstimates`와 동일 순회 패턴). ③ **멱등 2중 가드**: diff 기반 + `dispatch.preArrivalNotified: string[]` 마커(`"{stopId}:pre1"`/`"pre2"`, `arrayUnion`) — onDocumentUpdated는 같은 문서 update마다 재발화하므로 마커 없으면 stopArrivals 다른 필드 변경에도 중복 푸시. 발송 대상 0명이어도 마커는 기록(재평가 비용 차단). ④ **인덱스**: fcmTokens `routeId`+`stopId` 동등 필터 2개 — 단일 컬렉션 동등매칭은 자동 단일필드 인덱스 조합으로 처리, 복합 인덱스 신규 불필요. ⑤ v1 한계(주석 명시): routeId+stopId만 타겟 → 같은 routeId 다른 shift 배차에도 발송 가능 — shift별 정밀 타겟은 후속. fcmTokens write 룰은 `isAuth()` 필드무제한이라 직원의 routeId/stopId 추가 허용 — rules 변경 불필요.

- `[패턴]` **인앱 공지함 = 푸시 도달 보장 pull 폴백 + 사번별 localStorage 읽음 추적**(2026-05-22): PWA 푸시는 OEM 절전(삼성 딥슬립 특히)으로 누락 가능 → 푸시를 "즉시성 보너스"로 두고 인앱 공지함을 도달 보장 통로로 둠. EmployeeApp `/p`에 5번째 "공지" 탭(`NoticesTab`) 신설 — 기존 공지배너의 `notices`(`where active==true`+`orderBy createdAt desc`) 구독을 **부모 레벨에서 전체 목록으로 확장**(배너는 `visible[0]` 한 건, 공지함은 전체 공유 — 구독 1개). **partnerCode 필터는 클라이언트**(`n.partnerCode||null === null || === 세션 partnerCode`) → 기존 `notices(active+createdAt)` 인덱스 그대로 재사용, 신규 복합 인덱스 회피(공지배너와 동일 규칙). **안 읽음 배지**: `buslink_notice_read_{empNo}` localStorage 키(사번별 분리·`buslink_` 접두사 컨벤션)에 마지막 공지함 진입 시각(ms) 저장 → `createdAt > readAt` 공지 수를 탭 아이콘 배지로. 진입 시 읽음 시각 = `max(Date.now(), 최신공지+1)`(목록보다 미래로 강제해 안 읽음 0 보장). `createdAt`은 serverTimestamp(`toMillis()`)·number 둘 다 정규화. 공지 구독 `useEffect` deps에 `useWakeTick` 포함 필수(백그라운드 복귀 신선화 — issues.md 패턴). 배너 본문 탭 → 공지함 탭 이동(연결 통로).

- `[패턴]` **OEM 절전 배터리 안내 = UA 분기 텍스트 카드(PWA는 시스템 설정 못 엶)**(2026-05-22): 삼성 갤럭시 딥슬립 등 OEM 절전이 SW를 잠재워 공지 푸시 누락 → SettingsTab에 "BusLink를 절전 예외로" 안내 카드. `detectBatteryGuidePlatform()` UA 헬퍼 — `SM-[A-Z0-9]+`/`SamsungBrowser`/`Samsung` → 갤럭시용 단계, 그 외 `Android` → 일반 단계, **iOS·데스크톱은 null → 미표시**(iOS는 배터리 최적화 개념 없음). PWA는 시스템 설정 화면을 열 API가 없어 **텍스트 안내만** — One UI 등 버전차 감안해 메뉴명 너무 구체적이지 않게(`설정 → 배터리 → 백그라운드 사용 제한/앱 절전 관리에서 제외`) + "기종·버전마다 메뉴명 다를 수 있음" 단서 동반.

- `[패턴]` **InstallPrompt 단계 일러스트 + `InstallGuide` named export 분리(바텀시트·설정 인라인 공유)**(2026-05-22): iOS 설치 안내를 1줄 텍스트 → 단계 번호 일러스트 바텀시트(①Safari 공유 버튼 탭 — 하단 툴바 모형에 화살표로 공유 위치 지시 ②'홈 화면에 추가' ③'추가'). `ShareGlyph`/`AddToHomeGlyph` SVG + `SafariToolbarHint`/`StepRow` 헬퍼. **가드 없는 `InstallGuide`를 named export로 분리** → `InstallPrompt`(자동 바텀시트, standalone/스누즈 가드) 와 SettingsTab 상시 "📲 앱 설치하기"(3일 스누즈 무관 진입)가 같은 단계 UI 공유. `InstallGuide platform="auto"`는 기기 감지(iOS→ios·그 외→android-manual). **불변 보존**: `InstallPrompt.js` Firebase import 0(순수 프레젠테이션+브라우저 API), `android`/`android-manual` 모드, BIP 글로벌 stash 다중 소비자 공유(`window.__buslinkDeferredBIP` 절대 null로 비우지 않음 — issues.md BIP 패턴), `appinstalled`/스누즈 가드 그대로.

- `[패턴]` **PWA 알림 아이콘 정본 = `/icons/notification-employee.png` (CRA `logo192.png` 사용 금지)**(2026-05-27): CRA 초기화 시 자동 생성된 `public/logo192.png`는 **React 기본 로고**(청록 원자 그림). 알림 아이콘으로 두면 ① 본문 옆 큰 아이콘 = React 로고 표시 ② 안드로이드 상태바 badge 변환 시 모노크롬 silhouette 실패해 Chrome 폴백 → 사용자가 "Chrome 아이콘"으로 인식. **해법**=`public/icons/notification-employee.png`(passenger-1024.png 사본) 신설 + CF 2곳(`sendNoticeToCompany`/`buildPreArrivalMessage`) `webpush.notification.icon`+`badge` 교체 + SW `firebase-messaging-sw.js` `showNotification` icon/badge 교체 + `manifest-employee.json icons`에 192~512 사이즈 등록(WebAPK 발신앱 표시 개선). **별건**: 안드로이드에서 알림 발신자가 "Chrome `<URL>`" 형태로 표시되고 본문이 "탭하여 URL 복사하기"면 = **WebAPK 미설치**(Chrome 단축으로만 설치). 정상 WebAPK는 발신앱="BusLink 직원"·URL 미표시. 사용자 재설치(`Chrome → 메뉴 → 앱 설치`, 홈 화면 추가 ❌) 안내가 정공법 — 사이트가 강제 못 함.

- `[패턴]` **PWA 푸시 OS 누락 대비 = 강제 공지 모달(앱 단 도달성 보장)**(2026-05-27): 안드로이드 OS 절전·Doze로 PWA 푸시는 100% 도달 보장 불가, 사이트가 시스템 설정 강제 못 함(`PWA는 시스템 설정 화면을 못 엶` 한계). **해법**=`NoticeForceModal` 풀스크린 모달 — EmployeeApp 마운트 시 `notices` onSnapshot + `noticeReadAt` localStorage 기반 `unreadCount > 0`이면 가장 최신 안 읽음 공지 자동 노출, "확인했습니다" 클릭 시 `markNoticesRead()` 호출 → `unreadCount=0` → 자동 사라짐. **type별 분기**: 일반=즉시 닫기 / `type==='emergency'`=5초 카운트다운 + `navigator.vibrate([200,100,200,100,200])` 진동. 새 공지 도착 시 자동 재노출(별도 dismissed state 불필요 — `unreadCount` 양수 동안 노출, 닫기=클리어). **인프라 재사용**: `notices`/`unreadCount`/`markNoticesRead`/`noticeCreatedMs` 모두 인앱 공지함 패턴에서 그대로 활용 — 추가 Firestore 구조 0, 코드만 ~80줄. 카카오 알림톡 승인 전 도달성 보장 1차선.

- `[패턴]` **신규 PWA 마운트 시 InstallPrompt + applyAppManifest 동반 필수**(2026-05-27 AdminApp 누락): 단일 origin 다중 PWA 패턴(관제 `/`·직원 `/p`·기사 `/driver`)에서 새 앱 추가 시 `<InstallPrompt/>` 마운트 + `applyAppManifest({manifestHref, appleTouchHref, title})` 호출 둘 다 동반해야 PC Chrome 자동 안내·iOS 단계 가이드·아이콘 동적 교체가 모두 동작. AdminApp은 manifest.json만 있고 InstallPrompt 누락 → PC 데스크톱에서 주소창 설치 아이콘은 떴어도 자동 바텀시트 비표시. 직원/기사 패턴 일관 적용. **모바일 관제 사용 시**: 관제 scope `/`가 직원앱 `/p` 와 중첩 → 충돌 발생하면 `/admin` 경로 분리(issues.md 단일 origin 다중 PWA 패턴 참조).

- `[패턴]` **partnerCode sync 정공법 = `passengers/{empNo}.partnerCode` 데이터 부여**(2026-05-27 진단): 협력사 한정 공지 발송 0건 호소의 흔한 근인 = 테스트/임시 직원 계정이 PartnerApp 엑셀 업로드 흐름(`lib/partner.js importEmployees`)을 안 거치고 다른 경로(Firestore Console 직접·AdminApp 별도) 생성 → `passengers.partnerCode` 미설정 → EmployeeApp 로그인 시 `session.partnerCode=null` → `initNotifications`가 `fcmTokens.partnerCode=null` 저장 → CF where 필터 미매칭. **데이터 차원 해결 우선**: PartnerApp 직원 1행짜리 엑셀 업로드(`importEmployees`가 partnerCode/partnerName upsert) 또는 Firestore Console에서 `passengers/{empNo}` 도큐먼트에 `partnerCode` 필드 직접 추가(값=`partnerCodes.code`와 일치). 부여 후 EmployeeApp 재로그인만 하면 useEffect deps에 `session.partnerCode` 포함되어 `initNotifications` 자동 재호출 → fcmTokens sync. **코드 폴백 한계**: `notifications.js` Line 60-68 폴백은 `partnerCode === undefined` 조건이라 EmployeeApp이 `partnerCode: session.partnerCode || null`로 명시 null 전달 시 폴백 미발화 — `passengers` 직접 조회 안 됨. 폴백 보강(undefined 대신 nullish)도 가능하나 결국 passengers 자체에 partnerCode 없으면 동일 — 데이터 부여가 본질.

- `[패턴]` **정류장 estimates chain 우선 정책 전환**(2026-05-27 `computeStopEstimates` 후보 결합): 이전 정책 `max(planCandidate[plan+누적지연], chainCandidate[직전 estMs+segTravel])`는 출발 지연(예: 33분)이 발생하면 누적지연이 모든 미래 정류장 plannedMs에 carry되어 chain이 더 빨라도 plan+delay 채택 → "33분 지연"이 미래 전 정류장에 동일 carry. 사용자 의도(스크린샷+명시 결정)="실제 정류장 도착시간 기반으로 바로 다음 정류장 도착예정시간 추정" → **chain 우선이 정본**. upcoming: chainCandidate 있으면 그대로 채택·없으면 planCandidate fallback. next: chain·gps **0.7:0.3** 가중(chain 정본·GPS 미세 보정), chain 부재면 gps→plan+delay 순. 지연 라벨이 chain 기반 estMs로 자연히 줄어드는(33분→20분 등) 동작은 사용자가 수용. **보호장치 보존**: 단조증가(MIN_STOP_GAP_SEC=60)·과거금지(MIN_FUTURE_BUFFER_SEC=30)·segTravelMs 유효속도 20km/h 하한·DWELL_SEC=30 그대로. **source enum 4종 호환**(actual/plan+delay/gps/fallback) — chain 채택 시 'plan+delay' 유지(describeEtaSource "계획+지연" 한국어 라벨이 actual 후 추정=계획 갱신 형태로 사용자에게 자연스러움). 출력 계약 절대 불변 → 다운스트림(useSmoothedEta/formatPassengerEta/describeEtaSource/formatDelayLabel·EmployeeApp/PassengerApp/DriverApp) 무영향. **회귀 가드(복원 금지)**: 이전 max 정책으로의 되돌림은 동일 결함 재발 — 사용자 명시 결정.
- `[패턴]` **정류장 estimates 단조증가 재설계 — order 순회 + chain 전파 + 과거금지**(2026-05-22 도착시간 정밀화): "도착예정시간이 현재시간 이전"·"도착지·이전 정류장과 동일 시각" 결함의 근인 = `computeStopEstimates`가 각 정류장을 독립 산출(`planDelayMs = plannedMs + cumulativeDelaySec`)하고 과거면 `(e)` 단계 단순 clamp(`estMs < T_NOW-30s → T_NOW`)로만 보정 → ① 여러 upcoming이 모두 과거면 전부 `T_NOW`로 clamp되어 **동일 시각** ② 단조증가 미보장으로 **역전**(nextIdx보다 그 다음이 더 이른 시각) ③ 30초 마진 안이면 보정 안 됨 → **과거 표시**. **해법 = 정류장을 order 순서대로 순회하며 estMs를 단조증가로 강제**: 각 미통과 정류장 `estMs[i] = max(planCandidate[plan+누적지연], chainCandidate[직전 estMs + 구간이동시간], [next만]gpsCandidate)` 후 후처리 `estMs[i] >= estMs[i-1] + MIN_STOP_GAP_SEC(25s)`(동일값·역전 차단) + `estMs[i] >= T_NOW + MIN_FUTURE_BUFFER_SEC(30s)`(과거 금지). 구간이동시간 = routePath 구간거리(`projectToPolyline` progress 차분) ÷ 유효속도, routePath 없으면 haversine 직선 ÷ 기본속도. **유효속도 = `max(차량속도, 20km/h)` 하한 필수** — 정차(속도 0) 시 chain이 무한대로 늘어남 방지. arrived 정류장은 actualMs가 체인 기준점(`prevEstMs`), 노선 시작·기준 부재면 `T_NOW`. next는 plan+delay 70:gps 30 가중 후 같은 단조증가·과거금지 후처리. **출력 계약 절대 불변**(stopId/plannedAt/estimatedAt/delaySec/status enum/source enum) — 다운스트림(`useSmoothedEta`/`formatPassengerEta`/`describeEtaSource`/`formatDelayLabel`) 무변경 호환. 상수는 파일 상단 명명 분리(`MIN_STOP_GAP_SEC`/`MIN_FUTURE_BUFFER_SEC`/`DEFAULT_SPEED_KMH`/`MIN_EFFECTIVE_SPEED_KMH`). EmployeeApp/PassengerApp은 이미 vehiclePos/speed/routePath 주입 중이라 알고리즘 개선 자동 반영. DriverApp은 routePath만 주입(vehiclePos null → gpsCandidate 미발생, 구간전파만 정밀화 — 회귀 표면 0). 옛 `(e)` 단순 clamp는 완전 제거.

- `[패턴]` **GPS 복구 시 정류장 진행률 백필 — 통신장애 통과 누락 회복**(2026-05-22): `gps.js startGPS`의 100m 직접 감지(`getDistance ≤ STOP_ARRIVE_M`)는 watchPosition 콜백마다만 동작 → GPS 끊김(터널·약전계)으로 콜백이 끊기면 버스가 정류장 100m 반경을 통과하는 순간을 못 잡아 `visitedStops`에 영구 미등록 → `currentStopIdx`가 옛 정류장에 멈춤. **해법 = `startGPS`에 `routePath` 인자 추가 + `detectStops(curr)` 공통 함수**: ① 기존 100m 직접 감지 그대로 유지(`onStopReached(stop,dist,false)`) ② routePath 유효(길이≥2)면 버스/정류장을 `projectToPolyline`로 투영, 정류장 progress는 startGPS 진입 시 1회 캐시 → `busProgress > stopProgress + PASS_MARGIN_M(40m)`인데 visitedStops에 없으면 통과 누락 → order 낮은 것부터 순차 `onStopReached(stop,approxDist,true)`. **TIMEOUT 재시작 watch 콜백도 `detectStops` 호출 필수**(원본은 재시작 watch가 flush만 하고 감지 누락 — GPS 복구 직후가 백필이 가장 필요한 시점). routePath 없으면 백필 skip(직선 폴백·회귀 0 — 운영 노선 다수가 routePath 미설정). **호출측 멱등 필수**: 백필이 한 콜백에서 여러 정류장을 순차 호출 → DriverApp `setCurrentStopIdx(prev => Math.max(prev,newIdx))`(역행 방지) + `stopArrivals` 기록은 첫 도착만 멱등 가드. `onStopReached` 3번째 인자 `viaBackfill` → `stopArrivals.{stopId}.estimated` 플래그(백필 기록분 식별 — 통신장애 통과분은 actualAt=복구시각이라 delaySec 부정확할 수 있음, 진단용. computeStopEstimates는 actualAt만 쓰므로 무영향).

- `[패턴]` **단일 origin 다중 PWA = manifest 고유 `id` 필수 + /p 조기 manifest 교체**(2026-05-22 prod 결함 — 앱 설치 충돌·교차 실행): buslink는 단일 origin(`buslink-prod.web.app`)에서 경로 분기로 3앱(관제 `/`·기사 `/`·직원 `/p`). PWA는 origin+식별자로 구분되는데 **manifest 3종에 `id` 필드 전부 부재** → W3C 표준상 `id` 없으면 `start_url`이 식별자 → `manifest.json`(관제)·`manifest-driver.json`(기사)이 **둘 다 `start_url:"/"`로 완전 동일** → Chrome이 같은 PWA로 인식. 증상: 직원앱 실행 시 기사앱 화면·반대도, 한 앱 삭제해도 "이미 설치됨". **추가 결함 — 동적 manifest 교체 타이밍**: `applyAppManifest`(`pwaManifest.js`)는 EmployeeApp/DriverApp **마운트 useEffect**에서 `<link rel=manifest>` href 교체 → 익명로그인+데이터로드 후라 지연 → 그 사이 Chrome이 `index.html` 기본 `manifest.json`(start_url `/`)으로 `beforeinstallprompt` 평가 → 직원앱이 `/`(기사/관제)로 잘못 설치. **해법**: ① 3 manifest에 고유 `id` + `start_url` 분리 — 관제 `/?app=admin`·기사 `/?app=driver`·직원 `/p`(쿼리는 App.js pathname 분기에 무영향, Auth role이 admin/driver 결정). scope: 직원 `/p`, 관제·기사는 같은 `/` 경로라 scope 공유 불가피하나 `id`로 별개 PWA 보장. ② `src/index.js`에서 React render·BIP 리스너 **전에** pathname `/p`(≠`/partner`) 감지 시 `<link rel=manifest>`를 `manifest-employee.json`으로 동기 조기 교체(마운트 지연 우회). `/`의 admin/driver는 Auth 분기 후라 마운트 시 `applyAppManifest`가 처리(로그인 후 시점 — 설치 액션 전 충분). **id 변경 시 기존 잘못 설치된 PWA는 orphan** — 사용자 재설치 필요. **교훈**: 단일 origin 다중 PWA는 manifest마다 명시적 고유 `id` 필수 + 경로 확정 가능한 앱은 index.js 조기 교체(마운트 useEffect 교체는 BIP 평가보다 늦을 수 있음).

- `[패턴]` **PWA scope 중첩 = 경로 분리 필수(id만으론 부족)**(2026-05-22 후속): 위 id 분리(`/?app=admin`·`/?app=driver`·`/p`) 후에도 기사앱↔직원앱 설치 충돌 지속 — 직원앱 설치 시 "이미 설치됨"·아이콘 없음, 기사 아이콘 실행 시 직원앱(`/p`)이 열림. **근인 = Chrome은 PWA를 `scope`(path prefix)로도 dedupe** — 기사앱 scope `/`가 직원앱 `/p`를 경로상 **포함** → `/p`가 "기사앱 영역"으로 귀속 → 직원앱 독립 설치 거부, 기사 WebAPK(scope `/`)가 `/p`를 자기 창에 염. scope는 path prefix라 쿼리(`?app=`)·`id`로 못 나눔. **해결 = 경로 자체 분리**: 기사앱 전용 경로 `/driver` 신설 — `App.js`에 `isDriverRoute=path.startsWith("/driver")`(`/driver`는 isPassenger/Boarding/Partner/Employee 전부 false라 기존 "그 외(/)" 분기에 자연 포함 → Auth·loading·kakao 자동 적용, 렌더 분기만 추가). `manifest-driver.json` id/start_url/scope 전부 `/driver`. `/` 진입+기사 역할 → `window.location.replace("/driver")` 자동 이동(기존 `/` 북마크·로그인 흐름 흡수). 직원앱 `/p`·기사앱 `/driver` scope **완전 분리** → 충돌 0. 관제는 `/`(scope `/`) 유지 — PC 전용이라 모바일 직원앱과 충돌 낮음(모바일에 관제 PWA 설치 시 재발 가능 — 그땐 `/admin` 분리). + `firebase.json headers` 신설 — `**/*.html`·`**/manifest*.json`·`/firebase-messaging-sw.js`에 `Cache-Control: no-cache`(기존 헤더 전무 → manifest stale 캐시로 새 id 미반영 위험 차단). **id 변경 시 기존 잘못 설치된 WebAPK는 orphan** — 사용자 완전 재설치 필요(홈 아이콘 + 안드로이드 시스템 앱 목록 WebAPK까지). 교훈: 단일 origin 다중 PWA = `id` + `scope`(=경로) 양쪽 분리 필수, id만으론 Chrome scope dedupe 못 피함.

- `[패턴]` **PWA 설치 안내 영구 차단 + Chrome BIP 기억 = 자동회복 + 수동안내 폴백 2중 가드**(2026-05-21 prod 결함): `InstallPrompt.js` 가드 두 계층 결함. ① **localStorage `installed:true` flag가 PWA 삭제 후 영구 잔존** → 사용자가 홈 아이콘 길게 눌러 삭제해도 origin localStorage는 보존 → 다음 접속 시 `isSnoozed()=true` → 안내 카드 영구 비표시. ② **Chrome 안드로이드 BIP 영구 기억** → Chrome 내부가 한 번 설치된 origin을 "이미 설치됨"으로 기억해 `beforeinstallprompt` 이벤트 미발화 → 우리 가드 풀려도 BIP 부재로 "android" 모드 진입 불가. **해법 1 — 자동 회복**: `isSnoozed()`에서 `installed && !isStandalone()` 검출 시 localStorage flag 자동 청소 후 false 반환. iOS PWA 삭제 / 안드로이드 홈 아이콘 삭제 모두 자동 감지. **해법 2 — 안드로이드 수동 안내 폴백**: 신규 `isAndroidPwaCapable()` 헬퍼(Chrome/Edge/SamsungBrowser + 인앱 브라우저 제외: FBAN·KAKAOTALK·Line). 안드로이드 감지 시 3초까지 BIP 안 오면 `setMode("android-manual")` → "Chrome 메뉴(⋮) → 홈 화면에 추가/앱 설치" 가이드 카드. iOS Safari 분기와 같은 패턴(BIP 부재 → 수동 안내). mode 분기: `android`(네이티브 프롬프트 가능 → 설치 버튼) / `android-manual`(BIP 부재 → 가이드만, 설치 버튼 숨김) / `ios`(공유 → 홈 화면). **회수 결함 안티-패턴**: BIP 의존 가드 단독 = iOS는 아예 못 받고 안드로이드도 Chrome 기억으로 영구 차단 가능 → 항상 OS별 수동 안내 폴백 필수. `usePermissions.js`의 PermissionGate "📲 홈 화면에 앱 추가" 카드도 BIP 의존이라 동일 결함 잠재 — 현 시점은 InstallPrompt가 커버하나 추후 BIP 가드 사용 시 동일 폴백 패턴 적용.

- `[패턴]` **모바일 백그라운드 푸시 = PWA 설치 + SW `if (payload.notification) return;` 가드**(2026-05-21, callcenter 성공 패턴 이식): 협력사 직원이 모바일에서 공지를 OS 알림 트레이로 받으려면 ① 모바일 Chrome/Edge로 진입 ② 알림 권한 허용 ③ **홈 화면에 추가(PWA 설치)** — 모바일 브라우저 탭은 백그라운드 시 OS가 폐기하나 PWA standalone은 별도 프로세스로 SW 유지 → 백그라운드 푸시 도달. SW 측 가드 = `firebase-messaging-sw.js onBackgroundMessage` 첫 줄 `if (payload.notification) return;` — CF가 `notification + data` 둘 다 보내면 Firebase SDK 자동 알림 + SW showNotification 이중 호출되어 모바일 환경에 따라 토스트 2회/SDK 자동만 / 환경별 불일치. 가드 = SDK 자동 알림에 1회 위임(callcenter `public/sw.js:L25` 검증). data-only 메시지에만 SW가 수동 표시. **PC 활성 탭은 별개**(Chrome 표준상 인앱 배너만, OS 토스트 안 뜸 — 이중 알림 방지). buslink는 PC 진단 후 SW 가드 부재로 이중 표시 가능성 → callcenter 패턴 이식(`main.ca61bae8.js` 빌드와 무관·SW 단독 prod 배포). **검증 절차**: 모바일에서 `/p` 진입→권한 허용→설정 탭 🔔 알림 진단으로 토큰 확인→Chrome 메뉴 "홈 화면에 추가" 또는 `InstallPrompt` 배너 클릭→홈 화면 BusLink 아이콘으로 standalone 실행→탭/앱 백그라운드→AdminApp 공지 발송→모바일 알림 트레이 토스트 + 진동/소리. 안 되면 (a) Android: 설정→앱→Chrome→알림 허용 (b) iOS: Safari `addToHomeScreen` 후 standalone에서만 허용 모드 (c) PWA 설치 실패면 `manifest-employee.json`/`display:standalone` 확인.

- `[패턴]` **VAPID 키 1글자 오타 → FCM 등록 401 무한 — Firebase Console 정본 byte-level 검증**(2026-05-21 prod 결함): `.env.local REACT_APP_VAPID_KEY`가 Firebase Console "웹 푸시 인증서" 공개키와 **1글자(I/l, 0/O 등 시각적으로 거의 동일한 문자)** 다르면 `getToken` 시 `POST .../fcmregistrations.googleapis.com/v1/projects/{p}/registrations 401 (Unauthorized)` + `messaging/token-subscribe-failed`. 토큰 발급 영구 실패 → fcmTokens 영구 0건 → 공지 발송 무의미. **오진 함정**: 같은 401 메시지가 "Cloud Messaging API V1 비활성"으로도 발생 → Firebase Console 클라우드 메시징 탭에서 V1 활성화 상태 먼저 확인(✓ 사용 설정됨이면 V1 아님). **byte-level 검증**: `grep -E '^REACT_APP_VAPID_KEY=' .env.local | od -c | head -10`로 한 글자씩 hex 확인 → Firebase Console "웹 푸시 인증서" 키쌍 공개키 1줄 복사값과 1:1 비교(눈으로는 `I↔l`/`0↔O`/`1↔l` 구별 어려움). **운영 측면**: `.env.local`은 PC-local·gitignore라 PC 옮길 때마다 사용자가 직접 작성 → 1글자 오타 회귀 가능. 의심 시 `.env.example` 가이드 + byte-level 비교 절차 안내. **메모리 정정 케이스**: "Cloud Messaging API V1 비활성" 첫 단정은 콘솔 ✓ 활성 확인 후 정정 — 외부 콘솔 설정 단정 전 사용자 실측 체크리스트 필수(`feedback-external-setup-guidance` 메모리 적용 사례).

- `[패턴]` **공지 토큰 invalid 자동 삭제 → 0건은 정직한 결과 + 사용자측 자가 회복 통로**(2026-05-21): "공지 발송이 작동 안 됨" 호소의 흔한 근인 = CF는 정상(`sendNoticeToCompany` 로그 "성공: 0 실패: 1")인데 만료/무효 토큰만 있어 발송 직후 `functions/index.js:101-110` 자동 삭제 → fcmTokens=0건. 진단 패널이 0건 표시하는 건 정직한 상태(silent fail 아님). **진단 순서**: ① `firebase functions:log --only sendNoticeToCompany -n 3` → "성공/실패" 카운트 확인(코드 결함 여부) ② 라이브 fcmTokens 콜렉션 카운트 확인 ③ 토큰이 0이면 직원측 회복 필요. **회복 인프라**: ① AdminApp NoticeTab `no_tokens` 카드에 "EmployeeApp → 설정 → 🔔 알림 진단 → 재발급" 명시 안내 + 0건 진단 패널에도 동일 가이드. ② **EmployeeApp SettingsTab 🔔 알림 진단 카드**: `Notification.permission` 색칩(허용됨/거부됨/미선택) + `getDoc(fcmTokens/{empNo})` 토큰 존재 여부·갱신 시각·마지막 4자리(PII 최소화) + 🔄 재발급 버튼(`initNotifications` 강제 재호출 → 권한거부면 자물쇠 아이콘 안내 / 성공 시 fcmTokens 자동 upsert). 사용자가 본인 권한·토큰 자가 점검·즉시 회복. **silent fail 가드**: NoticeTab `onSnapshot` 3개(notices/partnerCodes/fcmTokens) error 콜백으로 `snapshotError` state 누적 + 진단 패널 상단 빨간 박스 — 향후 권한/네트워크 silent fail 즉시 가시화. CF는 변경 불필요(자동 삭제 로직 정상). **VAPID 키 함정**: prod 토큰은 prod VAPID로만 매칭 — localhost에서 발급된 토큰은 prod 발송 대상 아님.

- `[해결]` **BoardingApp 익명 인증 누락 → QR 탑승 보드링 생성 차단(2026-05-26, `main.3c4d91c8.js`)**: BoardingApp(`/board?t=...`)이 `signInAnonymously` 호출 없이 사용자 사번/이름 입력만으로 `validateAndBoard` 호출 → firestore.rules `boardings create: if isAuth()` 통과 실패 → boarding 도큐먼트 생성 차단 → 통계 화면에 노출 안 됨(prod 사용자 보고). **원인 누적**: ① 기존 deployed rules가 더 permissive했을 가능성(memory `[보류]`) ② 2026-05-26 partnerCodes delete rule 변경 deploy 시 strict 정합으로 동기화 → 잠재 BoardingApp 결함 표면화. **해법**: BoardingApp에 `signInAnonymously` 마운트 시 호출(EmployeeApp/PassengerApp/PartnerApp 패턴 일관), `authReady` state로 가드(인증 전 탭 시 안내). **데이터 유실**: 이전 QR 탑승은 boarding 도큐먼트가 만들어지지 않은 채 토큰만 만료(5분) → 복구 불가. 새 QR 탑승부터 정상 누적. **교훈**: 새 firebase 인증 의존 화면 추가 시 4개 SPA 분기(`/`·`/p`·`/bus`·`/partner`·`/board`·`/driver`) 모두 인증 패턴 일관 확인.
- `[해결]` **`Map` shadow 회귀(2026-05-26 운행이력 탭, `main.60c54a0d.js`)**: HistoryTab `dispatchGroups` 파생에서 `new Map()` 사용 → minified `new ae()`(forwardRef 객체 비-생성자) → "ae is not a constructor" throw → ErrorBoundary가 격리한 노란박스 노출. `main.c2b238ce.js`에서 `new window.Map()`으로 수정 + 같은 위치에 가드 주석. **교훈**: 이 패턴은 카카오 SDK `Map`을 import한 어느 파일에서든 재발 — 새 코드 작성 시 `new window.Map`/`new window.Set` 컨벤션 일관 적용, 코드리뷰 시 `new Map(` grep 점검.
- `[패턴]` **카카오 SDK `Map` import 가 native `Map` 클래스를 shadow**(2026-05-21 Issue #4 prod 결함): `import { Map, MapMarker, ... } from "react-kakao-maps-sdk"` 한 파일 안에서 `new Map()`을 쓰면 minified 후 `new ee()`(ee=forwardRef 반환 객체=비-생성자)로 빌드 → 런타임 `ee is not a constructor` throw → 해당 탭/페이지 render 사망(ErrorBoundary로 격리되지만 진단 패널 자체 사망). 해법=`new window.Map()` 명시 참조(globalThis는 CRA ESLint no-undef로 빌드 차단·window는 호환성 안전). 같은 함정 가능 식별자: `Set/Promise/Date` 등(향후 react-kakao 이외 라이브러리 import도). **전수 점검**: `import { Map | Set | Promise } from` 줄이 있는 모든 페이지 파일에서 `new Map(|Set(|Promise(` grep → native 호출이면 `window.X` 또는 함수 상단 `const NativeX = window.X;` 별칭. `lib/*.js`처럼 카카오 import 없는 파일은 안전. **★ 2026-07-09 재발(EmployeeApp HomeTab Phase3 진척 요약바 `const seen = new Map()`, prod 승객앱 백지 `main.fa98be10.js`로 핫픽스)**: EmployeeApp `/p` 는 HomeTab 에 **ErrorBoundary 없어** 노란박스 아닌 **전체 백지**(첫 프레임 잠깐 뜨고 사망). 위임 에이전트가 `new Map()` 작성·부모 diff 리뷰가 구조/로직만 보고 shadow 놓침. **`npm run build`·`node --check` 모두 이 런타임 shadow 못 잡음**(구문 유효·빌드 통과·런타임만 throw). **교훈**: 위임 frontend diff 를 카카오 `Map` import 파일(EmployeeApp/PassengerApp/AdminApp/PartnerApp)에서 머지/배포하기 전 ① `new Map(`/`new Set(` grep(native면 `window.` 명시) ② **브라우저 1회 실로드**(F12 Console TypeError 확인) — 빌드 통과 ≠ 런타임 정상.
- `[패턴]` **웹 로그인 세션 충돌(튕김) = 같은 origin Firebase Auth currentUser 1개(2026-07-09, `firebase.js`)**: 같은 origin(web.app/localhost)에서 관리자/기사(이메일·LOCAL/IndexedDB, cross-tab 공유) + 승객/직원/협력사/탑승(익명) 탭 동시 열면 익명 `signInAnonymously`가 공유 IndexedDB auth 덮어써 **관리자 튕김**. **fix**: `firebase.js`가 App.js 라우트판정(`_isAnonApp`=/bus·/board·/partner·/p·host p/partner) 미러 → 익명 앱은 `initializeAuth(inMemoryPersistence)`(공유 저장소 미접근·익명 매진입 재로그인 무손실), 관리자/기사는 `indexedDBLocalPersistence`. `getAuth`→`initializeAuth`(1회)·앱/lib 무변경. **★ 관리자↔기사(둘 다 이메일)는 같은 origin 동시유지 불가**(currentUser 1개)=Firebase 구조·**서브도메인(admin. vs d., 다른 origin)으로만 격리**(prod 정상·localhost 동시테스트는 시크릿창). 신규 익명 앱 라우트 추가 시 `firebase.js` `_isAnonApp` 판정에 반영 필수(누락 시 그 앱이 LOCAL 써서 충돌 재발). rules/CF 변경 0.

- `[패턴]` **백그라운드 탭 frozen → onSnapshot stale** = `useWakeTick` 훅으로 강제 재구독(2026-05-21 Issue #3): 모바일/PWA가 장시간 백그라운드(전날부터 등) 시 브라우저가 탭 frozen → Firestore 리스너가 살아있는 듯하나 새 doc 변경 못 받음 → 사용자가 "GPS 안 보임/안 갱신" 호소·노선 재선택해야 깨어남. 해법 = `src/lib/useWakeTick.js`(순수 React 훅, 25줄): `document.visibilitychange` hidden→visible 전이 + `window.pageshow` event.persisted=true(bfcache 복귀) 시 tick(+1). 첫 마운트(visible)는 무시(useRef로 기억). 모든 `onSnapshot useEffect`의 deps에 `wakeTick` 추가 → tick 증가 시 unsub→재구독 → Firestore가 현재 docs 즉시 fire → state 신선화. 적용처: EmployeeApp(HomeTab GPS·dispatch / RoutesTab gps·modalBuses·modalDispatch 3곳) + PassengerApp(GPS·dispatch 2곳). DriverApp은 본인이 GPS 발신측이라 적용 불필요. 회귀 0(첫 구독은 변화 없음, deps 1개 추가뿐).

- `[패턴]` **scrollIntoView({block:'center'})는 가장 가까운 overflow:auto 컨테이너 안에서 동작** → 내부 컨테이너에 `maxHeight + overflowY:auto` 없으면 페이지 전체가 점프(2026-05-21 Issue #1 DriverApp 카지노 스크롤 미작동): "현재 정류장이 중앙으로 와야" 같은 UX는 (a) `currentStopRowRef` + (b) `useEffect([currentStopIdx])`에 `scrollIntoView` 둘다 있어도 **(c) 부모 컨테이너 자체 스크롤이 빠지면** 효과가 페이지 body로 새어 위쪽 카드 점프로 발현. 해법 = 리스트 div에 `maxHeight:'60vh' + overflowY:'auto' + WebkitOverflowScrolling:'touch'`(iOS 관성). 스크롤바 시각 노이즈는 인라인 불가 → `data-stop-list` attribute + `index.css [data-stop-list]::-webkit-scrollbar{display:none}` 패턴(`data-route-strip`과 일관). stops 5개 이하면 자연 높이로 자동 비활성(maxHeight 무영향).

- `[패턴]` **ErrorBoundary 탭 단위 격리**(2026-05-21, `54bcdab`): 다중 탭 SPA(AdminApp 11탭)에서 한 탭 render throw → React가 부모까지 unmount → **사이드바·헤더 포함 전체 흰 화면**. 진단 거의 불가. **해결**: `src/components/ErrorBoundary.js`(클래스 컴포넌트 + componentDidCatch + 노란 박스 + 메시지/스택/다시시도). 각 탭 `{tab===N && <ErrorBoundary label="..."><XxxTab/></ErrorBoundary>}`로 감쌈 — 한 탭 throw가 그 탭만 노란 박스로 격리. 운영 진단·복구 인프라(메시지 직접 가시화로 F12 콘솔 없이도 원인 좁힘). 큰 위임 변경(NoticeTab 전면 개편 같은) 후 사용자 검증 단계에서 회귀 가시화에 특히 유효. 향후 신규 탭/큰 컴포넌트 추가 시 ErrorBoundary로 감싸기 일관 적용.

- `[패턴]` **공지 발송 협력사 단위 분리 + 발송 결과 가시화**(2026-05-21): 사용자 "공지 발송이 작동 안 됨" 진단 = 코드는 정상(sendNotice→fcmQueue→sendNoticeToCompany→multicast), AdminApp 결과 가시화 부재로 "안 됨"으로 보임 + 협력사 단위 분리 부재. **데이터 계약**: ① `fcmTokens.partnerCode` 신규 필드 — EmployeeApp 로그인 시 passengers/{empNo}.partnerCode 가져와 session 포함 → `initNotifications({companyId,empNo,partnerCode})` deps 변경 시 자동 재upsert(idempotent setDoc merge). 인자 미제공 시 passengers 자동 조회 폴백(notifications.js). ② `notices.partnerCode` + `fcmQueue.partnerCode` 동일 저장 — `sendNotice({...,partnerCode})` 반환=`{noticeId, queueId}`. ③ CF는 partnerCode 있으면 `where("partnerCode","==",X)` 토큰만, 없으면 전체(필드 누락 기존 토큰 포함=폴백 자연흐름). 결과는 `fcmQueue.{status, totalTokens, successCount, failureCount}` 갱신. ④ **rules `fcmQueue` admin read 허용**(자기 회사 companyId 일치) — 안 그러면 NoticeTab onSnapshot 불가. update는 여전히 false(Admin SDK는 룰 우회). ⑤ **AdminApp NoticeTab 진단 패널**=전체+협력사별 토큰 카운트 표시(0건=빨강 "수신자 없음"), 발송 폼 위 "📡 알림받을 직원 N명"(0이면 노란 배너), 발송 후 fcmQueue 단일 doc 구독→pending→완료/0건/오류 카드(성공 N건/실패 M건 가시화). ⑥ 발송 이력 카드에 협력사 배지(전체/미지정/특정 협력사). 기존 partnerCode 누락 데이터 무파괴(null=전체로 폴백).

- `[패턴]` **PartnerFilter 공통 컴포넌트로 다탭 협력사 필터 통일**(2026-05-21): `src/components/PartnerFilter.js`(companyId/value/onChange/activeOnly/allLabel/compact). top-level `partnerCodes` where companyId==X 구독 + active 필터 옵션. compact=true(필터바·12px·minWidth 110)·compact=false(폼·14px·100%). 적용 탭: 대시보드(routeId→partnerCode 매핑으로 dispatches/gpsVehicles/boardings 필터), 배차 관리(routeId→partnerCode), 배차 일정(routeId→partnerCode), 운행 이력(차량 필터: dispatchSchedules.vehicleId→routes.partnerCode 추정 — schedule 없는 차량은 "전체"에서만 보임), 공지 발송(폼·완전한 선택지). 노선 관리는 기존 partnerFilter 별도 유지(이미 routes.partnerCode 직접 필드). 회사 안 협력사 별 운영(KT·삼성 등) 가시·집계의 1차 진입점.

- `[패턴]` **배차 일정 자동 펼침**(2026-05-20): `dispatchSchedules`(반복 패턴 정본) + CF `expandDispatchSchedules`(onSchedule `30 0 * * *` KST·`us-central1`)가 향후 7일치 dispatches 펼침 + `expandDispatchSchedulesNow`(onCall) 즉시 트리거. **멱등 dispatchId=`${scheduleId}_${day}`** → exists() skip → 일별 수동 수정·기사교체 영구 보존. 한국 공휴일=정적 배열 `functions/holidays.js`+`src/lib/holidays.js`(2026~2028, **매년 갱신 책임=운영자**, 양쪽 동기화). 자연흐름: 일정 변경 시 이미 펼친 미래 dispatch는 그대로 → 다음 새벽이 새 조건으로 보충. Cloud Scheduler API 첫 배포 시 자동 enable(`cloudscheduler.googleapis.com`). 회사별 휴무일=`excludeDates:string[]`. weekdays는 일=0~토=6.

- `[패턴]` **정류장 진입시각 음수=입력오류**(2026-05-20): AdminApp 정류장 폼은 HH:MM 입력→`offsetMinFromPlanTime(departTime,planned)` 변환 저장. **`planned<depart`이면 null 반환·저장 거부**(자정 넘김 보정 폐기 — 통근버스 모델). 자정 보정을 두면 1번 정류장(06:50) < 노선 departTime(06:55) → `-5분`이 `+1435분`으로 폭증해 카드/승객앱 표시 깨짐(과천라인 실회귀, 2026-05-20). 첫 정류장은 노선 출발시각과 동일해야 0분 — `openStopAdd`가 `stops.length===0 && route.departTime` 시 `plannedTime`을 `route.departTime`으로 prefill. 야간/자정 넘김 노선이 필요해지면 별도 모델 (예: `dispatches.departTime` per dispatch + crossesMidnight 플래그)로 처리.

- `[패턴]` **정류장 계획·예상시각**(2026-05-20, 가중치 갱신 2026-05-21): stops `offsetMin`(분) 옵션 + `dispatches/.../list/{id}.stopArrivals.{stopId}={actualAt:serverTimestamp,plannedAt,delaySec}` 누적기록. 공용 헬퍼=`src/lib/stopSchedule.js` 순수함수(`planTimeForStop/computeStopEstimates/formatDelayLabel/formatPassengerEta/describeEtaSource`, Firebase import 금지). 알고리즘: ① arrived(실측있음) → actual ② 미통과 정류장 = 마지막 통과 정류장의 누적지연(actual-planned) 적용(`plan+delay`) ③ '다음 1개' 정류장만 GPS 잔여거리/속도와 **plan+delay 70 : gps 30 가중평균**(2026-05-21 ETA 안정화, 기존 50:50에서 변경 — GPS 노이즈 30~70km/h 흔들림보다 plan+delay(정류장 통과 후 1회만 갱신)가 안정적) (`gps`) ④ `offsetMin` 미설정=`status='unplanned'`, 페이지가 calcETA 폴백. 표시 임계 ±2분=정시·≥3분=지연(danger)·≤-3분=조기(warn). 기사 도착감지(`onStopReached`)는 첫 도착만 `updateDoc({[`stopArrivals.${stopId}`]:...})` 멱등 가드. Firestore 룰: `dispatches` update를 admin 전체 OR (기사 본인+stopArrivals 필드 한정)로 좁힘.

- `[패턴]` **승객 ETA 표시 안정화 — EMA + 버킷 + 신뢰도 소스**(2026-05-21): "도착 예정 시간이 갑자기 늘어났다 줄어드는" 현상 4계층 완충. (a) `stopSchedule.computeStopEstimates` plan+delay 70 : gps 30(GPS 노이즈 표면 축소). (b) `src/lib/useSmoothedEta.js` React 훅(EMA α=0.25 + rate-limit maxIncrease 0.5s/s·maxDecrease 1.5s/s + jumpThreshold 5분 통과는 즉시 raw — 도착 이벤트 후 누적지연 갱신 보존). (c) `stopSchedule.formatPassengerEta(etaSec)` 버킷 라벨화: <60s='곧 도착'·<5분='N분 후'·<60분='약 N분'(5분 단위 반올림)·≥60분='HH:MM 예상' + tone(primary/warn/mute). (d) `describeEtaSource(source)` 보조 11px 표시('실측'/'계획+지연'/'GPS 추정'/'대략') — 사용자가 어떤 데이터 기반인지 알아 신뢰도 향상. EmployeeApp `/p` myStop 큰 카운트다운 + PassengerApp `/bus` 큰 ETA 카드에 (b)(c)(d) 적용. 정류장 카드(많음)는 (c)만(버킷 자체로 안정). 색상 임계(≤3분 destructive·≤10분 cautionary)도 smoothed 기반. **회귀-0**: `etaStatus.eta` 분 단위 유지(기존 텍스트/색 분기 호환), `passed`/`arriving`/`waiting` 분기 그대로. myStopEst의 estimatedAt(HH:MM)이 정본 우선(분 ceil 점프 회피)·없으면 routePath/직선 폴백. **로직 보존**: routePath/calcETA/sendGPS/onSnapshot/stopArrivals 구조 100% 유지·DriverApp/AdminApp 무영향.

- `[패턴]` **BIP 글로벌 stash는 다중 소비자 공유 모델**(2026-05-20, cac9f4b/eb1c2e7): `index.js`가 `preventDefault()+stash`한 BIP는 **여러 컴포넌트가 회수해야 할 수 있음** — buslink엔 `InstallPrompt`(하단 배너) + `usePermissions`/`PermissionGate`("📲 홈 화면에 앱 추가" 카드) 둘 다. 한 곳이 stash를 `= null`로 비우면 다른 곳은 영구 미작동(usePermissions가 mount 후 리스너만 의존 → 이미 발화된 BIP 못 받음 → 카드 영구 비표시 실회귀). **회수 후 비우지 말 것** — `promptInstall` 호출 시 이벤트 객체가 consumed 되는 건 자연스러움(설치는 1회). 신규 BIP 소비자 추가 시 mount-time stash 회수 코드 동반. PermissionGate 설치 카드는 InstallPrompt 3일 스누즈와 무관하게 살아있어 사용자 강제 진입 통로.

- `[패턴]` **운행 상태 기반 지연 표시 게이팅**(2026-05-20, eb1c2e7): `computeStopEstimates`는 `now` vs `plannedAt`만 보고 지연을 산출 — 운행 시작 전(`!driving`)에도 과거 계획 정류장에 "지연 866분" 같은 무의미 라벨 노출(실회귀 스크린샷). **DriverApp 정류장 리스트는 `(arrived || driving) && lab.label`** 조건으로 지연/예상 라벨 게이팅. 운행 전엔 "계획 HH:MM"만. 헬퍼 자체는 그대로(다른 앱이 GPS 신호로 자체 게이팅).

- `[패턴]` **`beforeinstallprompt`는 페이지 로드 직후 한 번 발생 → 글로벌 stash 필수**: `InstallPrompt.js` `useEffect` 내부 `addEventListener` 단독으로는 리스너가 늦게 붙는 라우트(기사앱처럼 Firebase Auth+데이터 로드 동안 마운트 지연)에서 BIP 미캐치 → 설치 가이드 영구 미동작. `EmployeeApp(/p)`은 익명로그인 즉시 마운트라 우연히 동작. **해결**: `src/index.js`에서 React render 전 `window.__buslinkDeferredBIP` 전역 stash → 다중 소비자(InstallPrompt+usePermissions) 공유 회수(위 [패턴] 보강). DriverApp 같이 early-return(loading/error) 거치는 라우트는 early-return JSX에도 `<InstallPrompt/>` 1줄 추가(안전망, idempotent — standalone/snoozed면 null 반환). `appinstalled` 이벤트도 같은 스코프에서 stash 비워 정합 유지. `index.js` 변경 → SW 캐시 무관(루트 SW는 `firebase-messaging-sw.js`로 fetch no-op).
- `[패턴]` **bundle escape 검증 = ASCII `\\uXXXX` literal로 -F grep**: CRA terser는 한국어를 `\u` ASCII로 escape(파일이 ASCII text). 단순 `grep -F '오늘 배차'`는 UTF-8 bytes(`ec98a4 eb8a98 20 ebb0b0 ecb0a8`)로 매칭 시도 → 0건 오탐. needle을 `Array.from(s).map(c => c.codePointAt(0)<0x80 ? c : '\\u'+cp.toString(16).padStart(4,'0')).join('')`로 만들어 `grep -Fo -f` 사용. PowerShell/printf single-quote에서 `\u`는 변환 안 됨 보장 어려움 → Node로 needle 파일 생성이 안전. **단일 한자 빈도(uc624 38건 등)만 보면 안 됨** — 인접 escape 패턴이 실증.
- `[패턴]` **카카오 `<CustomOverlayMap>` 라벨에 다중행 시각 추가**(2026-05-20 승객앱 UI 강화): 정류장 이름 라벨에 `stopEstimates` status별 시각(도착/예상/계획) 둘째 줄 표시. 라벨 컨테이너에 `textAlign:'center'`+`lineHeight:1.25` 추가, `<div>`로 이름과 시각 분리, 강조(배경 색) 라벨은 #fff 계열 색·일반 라벨은 토큰 색(positive/primary-deep/label-mute). offsetMin 미설정(`status='unplanned'`) 시 timeLabel=null → 이름만(폴백 회귀 0). yAnchor 보존. `<MapMarker>` 이미지 props 무변경. **버스 마커 펄스 ring** = `position:relative` 컨테이너 + `position:absolute inset:0` ring(animation `buspulse 2s ease-out infinite`, pointer-events:none) + 본체 div. keyframes `buspulse`는 `tokens.css` 전역(blpulse와 분리 — blpulse는 StatusDot 2.4배 빠른 펄스용). `<CustomOverlayMap yAnchor>` 차이로 일부 마커는 inset 활용 / pill형(PassengerApp 버스)은 inset:-2로 외곽 ring 확장. **노선도 스트립 가로 스크롤바 숨김** = `data-route-strip` 컨테이너에 `scrollbar-width:none + ::-webkit-scrollbar{display:none}`(index.css), 인라인 불가라 data attribute 패턴.
- `[패턴]` **카카오 SDK는 `autoload=false`로 로드하고 `window.kakao.maps.load(cb)` 콜백에서만 ready 처리**. `!!window.kakao?.maps` truthy 판정 금지(엔진 초기화 전 true). `<Map>` 흰화면 2대 원인: ①SDK 게이팅 함정 ②컨테이너 0px init(`onCreate` relayout 방어).
- `[패턴]` **배포 안전 절차**(2026-05-18 prod 장애·"특정 PC만" 규율 폐기): 이 저장소가 정본 — 어느 PC든 `git pull`+`.env.local`(`.env.example` 참고)이면 빌드·배포 가능. 실원인 = **카카오 키 단일**: `.env.local` 활성 키 `d464b4`(전용키·도메인 미등록)면 prod 지도 전체 사망 → **빌드 전 운영 공유키 `58bf34` 확인 필수**(아래 앵커 grep). ~~Auth 승인 도메인 누락~~ = **오진**(`buslink-prod.web.app`·`firebaseapp.com`은 Firebase 기본 자동 등록, 콘솔 실측 반증). prod 권한오류 재발 시 도메인 아닌 다른 원인(규칙/익명인증 흐름) 조사. 절차: 키확인→`CI=false npm run build`→`npm start` localhost `/p`·`/bus` 지도→`firebase deploy --only hosting`→prod curl 재확인. **회귀 시 가설-재배포 금지·콘솔 Hosting 롤백 먼저·재현은 localhost.**
- `[패턴]` **Kakao 키 판독은 줄머리 앵커 grep만**: `grep -nE '^REACT_APP_KAKAO_MAP_KEY=' .env.local` → 활성=`58bf34`(운영). 9번 `# … d464b4`는 비활성 복구값 주석 — STOP 아님. `grep -i kakao | head -1` 류는 주석을 매칭해 d464b4 오독→불필요 STOP(2026-05-19 직전 에이전트 오탐). 활성 키가 `d464b4` 시작일 때만 STOP.
- `[패턴]` **"승객앱"=EmployeeApp(`/p`)**, PassengerApp(`/bus`) 아님. `/p`=사번+PIN·localStorage 세션·홈/노선/탑승/설정 탭. `/bus`=익명·URL 딥링크/QR 배포용. 사용자 "승객앱" 기능 요청 시 기본 대상은 `/p` — 2026-05-17 세션 오대상 재작업 이력. 작업 전 화면/URL 확인.
- `[패턴]` **노선 사전경로 `routes/{id}.routePath`**: `[{lat,lng}, ...]` plain number 배열(GeoPoint 아님). AdminApp RoutesTab "경로 그리기" 모달이 수동 작성. 빈 배열/필드 없음=미설정. EmployeeApp/PassengerApp는 **유효좌표 ≥2면 사전경로, 아니면 `stops.map` 직선 폴백**(하위호환 — 기존 노선 무파괴, 신규/편집부터 점진 적용). 실시간 경로진행·도착판정은 `lib/routeProgress.js`(투영 progress 기반), 폴백 시 기존 노선순서 로직. **이어그리기는 `openPathDraw`가 기존 `route.routePath`를 초기 `pathPoints`로 로드해 자동 지원**.
- `[패턴]` **GPS 콜드스타트 출발지 공백 = `watchPosition` 단독+엄격 옵션**: 첫 측위 10초 초과 시 TIMEOUT 묻혀 출발 직후 수십초 `gps` 미기록. 표준 해결: ①`startGPS` 진입 즉시 `getCurrentPosition`(`maximumAge:30000,timeout:8000`) 1회 선발행(역행가드로 watch 첫콜백 중복 차단) ②watch `timeout`30s·`maximumAge`10s 완화 ③TIMEOUT(code3) 1회 watch 재시작 가드(무한루프 방지)+`onGpsError` 상위 전파(권한 code1 포함). 상수 분리(`INITIAL_FIX_OPTS`/`WATCH_OPTS`). `sendGPS`/throttle/heartbeat/visitedStops 불변.
- `[패턴]` **권한·PWA설치 게이트 = `lib/usePermissions.js`+`components/PermissionGate.js`** (순수 브라우저API, Firebase import 금지). `beforeinstallprompt`는 등록 SW 필요 → `index.js`가 `firebase-messaging-sw.js` 1회 선등록(`lib/notifications.js` 재등록과 idempotent). EmployeeApp 홈·DriverApp 상단에 마운트.
- `[패턴]` **PWA 설치형 SW(`firebase-messaging-sw.js`)에 캐시 절대 금지**: 설치가능 조건은 *fetch 핸들러 존재* 만 요구 → `self.addEventListener('fetch',()=>{})`(respondWith 없음=네트워크 통과)로 충분. precache/respondWith/workbox 추가 시 옛 빌드 shell 영구 서빙(stale 장애 계열). `<InstallPrompt/>`는 순수 프레젠테이션, 재노출 localStorage 14일 스누즈, standalone/appinstalled 영구비표시.
- `[패턴]` **앱별 PWA 아이콘 = manifest 3종 + 마운트 동적 교체**: 정적 path-scope 는 `/p`만 가능·기사앱은 `/`에서 Auth 분기 마운트라 단일 정적 manifest 불가 → `manifest.json`(관제·기본·favicon·apple-touch) + `manifest-employee.json`(scope `/p`) + `manifest-driver.json`, `src/lib/pwaManifest.js applyAppManifest`(순수·idempotent)를 EmployeeApp/DriverApp 마운트 `useEffect`(빈 deps)에서 1회 호출. **로컬 래스터 도구(ImageMagick/sharp/resvg) 부재** → manifest 아이콘은 SVG(`sizes:"any"`) 정본 + 1024 PNG raster/iOS apple-touch fallback. **purpose `any`만**(maskable 미지정 — Route 풀블리드 가장자리 보존). `design/`은 gitignore 스크래치 — `public/icons/`로만 복사.
- `[패턴]` **정류장 사진은 Firestore 직저장**(Storage 미사용): `lib/image.js` 긴변 1000px·JPEG q0.6→0.45→0.3 단계압축, base64 >700KB 거부(1MB 문서 한도). data URI가 stop 문서에 포함돼 onSnapshot 페이로드 증가 — 정류장당 1장 전제. 다수/고해상 추가 시 Storage 전환 검토.
- `[패턴]` `lib/notifications.js`는 `getToken` 전 기존 push subscription `unsubscribe` — VAPID 키 교체 충돌 방지(삭제 금지).
- `[패턴]` 루트 `CLOUD_FUNCTION_FCM.js`는 v1 옛 참고 스니펫 — 실배포는 `functions/index.js`(v2).
- `[패턴]` `boardingTokens`/`partnerCodes`는 규칙상 read 공개(`true`) — 토큰 추측·코드 유출 주의.
- `[패턴]` 카카오 `services`(Geocoder/Places) 사용 시 `window.kakao?.maps?.services` + 콜백 `status` 가드 필수 — SDK 미로드/한도초과 시 우아 실패+수동 경로(지도선택/좌표직접/자유텍스트) 보존. RoutesTab 정류장 검색이 이 패턴(`handleAddrSearch`).
- `[패턴]` **임시 진단 박스 = `[DIAG-X 제거예정]`/`[/DIAG-X]` 경계 + 모든 return 분기 결합**: `return null` 자리도 `return diagBox`로 바꿔야 standalone/snoozed/null-mode 어떤 분기든 가시화 가능. 다음 커밋에 `grep DIAG-X` 1줄로 통째 제거. DIAG-B(`/p` 흰화면, 2026-05-19) · DIAG-INSTALL(설치팝업, 2026-05-20) 둘 다 이 패턴으로 1회 사용자 확인 후 종결.
- `[패턴]` **minified 번들 검증은 `grep -o TOKEN file \| wc -l`** (매칭 횟수). `grep -c`는 단일행 prod 번들에 늘 1 — 오탐. 한국어 라벨은 terser가 ASCII `\uXXXX`로 escape하므로 fixed-string grep 0건이 정상 — **ASCII 식별자**(window 속성 `__buslinkDeferredBIP`·LS 키 `buslink_*`·Firestore 필드 `routePath`·CSS 클래스 `pickerModal`·SVG 색 코드 `00BF40`) 또는 Node로 escape needle 생성해 검증.
- `[패턴]` 실 테스트/별도 lint 없음 — 수동 검증. 배포는 사용자 명시 요청 시에만.

## 저장소 메모
- `src.zip` git 추적되나 작업트리 삭제 상태(소스 백업, 빌드 미사용).
