# Issues & 주의할 패턴

> 새 발견사항은 이 파일에만 추가. 형식: `[미해결]` / `[해결]` / `[보류]` / `[패턴]`.

## 미해결 / 함정

- `[미해결]` **firestore.rules 이중화**: 배포 정본은 루트 `firestore.rules`(`firebase.json` 지목, fcmTokens/notices/fcmQueue 포함). `src/firestore.rules`는 오래된 사본 — 수정해도 효과 없음. 규칙 변경은 반드시 루트 파일에.
- `[해결]` 런타임 정렬: `firebase.json` `nodejs22` → `nodejs20`(`package.json` `engines.node:"20"`, 2026-05-16). 데드라인 2026-10-30 전 22 재상향 필요(tasks.md 백로그).
- `[미해결]` `public/firebase-messaging-sw.js` Firebase 설정 **하드코딩**(env 아님). 프로젝트 변경 시 수동 동기화. `notificationclick`에 `buslink-prod.web.app`·fallback `dy001` 하드코딩.
- `[미해결]` `companyId` 기본값 `dy001`이 App.js·DriverApp·PassengerApp·EmployeeApp·SW에 하드코딩. 신규 회사 추가 시 전부 추적.
- `[해결]` `.env.example` 추가됨 (`5fa0737`, 2026-05-18) — 키 목록+가이드, 값은 .env.local에. 어느 PC든 부트스트랩 가능.
- `[해결]` **partnerCodes 복합 인덱스 누락**: `firestore.indexes.json`에 `partnerCodes (companyId ASC + active ASC)` 추가·배포(2026-05-16).
- `[해결]` **기사 onCall 4종 admin 역할 미검증**: `functions/index.js` `assertAdmin(request)` 헬퍼로 `users/{uid}.role∈{admin,superadmin}` 검증, 4종 진입부 적용·배포(2026-05-16). 익명 토큰만으로는 차단.
- `[보류]` **firestore.rules `passengers` write 개방** (`allow write: if isAuth()`): PartnerApp이 `signInAnonymously` 호출 안 함(App.js L48-49 분기 누락) → `request.auth==null` 이라 `isAuth()` 좁히기가 직원 등록·PIN초기화를 100% 차단. 안전한 제약엔 PartnerApp 익명화+규칙 재설계 동반 필요. ※ 현 규칙대로면 PartnerApp write 거부되어야 하는데 운영 정본이라는 점에서 **배포 실제 규칙 ≠ 저장소 정본** 가능성. 재설계 전 규칙 변경 금지.
- `[미해결]` **Kakao 키 임시 공유**: callcenter 앱 키 `58bf34…` 공유 사용(원 buslink 키 `d464b4…`는 `.env.local` 주석 보존). Geocoder/Places 일일 한도 callcenter와 공유(한쪽 급증=양쪽 장애). 비즈앱 심사 통과 후 전용 키 분리 → tasks.md 백로그. ⚠ 빌드 PC `.env.local` 활성 키가 `d464b4`면 prod 지도 전체 사망 — 빌드 전 앵커 grep 확인 필수(아래 `[패턴]`).
- `[해결]` **EmployeeApp `/p` 지도 흰화면 — 2026-05-18~19 2단계 해결**: 
  - ① **SDK 게이팅(2026-05-19, `1597097`)**: `public/index.html` `&autoload=false` 추가 + `App.js`를 `window.kakao.maps.load(cb)` 콜백 only로 게이팅(초기 state `false`). `autoload=false` 없으면 `window.kakao.maps` 가 엔진/services 초기화 **전** truthy → `<Map>` 미완성 엔진에 생성→영구 흰화면. 사용자 localhost 육안확인(`Map=true mapBox=1920x436`).
  - ② **컨테이너 0px init 방어(2026-05-18, `5fa0737`)**: `EmployeeApp.js` `<Map onCreate={m=>{m.relayout();setTimeout(()=>m.relayout(),300)}}>` — `appWrap 100dvh→content flex:1→HomeTab flex:1→map flex:'0 0 55%'` 체인이 init 시 0px일 수 있고 카카오는 CSS와 달리 자동 relayout 안 함. `/bus`는 고정 `height:100vh`라 무관. 즉시+300ms 더블 relayout = idempotent·무위험.
  - 두 방어선 공존 = 더 견고. **플렉스/동적높이 카카오 맵엔 항상 onCreate relayout + `autoload=false`+`maps.load(cb)` 부착.**

## 주의할 패턴

- `[패턴]` **배차 일정 자동 펼침**(2026-05-20): `dispatchSchedules`(반복 패턴 정본) + CF `expandDispatchSchedules`(onSchedule `30 0 * * *` KST·`us-central1`)가 향후 7일치 dispatches 펼침 + `expandDispatchSchedulesNow`(onCall) 즉시 트리거. **멱등 dispatchId=`${scheduleId}_${day}`** → exists() skip → 일별 수동 수정·기사교체 영구 보존. 한국 공휴일=정적 배열 `functions/holidays.js`+`src/lib/holidays.js`(2026~2028, **매년 갱신 책임=운영자**, 양쪽 동기화). 자연흐름: 일정 변경 시 이미 펼친 미래 dispatch는 그대로 → 다음 새벽이 새 조건으로 보충. Cloud Scheduler API 첫 배포 시 자동 enable(`cloudscheduler.googleapis.com`). 회사별 휴무일=`excludeDates:string[]`. weekdays는 일=0~토=6.

- `[패턴]` **정류장 진입시각 음수=입력오류**(2026-05-20): AdminApp 정류장 폼은 HH:MM 입력→`offsetMinFromPlanTime(departTime,planned)` 변환 저장. **`planned<depart`이면 null 반환·저장 거부**(자정 넘김 보정 폐기 — 통근버스 모델). 자정 보정을 두면 1번 정류장(06:50) < 노선 departTime(06:55) → `-5분`이 `+1435분`으로 폭증해 카드/승객앱 표시 깨짐(과천라인 실회귀, 2026-05-20). 첫 정류장은 노선 출발시각과 동일해야 0분 — `openStopAdd`가 `stops.length===0 && route.departTime` 시 `plannedTime`을 `route.departTime`으로 prefill. 야간/자정 넘김 노선이 필요해지면 별도 모델 (예: `dispatches.departTime` per dispatch + crossesMidnight 플래그)로 처리.

- `[패턴]` **정류장 계획·예상시각**(2026-05-20): stops `offsetMin`(분) 옵션 + `dispatches/.../list/{id}.stopArrivals.{stopId}={actualAt:serverTimestamp,plannedAt,delaySec}` 누적기록. 공용 헬퍼=`src/lib/stopSchedule.js` 순수함수(`planTimeForStop/computeStopEstimates/formatDelayLabel`, Firebase import 금지). 알고리즘: ① arrived(실측있음) → actual ② 미통과 정류장 = 마지막 통과 정류장의 누적지연(actual-planned) 적용(`plan+delay`) ③ '다음 1개' 정류장만 GPS 잔여거리/속도와 50:50 가중평균(`gps`) ④ `offsetMin` 미설정=`status='unplanned'`, 페이지가 calcETA 폴백. 표시 임계 ±2분=정시·≥3분=지연(danger)·≤-3분=조기(warn). 기사 도착감지(`onStopReached`)는 첫 도착만 `updateDoc({[`stopArrivals.${stopId}`]:...})` 멱등 가드. Firestore 룰: `dispatches` update를 admin 전체 OR (기사 본인+stopArrivals 필드 한정)로 좁힘.

- `[패턴]` **`beforeinstallprompt`는 페이지 로드 직후 한 번 발생 → 글로벌 stash 필수**: `InstallPrompt.js` `useEffect` 내부 `addEventListener` 단독으로는 리스너가 늦게 붙는 라우트(기사앱처럼 Firebase Auth+데이터 로드 동안 마운트 지연)에서 BIP 미캐치 → 설치 가이드 영구 미동작. `EmployeeApp(/p)`은 익명로그인 즉시 마운트라 우연히 동작. **해결**: `src/index.js`에서 React render 전 `window.__buslinkDeferredBIP` 전역 stash → `InstallPrompt` mount 시 회수+비움(중복 방지). DriverApp 같이 early-return(loading/error) 거치는 라우트는 early-return JSX에도 `<InstallPrompt/>` 1줄 추가(안전망, idempotent — standalone/snoozed면 null 반환). `appinstalled` 이벤트도 같은 스코프에서 stash 비워 정합 유지. `index.js` 변경 → SW 캐시 무관(루트 SW는 `firebase-messaging-sw.js`로 fetch no-op).
- `[패턴]` **bundle escape 검증 = ASCII `\\uXXXX` literal로 -F grep**: CRA terser는 한국어를 `\u` ASCII로 escape(파일이 ASCII text). 단순 `grep -F '오늘 배차'`는 UTF-8 bytes(`ec98a4 eb8a98 20 ebb0b0 ecb0a8`)로 매칭 시도 → 0건 오탐. needle을 `Array.from(s).map(c => c.codePointAt(0)<0x80 ? c : '\\u'+cp.toString(16).padStart(4,'0')).join('')`로 만들어 `grep -Fo -f` 사용. PowerShell/printf single-quote에서 `\u`는 변환 안 됨 보장 어려움 → Node로 needle 파일 생성이 안전. **단일 한자 빈도(uc624 38건 등)만 보면 안 됨** — 인접 escape 패턴이 실증.
- `[패턴]` **카카오 `<CustomOverlayMap>` 라벨에 다중행 시각 추가**(2026-05-20 승객앱 UI 강화): 정류장 이름 라벨에 `stopEstimates` status별 시각(도착/예상/계획) 둘째 줄 표시. 라벨 컨테이너에 `textAlign:'center'`+`lineHeight:1.25` 추가, `<div>`로 이름과 시각 분리, 강조(배경 색) 라벨은 #fff 계열 색·일반 라벨은 토큰 색(positive/primary-deep/label-mute). offsetMin 미설정(`status='unplanned'`) 시 timeLabel=null → 이름만(폴백 회귀 0). yAnchor 보존. `<MapMarker>` 이미지 props 무변경. **버스 마커 펄스 ring** = `position:relative` 컨테이너 + `position:absolute inset:0` ring(animation `buspulse 2s ease-out infinite`, pointer-events:none) + 본체 div. keyframes `buspulse`는 `tokens.css` 전역(blpulse와 분리 — blpulse는 StatusDot 2.4배 빠른 펄스용). `<CustomOverlayMap yAnchor>` 차이로 일부 마커는 inset 활용 / pill형(PassengerApp 버스)은 inset:-2로 외곽 ring 확장. **노선도 스트립 가로 스크롤바 숨김** = `data-route-strip` 컨테이너에 `scrollbar-width:none + ::-webkit-scrollbar{display:none}`(index.css), 인라인 불가라 data attribute 패턴.
- `[패턴]` **카카오 SDK는 `autoload=false`로 로드하고 `window.kakao.maps.load(cb)` 콜백에서만 ready 처리**. `!!window.kakao?.maps` truthy 판정 금지(엔진 초기화 전 true). `<Map>` 흰화면 2대 원인: ①SDK 게이팅 함정 ②컨테이너 0px init(`onCreate` relayout 방어).
- `[패턴]` **배포 안전 절차**(2026-05-18 prod 장애·"특정 PC만" 규율 폐기): 이 저장소가 정본 — 어느 PC든 `git pull`+`.env.local`(`.env.example` 참고)이면 빌드·배포 가능. 실원인 = **카카오 키 단일**: `.env.local` 활성 키 `d464b4`(전용키·도메인 미등록)면 prod 지도 전체 사망 → **빌드 전 운영 공유키 `58bf34` 확인 필수**(아래 앵커 grep). ~~Auth 승인 도메인 누락~~ = **오진**(`buslink-prod.web.app`·`firebaseapp.com`은 Firebase 기본 자동 등록, 콘솔 실측 반증). prod 권한오류 재발 시 도메인 아닌 다른 원인(규칙/익명인증 흐름) 조사. 절차: 키확인→`CI=false npm run build`→`npm start` localhost `/p`·`/bus` 지도→`firebase deploy --only hosting`→prod curl 재확인. **회귀 시 가설-재배포 금지·콘솔 Hosting 롤백 먼저·재현은 localhost.**
- `[패턴]` **Kakao 키 판독은 줄머리 앵커 grep만**: `grep -nE '^REACT_APP_KAKAO_MAP_KEY=' .env.local` → 활성=`58bf34`(운영). 9번 `# … d464b4`는 비활성 복구값 주석 — STOP 아님. `grep -i kakao | head -1` 류는 주석을 매칭해 d464b4 오독→불필요 STOP(2026-05-19 직전 에이전트 오탐). 활성 키가 `d464b4` 시작일 때만 STOP.
- `[패턴]` **"승객앱"=EmployeeApp(`/p`)**, PassengerApp(`/bus`) 아님. `/p`=사번+PIN·localStorage 세션·홈/노선/탑승/설정 탭. `/bus`=익명·URL 딥링크/QR 배포용. 사용자 "승객앱" 기능 요청 시 기본 대상은 `/p` — 2026-05-17 세션 오대상 재작업 이력. 작업 전 화면/URL 확인.
- `[패턴]` **노선 사전경로 `routes/{id}.routePath`**: `[{lat,lng}, ...]` plain number 배열(GeoPoint 아님). AdminApp RoutesTab "경로 그리기" 모달이 수동 작성. 빈 배열/필드 없음=미설정. EmployeeApp/PassengerApp는 **유효좌표 ≥2면 사전경로, 아니면 `stops.map` 직선 폴백**(하위호환 — 기존 노선 무파괴, 신규/편집부터 점진 적용). 실시간 경로진행·도착판정은 `lib/routeProgress.js`(투영 progress 기반), 폴백 시 기존 노선순서 로직. **이어그리기는 `openPathDraw`가 기존 `route.routePath`를 초기 `pathPoints`로 로드해 자동 지원**.
- `[패턴]` **GPS 콜드스타트 출발지 공백 = `watchPosition` 단독+엄격 옵션**: 첫 측위 10초 초과 시 TIMEOUT 묻혀 출발 직후 수십초 `gps` 미기록. 표준 해결: ①`startGPS` 진입 즉시 `getCurrentPosition`(`maximumAge:30000,timeout:8000`) 1회 선발행(역행가드로 watch 첫콜백 중복 차단) ②watch `timeout`30s·`maximumAge`10s 완화 ③TIMEOUT(code3) 1회 watch 재시작 가드(무한루프 방지)+`onGpsError` 상위 전파(권한 code1 포함). 상수 분리(`INITIAL_FIX_OPTS`/`WATCH_OPTS`). `sendGPS`/throttle/heartbeat/visitedStops 불변.
- `[패턴]` **권한·PWA설치 게이트 = `lib/usePermissions.js`+`components/PermissionGate.js`** (순수 브라우저API, Firebase import 금지). `beforeinstallprompt`는 등록 SW 필요 → `index.js`가 `firebase-messaging-sw.js` 1회 선등록(`lib/notifications.js` 재등록과 idempotent). EmployeeApp 홈·DriverApp 상단에 마운트.
- `[패턴]` **PWA 설치형 SW(`firebase-messaging-sw.js`)에 캐시 절대 금지**: 설치가능 조건은 *fetch 핸들러 존재* 만 요구 → `self.addEventListener('fetch',()=>{})`(respondWith 없음=네트워크 통과)로 충분. precache/respondWith/workbox 추가 시 옛 빌드 shell 영구 서빙(stale 장애 계열). `<InstallPrompt/>`는 순수 프레젠테이션, 재노출 localStorage 14일 스누즈, standalone/appinstalled 영구비표시.
- `[패턴]` **앱별 PWA 아이콘 = manifest 3종 + 마운트 동적 교체**: 정적 path-scope 는 `/p`만 가능·기사앱은 `/`에서 Auth 분기 마운트라 단일 정적 manifest 불가 → `manifest.json`(관제·기본·favicon·apple-touch) + `manifest-employee.json`(scope `/p`) + `manifest-driver.json`, `src/lib/pwaManifest.js applyAppManifest`(순수·idempotent)를 EmployeeApp/DriverApp 마운트 `useEffect`(빈 deps)에서 1회 호출. **로컬 래스터 도구(ImageMagick/sharp/resvg) 부재** → manifest 아이콘은 SVG(`sizes:"any"`) 정본 + 1024 PNG raster/iOS apple-touch fallback. **purpose `any`만**(maskable 미지정 — Route 풀블리드 가장자리 보존). `design/`은 gitignore 스크래치 — `public/icons/`로만 복사.
- `[패턴]` **정류장 사진은 Firestore 직저장**(Storage 미사용): `lib/image.js` 긴변 1000px·JPEG q0.6→0.45→0.3 단계압축, base64 >700KB 거부(1MB 문서 한도). data URI가 stop 문서에 포함돼 onSnapshot 페이로드 증가 — 정류장당 1장 전제. 다수/고해상 추가 시 Storage 전환 검토.
- `[패턴]` `lib/notifications.js`는 `getToken` 전 기존 push subscription `unsubscribe` — VAPID 키 교체 충돌 방지(삭제 금지).
- `[패턴]` 루트 `CLOUD_FUNCTION_FCM.js`는 v1 옛 참고 스니펫 — 실배포는 `functions/index.js`(v2).
- `[패턴]` `boardingTokens`/`partnerCodes`는 규칙상 read 공개(`true`) — 토큰 추측·코드 유출 주의.
- `[패턴]` 카카오 `services`(Geocoder/Places) 사용 시 `window.kakao?.maps?.services` + 콜백 `status` 가드 필수 — SDK 미로드/한도초과 시 우아 실패+수동 경로(지도선택/좌표직접/자유텍스트) 보존. RoutesTab 정류장 검색이 이 패턴(`handleAddrSearch`).
- `[패턴]` **임시 진단 박스 = `[DIAG-X 제거예정]`/`[/DIAG-X]` 경계 + 모든 return 분기 결합**: `return null` 자리도 `return diagBox`로 바꿔야 standalone/snoozed/null-mode 어떤 분기든 가시화 가능. 다음 커밋에 `grep DIAG-X` 1줄로 통째 제거. DIAG-B(`/p` 흰화면, 2026-05-19) · DIAG-INSTALL(설치팝업, 2026-05-20) 둘 다 이 패턴으로 1회 사용자 확인 후 종결.
- `[패턴]` **minified 번들 검증은 `grep -o TOKEN file \| wc -l`** (매칭 횟수). `grep -c`는 단일행 prod 번들에 늘 1 — 오탐. 한국어 라벨은 terser가 ASCII `\uXXXX`로 escape하므로 fixed-string grep 0건이 정상 — **ASCII 식별자**(window 속성 `__buslinkDeferredBIP`·LS 키 `buslink_*`·Firestore 필드 `routePath`·CSS 클래스 `pickerModal`·SVG 색 코드 `00BF40`) 또는 Node로 escape needle 생성해 검증.
- `[패턴]` 실 테스트/별도 lint 없음 — 수동 검증. 배포는 사용자 명시 요청 시에만.

## 저장소 메모
- `src.zip` git 추적되나 작업트리 삭제 상태(소스 백업, 빌드 미사용).
