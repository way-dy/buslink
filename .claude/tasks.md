# Tasks

> 작업 시작/완료 시 이 파일만 수정. 체크박스 관리. 어느 PC든 이어작업용.

> **2026-09-02 — QR 탑승 4단계 → 3단계(세브란스병원 총무팀 요청 · 최우석 매니저 전달 · ✅ prod 배포 완료 · `main.593eab1f.js` · `--only hosting`)**: "qr탑승 누르면 바로 카메라 open > qr태깅 > 탑승완료". 직원앱 `EmployeeApp.js ScanTabDriverQR` 단독 — `ScanTabPassengerQR`·`BoardingApp`(폰 기본 카메라) 무변경 · **전 거래처 공통 기본 동작**(플래그 신설 0) · rules/CF/indexes 변경 0.
> - [x] 진입 즉시 카메라(마운트 `startScan` + StrictMode 세대 가드 `scanGenRef`)
> - [x] 태깅 즉시 탑승 처리(`confirm` 단계·`handleBoard` 제거 → 인자로 받는 `runBoarding({ staticQr, token })`)
> - [x] 서버 왕복 절반(정적 QR `resolveStaticDispatch` 프리뷰 제거 · 동적 토큰 `boardingTokens` 사전 조회 제거)
> - [x] 완료 화면에 노선·차량·탑승자·부서 표 이동(고정 QR 뱃지 유지)
> - [x] 회귀 테스트 `scripts/test_quick_boarding.cjs` **40단언**(옛 코드 대조군 19건 실패로 공허하지 않음 확인) · 게이트 43→**44/44**
> - [x] 실화면 `scripts/headless_check_quick_boarding.cjs`(카메라 있음/없음 2갈래 전부 통과 · 옛 빌드 대조군 4건 실패)
> - [x] 빌드 신규 경고 0(21↔21) · 번들 −189B
> - [x] **prod 배포**(2026-09-02 · `--only hosting` · 게이트 44/44 → `Deploy complete!`) · [x] **커밋·푸시**
> **배포 실측** = 서빙 번들이 `main.4cc37866.js` → `main.593eab1f.js` 로 교체(옛 파일은 404→SPA 리라이트로만 응답) · 라이브 번들에 `boardStatic` **있고** `resolveStaticBoarding` **없음**(왕복 2→1 이 실제로 올라갔다는 증거) · 배포 전 카카오 운영키 `appkey=58bf34` 확인. ⚠ **실기기 태깅은 여전히 미검증** — 아래 ⓐ~ⓓ 를 실제 폰으로 볼 것.
> 🔴 **회귀 가드(복원 금지)**: ready(카메라 열기) 폴백 화면 제거 금지 · 완료 화면의 노선·차량 표시 제거 금지 · 서버 오탑승 가드 3종(`boardingRouteId`·`token.includes("/")`·회사 불일치) 유지 · 완료 후 자동 재시작 금지 · `scanGenRef` 제거 금지. 상세는 `.claude/issues.md` 맨 위.
> **고객 설명서** = `docs/manual/EMPLOYEE_GUIDE.md` 4장 「기사님 QR을 찍는 방식」을 새 흐름으로 갱신(탭을 누르면 카메라가 바로 켜지고 인식 즉시 완료 · 완료 화면의 노선·차량 표시 · 안 켜질 때 `카메라 열기` 폴백). ⚠ **PDF 재빌드는 아직**(협력사 안내서와 같이 미룬 건).
> ⚠ **다음 사람에게 — 눈으로 볼 것**(실기기): ⓐ **iOS Safari** 에서 탭을 누르는 것만으로 카메라가 열리는지(안 열리면 오류 화면으로 떨어지고 "다시 시도"를 한 번 눌러야 한다 = iOS 만 예전 4단계) ⓑ 안드로이드에서 태깅 한 번으로 탑승이 적재되고 완료 화면에 **노선·차량이 뜨는지** ⓒ 같은 차량 재태깅 시 "이미 탑승 처리됨" ⓓ 완료 후 "확인" → 카메라가 **꺼진 채** ready 로 돌아오는지. 🔴 실제 태깅→적재는 prod boardings 에 쌓이므로 하네스에서 하지 않았다(2026-08-25 가짜 탑승 사고 교훈).

> **2026-09-02 — 협력사 포털: 뒤로가기·인증 유지·PC 레이아웃(way 요청 · ✅ prod 배포 완료 · `main.4cc37866.js` · `--only hosting` · 커밋 `b787582`)**: way "뒤로가기하면 무조건 페이지를 빠져나온다 · 로그인 계속 유지 · 나갈 땐 모달로 확인 · 뒤로 갈 땐 기존 페이지로 / PC 에서는 화면이 가로로 채워져 어드민처럼(모바일 반응형은 그대로 두고)".
> **배포 실측** = 5개 도메인(p·d·admin·partner·web.app) 번들 해시 일치 · **prod 실화면 E2E 전 항목 통과**(샘플 거래처 실로그인 → 탭 2회 이동 → 뒤로 2회 앱 안 복귀 → 3회째 확인 모달 → 머무르기 → 새로고침 인증 유지 → 나가기로 이전 페이지 복귀 → 모바일 폭 회귀 · 페이지 오류 0). 고객 설명서 `docs/manual/PARTNER_GUIDE.md` 도 같이 갱신(인증 30일 유지·인증 해제·뒤로가기·PC 표) — ⚠ **PDF 재빌드는 아직**(아래 별도 항목이 추적).
> **신설** = `src/lib/backNav.js`(순수·앱 안 뒤로가기 판정) · `src/lib/useBackNav.js`(React 껍데기) · `src/lib/partnerSession.js`(업체코드만 30일 저장). **수정** = `PartnerApp.js`(탭 정본 `MAIN_TABS` · PC 셸 · 승객 관리 PC 표 · 통계/운영포털 2열) · `lib/partner.js`(만료일 널가드). 상세·회귀 가드는 `.claude/issues.md` 맨 위.
> **검증** = 게이트 41→**43/43** · 빌드 신규 경고 0(21↔21) · 로컬 빌드 실화면 E2E 전 항목 통과(샘플 거래처 `DY001-삼성전자샘플-2026-SMPL`).
> 🔴 **부수 발견 — 시연용 샘플 거래처는 지금까지 포털 로그인이 아예 안 됐다**: `seed_sample_partner.cjs` 가 `expiresAt: null` 로 만드는데 `validatePartnerCode` 가 널가드 없이 `.toDate()` 를 불러 화면에 `Cannot read properties of null` 이 떴다. **삼성전자 시연에서 포털을 열 계획이면 이 수정이 배포돼 있어야 한다.**
> ⚠ **다음 사람에게 — 눈으로 볼 것**(실기기): ⓐ **안드로이드 크롬**에서 뒤로가기 — 탭을 옮긴 뒤 뒤로가면 이전 탭으로 가고, 첫 화면에서 한 번 더 누르면 모달이 뜨고, "나가기"가 **한 번에** 먹는지(설치형 PWA standalone 의 OS back 은 원래 100% 강제 불가) ⓑ 새로고침·앱 전환 후에도 업체코드를 다시 안 묻는지 ⓒ 공용 PC 라면 "인증 해제"로 지워지는지 ⓓ 실제 명부가 큰 거래처(신촌세브란스 16,155명)에서 **PC 표**가 무겁지 않은지 — 표시 상한 100명은 그대로다.
> 배포 = `--only hosting`(rules·indexes·CF 변경 0). 되돌리려면 직전 prod 번들로 롤백.

> **2026-09-01 — 🔴 «내 정류장이 자꾸 풀린다» 의 진짜 근인은 rules 였다(✅ 배포 완료 · `firestore.rules`)**: 노선 갈라짐을 고치고 **실화면 검증을 돌리다 드러났다** — 노선은 유지되고 서버(`fcmTokens`)에도 내 정류장이 남아 있는데 **화면이 복원을 못 했다**(21초를 기다려도). 콘솔이 답을 그대로 뱉었다: `[내 정류장 복원 실패] Missing or insufficient permissions.`
> 규칙이 `match /fcmTokens/{empNo} { allow read: if isAdmin(companyId); allow write: if isAuth(); }` 였다 — 앱이 내 정류장을 **쓸 수는 있어도 다시 읽을 수 없었다.** 2026-05-22 도입 이래 **한 번도 복원된 적이 없다.** `.claude/issues.md` 에 2026-05-26 부터 «비치명적(사용자가 다시 정류장을 고르면 됨)» 으로 적혀 있던 그 항목이 **2026-09-01 신고의 정체**였다(그날 하루에만 287명이 다시 지정했다).
> 🔴 **같은 이유로 조용히 죽어 있던 것 하나 더** — 설정 탭의 «도착 임박 알림 켜짐/꺼짐» 은 같은 문서를 읽어 판정하므로 **지정한 사람에게도 늘 «꺼짐»** 이었다.
> **수정** = 규칙 헬퍼 `isSelfPassenger(companyId, empNo)` 신설(커스텀 토큰 클레임 role·companyId·empNo **셋 다** 일치) → `allow read: if isSelfPassenger(...) || isAdmin(...)`.
> 🔴 **onCall 위임을 쓰지 않은 이유(플레이북과 갈리는 지점)**: playbook backend-auth 는 «익명 화면이 admin-only 컬렉션을 읽으면 rules 를 열지 말고 onCall 로 위임» 이라고 못박고 buslink 가 이미 2회 재발했다. 그러나 그 처방의 전제는 «**익명** 화면이 **남의 데이터가 든** 컬렉션» 이다. 여기는 2026-08-25(P1)부터 승객에게 **진짜 신원**이 있고 여는 것은 **자기 문서 한 건**이라 전제가 다르다. 판단이 바뀌면 여기부터 다시 볼 것.
> **검증(플레이북이 요구하는 실토큰 프로브)** = 신규 `scripts/test_fcmtoken_self_read.cjs` **6/6** — ⓐ 승객 토큰으로 자기 문서 읽기 성공 ⓑ 🔴 **같은 토큰으로 남의 문서(신고자 사번) 읽기 거부** ⓒ 🔴 **익명 세션은 여전히 거부**(규칙을 안 넓혔다는 증거) ⓓ 쓰기 계약 불변. 🔴 서비스 계정으로 재면 늘 통과하므로 **클라이언트 SDK + 실제 커스텀 토큰**으로 쟀다.
> **실화면 최종 확인** = `scripts/headless_check_route_binding.cjs` **전 항목 통과**(신촌세브란스 검토용 계정을 신고자 모양으로 잠깐 맞췄다 원복). 탭 왕복 후 내 정류장 복원 **대기 0ms**. 그 전 실행에서는 이 검사가 **21초를 기다려도 실패**했다 = 이 하네스는 공허하지 않다.
> ⚠ **하네스 함정 2건(이 세션에서 실제로 밟음)**: ⓐ 정류장은 `<button>` 이 아니라 `<div onClick>` 이라 태그 셀렉터가 0개를 반환했고, 그 뒤 단언들이 `undefined === undefined` 로 **거짓 통과**했다 → Firestore 에서 실제 정류장 **이름**을 받아 그 글자를 누른다 ⓑ 복원 판정을 `/내 정류장/` 로 하면 **미선택 안내문**(«아래 노선도에서 **내 정류장**을 클릭하세요»)에 걸려 늘 통과한다 → «안내문이 사라졌는가 + «정류장 변경» 버튼이 떴는가» 로 바꿨다.
> 🔴 **옛 번들 대조군은 불가능하다**(실측): Firebase Hosting 은 **현재 릴리스의 파일만** 서빙하고 없는 경로는 SPA rewrite 로 index.html 을 돌려준다 — 옛 `main.*.js` 를 부르면 HTML 이 와서 `Unexpected token '<'` 로 죽는다. `/static/**` immutable 은 **브라우저 캐시** 이야기지 서버 보존이 아니다. 대조군이 필요하면 옛 코드를 **로컬에 빌드**해 `BASE=http://localhost:3000` 으로 태울 것.

> **2026-09-01 — 로그인 화면 「비밀번호를 잊으셨나요」 신설(way 승인 · ✅ prod 배포 · `main.7ddf8b88.js`)**: 위 신고를 파 보니 **결함이 아니라 회복 경로가 없는 것**이 문제였다 — 로그인 화면에 비밀번호 찾기 문구가 **소스에 0건**이었고, 잊은 사람은 앱 안에서 스스로 회복할 방법이 아예 없었다(재발급은 협력사 포털에서 담당자만).
> 🔴 **셀프 재설정은 만들지 않았다** — 본인 확인에 쓸 값(사번·이름)이 `passengers` read `isAuth()` 라 **익명에게 다 열려 있어**(P4 미해결) 그걸로 재설정을 열면 아무나 남의 계정을 가져간다. P4 가 끝나기 전에는 담당자 재발급이 유일하게 맞는 답이다.
> 🔴 **링크만 달지 않았다** — `handleSubmit` 의 catch 에서 `setHelpOpen(true)` 로 **실패한 그 순간 펼친다**. 링크만 두면 아무도 안 누르고 문의는 그대로 담당자에게 간다.
> **문구는 실측에 맞췄다**(틀린 안내는 없느니만 못하다): ① 사번은 안내문 그대로 · **앞의 0 도 빼지 말 것**(신촌세브란스 16,150명이 0 으로 시작) ② 안 바꿨으면 **000000**(prod 15,109명이 이 해시) ③ **한 번이라도 바꿨으면 000000 은 안 된다**(로그인한 1,066명 중 1,046명) ④ 그래도 안 되면 담당자에게 **비밀번호 재발급** → 000000 으로 돌아간다.
> 검증 = 신규 `scripts/test_login_help.cjs` **12단언**(🔴 [2] 는 소스를 grep 하지 않고 **태그를 걷어낸 평문**으로 잰다 — JSX `<b>` 때문에 문장이 멀쩡한데도 빨개지는 걸 실제로 밟았다 · [3] 은 «셀프 재설정을 안 만들었는가» 를 잠근다) · 게이트 39→**40/40** · 빌드 신규 경고 0(21↔21) · **prod 실화면**(없는 사번으로 실패시켜 확인 — 처음엔 접혀 있고, 실패 직후 자동으로 펼쳐지고, 문구 4줄이 그대로 나오고, 페이지 오류 0).

> **2026-09-01 — 📌 앱 스토어(iOS·Play) 등록 = 보류 결정(way)**: 회의 미해결 질문([09:27] «고민해보자고»)에 대한 답 — **「고객사가 강력하게 원할 때 다시 고민」**. 그 전에는 착수하지 않는다.
> **판단 근거(다시 묻지 않도록 남긴다)**: ⓐ PWA 는 스토어에 그대로 못 올린다 — **네이티브 래퍼 필요**(선례 `app/callcenter-native` Capacitor 8.4) ⓑ **iOS 빌드·서명은 macOS+Xcode 에서만** 되는데 현 작업 PC 는 Windows ⓒ 🔴 **심사 가이드라인 4.2(Minimum Functionality)** — 웹사이트를 재포장한 앱은 반려된다. 웹뷰가 `p.buslink.co.kr` 을 로드하는 구조면 통과가 불확실하고, 통과시키려면 QR·위치·푸시를 **네이티브로** 다시 만들어야 해 래퍼 작업이 커진다 ⓓ **업데이트마다 심사 1~3일** — 🔴 **2026-09-01 이 그 실증이다: 하루에 hosting 5회 배포했고 그중 3건이 승객이 못 타던 긴급 수정**이었다. 스토어였다면 오전 신고 → 오전 수정이 불가능했다 ⓔ 승객앱은 사내 통근용이라 **검색으로 발견될 필요가 없고** 안내문 QR 이 이미 최단 경로다.
> 🔴 **Apple Developer Enterprise Program($299/년)은 검토 대상이 아니다** — 약관상 **자사 직원 전용**이라 고객사 직원 배포는 위반이고 계정이 취소될 수 있다. 착수하게 되면 후보는 ① 일반 App Store 또는 ② **Apple Business Manager 커스텀 앱**(특정 조직 배포·스토어 미노출·**심사는 그대로**)뿐이다.
> 📌 **착수 시 첫 단추 = 개인정보처리방침** — 스토어 필수 제출물인데 **저장소에 없다**(grep 0건). 위치·사번·이름을 수집하므로 스토어와 무관하게도 있어야 하는 문서다.
> ⚠ 금액·D-U-N-S 소요기간·가이드라인 세부는 Apple 이 수시로 바꾼다 — 착수 시 developer.apple.com 에서 현재 조건 재확인.

> **2026-09-01 — 회의 「신촌세브란스 서비스 오픈 미팅」 점검 완료(`/meetings` · 10:52 · 9분24초)**: 녹음 `hbnRCuLcQWDhPEEAzPSU` · status=done · **pending 0건**(sync 재실행 불필요) · 산출 `meetings/2026-09-01_신촌세브란스_서비스_오픈_미팅.md`(+full).
> **§2 교차검증 통과** — 전문 166줄·**중복 줄 0건**(퇴행 없음 · 9분이라 길이 위험도 낮음) · 요약↔전문 안건 일치(핀번호·CS 정류장·카톡 채널·유비칸·스토어) · 🟠배너 없음.
> ⚠ **고유명사 2건 확인**(issues.md 경고대로 요약·전문이 같이 틀릴 수 있어 프로젝트 데이터에 대조): ⓐ 요약 «동영케어» vs 전문 «동형케어» → **동영케어가 맞다**(portfolio-memory: 카카오 채널 2개 = @드라이버스 기사 전용 / **동영케어 고객 CS**). ⓑ 회의 내내 «0000» 으로 전사됐으나 **실제 값은 `000000`**(오늘 해시 실측 `d05ab5dffb`) — 참석자가 통칭으로 말한 것이라 결정 내용에는 영향 없다.
> 🟢 **회의 결정이 오늘 배포한 수정과 일치한다** — [00:20] way «초기 비밀번호를 변경하지 않으면 **난수 생성이 됐었거든**» · [01:11] «그럼 놔두고 그냥 **초기 비밀번호만 접속 가능하게끔** 계속 해주면 될 거 같고» = 위 (A) 수정 그대로. **액션 아이템 「신촌세브란스 앱 초기 비밀번호 접속 가능 유지」는 완료**로 볼 수 있다.
> ✅ **로그인 페이지 카카오톡 문의 링크 완료·배포(2026-09-01 · way 가 URL 전달 · `main.0bb8aea2.js`)** — 채널 `https://pf.kakao.com/_gxlHfX`(동영케어·고객 CS). 상수 `KAKAO_CS_CHANNEL_URL` 로 한 곳에 둔다.
> 🔴 **도움말 안에만 두지 않았다** — 로그인 버튼 바로 아래에 «비밀번호를 잊으셨나요? | 카카오톡 문의» 로 **항상 보이게** 뒀다. 비밀번호 문제가 아닌 문의도 있고, **로그인 전에는 문의 탭(dycs 위젯)에 닿을 수 없다**(그 탭은 거래처별 opt-in 이라 꺼진 곳도 많다). 도움말 안에도 노란 «카카오톡으로 문의하기» 버튼을 둬 실패 직후 바로 보이게 했다.
> 🔴 **기사 전용 채널(@드라이버스)과 다른 채널이다** — 잘못 넣으면 승객 문의가 기사 채널로 가서 받을 사람이 없다(portfolio-memory 2026-07-28). 가드가 «파일에 실린 pf.kakao.com 주소가 CS 채널 하나뿐인가» 로 잠근다.
> **검증** = `test_login_help.cjs` 12→**17단언** · 게이트 **41/41** · **prod 실화면**(상시 링크 `target=_blank`·`rel=noopener noreferrer`·보임 / 로그인 실패 시 도움말 자동 노출 + 버튼 노출 / 페이지오류 0).
> ⚠ **하네스 함정** — 「드라이버스」를 문자열로 찾는 단언은 **그러지 말라고 적어 둔 주석**에 걸려 빨개진다(실제로 밟았다). 금지어 검사는 주석이 아니라 **실제로 링크되는 값**으로 할 것.
> 📌 옛 항목(참고) — 남은 개발 건 1개 = 로그인 페이지에 동영케어 카카오톡 채널 안내·링크([03:45]~[05:14] «로그인이 안 되면 카톡으로 문의하라고» · «카카오톡 채널 안내 동영케어 안내 페이지를 링크를 넣어야겠다»). 오늘 만든 「비밀번호를 잊으셨나요」 도움말 **바로 아래가 그 자리**다. 🔴 **착수 전 way 에게 채널 URL 을 받아야 한다** — 저장소에 `pf.kakao.com` 주소가 없고, 전사도 «동영케어 검색하면 신촌세브란스 치면 나온다» 는 검색 안내라 **주소를 지어내면 안 된다**.
> 그 밖 = CS 정류장 회신·유비칸 연동 모니터링(내일까지)은 **영업담당자 몫** · 앱 스토어 등록은 way 미결정([09:27] «고민해보자고» — 관리 부담 vs 고객 요청).
> 📌 **이월 액션 아이템 16건 중 다수가 이미 완료**인데 md 가 계속 이월하고 있다(형식상 «완료된 줄은 직접 삭제»). 확인된 완료분 = 홈페이지 연결 탭·QR 태깅 소리·노선 꼬리표 진하게·슬리핑차일드 관리자 화면·로그인 초기비번 안내·고정QR 노선매핑·협력사 노선도/노출옵션·UI 커스터마이징·요약 매뉴얼·운행 시간창. 진짜 미완 = 그룹 설정 구조(설계 초안만)·셔틀 다차량 관리·buslink↔콜센터 통합. **way 확인 후 정리 권장**(안 지우면 다음 회의로 계속 따라간다).

> **2026-09-01 — 🔴🔴 초기 PIN 이 클라 「000000 고정」 · 서버 「랜덤 6자리」로 **갈라져 있다**(원인 규명 완료 · 수정 대기)**: way 질문(「초기비밀번호를 미처 변경 못하고 다시 접속해 초기비밀번호를 입력하면 로그인이 안 된다」)을 파다 나왔다.
> · 클라 `src/lib/accountCards.js generateInitialPin()` → `DEFAULT_INITIAL_PIN` = **"000000"**(2026-08-25 way 「그냥 셋팅해줘」 결정)
> · 서버 `functions/index.js:4119 generateInitialPinAdmin()` → **`crypto.randomInt` 랜덤 6자리**. 게다가 `isValidInitialPinAdmin` 은 `t !== "000000"` 이라 **000000 을 아예 무효로 취급**한다.
> 🔴 **2026-08-25 에 클라만 고치고 서버를 안 고쳤고, 2026-08-28 P3-a 로 등록·재발급이 CF(`partnerImportPassengers`·`partnerReissuePins`)로 이관되면서 실제로 도는 것은 서버 쪽 = 랜덤이다.** 즉 **그날 이후 신규 등록·재발급된 사람의 초기 PIN 은 000000 이 아니다** — 그런데 로그인 화면은 「초기 비밀번호는 000000 입니다」라고 안내한다. 그 사람이 000000 을 넣으면 **로그인이 안 된다**(= 신고 문장 그대로).
> **prod 실측**: 신촌세브란스 16,155명 중 «안 바꿨다는데 000000 으로 로그인 불가» = **2명**(`0032732 이승혜`·`0033849 신경미` — 둘 다 **오늘 09:43 에 secrets 가 갱신**됐고 `pinInitial:true` 로 되돌아 있다 = 재발급을 받은 흔적). 명부↔secrets 불일치 0명, secrets 누락 0명이라 **P3-a 이관 자체는 깨끗하다**. 15,095명은 여전히 000000 으로 정상 로그인된다(8/27 업로드분은 000000 해시).
> ⇒ **지금은 2명이지만, 앞으로 재발급·신규등록되는 사람은 전부 이 문제를 겪는다.** 「안 되는 직원」에게 담당자가 재발급을 해 줄수록 늘어난다.
> 🔴 **내가 오늘 배포한 로그인 도움말 문구가 이 때문에 틀렸다** — 「재발급하면 000000 으로 돌아가고」는 **사실이 아니다**(랜덤 번호가 발급되고 담당자 화면에 한 번만 표시된다). 어느 쪽으로 고치든 이 문장은 같이 고쳐야 한다.
> ✅ **(A) 로 수정·배포 완료(2026-09-01 way 승인)** — `generateInitialPinAdmin()` → `"000000"` 고정 · `isValidInitialPinAdmin` 의 `!== "000000"` 거부 해제(기본값을 엑셀에 적으면 그 행만 조용히 실패하던 것). CF `partnerImportPassengers`·`partnerReissuePins` 재배포 · hosting 재배포(주석만 바뀌어 번들 해시는 `main.7ddf8b88.js` 그대로 = 정상).
> **실호출 검증**(검토용 계정 REVIEW 하나로 · 실제 사람 미접촉 · 끝나고 원복): `partnerReissuePins` 호출 → **발급 평문 PIN "000000"** · secrets 해시 `d05ab5dffb`(=000000) · `pinInitial:true`. **✅ 재발급 = 000000 — 로그인 화면 안내와 일치.**
> **회귀 가드** = 신규 `scripts/test_initial_pin_parity.cjs` **12단언** — 🔴 **클라·서버·화면 문구 세 곳이 같은 값을 말하는지**를 잠근다(클라 순수모듈을 vm 으로 태우고, 서버는 `index.js` 가 `defineSecret` 이라 **함수 본문만 뽑아** 평가). 「호출마다 같은 값」 단언이 랜덤 복귀를 직접 잡는다. 게이트 40→**41/41**.
> ⚠ **아직 갈려 있는 것 하나** — 지정 초기 PIN 길이 검사가 클라 `isValidInitialPin`=**4~6자리** vs 서버 `isValidInitialPinAdmin`=**6자리 고정**. 이번 범위 밖이라 손대지 않았다(넓히는 변경이라 별도 판단 필요). 담당자가 엑셀 `초기PIN` 에 4~5자리를 적으면 **그 행만 서버에서 거부**된다.
> 🔴 **아직 갇혀 있는 실제 승객 2명** — `0032732 이승혜`·`0033849 신경미`(오늘 09:43 랜덤으로 재발급받은 사람들). 이 수정은 **앞으로의 발급**만 고치므로 그 둘은 여전히 000000 으로 못 들어간다. **담당자가 협력사 포털에서 두 사람을 다시 «비밀번호 재발급» 하면 이제 000000 으로 풀린다** — way 판단(포털에서 담당자가 직접 하는 편이 기록·안내 면에서 낫다).
> 📌 **옛 후보(참고용) (A) 서버를 클라에 맞춘다** — `generateInitialPinAdmin` → "000000" 고정 + `isValidInitialPinAdmin` 의 000000 거부 해제. 2026-08-25 결정을 **복원**하는 것이라 이쪽이 정합적이다. ⚠ 단 그러면 «발급 즉시 남이 먼저 로그인해 PIN 을 바꿔 본인이 잠긴다» 는 위험(이미 tasks.md 에 적힌 감수 사항)이 재발급분에도 그대로 적용된다. **(B) 안내를 서버 동작에 맞춘다** — 「재발급하면 담당자 화면에 새 번호가 한 번 표시됩니다. 그 번호로 로그인하세요」 + 로그인 화면 상단의 「초기 비밀번호는 000000」 문구도 조건부로. 🔴 **둘 중 하나만 고르면 안 되고, 고른 쪽으로 클라·서버·안내문구 3곳을 함께 맞춰야 한다**(이번 사고가 정확히 «한쪽만 고쳤다» 였다).

> **2026-09-01 — 🚧 미해결: 「초기 PIN 안 바꾸고 앱 끄면 재로그인 불가」(최우석 신고 · 재현 실패 · 정보 필요)**: 신고문 = 「최초 로그인때 초기비밀번호를 변경하세요 창에서 변경을 하지 않고 어플리케이션을 끈 직원들이, 다시 어플 작동하여 로그인하려고 하면 로그인이 안된다」.
> **재현 실패** — 검토용 계정을 «미시작»(PIN 000000·`pinInitial:true`) 으로 맞추고 두 경로를 다 태웠으나 **둘 다 정상**이었다: ⓐ localStorage 를 지운 뒤 재로그인 → 성공 ⓑ 앱만 끄고 재실행(localStorage 유지 → `passengerResume`) → 성공. 콘솔 오류 0.
> **prod 실측(16,155명)이 가리키는 것은 다른 그림이다**: 초기 PIN 은 **전원 000000 이 맞다**(해시 `d05ab5…` = sha256("000000"+salt) · 15,109명. 다른 값 11종은 합쳐 11명뿐). 그런데 **로그인한 1,066명 중 1,046명이 이미 PIN 을 바꿨다** — 아직 안 바꾼 사람은 **20명뿐**이다. PIN 설정 화면에 **«건너뛰기»가 없어**(버튼은 «설정하고 시작하기» 와 «다른 계정으로 로그인» 뿐) 화면을 본 사람은 사실상 전원이 바꾼다. 즉 신고문이 말하는 «안 바꾸고 끈» 집단은 20명이고 그들은 실측상 재로그인이 **된다**.
> 🔴 **훨씬 큰 집단은 «바꾼 1,046명»이고, 그중 자기가 정한 번호를 잊은 사람은 000000 으로 로그인할 수 없다** — 그게 «로그인이 안된다» 의 유력한 정체다. 그리고 **로그인 화면에 비밀번호 찾기·재설정 경로가 없다**(실측 — 그 문구가 소스에 0건). 재발급은 협력사 포털에서 담당자만 할 수 있으니, 잊은 사람은 앱 안에서 스스로 회복할 방법이 **아예 없다**. 부차 후보 = **사번 16,150명이 0 으로 시작**(앞의 0 을 빼고 입력하면 «없는 사번»)·전화번호 형식 사번 3명.
> **추가 실측(way 가 «세브란스 총무팀에서는 전체가 안된다» 전달 · 최우석 본인은 재현 실패)**:
> ⓐ 🔴 **로그인은 지금도 정상이다** — 성공 로그인(`lastLoginAt`) 시각 분포: 09-01 06시 40명 · 07시 66명 · 08시 42명 · **09시 24명**, 가장 최근이 **조회 1분 전(09:43:46)**. **내 배포 4건 전후로 끊김이 없다**(08:1x·08:4x·09:xx 호스팅 3회 + 10:xx rules). «전체가 안된다» 는 데이터와 맞지 않는다.
> ⓑ 🔴 **총무팀 계정은 명부에 2개뿐이고 둘 다 이미 PIN 을 바꿨다** — `629529 김용호`(8/27 로그인)·`ssh_admin 테스트`(8/24). **그래서 000000 으로는 못 들어간다**(정상 동작이지 결함이 아니다). 부서 분포는 «연세의료원» 16,149명 + 총무팀 2명이 전부다.
> ⓒ 김용호 님은 **계정이 3개**다 — `010-5405-1140`(초기 000000 그대로) · `0629529`(바꾼 번호 · **오늘 08:45 로그인 성공**) · `629529`(바꾼 번호 · 8/27). 담당자 본인이 어느 계정으로 뭘 넣는지 헷갈릴 수 있는 상태다.
> ⓓ 사번 «0 붙은/뗀» 중복 쌍은 **전체에서 1건뿐**(김용호 본인) — 광범위한 원인이 아니다. 다만 신촌세브란스 사번은 **16,150명이 0 으로 시작**하므로 앞의 0 을 빼고 입력하면 «없는 사번» 이 된다.
> **⇒ 현재 가장 유력한 정체 = «PIN 을 바꾼 1,046명이 000000 을 넣는 것».** 결함이 아니라 **회복 경로가 없는 것**이 문제다.
> 🔴 **«앱을 끄고 다시 접속하면 어떻게 되는가» 실측(2026-09-01 · 이것이 신고의 유력한 정체다)**:
> ① 로그인 화면조차 안 뜬다 — 세션이 유지되어 **자동으로 들어간다** ② 그리고 **똑같은 「비밀번호를 정해주세요」 화면이 다시 뜬다** ③ 그 화면의 버튼은 **「설정하고 시작하기」와 「다른 계정으로 로그인」 둘뿐 — 건너뛰기가 없다**(2026-07-27 의도된 설계: 초기 비밀번호를 쓰는 동안에는 앱을 못 쓴다) ④ 입력칸은 「새 비밀번호(숫자 4~6자리)」·「새 비밀번호 확인」.
> 🔴 **그 화면에서 새 비밀번호로 `000000` 을 넣으면 거부된다** — «너무 쉬운 비밀번호입니다»(`EmployeeApp` 클라 가드 + CF `passengerSetPin` 서버 가드 이중). 그리고 **그 화면에 그대로 갇힌다**(실측 확인).
> **⇒ 그래서 사용자 눈에는 이렇게 보인다**: 로그인을 눌렀는데 앱이 안 열리고 같은 화면이 반복된다 → 「다른 계정으로 로그인」을 눌러 돌아가 000000 을 다시 넣는다 → 또 같은 화면. **무한 루프처럼 느껴지고 «로그인이 안된다»로 신고된다.** 실제로는 로그인은 성공했고 **앱 진입만 막힌 것**인데 사용자는 둘을 구분하지 못한다.
> 📌 **코드 결함이 아니다 — 화면이 상황을 설명하지 않는 것이 문제다.** 후속 후보(way 판단): ⓐ 그 화면에 «로그인은 되었습니다. 비밀번호만 정하면 바로 이용하실 수 있습니다» 안심 문구 ⓑ 000000 거부 문구를 «안내문의 000000 은 다시 쓸 수 없습니다. 본인만 아는 다른 번호로 정해주세요» 로 — 지금 문구는 **왜 안 되는지**를 말하지 않는다.
> 📌 **다음 한 걸음 = 실패한 직원 사번 1~2개를 받는 것.** 그 문서의 `pinInitial`·`lastLoginAt` 만 보면 «안 바꿨는데 안 된다»(=진짜 결함) 인지 «바꿔 놓고 잊었다»(=회복 경로 부재) 인지 **즉시 갈린다**. 추측으로 고치지 말 것.
> 📌 **어느 쪽이든 필요한 것** = 로그인 화면의 «비밀번호를 잊으셨나요» 통로(담당자 재발급 안내 또는 셀프 재설정). 지금은 16,141명이 첫 로그인을 앞두고 있는데 회복 경로가 없다.

> **2026-09-01 — 새 빌드 자동 반영(way 요청 · ✅ prod 배포 완료 · `main.3c73a831.js`)**: 같은 날 긴급 수정을 배포하고 나서 **이미 열려 있는 앱에는 닿을 수단이 없다**는 것이 드러났다(way: 「모두 앱을 종료하라는 건 현실적으로 어렵다」). 실측 = 서비스워커는 **FCM 전용**(`fetch` 는 PWA 설치 조건용 빈 껍데기·캐시도 `message` 핸들러도 없다) · 앱에 버전 확인·자동 새로고침이 **없다**(`location.reload()` 는 권한 카드의 수동 버튼 하나) · `useWakeTick` 은 Firestore 재구독만 하고 페이지를 다시 읽지 않는다. 🔴 **그래서 그날은 그 장치를 배포해도 소용이 없었다** — 신호를 받을 코드가 이미 옛것이다. 사고 **전에** 심어 두는 장치라 이번에 넣었다.
> 신설 `src/lib/appUpdate.js`(순수 · Firebase·React import 0) — 지금 우리를 로드한 `<script>` 의 번들 해시 ↔ 서버 `index.html` 의 번들 해시를 대조해 다르면 조용히 `location.reload()`. **빌드 시각을 주입하지 않는다**(빌드 설정 무변경 — 해시가 곧 버전이라 도입 비용이 0에 가깝다). `EmployeeApp` 최상위에서 **`wakeTick` 전이에서만** 호출.
> **🔴 회귀 가드(복원 금지)**: ⓐ **wake 시점에만** 부를 것 — 그때 사용자는 아직 아무것도 입력하지 않았고 새로고침이 «앱이 열리는 것»과 구별되지 않는다. `setInterval` 폴링으로 바꾸면 **쓰고 있는 도중에 화면이 날아간다** ⓑ `busy`(탑승 탭 = QR 스캔·카메라·탑승 처리 중) 제외를 지우지 말 것 ⓒ **`sessionStorage` 목표 해시 표시를 지우지 말 것 = 무한 새로고침 차단**(새로고침했는데도 옛 번들이면 그 해시로는 영영 재시도 안 한다) ⓓ 판정을 «모르면 안 한다»에서 «모르면 한다»로 뒤집지 말 것(한쪽이라도 못 읽으면 false) ⓔ `cache:"no-store"` 유지(클라 캐시가 옛 index.html 을 주면 «새 빌드 없음»으로 오판한다) ⓕ 번들 해시를 빌드타임 상수로 박지 말 것.
> **적용 범위 = 승객앱(`/p`)만**. 기사앱은 일부러 뺐다 — 운행 중 새로고침은 GPS watch 를 다시 걸어 기사가 모르는 사이 추적이 끊길 수 있고, 기사는 31명이라 전화로 닿는다. 관리자·협력사도 제외(사람 수가 적다).
> 검증 = 신규 격리 `scripts/test_app_update.cjs` **35단언**(🔴 절반이 «새로고침하지 않는다» 쪽이다 — busy·재시도·통신 실패·번들 미상·`sessionStorage` 접근 거부 + 배선 잠금 3종) · 게이트 38→**39/39** · 빌드 신규 경고 0(21↔21).
> ⚠ **실기기 미검증** — 헤드리스로 «백그라운드 → 복귀» 전이를 재현하지 못했다(분류기가 브라우저 실행을 막았다). 다음 배포가 첫 실전이다. **볼 것**: 앱을 백그라운드에 뒀다가 배포 후 돌아왔을 때 한 번 깜빡이고 새 화면이 뜨는지, 그리고 **두 번 이상 깜빡이지 않는지**(깜빡임이 반복되면 즉시 롤백 — 직전 번들 `main.27696bec.js`).

> **2026-09-01 — 🔴 긴급: 내 정류장이 풀리고 탑승이 막히던 것(조수빈 문의 · ✅ prod 배포 완료 · `main.3c67131c.js` · `--only hosting`)**: 홈이 보는 노선(`activeRouteId`)과 스캐너가 서버에 보내는 노선(`session.routeId`)이 **다른 값**이었고, 홈 탭은 탭 전환마다 언마운트되므로(QR 탑승 버튼도 탭 전환이다) 돌아올 때마다 화면이 첫 즐겨찾기로 튕겨 ① 내 정류장 복원 실패 ② "선택한 노선의 차량이 아닙니다" 가 동시에 났다. **prod 실측 390명**(즐겨찾기 보유 469명 중) — 신고자는 그날 탑승 적재 0건. 수정 = 노선을 바꾸는 길을 `bindRoute` 하나로 모으고 세션에 영속(`routePinned`), 판정은 순수 함수 `routeOrder.pickHomeRoute` 로 분리. 상세·회귀 가드는 `.claude/issues.md`.
> **배포 실측(2026-09-01)**: 앵커 `58bf34` 확인 → 게이트 38/38 → `--only hosting` → **4개 도메인 해시 일치**(p·d·admin·partner: `main.db7b83e6.js` → `main.3c67131c.js`) → 서빙 번들을 내려받아 `routePinned` 실재 확인. rules·indexes·CF 변경 0.
> 🔴 **실화면 검증은 못 했다** — `headless_check_home_qr_fold.cjs`(BASE=prod)를 태우려 했으나 **분류기가 막았다**(prod Firestore 쓰기 + 브라우저 실행 · 2026-08-27 과 같은 벽). 번들 수준까지만 확인했다. way 가 직접 돌리려면 `! BASE=https://p.buslink.co.kr node scripts/headless_check_home_qr_fold.cjs`.
> ⚠ **다음 사람에게 — 배포 후 볼 것**: ⓐ 신고자(사번 0879824)가 홈에서 `06:48 광명` 칩을 한 번 누르면 그 뒤로는 탭을 옮겼다 와도 그 노선이 유지되는지 ⓑ 내 정류장이 그대로 남는지 ⓒ QR 태깅이 통과하는지. 🔴 **첫 진입에는 배정 노선(갈현동)이 아니라 즐겨찾기(광명)로 정착한다** — `routePinned` 가 없는 기존 사용자는 홈 목록 안으로 한 번 자동 정착하고 그 값이 세션에 남는다(그게 스캐너가 보내는 노선이 된다). ⓓ 즐겨찾기가 없는 절대다수(15,940명)는 **동작 변화 0** 이어야 한다.
> 🔴 **운영 발견(코드 아님)**: 신촌세브란스 승객이 **전원 배정 `07:19 갈현동`** 이다(2026-08-28 에 이미 적어 둔 «16,149명이 한 노선» 과 같은 사실). 즉 그 거래처에서 **배정 노선은 의미가 없고 즐겨찾기가 진짜 신호**다 — 명부 엑셀의 노선코드를 고치기 전에는 이 전제가 계속 유효하다.
> 🔴 **신촌세브란스 기준 실측(2026-09-01 · way 지적으로 다시 셈)** — `scripts/inspect_mystop_impact_by_partner.cjs`:
> · 실피해(내 정류장 노선 ≠ 스캐너가 보내는 노선) **197명 중 191명이 신촌세브란스**(채드윅 6). 노출 375 / 즐겨찾기 390 / 승객 16,155.
> · **오늘 하루에만 287명이 내 정류장을 다시 지정했다**(전체 지정자 501명 중). "아무리 지정해도 자꾸 풀린다"는 한 사람의 문의가 아니라 **287명이 같은 일을 반복**하고 있던 것이다.
> · **9/1 이 이 거래처 QR 탑승 실가동 첫날**이다 — 최근 7일 적재: 9/1 **156건** · 8/31 15 · 8/28 5 · 나머지 0. 그래서 오늘 아침에 터졌다.
> · 오늘 156건은 **전부 `via:"static"`(차량 부착 QR)**, 노선은 27개로 흩어져 있고 갈현동은 5건뿐이다.
> 🔴 **그래서 알게 된 것 — 차단은 「앱 안 스캐너」에서만 일어난다**: `BoardingApp`(폰 기본 카메라, `/board?c=&v=`)은 `selectedRouteId` 를 **안 보내서 노선 검증 자체가 없다**. 앱 안 QR 탑승 버튼(`ScanTabDriverQR`)만 `session.routeId` 를 보내고 막힌다. 오늘 156명은 폰 카메라로 찍어 통과했고, 신고자는 앱 안 버튼을 눌러 막혔다(그날 적재 0건). ⚠ **차단된 시도는 어디에도 기록되지 않는다** — boardings 에 안 남고 `nfcRejects` 같은 대응물도 없어 **실제로 몇 명이 못 탔는지 셀 수 없다**. 이 관측 공백 자체가 후속 과제다.
> 🔴 **남은 위험(코드 아님·데이터) — 즐겨찾기 없는 15,765명**: 신촌세브란스 승객 **16,155명 중 16,149명이 배정 `07:19 갈현동`**(명부 엑셀 노선코드가 한 값 · 2026-08-28 에 이미 적어 둔 그것). 즐겨찾기로 자기 노선을 표현한 390명은 이번 수정으로 해결되지만, **나머지 15,765명은 앱 안 QR 로 찍으면 여전히 갈현동으로 검증되어 막힌다**(수정 전후 동일 = 회귀는 아니다). 홈 화면이 QR 탑승 버튼을 크게 밀고 있어 이 경로로 몰릴수록 드러난다. **근본 해결은 명부의 배정 노선을 실제 노선으로 고치는 것**(고객사 작업). 코드 완화안은 아래.
> ✅ **완화 적용·배포 완료(2026-09-01 way 승인 · `main.27696bec.js`)** — 오탑승 방지 게이트를 **「직접 고른 사람」에게만** 건다: `routePinned` 가 없으면 `selectedRouteId` 를 보내지 않는다(= 폰 카메라와 같은 동작). 근거 = 그 게이트(2026-07-16 회의 #1)는 «승객이 앱에서 선택한 노선» 을 전제로 하는데, 예전엔 **명부의 일괄 기본값을 '선택'으로 취급**했다 — 이번에 고친 결함과 같은 계열(사용자가 고르지 않은 값을 선택으로 읽는 것). 고른 적 없는 사람에게 그 값으로 거는 건 보호가 아니라 **거짓 차단**이다. 정본 = `routeOrder.boardingRouteId` — 「선택했다」로 인정하는 신호는 **사용자가 앱에서 한 행동 둘뿐**이다: `routePinned`(노선 변경·칩으로 직접 고름) · **즐겨찾기 안의 노선**(별을 눌러 고른 것 중 하나로 정착). 그 밖은 `null` = 폰 기본 카메라(`BoardingApp`)와 같은 동작. **🔴 회귀 가드**: ⓐ `session.routeId` 를 스캐너에 직송하는 코드로 되돌리지 말 것 ⓑ 인정 신호를 넓힐 때는 그것이 정말 **사용자의 행동**인지부터 물을 것(명부·기본값·파생값은 행동이 아니다) ⓒ pinned·즐겨찾기 경로의 차단은 **그대로 살아 있어야 한다**(완화가 과하면 격리 [4b] 의 뒤 두 단언이 깨진다). 검증 = `test_home_route_bind.cjs` 17→**26단언**(신규 [4b] 7 + 구조 잠금 3) · 게이트 38/38 · 빌드 신규 경고 0.
> 📌 **재실측 도구**: `node scripts/inspect_mystop_route_mismatch.cjs [이름]`(읽기 전용 — 홈 노선과 배정 노선이 갈라지는 사람 수 + 신고자 개인 진단).

> **2026-08-30(2) — 홈 화면 아이콘 이름 `카카오통근` · 조사 · 설치 팝업 다시 부르기 · ✅ prod 배포 완료 · 빌드 `main.db7b83e6.js`**: way 2건. ⓐ 「설치 가이드 팝업이 다시 안 보여」 = **결함이 아니라 3일 스누즈**(한 번 닫으면 3일). 스누즈는 그대로 두고 **`?install=1`** 로 그 판정만 건너뛰게 했다(standalone 은 여전히 안 뜬다). 설정 탭 **📲 앱 설치하기**가 원래부터 상시 진입점이라는 것도 함께 안내할 것. ⓑ 「카카오 T는 기존 앱하고 이름이 겹칠 것 같아」 → **홈 화면 아이콘 이름만** `카카오통근` 으로 가르고(`theme.appName` + `manifest-kakao.json.short_name`) **앱 안 표기는 카카오모빌리티가 지정한 「카카오 T」 유지**. 🔴 이름을 바꾸니 **조사가 깨졌다**(「카카오통근를」) → 순수 모듈 `src/lib/josa.js` 신설. 검증 = `test_partner_theme.cjs` 64→**82단언** + 가드가 이제 문구를 **조사까지** 보고 `short_name` 을 실제로 fetch 한다 + 스누즈 우회 실측 통과 + 빌드 신규 경고 0(22↔22).
> 🔴 **way 확인 결과 — 신촌세브란스는 건드리지 않는다**(「거기는 거기만의 셋팅이 있어」). 처음엔 이름을 `THEME_PRESETS.kakao` 에 넣었는데 **카카오 프리셋을 켠 거래처가 삼성전자 샘플과 신촌세브란스 2곳**이라 실운영 고객까지 딸려갔다 → **거래처 문서로 내렸다**: `partnerCodes/DY001-삼성전자샘플-2026-SMPL.theme = {preset:kakao, appName:"카카오통근", manifest:"/manifest-kakao-commute.json"}`(**prod 적용 완료**). 공용 `manifest-kakao.json` 은 **직전 정본과 바이트 단위로 동일**함을 diff 로 확인했고, 샘플 전용 `public/manifest-kakao-commute.json` 을 새로 팠다(아이콘·색 동일·이름만 다름).
> **대조군 실측(로컬 빌드 + 배포 후 prod 재실측, 둘 다 통과)**: 신촌세브란스 = 제목 `카카오 T 승객` · `/manifest-kakao.json` · short_name `카카오 T` — **전부 예전 그대로**. 삼성전자 샘플 = `/manifest-kakao-commute.json` · `카카오통근` · 팝업 「홈 화면에 카카오통근을 추가하면」.
> ⚠ **다른 거래처에 다른 이름을 주려면**: 매니페스트 파일을 하나 더 만들고(`short_name` 을 그 이름으로) `node scripts/set_partner_app_options.cjs <거래처> --app-name <이름> --app-manifest /<파일> --apply`. 🔴 스크립트가 **파일의 `short_name` 과 `--app-name` 이 다르면 거부**한다(팝업과 실제 아이콘 이름이 갈리는 것을 막는 유일한 자동 검사다). 되돌리기 = `--app-name-off`.
> **2026-08-30 — 설치 팝업도 거래처 표기·아이콘으로 · ✅ prod 배포 완료 · 빌드 `main.8a5d1ed3.js`**: way 질문 「카카오 적용사업장은 설치 아이콘도 카카오 T 로 적용가능해?」 → **실측 먼저**: `<head>` 파비콘·애플터치·매니페스트·탭 제목은 **이미 카카오 T 로 나가고 있었다**(prod 확인 — 삼성전자 샘플·신촌세브란스 둘 다 `kakao-t.svg`/`manifest-kakao.json`, 채드윅 등 비카카오는 그대로). 남아 있던 곳은 **설치 유도 팝업 하나** — `InstallPrompt` 가 앱 고정 매핑만 봐서 카카오 톤 화면에 파란 BusLink 아이콘이 떴다. 고침 = `InstallPrompt({brandName, iconHref})`(부재=현행) + `EmployeeApp` 에서 전달. 샘플 거래처(`DY001-삼성전자샘플-2026-SMPL`)는 `seed_sample_partner.cjs` 가 이미 `theme:{preset:"kakao"}` 라 **따로 켤 것이 없다**. 검증 = 신규 가드 `headless_check_install_prompt_brand.cjs`(카카오·비카카오 2건 전부 통과·페이지오류 0) + 빌드 신규 경고 0. **배포 후 prod 재실측도 2건 전부 통과**(`BASE=https://p.buslink.co.kr`). `--only hosting` 단독 — rules·indexes·CF 변경 0.
> **2026-08-28 — 매뉴얼 `.md` 정정(PDF 재빌드는 미완)**: wrapup 설명서 점검에서 **이번 변경과 무관한 오안내**를 찾았다 — `EMPLOYEE_GUIDE`·`PARTNER_GUIDE`·`PASSENGER_QUICK` 셋 다 **첫 PIN 을 `000000` 으로** 안내하고 안내문의 개인 번호를 «8월 이전 발급분» 예외로 뒤집어 적어 뒀다. 2026-07-27 부터 **사람마다 다른 임의 6자리**이고, 명부가 익명에게 열려 있는 지금은 «전원 PIN 이 000000» 안내가 그 자체로 위험하다 → 셋 다 «안내문에 적힌 번호가 정본, 2026-07 이전만 000000» 으로 정정. 함께 `PARTNER_GUIDE` 에 이번 변경 2건 반영(승객 관리 **100명씩 표시**·검색은 전체 기준 / 일괄등록 **진행률 표시**)과 `PIN초기화(→000000)` → `비밀번호 재발급`(개인별 6자리·한 번만 보임) 교정. 🔴 **PDF 는 여전히 2026-07-16 빌드** — 아래 별도 항목이 추적한다(캡처 하네스는 이번에 인덱스를 고쳐 `EMP=` 우회 없이 돈다).

> **2026-08-28 — P3-a 완료(승객 PIN 해시 분리) · ✅ prod 배포 + 1단계 백필 완료 · 빌드 `main.272ceee3.js` · CF 6종**: 익명이 명부를 **읽고 고칠 수 있다**는 실측(위 항목)에 대한 첫 조치. `pinHash` 를 `companies/{cid}/passengerSecrets/{empNo}`(rules `read,write:false`)로 옮기고, 서버 읽기 5곳을 `readPinHash`(secrets 우선→명부 폴백)로, 포털의 명부 쓰기를 신규 CF `partnerImportPassengers`·`partnerReissuePins` 로 이관했다. 판정은 순수 모듈 `functions/passengerRoster.js` 로 분리(`index.js` 는 `defineSecret` 이라 격리 테스트 불가). 클라 `partner.js` 에서 `hashPin`·`verifyPassenger`(호출부 0) 제거 = **클라에 해시 코드가 없다**. ⚠ 같은 날 오전에 넣은 클라측 배치(`documentId() in`+`writeBatch`)와 그 테스트 `test_import_employees.cjs` 는 이 이관으로 대상을 잃어 **폐기**했다(지키려던 «왕복하지 않는다»는 `test_passenger_roster.cjs` [9] 로 이었다).
> **데이터**: 1단계 복사 **16,409건 완료**(재실행 시 대상 0 = 멱등). 🔴 **2단계 `--strip --apply` 는 대기** — 대상 16,409건 전부 준비됐고 **이걸 해야 값 노출이 실제로 사라진다**(1단계만으로도 덮어쓰기 공격은 막힌다 — secrets 가 우선이라 명부 해시를 고쳐도 로그인이 안 된다). 🔴 2단계 뒤에는 `readPinHash` 의 **명부 폴백을 지워야** 한다.
> **검증**: 격리 41단언 + **실호출 18단언**(익명 클라이언트로 CF 호출 · 샘플 거래처에 시험용 승객 만들고 끝에 삭제) + 규칙을 익명으로 재서 secrets 읽기·쓰기 거부 확인 + 협력사 포털·승객앱 실화면 통과 + 빌드 경고 24→**22**(신규 0). **미검증 = 실제 고객 일괄업로드**(다음 업로드가 첫 실물).
> **다음**: P3-b(포털 인증) **설계만 완료**(`passenger-auth-design-draft.md`) — 담당자 초기 비밀번호 배부가 선행이라 way 일정 대기. 🔴 한 번에 전 거래처를 켜지 말 것.

> **2026-08-28 — 승객 인증 P1·P2 는 이미 prod 에 있다(문서가 낡았던 것) · 🔴 명부 노출은 258건 → 16,409건으로 커졌다**: 아래 체크박스가 「🚧 배포 전(2026-08-25)」로 남아 있었으나 **실측하면 배포돼 있다** — CF 5종(`passengerLogin`/`Resume`/`Logout`/`Migrate`/`SetPin`) 전부 `functions:list` 에 있고, 세션 시작 시점 prod 번들 `main.4a34b4fd.js` = 커밋 `66475ba` 산출물이며 그 빌드에 `passengerSetPin`/`passengerResume` 호출이 들어 있다. **남은 것은 P3(협력사 포털 명부 CRUD 서버 이관) → P4(명부 닫기)** 다.
> 🔴 **그런데 P4 를 미룬 대가가 자릿수로 커졌다** — 신규 진단 `scripts/inspect_passenger_exposure.cjs`(읽기 전용·값은 안 찍고 건수와 필드 '이름'만 센다) 로 **익명 로그인 후 실측**: 명부 **16,409건 전건 집계·문서 내용 읽기 모두 성공**, 필드 13개 중 `name·dept·empNo·partnerName·pinHash` 가 그대로 보인다(대조군 `passengerSessions` 는 정상 거부 — 규칙이 통째로 열린 건 아니다). 2026-08-25 에 이 위험을 적을 당시 명부는 258건이었고 지금은 **병원 통근 명부 16,155명**이 들어 있다. 게다가 그중 **미시작 16,141명**이 앞으로 발급 PIN 으로 첫 로그인을 하는데 **`passengerLogin` 에 시도 제한이 없다**(P4 와 함께 넣기로 미뤄 둔 것). ⏳ **레거시 유예 `PASSENGER_LEGACY_UNTIL=2026-09-30` 까지 33일** — 그날 `passengerMigrate` 와 `boardStatic` 의 `pinHash` 경로가 닫힌다. **P3·P4 는 규칙·아키텍처 변경이라 way 승인 후 착수**(이 세션에서는 실측과 기록만).

> **2026-08-28 — 명부 16,000명대 규모 대응(게시판 `m00ghlRl` 최우석 「관리자페이지 느려짐」) · ✅ prod 배포 완료 · 빌드 `main.389b5d14.js`**: 신고 = 「운영포탈 통하여 임직원일괄등록 시 홈페이지가 느려집니다. 협력사페이지, 관리자페이지」. **근인은 하나 — 화면들이 «한 거래처 250명» 전제로 짜여 있는데 신촌세브란스병원 명부가 8/27 에 16,155명이 됐다**(전사 16,409명 · 채드윅 244 · 나머지 7곳은 1~3명). 고친 곳 4개:
> ① `src/lib/partner.js importEmployees` — 사번마다 `getDoc`→`setDoc` **순차 왕복**이었다(실측 1건 18.9ms → 16,155명이면 **읽기만 5.1분** + 쓰기 16,155회). **읽기는 쿼리로**(이 거래처 명부 1회 + 그 명부에 없는 사번만 `documentId() in` 30개 청크·동시 12) **쓰기는 배치로**(450건×동시 4). 🔴 청크 조회를 빼면 **다른 협력사에 있는 사번(이관자)을 신규로 오판해 PIN 이 새로 발급되고 그 사람이 로그인 못 한다** — 이 검사가 이 변경의 안전핀이다. 배치 실패 시 그 450명만 문서별 재시도. `uploadCount` 는 `increment(1)`(예전 `(await getDoc).uploadCount + 1` 은 필드가 없으면 NaN 이었다). 협력사 화면에 **진행률**(`등록 중... 1,234 / 16,155명`) 추가.
> ② 협력사 **승객 관리** — 걸러진 인원을 **전부** 카드로 그렸다. 실측 전·후(같은 하네스·prod): **16,155장 / DOM 226,378노드 → 100장 / 1,624노드**. 집계·검색은 전체를 그대로 쓰고 **그리는 것만** 100장씩 끊는다(`N명 중 M명 표시 중` + 더 보기). PIN 발급 결과 모달도 200명까지만(전원분은 안내문 인쇄에 포함).
> ③ 협력사 **운영 포털** — `getDocs(partnerCode+active)` 로 16,155건을 문서째 받아 노선별로 세었다 → **Firestore 집계(count)** 로(문서 0건 전송·노선 121개 × 37ms 동시 8). `partnerOpsRoutes` 에 집계 맵 입력 경로 신설. 미배정 = 총원 − 노선별 합계.
> ④ **관리자 노선 관리** — `getDocs(companies/dy001/passengers)` 로 **전사 16,409건**을 정원 계산 하나 때문에 통째로 받았다 → 노선별 `getCountFromServer`. `seatUsage` 에 집계 맵 입력 경로 신설.
> **검증** = 격리 78단언(신규 `test_import_employees.cjs` 23 — 가짜 Firestore 로 **왕복 횟수까지** 센다: getDoc 0회·쿼리 35회·배치 3회 / `test_seat_usage.cjs` 27 / `test_partner_ops_routes.cjs` 28) · 실화면 2종(신규 `headless_check_partner_manage_scale.cjs` — 대조군으로 옛 prod 의 16,155장·226,378노드를 재현한 뒤 배포 후 통과 / `headless_check_partner_ops_routes.cjs` 29노선 통과) · 빌드 경고 24↔24(신규 0). 🔴 **미검증 = 관리자 콘솔 노선 관리 탭**(Auth 로그인이 필요해 헤드리스로 못 띄운다) — 정원 표시(`12 / 45석`)가 예전 값과 같은지 화면에서 한 번 볼 것. 🔴 **일괄등록도 실제 업로드로는 미검증** — 실제 사람 명부에 쓰는 일이라 격리 테스트로만 잠갔다. 다음 업로드 때 진행률과 등록 인원을 확인할 것.
> 🔴 **운영 발견(코드 아님)**: 신촌세브란스 **16,149명이 한 노선(`07:19 갈현동`·45석)에 배정**돼 있고 **미시작 16,141명**이다. 명부 엑셀의 노선코드가 한 값으로 채워졌을 가능성이 크다 — 이대로면 노선별 인원·정원 표시가 무의미하고 도착 알림도 그 노선으로만 나간다. 고객사 확인 필요(게시판에는 안 적었다 — 요청 범위 밖).

> **2026-08-28 — 홈 QR 탑승을 첫 화면으로(게시판 `6StRo4Dy` 최우석) · ✅ prod 배포 완료 · 빌드 `main.8a52dc58.js`**: 요청 「현재 스크롤 후 QR탑승을 해야하는데, 스크롤없이 한 화면에」. 근인 = 홈 레이아웃이 **지도 `flex:'0 0 52%'` 고정**이라 지도가 먼저 절반을 가져가고 노선도·하단 ETA/QR 패널이 스크롤 컨테이너로 밀려 있었다(2026-07-09 #3 에 패널을 스크롤 안에 넣은 것이 원인). 실측(스크롤 0 기준): QR 버튼 **828~866px vs 탭바 783px**(390x844) · **722~760 vs 579**(360x640) — 둘 다 화면 밖. 고친 곳 = `src/pages/EmployeeApp.js` — ① 지도 `0 0 52%` → **`1 1 0` + `minHeight:150`**(남는 공간만) ② 스크롤 컨테이너는 **노선도만** 담고 `flex:'0 1 auto'` ③ 하단 ETA/QR 패널을 스크롤 **밖** 고정 형제로 ④ 패널 우측 여백 14→**58px**(도움말 '?' FAB 이 QR 탑승·정류장 변경을 덮고 있었다 — 패널이 화면 안으로 들어오며 드러난 겹침). 결과 = 두 기기 모두 **스크롤 자체가 사라졌고** QR 733~770 / 529~566 으로 탭바 위. 빌드 경고 24↔24(신규 0)·+6B.
> **가드 2벌**: 신규 `scripts/headless_check_home_qr_fold.cjs`(390x844·360x640 × 정류장 미선택/선택 4상태에서 **QR 버튼 아래끝 vs 탭바 윗변** + 도움말 FAB 겹침). 🔴 기존 `headless_check_home_strip.cjs` 는 **8/25 승객 인증 전환 이후 로그인이 안 돼 죽어 있었다**(localStorage 세션만으론 로그인 화면으로 떨어짐 → "스트립을 못 찾음"). 둘 다 `passengerSessions` 승계표(resumeToken) 발급 방식으로 통일하고 **기본 대상을 샘플 승객 `SAMPLE-SEC`** 로 바꿨다(실제 사람 계정 안 건드림). 같은 김에 strip 가드의 오판 하나 수정 — 정류장 이름은 `-webkit-line-clamp:2` 라 `scrollWidth>clientWidth` 가 **세로 말줄임을 가로 잘림으로** 읽었다(설계상 허용인데 빨간불). prod(`BASE=https://p.buslink.co.kr`) 실호출로도 전 항목 통과.

> **2026-08-28 — 거래처 워드마크(카카오 T) · ✅ prod 배포 완료(커밋 66475ba) · 빌드 `main.4a34b4fd.js`**: 카톡 요청(최주호 전달 · 카카오모빌리티 윤지영 부장) 「버스링크 대신 카카오모빌리티 또는 카카오T 문구 적용이 가능할까요」 → way 승낙. 2026-08-27 의 «색만» 결정을 **고객 요청으로** 뒤집는다. 고친 곳 = `src/lib/partnerBranding.js`(`theme.wordmark`/`wordmarkSub`·`sanitizeWordmark`·`brandOf`) · `components/ui/BusLinkLogo.js`(`name` prop) · `components/InstallPrompt.js`(`brandName` prop) · `pages/EmployeeApp.js`(워드마크 2곳·홈 밴드·설정 안내 5곳·꼬리말·탭 제목 효과) · `public/index.html`(og 태그 — 카톡 카드에서 «버스링크» 제거) · 안내서 `docs/manual/kakao-deck/`(`{{brand}}` 자리 + `BRAND` 기본값 `카카오 T`). **표기는 `카카오 T`** — 상대가 준 소개서 제목이 `[카카오 T] 통근셔틀 서비스 소개서` 라 그쪽 서비스명이 그것이다. 회사명으로 바꾸려면 `THEME_PRESETS.kakao.wordmark` 와 `build.cjs` 의 `BRAND` **두 줄**을 `카카오모빌리티` 로. 검증 = 격리 52단언 + 렌더 27단언 + 빌드 경고 24↔24(신규 0)·+358B.
> **안내서 재빌드 완료** — 캡처 7장을 **로컬 새 빌드**(`_serve_build.mjs` + `BASE=http://localhost:3000`)로 다시 찍어 `docs/manual/out/통근셔틀_이용안내서.pdf` 6쪽 생성(넘침·겹침 0·콘솔오류 0). 명령 = `EMP=SAMPLE-SEC BASE=… node scripts/capture_passenger_screens.cjs docs/manual/kakao-deck/shots 삼성전자` · `PC=DY001-삼성전자샘플-2026-SMPL BASE=… node scripts/capture_login_screen.cjs …/shots/login.png`(신규 스크립트) · 같은 `PC` 로 `capture_board_screen.cjs`. 🔴 **옛 파일 `버스링크_통근셔틀_이용안내서(_v2).pdf` 2개가 `out/` 에 그대로 있다** — 8/27 에 고객에게 나간 버전이다. 잘못 재전송하지 않게 정리 여부는 way 판단.
> ⚠ **캡처 하네스 함정 2건(2026-08-27 에 밟음)**: ⓐ ~~`capture_passenger_screens.cjs` 의 `where(partnerCode)+orderBy(lastLoginAt)` 가 복합 인덱스 없이 prod 에서 실패~~ → **2026-08-28 해결**: `firestore.indexes.json` 에 `passengers(partnerCode ASC + lastLoginAt DESC)` 추가·배포(빌드 약 5분). `EMP=` 우회 없이 도는 것을 실측 확인 — 채드윅 20명·신촌세브란스 **14명**(예전엔 '쓸 수 있는 승객이 없다'로 끝났다)·삼성전자 샘플 1명. 인덱스를 지우면 다시 죽는다 ⓑ `login`·`board` 캡처는 `?pc=` 없이 찍으면 워드마크가 `BusLink` 로 나온다(로그인 전엔 거래처를 모른다).
> **✅ 배포 완료(2026-08-28 · prod `main.4a34b4fd.js` · `--only hosting` 3회)** — 4개 도메인 해시 일치. **보낼 주소 = `https://p.buslink.co.kr/kakao`** (거래처 코드를 실어 승객앱으로 넘긴다). prod 실측: `/kakao` → 제목 `카카오 T 승객` · 파비콘 `/icons/kakao-t.svg` · 홈화면 아이콘 `/icons/kakao-t-1024.png` · 매니페스트 `/manifest-kakao.json` · 워드마크 `카카오 T` · 콘솔오류 0. **대조군 `/p` 는 전부 예전 값 그대로**(BusLink · passenger.svg · manifest-employee.json) = 나머지 9개 거래처 회귀 0.
> ⚠ **다음 사람에게 — 남은 것**: ⓐ 실계약이 되면 `public/kakao.html` 의 `PC` 한 줄을 **정식 업체코드**로 바꿀 것(지금은 삼성전자 샘플) ⓑ 다른 고객사에 같은 걸 해 주려면 **파일을 하나 더 만든다**(`/kakao.html` 복사 → og 태그·PC 교체 → `firebase.json` rewrite 추가). 🔴 공용 `public/index.html` 에 거래처 상표를 박지 말 것 — 그 파일 하나가 4개 앱·전 거래처를 서빙해서 **채드윅(245명)·신촌세브란스 링크 카드까지** 바뀐다 ⓒ 카카오톡 카드가 옛 제목으로 보이면 카카오 캐시다 — 카카오 디벨로퍼스 «캐시 초기화» 또는 주소 뒤에 `?v=2` 를 붙여 확인.
> **2026-08-25 배포 — 승객 인증 P1·P2 + 홈페이지 탭 + 태깅 소리(prod `main.e8d63c28.js` · CF 6종 · rules)**: 순서는 **functions → firestore:rules → hosting**(반대로 하면 새 번들이 없는 CF 를 불러 **전원 로그인 불가**). 신규 CF 5개 create + `boardStatic` update, 4개 도메인 번들 해시 일치, `functions:list` 6종 `state=ACTIVE`·`us-central1`. 🔴 **신규 onCall 에 `invoker: "public"` 을 명시했다** — 안 적으면 Cloud Run IAM 이 HTTP 401 로 먼저 막고 SDK 가 그걸 `functions/unauthenticated` 로 매핑해 **우리 코드의 거부와 구별이 안 된다**(플레이북 2026-08-24 DI_BMBOARD). **라이브 실측**: `scripts/test_passenger_login_live.cjs` **16/16**(거부 3종 → 실제 로그인 → `signInWithCustomToken` → 클레임·uid → `passengerResume` 복원 → 로그아웃 후 복원 거부) + `test_board_static_auth.cjs` **9/9**. 🔴 **이 검사가 재는 것 = "258명이 내일 아침 로그인할 수 있는가"** — 배포 로그·함수 목록으로는 절대 알 수 없다(함수는 떠 있는데 해시가 안 맞거나 IAM 이 막으면 전원 로그인 불가). ⚠ 라이브 검사는 **검토용 계정 REVIEW 만** 쓰고 탑승 성공 경로는 부르지 않는다(2026-08-25 가짜 탑승 적재 사고 교훈).
> ⚠ **다음 사람에게**: 기존 로그인 세션은 `passengerMigrate` 가 조용히 승계하므로 **재로그인 없다**. 그 승계와 `boardStatic` 레거시 `pinHash` 경로는 **`PASSENGER_LEGACY_UNTIL=2026-09-30` 에 자동으로 닫힌다** — 둘 다 명부에서 누구나 읽을 수 있는 `pinHash` 를 증거로 받기 때문이다. **기한을 늘리지 말 것**(늘리면 P2·P4 가 무의미해진다). 기한 전에 P3·P4 를 끝내는 게 목표다.

> **2026-08-25 미팅 — 고정 QR 본인 확인(prod `main.0e7e475e.js` + CF `boardStatic` 배포 완료)**: 외부 카메라로 차량 고정 QR(`/board?c=&v=`)을 열면 **사번 칸에 아무 문자열이나 넣어도 탑승이 적재**됐다(2026-08-25 배시현 실측 → **라이브 재현 확인**). 근인=`boardStatic` 이 `request.auth` 만 봤는데 그건 **익명 인증이라 누구나 통과**하고, `passengers/{empNo}` 조회는 partnerCode 채우기용이라 **없는 사번도 그대로 적재**됐다. 고친 곳 = `functions/index.js:1451` 에 본인 확인 2겹(① 명부 실재·active ② `pinHash` 일치)을 **배차 해석보다 먼저** 넣음 · `src/lib/boarding.js validateAndBoardStatic` 에 `pinHash` 관통 · `EmployeeApp.js:2989` 는 **세션에 이미 있는 `session.pinHash`** 를 넘겨 승객 입력 0 · `BoardingApp.js` 는 이름 칸을 **비밀번호 칸으로 교체**하고 성공 시 이 기기에 기억(두 번째부터 버튼 한 번, 키 `buslink_board_id` — 앱 세션 키와 **일부러 분리**). 실측 전제 = 승객 **263명 전원 pinHash 보유·비활성 0명**(`scripts/inspect_passenger_auth_fields.cjs`)이라 강제해도 막히는 사람이 없고, 세션에 pinHash 가 영속돼 **재로그인도 불필요**. SW 는 통과형(캐시 0)이라 배포 즉시 새 번들.
> ⚠ **다음 사람에게 — 이 검사를 배포 전에 돌리지 말 것**: `scripts/test_board_static_auth.cjs` 를 **배포 전** 대조군으로 돌렸다가 그 서버가 세 호출을 전부 성공시켜 **가짜 탑승 2건**(하나는 실재 승객 사번)을 prod 에 적재했다 — "거부를 재는 호출이니 안 쓴다"는 넘겨짚음이 틀렸다. 지금은 하네스가 **안전 프로브**(없는 차량 + 없는 사번)로 가드 유무를 먼저 물어 없으면 스스로 멈춘다. 적재분은 **정리 완료**(`node scripts/clean_auth_test_boardings.cjs --apply` · 2건 삭제 후 재조회 0건). 🔴 **남은 구멍(별건)**: `firestore.rules` 의 `passengers` 는 `allow read: if isAuth()` 라 **익명 누구나 명부 전체(이름·부서·거래처·사번·pinHash 263건)를 읽을 수 있다** — 즉 pinHash 는 비밀이 아니다. 승객 로그인이 클라에서 직접 문서를 읽는 구조라 CF 로그인으로 옮겨야 닫힌다(설계 결정 필요·way 판단 대기).

> **2026-08-24 게시판 2건 — 조기 도착 표기(배시현 `szVRhnq7`·`Ob5yVpmu`, prod `main.8e1420dd.js`·`--only hosting`·3개 도메인 해시 확인·둘 다 done)**: 두 건은 **한 근인**이었다. 차가 출발지에 계획보다 일찍 와서 대기하면 그 **실도착 시각이 체인 기준점**이 되어(`stopSchedule.js` (a) 분기 `prevEstMs = actualMs`) ① 홈 노선도 스트립이 `estimatedAt` 우선이라 출발지에 미리 도착한 시각을 띄우고 ② 이후 전 정류장 예상시각이 과거금지 가드(`now+30s`)에 눌려 **화면을 새로고침할 때마다 현재시각으로 바뀌었고**(요청자가 "첫로그인시 정상시간, 내렸다 올리면 바뀜"으로 정확히 짚음) ③ 지연 배지만 raw 체인 기준이라 `예상 06:38 · 조기도착 30분` 처럼 같은 줄에서 어긋났다. 고친 곳 = `src/lib/stopSchedule.js` 신규 `departureBaseMs()` — **출발지(index 0)만** 체인 기준점을 `max(실도착, 계획)` 으로 클램프(departTime = "이 시각에 출발한다"는 약속) · `src/pages/EmployeeApp.js:1546` 스트립 시각 `plannedAt` 우선 · `:2360` 정류장 목록에서 조기도착(tone `warn`) 배지 미표시. 검증 = 신규 격리 `scripts/test_early_arrival_delay.cjs` 16단언(스크린샷 화면 그대로 재현 후 수정) + 신규 실화면 `scripts/headless_check_stop_list_labels.cjs` **대조군 대비**(현 prod: 스트립 첫 정류장 `06:44`·조기도착 28분 1건 → 새 빌드: `06:55`(=노선 출발시각)·0건) + 기존 `headless_check_home_strip` 전부 통과.
> ⚠ **다음 사람에게**: **중간 정류장은 일부러 클램프하지 않았다** — 거기선 실제로 일찍 떠날 수 있고, 그때 예상을 늦게 잡으면 승객이 버스를 놓친다(일찍 잡는 쪽이 안전). `departureBaseMs` 의 `idx !== 0` 가드를 "일관성" 이유로 풀지 말 것. 그리고 **조기도착 배지는 직원앱 정류장 목록에서만 걷었다** — 내 정류장 카드(`EmployeeApp:1750`)·승객앱(`PassengerApp:745`)·기사앱·관리자에는 남아 있다(요청 범위 밖). 근인을 고쳤으니 이제 그 값들은 30분씩 튀지 않는다.

> **2026-08-18 게시판 1건 — 협력사 포털 관제(prod `main.f0acd6a5.js` 배포·`--only hosting`·done)**: 배시현 `GUkjQFbT7cffjUCZEbSK` "협력사도 전체노선 실시간 관제를 · 현재는 관제가 안된다". **버그였다** — 포털 지도가 버스를 **오늘 배차**로 걸렀는데 배차 읽기는 admin/driver 전용이라 익명인 이 화면에선 언제나 거부(=차량 집합 공집합) → Phase 1.3 이후 한 번도 버스가 뜬 적이 없다. 관리자 관제와 같은 축(`gps.routeId`)으로 바꿔 해결. 규칙은 **안 건드렸다**(익명에 배차를 열면 회사 전체 배차가 열린다). 검증=신규 하네스 `headless_check_partner_ops_buses.cjs`(옛 prod 대조군에서 `오늘 배차 없음`·0대 재현 → 로컬·prod 8/8 통과). **같은 스레드 후속 2건도 같은 날 배포**(prod `main.86ac7676.js`): 버스 마커를 승객앱과 같은 치수로 축소(184→91×28px) · **등교/하교/방과후 구분 필터**(정본 `src/lib/routeKind.js` — 🔴 `shift` 는 하교와 방과후가 같은 값이라 못 가른다. 이름으로 방과후를 먼저 가른다). 검증=격리 26단언 + 칩을 실제로 눌러 29→16/8/5 확인. **잔존 2건**은 다음 할 일 참조(노선도 ✓ · 🔴 승객앱 도착시각).
> ⚠ **다음 사람에게**: 이 화면에서 `todayDispatches` 는 **항상 빈 배열**이다(권한). 무언가를 배차 건수로 판정하려 들면 조용히 0 이 된다.

> **2026-08-18 게시판 2건(prod `main.b8fdbf4f.js` 배포·`--only hosting`·3개 도메인 해시 확인·둘 다 done)**: 배시현 ① 정류장 지도 버스 마커 축소(115×39→92×28px·면적 43%↓·차량번호 유지) ② **홈 노선 칩 = 즐겨찾기만**(없으면 배정 1개). 정본 `routeOrder.homeRouteList` + `initialRouteBoundRef`. prod 251명 중 화면이 바뀌는 사람 **3명**(요청자 포함) — way 판단 "그대로 둔다"(이준원·오재우는 자기 배정 노선이 홈에서 빠짐 · 노선 탭에서 별을 누르면 복구). 검증 격리 19단언 + 실화면 2계정(즐겨찾기 0 계정 포함) local·prod.

> **2026-08-18 세션(관제 차량 마커 · 운행 이력 직선 점검 · prod `main.9659fb3a.js` 배포 완료·`--only hosting`·3개 도메인 해시 확인 + CF `pollDeviceVehicleGps` 배포)**: way 스크린샷 2건. ① **실시간 관제** 기본 핀 → **버스 아이콘 + 차량번호·속도 칩**(선택 시 노선·기사 한 줄). 승객앱 마커와 같은 시각 언어. ② **운행 이력 "경로가 직선"** — 실측 결과 **그리기 결함이 아니었다**: 신고 배차 45점의 간격은 전부 50~74초·공백 0회인데 점간 거리가 `0,0,0,332,0,544,…` 로 한 칸 걸러 0 이고, 원천(busin)이 **2분에 한 번·중앙 463m** 만 준다. 그래서 선을 굽히는 대신 **등록 노선 경로(routePath) 밑선 + 연속/공백 구간 분리(공백은 점선) + 수신점 도트 + 표본 간격 요약**을 넣어 화면이 사실을 말하게 했다. 신규 정본 `src/lib/gpsTrack.js`. 검증=격리 48단언(실측 배열 그대로)·실지도 대조 하네스 2종·빌드 신규 경고 0(24↔24). ③ **폴러 기록 방식**(way 승인·같은 날 배포): 최신 1행만 `ts=폴 시각` → **원천 행을 측정 시각 그대로·`fix_{초}` 멱등 ID 로** 적재(워터마크 `gps.lastFixMs`). 배포 후 실측 = 간격이 차량마다 60/120/180초로 딱 떨어지고 **동일 좌표 0구간**. 격리 31단언 + prod 실문자열 파싱 70건 실패 0.
> ⚠ **다음 사람에게**: 직선 자체는 **원천(단말 보고 주기)이 정한다** — 코드로 더 굽힐 수 없다. 더 촘촘한 궤적이 필요하면 유비칸에 **단말 보고 주기 단축**을 요청하는 게 유일한 근본 해법이다(현재 60~180초·차량마다 다름).

> **2026-08-12 세션(배차일정 수정 후 잔존 배차 · prod `main.df05cf78.js` 배포 완료·`--only hosting`·3개 도메인 해시 확인)**: 배시현 개선요청 `k49E7GXEVfe0WT9h4QI6` "배차일정 8/18로 수정했는데 금일 오전 관제가 됨". **`expandDispatchSchedules` 는 create-only** 라 일정을 미래로 밀어도 이미 펼쳐진 배차가 남고, 단말 GPS 폴러가 그걸 오늘 배차로 읽어 승객앱에 `1대 운행중` 을 그렸다. **실측 잔존 54건**(8/12·13·14 각 18건 — 안 고치면 이틀 더 반복). 정본 순수 모듈 `src/lib/dispatchSchedule.js` 신설 + AdminApp 이 **일정 수정·비활성·삭제 3경로 직후** 안 맞는 펼침 배차를 세어 확인받고 삭제. 검증=격리 39단언(서버↔클라 미러 270조합 대조·신호 유무 검사 포함)·빌드 신규 경고 0(24↔24). **데이터 정리 완료**(way 승인 `--apply`): 삭제 52 / 운행흔적 보존 2 / 배차 172→120 / 재검증 0건. 되돌리기 = `node scripts/clean_orphan_schedule_dispatches.cjs --restore deleted_orphan_dispatches_2026-08-12.json --apply`. **이 배포에 2026-08-11 사이드바 스크롤 수정(`0e356ec`)이 함께 나갔다** — 그동안 미배포였다.
> ⚠ **다음 사람에게**: 이 정리는 **오늘 이후만** 손댄다. 과거 날짜의 안 맞는 배차는 기록으로 남겨 뒀고, 오늘 아침 실제로 운행 기록이 찍힌 2건(`[한남1] 등교`·`[강남1] 등교`)도 일부러 남겼다.

## 현재 상태 (2026-08-05 세션 종료)
> 이 저장소가 작업 정본. **어느 PC든** `git pull`(origin/master) + `.env.local`(↓부트스트랩, 카카오 키 `58bf34`)이면 빌드·배포 가능.
> prod 라이브 = **`main.e8d63c28.js`**(2026-08-25 배포 · 승객 인증 P1·P2 + 홈페이지 탭 + 태깅 소리. 4개 도메인 해시 실측)  ~~`main.4c9cccac.js`~~(2026-08-18 배포·협력사 포털 관제 + 익명 화면 도착시각. 실로드 하네스로 확인) + CF 33종(`getRouteStopArrivals`·`recordSleepingCheck`·`registerSleepTag` 신설). 옛 세션 로그는 @.claude/tasks-log.md.
> ⚠ 기사앱 화면의 빌드마커 `v2026-06-09-livegps` 는 **하드코딩 상수라 신선도 판정에 쓰지 말 것** — 판단 기준은 주소와 `curl | grep main.*.js` 뿐.
> ⚠ **배포 검증 함정(이번 세션 실측)**: 경고 대조용 `git stash` → `npm run build`(HEAD) 하면 **`build/` 가 옛 번들로 덮인다**(build/ 는 gitignore 라 `stash pop` 이 안 되돌림). 그 상태로 배포하면 "Deploy complete" 인데 **prod 번들 해시 불변**. 배포 후 `curl | grep main.*.js` 로 **해시가 실제로 바뀌었는지** 반드시 확인(첫 배포가 no-op 이라 회귀는 없었음).
### git 원격
- `origin/master` = 작업 정본. 3월 폐갈래는 `origin/archive/remote-march-2026`(`f63321c`) 영구 백업.

### 새 PC 부트스트랩 (이어작업 1회)
- `git pull` → `npm install` (functions 쓰면 `cd functions && npm install`).
- `.env.local` 생성: `.env.example` 참고, 값은 사용자 보유분. ⚠ `REACT_APP_KAKAO_MAP_KEY`=운영 공유키 `58bf34…`(`d464b4…`면 prod 지도 사망).

### 배포 절차
1. 앵커 grep: `grep -nE '^REACT_APP_KAKAO_MAP_KEY=' .env.local` → `58bf34` 시작 확인.
2. `CI=false npm run build` → `npm start` → localhost `/p`·`/bus` 지도·노선 그리기 확인.
3. `firebase deploy --only hosting` (rules 변경 시 `--only firestore:rules,hosting`) → curl 검증.
4. 회귀 시 가설-재배포 금지 → 콘솔 Hosting 롤백 먼저, 재현은 localhost.

## 다음 할 일
- [ ] 🔴 **승객 인증 구조 — P1·P2 구현 완료 · 🚧 배포 전(2026-08-25)**: 미팅 요청 2건(고정 QR 우회 차단 · 초기 PIN `000000`)과 작업 중 발견한 **명부 전체 익명 열람**이 한 뿌리(=승객에게 Firebase 신원이 없다)에 걸려 있다. 설계 `.claude/passenger-auth-design-draft.md`(P0~P5).
  - [x] **P0 `boardStatic` 본인 확인** — 배포 완료(2026-08-25). 무작위 사번 탑승 차단.
  - [x] **P1 승객 신원(커스텀 토큰)** — ✅**배포 완료(2026-08-25)**. CF `passengerLogin`/`passengerResume`/`passengerLogout`/`passengerMigrate`(`functions/index.js`) + 클라 `src/lib/passengerAuth.js` + `EmployeeApp`·`BoardingApp` 전환. PIN 대조가 **서버로 이동**했고 승객마다 uid(`passenger_{cid}_{empNo}`)·클레임이 생긴다.
  - [x] **P2 탑승·PIN 을 클레임 기준으로** — ✅**배포 완료(2026-08-25)**. `boardStatic` 이 클라가 보낸 사번 대신 **토큰의 사번**을 쓴다. PIN 첫설정·변경은 CF `passengerSetPin`. 세션(localStorage)에서 `pinHash` 를 **뺐다**.
  - [ ] **P3 PartnerApp 명부 CRUD → 포털 CF** (대) · [ ] **P4 `passengers` rules 신원으로 좁히기** · [ ] **P5 초기 PIN 정책**
  - 🔴 **배포 순서 = functions 먼저, hosting 나중**. 반대로 하면 새 번들이 없는 CF 를 불러 전원 로그인 불가. `firebase deploy --only functions:passengerLogin,functions:passengerResume,functions:passengerLogout,functions:passengerMigrate,functions:passengerSetPin,functions:boardStatic` → `firebase deploy --only firestore:rules` → hosting.
  - **배포 전 확인 완료**(`node scripts/verify_passenger_auth.cjs` · 읽기 전용): 서버/클라 해시식 일치 · **실명부 대조 통과**(REVIEW/112233) · 명부 258명 중 pinHash 없음 0·비활성 0 · 런타임 SA `1040702853398-compute@…` 가 `roles/iam.serviceAccountTokenCreator` **보유**(= `createCustomToken` signBlob 통과). 빌드 `main.08f82f42.js` 신규 경고 0.
  - [x] **배포 직후 실측 완료(2026-08-25)** — `node scripts/test_passenger_login_live.cjs` **16/16**(ⓐ 실제 로그인 ⓑ `passengerResume` 복원 ⓒ 클레임·uid ⓓ 로그아웃 후 복원 거부) + `test_board_static_auth.cjs` **9/9**. ⚠ **아직 사람 손으로 안 해 본 것**: 실기기에서 앱 안 고정 QR 1건 태깅, 새로고침 후 로그인 유지 체감. 라이브 하네스가 같은 경로를 서버 기준으로는 통과시켰다.
  - ⚠ **아직 안 닫힌 것**: 명부는 여전히 익명에게 열려 있다(`allow read: if isAuth()`) — P3·P4 전까지 `pinHash` 는 비밀이 아니다. 그래서 **전환 유예 경로 2개**를 `2026-09-30` 하드 기한으로 두었다(`PASSENGER_LEGACY_UNTIL`): ⓐ `passengerMigrate`(옛 기기 세션 승계 — 안 두면 로그인 중이던 124명이 한 번씩 튕긴다) ⓑ `boardStatic` 의 레거시 `pinHash` 경로(옛 번들 대응). **이 둘이 살아 있는 동안 '명부 읽은 사람이 대신 찍기'는 여전히 가능하다** — 기한이 지나면 자동으로 닫힌다.
  - ⚠ **P4 와 함께 넣을 것**: CF 로그인 **시도 제한**. 지금은 없다(의도 — 명부가 열려 있어 CF 를 막아도 클라에서 같은 대조가 가능하다). 명부를 닫는 순간 `passengerLogin` 이 유일한 관문이 되므로 그때가 아니면 늦다.
  - ⚠ **초기 PIN `000000` 의 남은 위험 — way 판단으로 감수하고 켰다(2026-08-25)**: way 근거 = 배부·문의 비용이 실제 병목이고 첫 로그인에서 본인 번호로 바꾸게 강제하므로 노출 창이 짧다. 🔴 **다만 "로그인해 있으면 남이 못 쓴다"는 이 시스템에서 성립하지 않는다** — 동시 로그인을 막지 않고 `passengerLogin` 이 다른 세션을 무효화하지 않는다(실측 확인). 실제 위험 = **아직 첫 로그인을 안 한 사람**이다: 명부가 익명에게 열려 있어(P4 전) 사번 목록을 얻을 수 있고, 남이 먼저 들어가 PIN 을 바꾸면 **본인이 잠긴다**. 실측 대상 규모 = `pinInitial` **133명 중 128명이 채드윅(학생)**, 신촌세브란스는 1명. **기존 133명의 PIN 은 건드리지 않았다**(요청 범위 밖 · 이미 개인별 안내문이 배부된 계정). 재발급이 필요하면 협력사 포털 "PIN 재발급"이 이제 `000000` 을 발급한다. **P4(명부 닫기)가 이 위험의 실질적 종결**이다.
  - 📌 **별건 발견** — 기사 QR 경로(`/board?t=`)는 화면이 **비밀번호를 받아 놓고 쓰지 않는다**(`validateAndBoard` 는 PIN 을 검증하지 않는다). 사용자는 확인받는다고 믿는데 아니다. 고정 QR 과 달리 5분 만료 QR 뒤에 있어 위험은 낮지만 **문구가 거짓**이다. 고치려면 이 경로도 로그인으로 바꿔야 하는데 그러면 **명부에 없는 사람(게스트·신규 입사)이 막힌다** — 정책 판단 필요(way).

- [ ] **📋 2026-08-25 미팅 2건 요구사항 — 점검 완료·착수 대기** (원문 `file/20260825/meeting1.txt`(신촌세브란스 도입)·`meeting2.txt`(채드윅) · 전사본 `meetings/2026-08-25_*.md`). 🔴 **같은 폴더 `summary.txt`(AI 생성 PRD)는 정본이 아니다** — 작성일자 2024.05.22 오기 · 회의에 없는 내용 4건(유비칸 `Hardware ID > Vehicle Number > Driver Name` 우선순위 재설계 · 사번 제한 "해제 확정" · DYCS 를 좌표계로 오해 · "노선 90개" 를 "정확도 90%" 로 오독). **판단은 원문·전사본으로**. 항목별 실측 상태:
  - [ ] 🔴 **고정 QR 로그인 우회 — 확인됨(코드 실측)**: 외부 카메라로 `/board?c=&v=` 를 열면 `BoardingApp` 이 **사번·이름만 받고 익명 인증으로 탑승 적재**한다. 서버 `functions/index.js:1451 boardStatic` 은 `request.auth`(익명이면 통과) 만 보고, `passengers/{empNo}` 조회는 **partnerCode 채우기용**이라 **없는 사번도 그대로 적재된다**(`:1467~1473`). 배시현 "아무거나 넣어도 탑승이 돼요" 는 정확한 신고. **최우선**.
  - [x] **초기 PIN `000000` 고정 + 로그인 화면 안내 — ✅배포 완료(2026-08-25 `main.3f650282.js`)**: way 지시 "그냥 셋팅해줘". 정본 `src/lib/accountCards.js` — `DEFAULT_INITIAL_PIN="000000"` 신설, `generateInitialPin()` 이 그 값을 돌려준다. 🔴 **옛 개인별 랜덤 발급기는 `generateRandomPin` 으로 남겨 뒀다** — 되돌리거나 거래처별 초기 PIN(설계 초안 결정 1)으로 갈 때 여기로 돌아온다. 지우지 말 것. 함께 고친 곳 = 로그인 화면 안내 문구(`EmployeeApp` LoginScreen) · 안내문 인쇄 꼬리말("초기 비밀번호는 000000") · 엑셀 양식 설명 2곳(`partner.js`·`PartnerApp`, "비우면 000000") · 테스트 `scripts/test_account_cards.cjs` **39통과**(옛 단언이 정반대 — "전부 같은 값이 아님"·"000000 고정이 아님" — 이었으므로 뒤집고 `generateRandomPin` 보존 단언 추가). 🔴 **엑셀 `초기PIN` 컬럼에 값을 채우면 그쪽이 이긴다**(개인별로 다르게 주고 싶을 때).
  - 🔴 **설명서 드리프트가 이때 드러났다(2026-08-25)** — 고객 설명서(`EMPLOYEE_GUIDE`·`PASSENGER_QUICK`)는 **원래부터 "첫 PIN은 000000"** 이라고 적혀 있었다. 2026-07-27 개인별 랜덤으로 바꿀 때 안 고쳐 **한 달간 틀린 상태**였고 오늘 되돌리면서 **우연히 다시 맞았다**. "이미 맞네"로 통과했으면 진짜 문제를 놓쳤을 것 — 그 사이 발급된 **133명(중 128명이 채드윅 학생)은 안내문에 인쇄된 개인별 번호**를 쓰므로 모집단이 둘이다. 정정 = 두 모집단을 한 문장에("기본 000000, 안내문에 다른 번호가 있으면 그 번호"). 같은 점검에서 "설정 탭에서 꼭 변경하세요"도 이미 거짓임이 드러났다(첫 로그인에서 **강제**로 바뀐 지 오래). ⚠ **PDF 는 여전히 2026-07-16 빌드** — .md 만 고쳤다. 교훈은 메모리 `policy-reversal-splits-the-population-already-served`.
  - [ ] **사번 입력 제한** — 회의에서 way "선택이야, 어떻게 하고 싶은지 알려줘"로 **미결**. 실측하면 `EmployeeApp.js:634` 사번 입력에 `maxLength` 없음 = **이미 제한 없음**. 배시현 회신 오면 그때 판단(작업 0일 수 있음).
  - [x] **노선 목록 특이사항 텍스트 굵게 — 배포 완료(prod `main.0e7e475e.js`·2026-08-25 최우석 캡처)**: 캡처가 지목한 것은 `04:39 고양일산 - 조기출근` 의 **꼬리표**였다. 정본 `src/lib/routeKind.js splitRouteNameNote` — **` - `(앞뒤 공백) 마지막 것 뒤**만 꼬리표로 본다. 🔴 **공백 없는 `-` 로 가르면 `[H1-1] 등교…` 가 `[H1` + `1] 등교…` 로 깨진다**(prod 실측). `/` 뒤는 영문 병기(29건)라 대상 아님. 적용 = `EmployeeApp` 노선 탭 카드 제목만(홈 헤더·바텀시트 제목은 미적용 — 요청 범위 밖). 검증 = 실화면 `scripts/headless_check_route_note_bold.cjs`(계산된 `fontWeight` 를 읽어 판정 · prod 5/5) + `scripts/test_route_name_note.cjs` 14단언(합성 + **prod 전수 112개**: 갈리는 노선이 정확히 2개 `05:45 군포⟦출근⟧`·`04:39 고양일산⟦조기출근⟧`, 둘 다 근무 구분어 — 이 수가 늘면 규칙을 다시 볼 것).
  - [x] **문의 탭 → 홈페이지 연결로 통합 — ✅배포 완료(2026-08-25, way 가 주소 전달)**: 주소 `https://sites.google.com/dongyeongtour.co.kr/ssh/%ED%99%88`. 🔴 **실측으로 설계가 바뀐 지점** — 이 사이트는 `X-Frame-Options: DENY` + `frame-ancestors https://google-admin.corp.google.com/` 를 준다. 즉 **문의 위젯처럼 iframe 으로 감쌀 수 없다**(감싸면 빈 화면). 그래서 탭은 "여는 화면"이다(거래처명 + `홈페이지 열기` 버튼 → 새 창). 탭을 누르자마자 자동으로 창을 열지 않는 이유 = 돌아왔을 때 빈 탭만 남고 팝업 차단에 걸리면 아무 일도 안 일어난 것으로 보인다. 정본 `src/lib/homepage.js`(순수 · `javascript:`·`data:` 스킴 차단 — 관리자 입력이라도 안 믿는다) · `EmployeeApp` `HOMEPAGE_TAB`/`HomepageTab` · 관리자 협력사 관리 → ⚙️ 포탈 설정에 **홈페이지 연결**(주소+열어보기) 신설 · 목록에 🌐 배지. 🔴 **홈페이지 탭은 문의 탭을 대체한다**(둘을 나란히 두면 승객이 어디로 문의할지 갈린다) — 저장 시 확인 창으로 경고하고, 켜는 순간 그 거래처의 dycs 유입은 끊긴다. **prod 설정 적용 완료**(`node scripts/set_partner_app_options.cjs 신촌세브란스 --homepage <URL> --sound-forced --apply`) — ⚠ hosting 배포 전에는 화면에 안 나온다. 되돌리기 = 같은 스크립트 `--homepage-off --apply`. 검증 = `scripts/test_partner_app_options.cjs` 24단언.
  - [x] **QR 태깅 소리 — ✅배포 완료(2026-08-25)**: 미팅 결정은 "기본 ON·강제 X·사용자 조절"이었고 그 뒤 way 지시로 **거래처 강제 스위치**가 더해졌다. 그래서 판정이 두 겹이다 — 거래처가 강제면 무조건, 아니면 개인 설정(기본 켜짐). 정본 `src/lib/tagSound.js`. 🔴 **음원 파일을 쓰지 않는다**(WebAudio 합성 2음·0.2초) — 번들·네트워크 0이고 지하 정류장에서도 난다. 🔴 **첫 태깅 무음 함정**: 브라우저는 사용자 제스처 없이 만든 오디오 컨텍스트를 `suspended` 로 둔다 → `unlockTagSound()` 를 앱 첫 `pointerdown`(BoardingApp 은 탑승 버튼)에 걸었다. 적용 지점 = `EmployeeApp` 탑승 성공 2곳 + `BoardingApp` 1곳, **진동은 그대로 둔다**(소리가 실패해도 피드백이 사라지면 안 된다). 설정 탭에 `태깅 소리` 카드(강제 거래처는 `고정` + 이유 표기 — 안 눌리는데 이유가 없으면 고장으로 읽힌다). ⚠ **"강제"라도 iOS 무음 스위치·미디어 볼륨 0 은 웹이 못 이긴다** — 고객 안내에 이 문장이 필요하다. **적용 범위**: 신촌세브란스만 강제, 나머지 거래처는 기본 ON(개인이 끌 수 있음) — 아이들 노선(채드윅)까지 강제로 묶는 건 별개 판단이라 안 건드렸다.
  - [x] **🔴 운행 종료 후 경로가 다시 파란색으로 살아난다 — 해결·배포(2026-08-25)**: 가설은 맞았지만 **지점이 달랐다** — 스트립의 `isReached` 가 아니라 지도 폴리라인 분할(`pathUpTo` 회색 / `pathFrom` 파랑)의 기준인 `busProgress` 가 **역행**하고 있었다. 운행이 끝난 버스가 종점을 지나 차고지로 가며 노선을 되짚으면 이탈이 `OFF_ROUTE_M(70)` 안이라 유효 좌표로 먹힌다. prod 실측 `[A] 방과후하교` 2026-08-24: 19:03 `54245m`(완주·전부 회색) → 19:14 `47404m` = **6.8km 가 다시 파랑**(요청자가 말한 "10분 20분 뒤"와 일치). 정본 `routeProgress.advanceProgress` 신설(역행 금지·이탈 폐기는 보존) · `EmployeeApp`(`/p`)·`PassengerApp`(`/bus`) **동시 수정**(같은 인라인 로직이 양쪽에 복사돼 있었다) · `lastBusProgressRef` 에 리셋이 없어 노선 변경 시 이전 노선 진행거리를 물려받던 잠재 결함도 함께 제거(`progressRouteRef`). 상세·회귀 가드·한계는 `.claude/issues.md`.
  - [x] **🛏 빈 차 확인 현황 관리자 탭 신설 — 2026-08-25 미팅 2건(배시현 "언제 찍었고 어디 찍었고" · way "노선 선택이 있어야겠네")**: 데이터는 2026-08-18 부터 이미 쌓이고 있었고(`dispatch.sleepingCheck{checkedAt,via,distanceM,distanceRef,nearOk,afterTerminalSec}`) **화면만 없었다** — 관제 좌측 레일은 미확인 목록 + 완료 **건수**만 보여 준다. 신설 = AdminApp 15번째 탭 `빈 차 확인`(`SleepCheckTab`·인덱스 14·`SUPER_TAB_INDEX` 는 `TABS.length` 라 자동 이동) + 노선 관리 폼 체크박스 `🛏 빈 차 확인 대상 노선`(`routes.sleepCheckEnabled`). 판정 정본 = `src/lib/sleepingCheck.js`(`sleepCheckRoutes`·`sleepCheckRows`·`sleepCheckedAtLabel`·`sleepCheckPlaceLabel`·`sleepCheckViaLabel`). 🔴 **실측이 설계를 두 번 바꿨다**: ⓐ **대상 노선 밖에서 찍힌 확인도 보여준다** — 2026-08-25 확인 6건이 **전부 등교 노선**이었다(배시현은 "등교는 필요 없다"고 했지만 기사들은 실제로 찍고 있다). 하교로만 좁혔으면 그 6건이 화면에서 사라져 "찍었는데 안 보인다"가 된다 → `대상 외` 칩을 달아 함께 표시 ⓑ **과거 날짜는 그 날 끝에 시계를 맞춘다** — `Date.now()` 로 재니 어제 미확인이 `종점 뒤 21시간 43분째` 로 나왔다(2026-07-20 `조기도착 수천분` 과 같은 계열). 🔴 **대상 노선을 하나도 안 정했으면 마지막 운행(하교·방과후) 노선으로 폴백** — 지정 전 빈 화면은 "기능이 없다"로 읽힌다. 검증 = `scripts/test_sleep_check_rows.cjs` 26단언(정본을 vm 으로 태움·폴백·정렬·표시 3종·🔴 "종점 도착 없이 미확인으로 세지 않는다" 회귀 가드) + **prod 양성 대조군**(`scripts/inspect_sleep_check_view.cjs` — 관리자 화면은 Auth 가 필요해 헤드리스로 못 열므로 **탭과 같은 함수에 같은 prod 문서를 태워** 배선 확인. 08-24: 확인 18 · 미확인 4 · `19:04 · 차량에서 23m · QR` 처럼 내역이 그대로 나온다). ⚠ **오늘 18시 하교는 아직 안 떠서 "운행 전·중"만 나온다** — 그것만 보고 배선이 죽었다고 판단하지 말 것(그래서 양성 대조군이 필요했다).
  - [ ] **(운영 소견) 빈 차 확인이 전부 QR · NFC 0건 · 먼 곳 확인 다수** — 08-24 실측에서 확인 18건 전부 `QR` 이고 거리 `873m`·`963m`·`492m` 가 섞여 있다(2026-08-18 way 지적대로 **인쇄 QR 은 사진으로 복제된다**). 아이들 노선은 NFC 태그 등록이 선행되어야 실효가 있다. 현황 탭 `점검` 칸이 이제 그걸 드러낸다 — way·배시현 판단 대기.

- [ ] **🛏 빈 차 확인(슬리핑 차일드) — 운영 준비(2026-08-18 배포 완료)**: 기능은 prod 에 올라갔지만 **뒷좌석 QR 을 인쇄해 붙여야** 동작한다. 관리자 → 차량 관리 → 해당 차량 `🖨 QR` → **빈 차 확인 QR 인쇄** → 맨 뒷좌석에 부착. NFC 로 쓰려면 기사앱 NFC 탭 `빈 차 확인` 모드에서 뒷좌석 태그를 한 번 대고 `이 태그를 이 차량에 등록`. 확인 현황은 실시간 관제 좌측 레일에 뜬다. 🔴 **아이들이 타는 노선(채드윅 등)은 QR 대신 NFC 태그를 권한다** — 인쇄한 QR 은 사진으로 복제되어 뒷좌석까지 가지 않아도 찍힌다(2026-08-18 way 지적·실측 증명). QR 에는 운행 시간창 거부 + 위치·경과초 기록을 걸어 뒀지만 **차 안에서의 사진 도용은 QR 로 막을 수 없다**. **미확인 자동 경보(구글챗·푸시)는 미도입** — 필요하면 후속.
- [x] **익명 화면의 `stopArrivals` onCall 위임 — 승객앱 '실제 도착시각' 되살림(2026-08-18 발견·같은 날 way 승인·배포 완료 `main.55ab95e2.js` + CF `getRouteStopArrivals`)**: 협력사 관제를 고치다 **같은 권한 벽에 걸린 화면 2곳**을 더 찾았다. `dispatches` read = `isAdmin || isDriverOf` 인데 `PassengerApp`(`/bus`)·`EmployeeApp`(`/p`) 은 익명이라 **오늘 배차 stopArrivals 구독이 항상 거부**된다(익명 클라로 실제 쿼리해 확인). 그래서 ⓐ 정류장 목록의 **실제 도착시각**이 절대 안 뜨고 ⓑ '내 정류장 직전 정류장 도착' **인앱 알림**이 안 나간다(⚠ 서버 푸시 `notifyPreArrival` 은 별개 경로라 정상). 협력사 포털 노선도의 **통과 정류장 ✓** 도 같은 원인. **완료(2026-08-18, way 승인)** — CF `getRouteStopArrivals` 신설·정본 훅 `src/lib/useRouteStopArrivals.js`·prod 배포. 세 화면 모두 살아났다(승객앱 `도착 HH:MM` 6개·협력사 노선도 ✓ 60개 실측). 처방은 예고대로=`resolveStaticBoarding`(2026-07-13) 선례대로 **오늘 그 노선의 stopArrivals 만 돌려주는 onCall** 신설(익명 통과·읽기 전용). 🔴 **rules 는 완화하지 않았다** — 익명에 배차를 열면 회사 전체 기사명·차량번호가 열린다. 반환도 도착 기록만(기사명·차량번호 미포함). 상세·회귀 가드는 issues.md.
- [ ] **📘 매뉴얼 `.md` 는 2026-08-18 갱신 · PDF 는 아직 2026-07-16 빌드**: 이번 세션에 `PARTNER_GUIDE`(구분 필터)·`ADMIN_GUIDE`(빈 차 확인 절 신설)·`DRIVER_GUIDE`(빈 차 확인 절 신설) 문구를 넣었다. **PDF 재생성은 안 했다** — 아래 절차로 캡처부터 다시 떠야 한다.
- [ ] **📘 고객 매뉴얼이 낡았다 — 승객앱 기준 갱신 필요(2026-08-11 확인)**: `docs/manual/out/*.pdf` 는 전부 **2026-07-16 빌드**인데 그 뒤 승객앱이 세 번 크게 바뀌었다(도움말·시간표·알림 설정 신설 → 브랜드 밴드 리스킨 → 이모지 정리). **실측 결과 두 가지**: ⓐ **문구는 안 깨졌다** — `EMPLOYEE_GUIDE.md`·`PASSENGER_QUICK.md` 가 인용한 이모지 라벨 **0건**(도움말이 “🕒 시간표”를 인용해 깨졌던 것과 달리 매뉴얼은 무사) ⓑ 🔴 **스크린샷이 옛 화면**이고 **신설 3기능(도움말·시간표·알림 설정)이 설명서에 0건** — 고객이 보면 "설명서가 멈춰 있다"가 된다. **갱신 방법**: `node docs/manual/_serve_build.mjs`(별도 터미널) → `node docs/manual/_capture_passenger.mjs` → `node docs/manual/_to_pdf.mjs`. ⚠ 캡처는 실 데이터라 **PII 마스킹 확인 필수**이고, 산출 PDF 는 **페이지별로 눈으로 봐야** 한다(넘침·잘림은 빌드 성공이 못 잡는다). way 판단 대기.
  - **2026-08-12 갱신분**: `ADMIN_GUIDE.md` §4(배차 일정)에 "시작일·요일을 바꾸거나 일정을 껐을 때 이미 만들어진 배차를 함께 정리한다" 안내를 추가했다. **`.md` 만 고쳤고 PDF 는 아직 2026-07-16 빌드** — 아래 캡처·PDF 재생성 때 같이 나간다.
  - 🔴 **관리자 매뉴얼 드리프트도 실측됨(2026-08-11)**: `docs/manual/ADMIN_GUIDE.md` 가 가리키는 메뉴 13개는 전부 실재하지만(깨진 참조 0), **소스 TABS 14개 중 `개선 요청`(2026-07-13 신설)·`부정승차`(2026-07-22 신설)·`시뮬레이터` 3개가 매뉴얼에 아예 없다**. 하필 사이드바 맨 아래라 **이번에 스크롤이 안 돼 접근도 안 되던 그 메뉴들** — 안 보이고 설명도 없으니 두 겹으로 없는 기능이었다. 갱신 시 이 3개 섹션 신설 필요. **점검 명령**(YDYOPS `manual-drift-test` 대응물이 buslink 엔 아직 없음 — 만들면 이 대조를 자동화): `grep -oE '"[가-힣 ]+"' <<< "$(grep '^const TABS' src/pages/AdminApp.js)"` 와 매뉴얼 목차 대조.
- [x] **승객앱 설정 탭 “배정 노선” DB ID 노출 해소(2026-08-11 발견·같은 날 배포 `main.3513b0af.js`)**: 학생 화면에 `yRhXBbOl…` 가 그대로 보이던 것 → `routes/{id}` 1회 조회해 노선명 표시(읽기 1건 증가·실패 시 `–`·🔴 ID 폴백 금지). 검증 `scripts/headless_check_settings_route_name.cjs`(화면 문자열 ↔ Firestore 실값 대조·옛 prod 에서 ID 노출 재현).
- [ ] **🧭 길안내 5차 주행 확인(2026-08-07 배포 `main.acb344cc.js`)** — **볼 것**: ⓐ 회전 지점에 뜨는 패널이 경로를 안 가리고 "저기서 꺾는다"가 읽히는지 ⓑ 긴 직진에서 안내가 사라져 있는지 ⓒ 남은 거리(364m 식)가 눈에 들어오는지 ⓓ 고리 애니메이션이 거슬리지 않는지 ⓔ **속도가 다시 보이는지**(배너 아래). **후속 후보**: ① 과속카메라 경고 — 카카오 API 엔 없고 **공공데이터포털 단속카메라 위치**로 별도 구축 가능(경로 위 카메라 사전 추출 → "300m 앞 단속카메라"). way 판단 대기 ② 옆길·진입 시 다음 100~150m 경로 구간 강조(좌우를 지어내지 않는 유일한 방법).
- [ ] ~~🧭 길안내 4차 주행 확인(2026-08-06 `main.b1360e51.js`)~~ — 5차로 대체됨. 당시 지적은 위 세션에서 전부 반영. — `https://d.buslink.co.kr/driver` 길안내 탭. **볼 것**: ⓐ 지도 한가운데 큰 화살표가 실제로 눈에 들어오는지(회전 200m 안이면 진해지고 커진다) ⓑ **지하차도/고가도로 옆길 아이콘이 우회전으로 헷갈리지 않는지** — 헷갈리면 갈래를 꺾지 말고 나란히 올라가게 다시 그린다 ⓒ 속도가 지도 아래 줄로 내려가 메뉴를 안 가리는지. **후속 후보(way 판단 대기)**: 옆길·진입 안내일 때 **다음 100~150m 경로 구간을 굵고 밝게** 강조 — 좌우를 지어내지 않고 "이쪽으로 빠져라"를 보여주는 유일한 정직한 방법(좌우 아이콘은 실측으로 기각, issues.md `[결론]`).
- [x] **💬 문의 탭 배포 + 신촌세브란스 ON 완료(2026-08-06)** — prod `main.96970a18.js`(4개 도메인 해시 확인)·`partnerCodes/DY001-신촌세브란스병원-2026-YZVH.inquiry = {enabled:true, tenantId:"sev"}`. E2E 통과. **다른 거래처를 켤 때**: 관리자 → 협력사 관리 → ⚙️ 포탈 설정 → 문의 게시판 체크 + dycs 거래처 ID → **열어보기**로 확인 후 저장(또는 `node scripts/set_inquiry_board.cjs <이름> <tenantId> --apply`, dry-run 기본·`--off` 로 끄기). **🔴 선결 = dycs 에 그 거래처가 먼저 있어야 한다** — 현재 dycs 10곳(`dongyeong`·`kolon`·`snu`·`abbott`·`hufs`·`hp`·`innen`·`hanwha`·`samsungsds`·`sev`) 중 buslink 와 겹치는 건 **`sev` 하나뿐**. 채드윅(승객 241명)을 켜려면 dycs `/settings/tenants` 에 채드윅 tenant 등록이 선행. **재점검** = `BASE=https://p.buslink.co.kr node scripts/headless_check_inquiry_tab.cjs <거래처이름>`(쓰기 0).
- [ ] **(사용자 확인) 문의 탭 실기기 1회** — 신촌세브란스 승객 계정으로 `https://p.buslink.co.kr/p` 접속 → 하단 '문의' → 위젯에서 **실제로 한 건 접수**해 보고 dycs 인박스(`dycschannel.web.app`)에 뜨는지 확인. 헤드리스로는 렌더까지만 봤고 **접수 write 는 안 태웠다**(prod 티켓이 생기므로). ⚠ 그때 확인할 것 = 접수 폼이 이름·연락처를 다시 묻는데 승객앱은 이미 아는 값이다 → 불편하면 프리필(dycs 위젯 파라미터 신설) 진행.
- [ ] **🔑 검토용 승객 계정 `REVIEW` — 🔴 지우지 말 것(2026-08-25 판단 뒤집음)**: 원래는 "신촌세브란스 오픈 전에 명부에서 빼자"였는데, 2026-08-25 승객 인증 전환에서 **이 계정이 인증 배포를 검증할 수 있는 유일한 수단**임이 드러났다 — 실승객은 `pinHash` 만 있어 **평문 PIN 이 없으므로 로그인을 재현할 수 없다.** `scripts/verify_passenger_auth.cjs`(배포 전 해시식 대조)와 `scripts/test_passenger_login_live.cjs`(배포 후 실제 로그인 16단언)가 둘 다 이 계정을 쓴다. 지우면 다음 인증 변경 때 "258명이 로그인되는가"를 잴 방법이 사라진다. **계정**: `companies/dy001/passengers/REVIEW`(이름 `화면검토`·부서 `검토용`·신촌세브란스 거래처·PIN `112233`·`pinInitial:false`). 명부에 한 줄 섞이는 비용은 감수한다. ⚠ **PIN 을 바꾸면 두 하네스가 같이 죽는다** — 바꿀 일이 있으면 스크립트 상수(`REVIEW`)도 함께 고칠 것. way 가 UI 변경을 바로 확인하는 창구로도 계속 쓴다. ⚠ 기존 계정(동영이·최우석)은 건드리지 않았다 — PIN 초기화는 그 사람 로그인을 막는다.
- [x] **과거 공지 100건 일괄 숨김 완료(2026-07-31, way `--apply` 실행)** — 앱 표시 중 **101건 → 1건**(오늘자 채드윅 공지만 유지). 재실측으로 숨김 대상 0건 확인. 되돌리기 = `node scripts/hide_old_notices.cjs --restore hidden_notices_2026-07-31.json --apply`(ID 목록 커밋됨). 숨김 내역=3월 25건·5월 68건·6월 2건·7월 5건, 전부 테스트·지난 당일공지. **문서는 남아 있다**(삭제 아님) — 관리자 콘솔 공지 발송 이력의 `숨김` 칩에서 열람·되돌리기 가능.
- [ ] **🛰 단말 GPS 다중 회차 — 오후 하교 실측(2026-07-31)** — 오전 회차 수정은 prod 실호출로 확인했으나(7012 가 `09:20 판교역` 회차로 전환), **하교(15:50) 시간대는 아직 실측 못 했다**(그 시각 이전에 세션 종료). 15:20 이후 `node scripts/inspect_device_gps.cjs` 로 다중 회차 차량(5377·7022·7029·6859·3305·3539·7026)이 하교 창에서 잡히는지 확인. 이전에는 등교가 뽑힌 3대가 오후에 죽던 구조였다.
- [ ] **(운영 판단) 잔존 mobile gps 문서 2건(2026-07-31)** — 서울72바7026·경기79사3539 에 `routeName="테스트노선"` mobile 신호가 남아 있다(각 14분·28분 경과 시점 관측). **device 차량인데 mobile 문서**라 `cleanupDeviceGpsDoc`(`source==="device"` 만 삭제) 이 영원히 못 지운다. 관제 → 🧹 잔존 운행 신호에서 정리하거나 `node scripts/clear_stale_run_signals.cjs --apply`. ⚠ **`cleanupDeviceGpsDoc` 의 source 조건을 풀어서 때우지 말 것**(2026-07-16 회귀 가드 — mobile 차량 오삭제). device 차량에 mobile 문서가 왜 생겼는지(기사가 테스트노선으로 운행 시작?)는 미규명.
- [ ] **🪑 좌석예약 승객앱 화면(2026-07-30, 스위치만 배포됨)** — `partnerCodes.seatReservation` 3단 토글은 prod 에 있고 **아직 켠 거래처 0곳**. 고객사가 `optional`/`required` 를 고르면 그때 승객앱 예약 화면 + 예약 데이터 모델 착수. **required 는 탑승 거부 아님** — 기사 화면 "미예약" 표시 + "태우기"(예외 승차 기록)로 설계 확정(way).
- [ ] **🌡 승객→기사 실시간 요청(더워요/추워요) — way 검토 대기** — 부품은 이미 있어 값싸게 됨. 단 3가지가 필수: ① **집계로 표시**(`최근 10분 · 더워요 4 / 추워요 1` — 개별 메시지는 기사가 판단 못 함) ② **쿨다운**(1인 5분 1회·연타 방지) ③ **기사 확인 응답**(승객 화면에 "확인했습니다" — 없으면 불만이 커짐). 버튼 고정 3~4개(자유 입력=민원 창구)·**정차 요청 제외**(안전)·소리·진동 없이 화면 배지만(운전 중 주의 분산). **기사분들 반응 확인 필요**(새 부담을 주는 기능).
- [ ] **🧭 길안내 — 3차 현장 테스트 대기(2026-08-05 우회·유턴 근절 반영·CF `getNaviRoute` 배포)** — 아침 지적 3건의 근인(양방향으로 안 재서 우회가 안 잡힘)을 고쳤다. **프론트는 안 바뀌었으니 앱 갱신 불필요** — `https://d.buslink.co.kr/driver` 에서 길안내 탭을 **다시 열면**(노선을 다시 받으므로) 새 경로가 온다. **볼 것**: ⓐ 영동2교 우회전 후 유턴이 없어졌는지 ⓑ 선암사거리에서 광창마을 쪽으로 내려갔다 되돌아오지 않는지(선바위역은 그 앞을 그냥 지나간다) ⓒ 안내 배너가 좌↔우로 뒤바뀌지 않는지 ⓓ 화면 아래 문구가 `등록된 노선을 따라가는 도로 경로` 인지. ⚠ **길안내 탭이 처음 열릴 때 최대 3초 정도 걸린다**(경로 후보를 여러 번 비교한다). 그 사이에는 관리자가 그린 노선이 먼저 보이고 `도로 경로를 불러오는 중…` 이 뜬다 — 정상이다. **재현 시 필요한 것**: 어느 노선·어느 교차로인지(스크린샷의 안내 문구 그대로)면 `node scripts/inspect_navi_turns.cjs <노선명>` 으로 그 노선 안내 전문을 뽑아 대조할 수 있다. **남은 이상치**: `05:45 군포 - 출근`·`[한남1] 등교(목)`·`[판교] 등교(목)` 은 유턴 1개가 남는다(회피하면 오히려 나빠져 기각됨 = 실제로 필요한 유턴일 가능성).
- [ ] **(참고) 2026-07-31 지적 4건 반영 내역(prod `main.42b8647a.js`)** — way 실주행 지적(사진·방향·그린노선·축척)을 전부 반영해 배포했다. ⚠ **테스트는 반드시 `https://d.buslink.co.kr/driver`(prod) 로** — 프리뷰 채널(`buslink-prod--navguide-*`)은 **live 배포와 함께 갱신되지 않는다**. 2026-08-04 way 가 "이전 수정이 반영 안 된 것 같다"고 한 건 폰에 남은 프리뷰 링크(07-30 빌드 `main.bb139088.js`)를 본 것이었다 — 코드·배포는 정상이었고 프리뷰만 하루 뒤쳐져 있었다. 검토용 프리뷰를 만들면 **live 배포 시 같은 채널도 함께 갱신하거나, 검토 끝난 채널은 지울 것**. ⚠ **기사앱 화면 아래 버전 뱃지(`v2026-06-09-livegps`)로 신선도를 판정하지 말 것** — `DriverApp.js` 의 하드코딩 상수라 06-09 이후 갱신이 안 됐다(원래 "옛 캐시 vs 신빌드"를 가르려고 넣은 표시가 지금은 아무것도 못 가른다). 판단 기준은 주소(`d.buslink.co.kr/driver`)와 `curl | grep main.*.js` 뿐. **후속 후보**: 뱃지를 빌드 번들 해시로 자동 반영. **다음 주행에서 볼 것**: ⓐ **진행 방향 위(heading-up)** 가 실제로 덜 헷갈리는지 — 남쪽으로 갈 때 지도 글자가 거꾸로 보이는 게 견딜 만한지(못 견디면 오른쪽 위 `N 북쪽` 버튼으로 전환·그 선택을 기억하게 할지 결정) ⓑ **그린 노선을 따라가는지** — 화면 아래 문구가 `등록된 노선을 따라가는 도로 경로` 로 뜨는지, 실제 다니는 길과 맞는지 ⓒ **축척(level 3)** 이 적당한지 — 좁으면 ＋/－ 로 조정 후 몇이 편한지 알려주면 기본값을 바꾼다 ⓓ 지도를 손으로 끌면 추적이 풀리고 `내 위치` 버튼으로 돌아오는지. 남은 것: ⓐ **음성(TTS)** — 회전 안내와 정류장 접근을 읽어주기(실기기 확인 필요·헤드리스 불가) ⓑ **경로 이탈 시 재요청**(지금은 진입 시 1회만 호출) ⓒ 외부 내비 버튼을 운행 중 잠글지 결정(도로 안내가 앱 안에 생겼으므로 필요성이 줄었다) ⓓ prod 배포. **way 결정 사항(기록)**: 카카오내비 전환은 **보류** — 경유지가 **최대 3개**뿐이라 정류장 8~15곳 노선을 한 번에 못 태운다(목적지1+경유지3=4곳). 그리고 **차량 31대 중 27대가 이미 GPS 단말**이라 외부 앱 전환의 GPS 끊김 위험은 폰 GPS 4대에만 해당한다(내가 비중을 과대 경고했음). heading-up 지도 회전은 카카오 로고·저작권 표시 약관 확인이 선행 필요. **현장에서 볼 것**: 회전 안내가 실제 도로와 맞는지 · 안내 시점(거리)이 적절한지 · 지도만으로 따라갈 수 있는지.
- [x] ~~길안내 마무리(2026-07-28 항목)~~ — 아래로 통합 — 프리뷰 `main.3e9079ca.js` 확인 후: ⓐ 정류장 접근 **음성 안내(TTS)** 얹기(500m 전 "잠시 후 ○○입니다"·통과 직후 다음 정류장·정류장별 1회·끄기 가능·음성 없는 기기 자동 폴백. **실기기 확인 필요** — 헤드리스 불가) ⓑ 외부 내비 버튼을 **운행 중 잠글지** 결정(잠그면 이유도 함께 표시) ⓒ prod 배포·게시판 없음(way 직접 요청 건). **미결 질문**: 차고지→첫 정류장 구간을 노선 경로에 포함할지(포함 시 진행률·도착예측 계산에 섞이므로 부작용 확인 필요).
- [ ] **🔴 (way 실행) 도착 임박 시간표 폴백 켜기(2026-07-29)** — CF `notifyPreArrivalBySchedule` **배포 완료·기본 꺼짐**. 켜기 = `node scripts/set_prearrival_fallback.cjs dy001 on --apply`(**분류기가 막아 Claude 가 실행 못 함** — 실제 승객에게 푸시가 나가는 스위치라 우회 안 함). 발송량 사전 확인 = `node scripts/simulate_prearrival_schedule.cjs`(오늘 기준 상한 3통). lead 조정 = `--lead 7`. 켠 뒤 Cloud Functions 로그에서 `[도착임박:시간표]` 확인. 상세·회귀 가드 issues.md `[부분해결]`.
- [ ] **(후속) 시간표 폴백 관리자 토글 UI** — 지금은 스크립트로만 켜고 끈다. AdminApp 회사 설정에 토글+lead 슬라이더 추가 필요(도착 반경 슬라이더 선례). **hosting 배포가 필요해 길안내와 함께 나가야 함**.
- [ ] **⭐ '내 정류장' 지정률이 진짜 병목** — 트리거를 100% 고쳐도 **도달 인원이 5명으로 묶인다**(푸시 토큰 8개 중 '내 정류장' 지정 5명). 명부 239~250명 업로드 시 승객앱에서 내 정류장 지정을 유도하는 경로(안내문·첫 로그인 흐름·홈 배너)를 같이 설계할 것. 이게 알림 체감의 상한선.
- [ ] (선택) **정류장 사진 채우기** — 206개 중 51개(25%)만 등록. 초보 기사 지원의 핵심 자산인데 **코드 작업 0**(관리자 화면에서 등록). 길안내가 이 사진을 "여기서 세우세요"로 크게 띄운다.
- [ ] (선택) **경로 미설정 실노선 4개** — `[등교] 강남1`·`[출근]잠실노선`·`[퇴근]잠실노선`·`등교(강남1)_월~수,금`. 안 그리면 길안내·승객앱이 정류장 직선으로 폴백(관리자 → 노선 관리 → 🛣 경로 그리기).
- [x] **🧹 잔존 신호 정리 완료(2026-07-28)** — `clear_stale_run_signals.cjs --apply` 실행: 신호 삭제 4건(조이형TEST 고아 12.9일·최우석 테스트노선 20.8일·박성환 등교TEST 32.1일·배시현(TEST) 테스트노선 46.0일) + 박상제 status 되돌림 1명. **재검증 0건/0명**(stale 0 · 운행중 오표시 0). 이후 이 스크립트는 위임 목록에 추가돼 Claude 가 직접 실행 가능(설정저장소 `cc719ff`). 재점검 = `node scripts/inspect_driver_status.cjs --gps`.
- [ ] **(운영) 계정 배부 실사용 확인(2026-07-27)** — 배시현님이 239~250명 명부를 올릴 때: ⓐ 엑셀 `초기PIN` 컬럼은 비워두는 게 권장(승객마다 다른 값 자동 발급) ⓑ **등록 완료 화면에서 안내문을 바로 인쇄**해야 함(평문 비밀번호는 저장 안 되므로 화면을 벗어나면 재발급 필요) ⓒ 나중에 다시 뽑을 땐 "승객 관리 → 📄 미시작 승객 계정 안내문 만들기". 인쇄물 = A4 2열·8명/장·점선 절취.
- [ ] **(관찰) 기존 승객 12명 첫 로그인 시 PIN 설정 화면** — 전원 `pinInitial:true` 라 배포 후 다음 접속 때 본인 비밀번호 설정 화면을 만난다(의도). 공용 계정(`pinLocked`)은 제외됨. 문의 들어오면 정상 동작으로 안내.
- [x] **배차 복제 실행상태 정리 완료(2026-07-22, way 직접 `--apply`)**: 근본 원인(배차 복사 스프레드) 수정·배포(`main.3a183a76.js`) 후 데이터 청소 실행 — **정리 배차 120건**(잔존 도착기록 215건 + 미래일 푸시마커 22건 제거). 재-dry-run 검증 = 630배차 전수 **0건/0건**. 도착시각 기록·도착 임박 푸시 정상 경로 복구.
- [ ] **🔴 NFC 실기기 검증(2026-07-22)** — Web NFC 는 헤드리스 테스트가 불가하다. **안드로이드 폰 + 실제 사원증 카드**로:
  - [ ] 기사앱 `/driver` 운행 시작 → **NFC 탭 노출**(탭은 항상 보인다. 크롬 아닌 브라우저면 탭 안에 "크롬으로 열어주세요" 안내가 뜨는 게 정상)
  - [ ] **카드 등록**: NFC 탭 → "카드 등록" → "카드 읽기 시작"(권한 프롬프트) → 승객 이름 선택 → 그 사람 카드 태그 → "○○님 카드가 등록되었습니다"
  - [ ] **탑승**: "탑승 처리" 모드 → 같은 카드 태그 → 초록 "승차 완료" + 카운트 증가
  - [ ] **중복**: 같은 카드 재태깅 → "이미 탑승 처리됨" (카운트 불변·중복 적재 0)
  - [ ] **미등록**: 등록 안 한 카드 태그 → 빨강 "미등록 카드" + UID 표시 → "이 카드 등록하기" 버튼으로 바로 등록되는지
  - [ ] **카드 뺏기 차단**: 이미 A에게 등록된 카드를 B로 등록 시도 → "이미 ○○에게 등록되어 있습니다" 거부
  - [ ] 관리자 "부정승차" 탭에 미등록 태깅이 남고 UID 복사가 되는지
  - [ ] **협력사 포털 일괄 등록**(안드로이드 크롬으로 `partner.buslink.co.kr` 접속): 등록 탭 → "📇 카드 등록" → "카드 읽기 시작" → 이름 선택 → 태그 → **자동으로 다음 사람 대기**로 넘어가는지 + 진행률(N/M) 증가
  - [ ] **엑셀 컬럼**: 양식 다운로드에 `NFC카드번호(선택)` 있는지 → 값 넣고 업로드 시 등록되는지 → **그 컬럼을 비운 채 재업로드해도 기존 카드가 유지되는지**(대량 업로드 삭제 방지 가드)
- [ ] **(운영 판단) NFC 도입 범위 결정** — 현재 등록된 카드 0장이라 기능은 켜져 있어도 아무 동작 없음(회귀 0). ⓐ 어떤 거래처부터 쓸지 ⓑ 카드 등록을 누가 돌릴지(기사 대행 vs 관리자 포털). **기사 단말 = 안드로이드 전용 확정(2026-07-22 way)** — 기사에게 배포/안내할 때 **크롬으로 열도록** 함께 안내할 것(삼성 인터넷 등은 Web NFC 미지원 가능·탭 안에서도 안내하지만 사전 공지가 확실).
- [x] **엑셀 NFC 컬럼 + 협력사 포털 카드 일괄 등록 (2026-07-22, prod `main.80732519.js`)**: way 결정 — 아이폰 사용자 비중이 높아 **승객 자가등록은 만들지 않음**. 담당자가 안드로이드 1대로 줄 세워 일괄 등록(협력사 포털 등록 탭 "📇 카드 등록"). 엑셀은 `NFC카드번호(선택)` 컬럼 추가(빈 셀=기존 등록 보존). CF `registerNfcCard` 가 협력사 익명 인증 경로 지원(업체코드+승객 소속 검증).
- [ ] **(운영 판단) 중복 배차 확인(2026-07-22)**: 오염 구간 07-13~07-24 는 하루 배차 35건, 깨끗한 날(07-17/18/25/28)은 13~15건 — 복사가 배차 행 자체를 늘린 정황. 완전중복(routeId+departTime+vehicleId 동일)은 하루 3건뿐이라 나머지는 코드로 판별 불가. 배차관리 화면에서 07-23·07-24 보고 불필요분 삭제 여부 판단 필요.
- [ ] **RQ/20260716 추가개선사항미팅 6건** (정본=`meetings/2026-07-16_추가개선사항미팅.md`·원 전사 `RQ/20260716/meeting.txt`):
  - [x] **#1 🔴 QR 오탑승 차단 + 선택 노선 매핑 (2026-07-16 prod 배포 `main.ab0d10a9.js` + CF `resolveStaticBoarding`/`boardStatic`·커밋 `2de00df`)**: `resolveStaticDispatchAdmin` selectedRouteId 매칭(불일치=차단·다중 회차=현재시각 근접)·직원앱 스캐너 session.routeId 전달·BoardingApp 무변경. 격리 7 assertion·신규 경고 0. **사용자 실기기 검증 대기**(카메라 스캔 자동화 불가): 7:10 선택→7:50 차량 태깅 시 "선택한 노선의 차량이 아닙니다" 확인. 상세 issues.md `[패턴]`.
  - [x] **#2 운행 중 표시 잔존 해소 (2026-07-16 prod 배포 CF `pollDeviceVehicleGps`·커밋 `f462f2e`)**: 폴러가 창 밖/미배차 시 device gps 문서 삭제(`cleanupDeviceGpsDoc`·mobile 보호) + POST 창 60→30(회의 결정 30/30). 격리 10 assertion. **운영 확인**: 내일 셔틀 종료 30분 후 노선탭 "운행중" 꺼지는지. 상세 issues.md `[패턴]`.
  - [x] **#3 협력사 포탈 노선도 + 노출 옵션 (2026-07-16 prod `main.b37d3f9c.js`·커밋 `5d15ed6`)**: 섹션 B2 정류장 스트립(통과 ✓·버스 위치) + `partnerCodes.opsControlEnabled`(부재=true) 게이팅 + AdminApp "⚙️ 포탈 설정" 토글. 상세 issues.md `[패턴]`.
  - [ ] **#4 그룹(관리회사) 설정 구조** — **설계안 초안 완료**: `.claude/group-design-draft.md`(partnerGroups 컬렉션+routes.groupId+가시성 4지점·G1~G3 단계·결정 3건). **way 검토·확정 후 구현**.
  - [x] **#5 거래처별 UI 커스터마이징 (2026-07-16 prod `main.b37d3f9c.js`·커밋 `5d15ed6`)**: `partnerCodes.branding{primaryColor,logo,logoHeight}` — 신규 `lib/partnerBranding.js`(CSS 변수 3종 덮어쓰기·mixHex 파생), 승객앱 헤더 로고+테마·포탈 테마, AdminApp 포탈 설정 모달(컬러 픽커·로고 200KB·크기 슬라이더·미리보기). 로고 위치 조정은 v2 후보(요청 시). 상세 issues.md `[패턴]`.
  - [x] **#6 협력사 포탈·승객앱 요약 매뉴얼 — 스크린샷 중심 v2 (2026-07-16 커밋 `06ec2c7`)**: 협력사포탈가이드.pdf **4쪽**(실캡처 9장 — DB하이텍 코드로 사용자 `!` 캡처·PII 비파괴 마스킹·카드 크롭) + 승객앱가이드.pdf 3쪽(기존 마스킹 자산). 캡처 하네스 `docs/manual/_serve_build.mjs`·`_capture_partner.mjs`(PARTNER_CODE env)·`_capture_passenger.mjs` + `scripts/manual-capture-ids.cjs`(ID 조회). **운행 시간대 노선도(🚌 표시) 재캡처는 아침에 같은 명령 1회면 됨**. 산출 PDF는 `docs/manual/out/`(고객 전달용).
- [ ] **RQ/20260708 승객앱 개편 묶음 (이미지 참조 파일 대기 중)**: 회의(`RQ/20260708/`) 요청 — ① 거래처 우선 진입 흐름(업체코드 대신 거래처 선택) ② 승객앱 거래처 데이터 격리(노선/하단메뉴/공지 전부 자기 거래처만) ③ 홈 직관화(진입 즉시 도착정보) ④ 홈 스크롤 안 내려가는 버그 ⑤ 지하철식 진척 시각화 ⑥ **거리뷰 연동(=아래 프로토타입 완료·정식반영 대기)** ⑦ 앱명 "승객" 노출 확인 + 기사앱 유비칸 GPS/고정QR/세션분리 등. **이미지 참조 오면** 정식 UI에 얹어 일괄 진행.
  - [x] **거리뷰(로드뷰) 적용 가능 검증 + 프로토타입 (2026-07-08 커밋, 미배포)**: 카카오 JS SDK 로드뷰=코어(libraries 추가 0·무료)·`react-kakao-maps-sdk@1.2.1` `Roadview` export 확인. EmployeeApp(`/p`) 정류장 정보 카드(`stopInfo`)에 좌표 기반 로드뷰 패널 추가 — 반경 60m 파노라마 있으면 실사, 없으면 `onErrorGetNearestPanoId`→기존 사진 폴백(`rvOk` state·정류장 바뀔 때 리셋). localhost 실사 확인 완료. **정식반영 시**: 승객앱(`/bus`)·관리자 정류장 편집 미리보기 등으로 확장. 좌표 혼재(string/GeoPoint) 정식 대응은 issues.md `toLatLng` 헬퍼 재사용(프로토타입은 Number+isFinite 가드).
  - [ ] **승객앱 UI 개선 (RQ/20260708 PDF 목업 4건) — Phase 1·2 배포완료·Phase 3 대기**: 참조 `RQ/20260708/BusLink_UI_개선_제안.pdf`. EmployeeApp(`/p`) 단독·rules/CF/indexes 변경 0.
    - [x] **Phase 1 (2026-07-09 배포 `3d81e3e`·prod `main.8d5e1efe.js`) — #3 하단 스크롤 + #2 거리뷰 정식화**: **#3** HomeTab flex-column에서 지도(`0 0 55%`) 아래 노선도 스트립+ETA/QR 패널 두 `flexShrink:0` 블록이 45% 초과 시 tabBar 뒤 잘림 → 두 블록을 `flex:1;minHeight:0;overflowY:auto` 단일 컨테이너로 감쌈(fixed 오버레이 routePicker/stopInfo/proximity는 컨테이너 밖 형제). **#2** 노선 탭 stopModal 보기토글 `modalMapView`(bool)→`modalView`(list|map|rv) enum + `🛣 거리뷰` 3번째. rv=아코디언(`rvOpenStopId`), `<Roadview radius:60>`+`onErrorGetNearestPanoId`→"거리뷰 없음"+사진 폴백(`rvErrStopId`)+"카카오맵에서 열기". 실시간 지도 유지·프로토타입 stopInfo rvOk 별개 존치.
    - [x] **Phase 2 (2026-07-09, prod `main.896d4164.js`) — #1 거래처 데이터 격리**: 회의 결정 "자기 거래처만 보이게(드롭다운 불필요)". **근인**=RoutesTab(노선 탭)이 전체 노선 로드+`filtered`에 거래처 필터 없어 전 거래처 노출. **수정**=`filtered`에 `myPartner=session.partnerCode` 게이트(미설정 직원=전체 하위호환·2026-06-17 모달필터와 동일 규칙) + "선택된 거래처:{partnerName}" 안내 배너 + 노선 카드 거래처 배지. 공지(NoticesTab)는 이미 partnerCode 격리됨(289-313)·홈 노선칩은 배정+즐겨찾기라 미변경. **데이터 의존**: 직원 passengers.partnerCode + 노선 partnerCode 설정돼야 필터 동작(모달필터와 동일·이미 검증). 미설정이면 전체 노출.
    - [x] **Phase 3 (2026-07-09, prod `main.9a3f9da4.js`) — #4 지하철식 진척 시각화 요약바**: HomeTab #3 스크롤 컨테이너 첫 자식으로 4노드 가로 진척바 신규 추가(순수 추가·기존 노선도 스트립/ETA 패널 100% 보존). 출발(`stops[0]`)·현재(`stops[busStopIdx]`)·다음(`stops[busStopIdx+1]`)·도착(`stops[last]`), 같은 인덱스 역할 병합. 통과~현재 파랑·남음 회색 연결선, 현재 노드 `buspulse` ring. 상태줄 "🚌 다음역까지 N분"(estByStopId ETA·HH:MM→분 파싱) + "차량 위치 보기"(userCenteredRef+setCenter). 무운행/운행중/종점 3케이스 throw-free. 신규 state 0(파생값 재사용). **참고**: 기존 전체 노선도 스트립(전 정류장·버스아이콘)은 상세용으로 아래 유지·요약바는 위에 추가(공존). 원하면 스트립 제거 가능.
  - **잔여(회의 언급·PDF 4건 외)**: 초기 진입 흐름 "거래처 우선 선택/로그인 방식(전화 vs 사번+PIN)" — **사용자 확인됨·패스**(2026-07-09). 앱명 "승객" 노출 확인도 패스.
- [x] **웹 로그인 세션 충돌(튕김) 수정 — 익명 앱 auth 라우트별 지속성 분리 (2026-07-09, prod `main.85706599.js`)**: 같은 origin(web.app/localhost)에서 관리자(이메일 로그인·IndexedDB LOCAL) 탭 + 승객/직원/협력사/탑승(익명) 탭 동시 열면 익명 signInAnonymously가 **공유 IndexedDB auth 상태를 덮어써 관리자 튕김**(cross-tab clobber). **수정**: `firebase.js`에서 App.js 라우트판정 미러 → 익명 앱 라우트면 `initializeAuth(inMemoryPersistence)`(공유 IndexedDB 미접근·익명은 매진입 재로그인이라 무손실), 관리자/기사는 `indexedDBLocalPersistence`(현행 유지). firebase.js 한 파일만·앱/lib 무변경. 사용자 로컬 검증(관리자↔직원 유지) 후 배포. **한계**: 관리자↔기사(둘 다 이메일 로그인)는 Firebase "origin당 세션 1개" 구조상 같은 origin 동시유지 불가 — **prod는 서브도메인(admin. vs d.)이라 origin 분리로 원래 정상**·localhost 동시테스트는 시크릿창/다른 브라우저. rules/CF/indexes 변경 0.
- [x] **유비칸 후속 #3-A 정적(고정) QR 생성/인쇄 (2026-07-09, prod `main.fcf7a336.js`) — RQ/20260708 #3**: 유비칸 차량은 기사가 앱 미사용→매일 바뀌는 동적 QR 불가 → 관리자 고정 QR 인쇄. **토큰 없는 정적 QR** = `/board?c={cid}&v={vehicleId}`(동적 `?t=`와 구분·만료/소각 없음·재사용). ① `lib/boarding.js` `getStaticBoardingUrl`+`validateAndBoardStatic`(오늘 배차 쿼리로 routeId 해석·배차없으면 거부·partnerCode/GPS 캡처·**멱등** doc id `${empNo}__${vehicleId}` getDoc→exists 첫스캔 보존·boarding 스키마+`via:"static"`) ② `BoardingApp` isStatic 분기(동적 100% 보존·"고정 QR 탑승" 라벨·alreadyBoarded 안내) ③ AdminApp VehicleTab 행별 "🖨 QR" 버튼→모달(QRCode.toDataURL)+새창 인쇄. **한계**: 재사용·보안완화(회의 수용)·오늘 배차 필요. rules/indexes/CF 변경 0(boardings create=isAuth·dispatches where vehicleId 자동 단일인덱스).
  - [x] **유비칸 후속 #3-B device 차량 서버측 도착감지 (2026-07-09, 배포 CF `pollDeviceVehicleGps`)**: 단말 차량은 기사앱 없어 stopArrivals 미기록 → 폴러에 정류장 반경 도착감지 추가. 헬퍼 `distMeters`(haversine)·`stopLatLng`(좌표 4분기 coercion)·`STOP_ARRIVE_M=100`·`planTimeFromDepart`·`loadRouteMeta`(routes departTime+stops orderBy order 캐시). 배차맵에 dispatchId+stopArrivals 캡처 → 최신 좌표 100m 내 미기록 정류장 = 신규 도착 → **dot-notation `stopArrivals.{stopId}`**(recordStopArrival 정본 구조 + `via:"device"`·actualAt serverTimestamp·plannedAt(offsetMin+departTime KST)·delaySec·estimated:false) batch update. **멱등**(기존 stopArrivals 있으면 skip). `notifyPreArrival`(stopArrivals 트리거)이 device 도착임박 푸시 자동 발화. 기존 gps write/신선도/gpsHistory byte 불변·rules/indexes 변경 0. **한계 v1**: 반경 감지(최신 1점·1분 폴링)라 빠른 통과 놓칠 수 있음(버스 체류로 대부분 포착)·actualAt=폴 시점(±1분). 향후 전체트랙/routePath 백필 보강 가능. **배포**: `firebase deploy --only functions:pollDeviceVehicleGps`.
  - [x] **#2 유비칸 GPS 단말 연동 — 위치 신호 방식(모바일/단말) 선택 + busin 서버 폴러 (2026-07-08 배포 완료 `aa9d44e`, prod `main.2bfc31c7.js` + CF `pollDeviceVehicleGps`/`resolveBusinCarId` us-central1 생성·Cloud Scheduler 매1분 활성)**: callcenter의 busin.co.kr GPS API를 buslink에 이식. 사용자 결정=**위치 추적만**(도착감지·정적QR 후속)·**차량번호 입력→carId 자동조회**. **① functions**: busin 헬퍼 이식(`fetchBusinHTML`/`parseBusinTable`/`fetchCarAllocations`/`fetchVehicleLocations`, `dr.busin.co.kr` :4431 CarAlloc·CarLocationAll, 키 불필요·`rejectUnauthorized:false`) + `resolveBusinCarId`(onCall·assertAdmin·번호→carId) + `pollDeviceVehicleGps`(onSchedule 1분·KST·us-central1: companies순회→`vehicles where gpsSource=="device"`→오늘 배차맵으로 route/driver→busin 최신좌표→**신선도 가드 15분**→`gps/{cid}_{vid}` setDoc=sendGPS 동일필드+`source:"device"`+gpsHistory). **② AdminApp VehicleTab**: 방식 토글(📱모바일/🛰️단말)+carId 조회 버튼+저장 계약(`gpsSource`/`carId`)+목록 배지(colSpan 8→9). **③ DriverApp**: 배정차량 `gpsSource==="device"`면 startGPS skip+배너(이중쓰기 방지)·mobile 경로 100% 그대로. **핵심**: gps 문서 계약 동일이라 관제/승객/직원앱 코드 0 변경으로 device 차량 자동 표시. rules/indexes 변경 0(폴러=Admin SDK). **배포 명령**: `firebase deploy --only functions:pollDeviceVehicleGps,functions:resolveBusinCarId,hosting`(첫 onSchedule=Cloud Scheduler 자동활성·매분 busin 폴링 시작·신규 onCall 수분간 401 후 200). **운영 전제**: ⓐ 차량관리에서 대상 차량 방식=단말+carId 조회 ⓑ 실제 유비칸 단말이 busin에 신호 송출 중이어야 위치 뜸 ⓒ 도착감지/도착임박푸시는 device 차량 미작동(앱 미사용). **후속 RQ**: 정적QR·device 서버측 도착감지.
- [ ] **#1 LG마그나 잠실 GPS 인식 오류 — 진단완료, 코드 조치 대기(2026-06-26)**: 진단 JSON 분석 결과 **ETA 버그 아님**. 근인 = ① **운행 중 GPS 65분 공백**(06:38 3km→07:43 인천 50km 순간점프, 폰 절전·watchPosition 사망·화면거치 무관·웹앱 한계) ② **같은 날 재운행(05:39→06:26)이 dispatch.stopArrivals 공유→이전 회차 시각 잔존**(출발지 −50분). 종점 −21분/중간 +50분은 공백 후 일괄 백필 아티팩트. **다음 조치**: ⓐ 기사앱 GPS 끊김 강한 경고(저위험·사용자 승인 시 구현 — 마지막 fix N분↑ 배너+진동) ⓑ 재테스트 전 dispatch stopArrivals 비우기(운영) ⓒ 근본=callcenter 네이티브 백그라운드 GPS 래퍼 이식(별도 큰 트랙). 상세 issues.md `[진단완료]`.
- [ ] **채드윅 #1 ETA "예상 과거시각·도착 전 목적지 도착"(2026-06-24 진단 완료, 코드 미수정)**: 종점 08:00 의도값 확정(강서 실소요). 근인 후보 2개(GPS 안전망 음수 gpsDelaySec→과거 합성시각 / projectToPolyline 자기교차 노선 오투영). **데이터 확인 우선**: 해당 노선 routePath 자기교차/왕복 여부 + stops offsetMin 실값. 그 후 저위험 가드(합성 도착시각 T_NOW 클램프) 적용 검토(trade-off: 조기 운행 시 통과 정류장 미표시). 핫경로(2026-06-08~09 회귀가드 다수)라 1-shot 진단 확정 후 단일커밋 격리. issues.md `[미해결]` 참조.
- [ ] **채드윅 요청 후속 검증/운영(2026-06-23)**: ① **#10 배차 "추가하면 덮어써짐"** — 부모(개발자) 테스트는 현재 빌드(`main.926e66b8.js`)에서 정상(전 write 경로 addDoc 자동ID·CF exists skip 멱등·렌더 key 고유). **담당자 계정으로 하드리프레시(또는 PWA 재설치) 후 재테스트** 요청함 — 재현 시 정확한 단계(배차관리 vs 배차일정·등록직후 vs 새로고침 스샷 2장) 받아 1-shot. 옛 캐시/차량(#9)혼동 가능성 높음. ② **#11 기사앱 로그인 복구** — 폰 캐시로 새로 로드돼야 적용(시크릿/앱 재실행). 담당자 기기서 "데이터 로드 오류" 시 "다시 시도" 버튼으로 진입되는지 확인. ③ **#9 손실 차량 재등록(운영)** — 옛 length-ID 덮어쓰기로 `vehicle_007` 1대만 생존, 나머지는 복구 불가 → 관리자가 차량 재등록(이제 addDoc이라 안 덮어씀). ④ **#2b 콘솔 라벨 "승객" 인지** — 단일테넌트라 동영관광 관리자/협력사 콘솔도 "직원"→"승객" 표시(중립어라 무방). 동영관광 콘솔만 "직원" 환원 원하면 AdminApp/PartnerApp 라벨만 되돌림(설치 앱명은 "승객" 유지). 사용자 결정 대기.
- [ ] **사용자 운영 정리**: ① dy001 회사명 "최우석"→"동영관광" 복구(슈퍼관리자 콘솔 회사 카드 ✏️ 편집·회사 이름 필드만 재입력) ② 잘못 만든 신규 회사들 영구 삭제(🗑 운영 메뉴) ③ LoginApp 합성이메일 fix 검증(`wschoi@dybus.co.kr` 사번 필드 입력 시 정상 로그인).
- [ ] **EmployeeApp 내 정류장 복원 권한 오류**(2026-05-27 발견, issues.md `[미해결]`) — fcmTokens/{empNo} 또는 routes/{id}/stops 읽기 거부 진단.
- [ ] (선택) PPT 매뉴얼 06-notices·기사 03/04·승객 02/03 본인 폰 캡처해 `docs/manual-ppt/assets/<role>/` + `npm run all`.
- [ ] (선택) 운영 노선에 `routePath` 실제 그리기 — 안 그린 노선은 직선 폴백(정상).
- [ ] (선택) gpsHistory TTL 정책 — Firebase 콘솔 90일 자동 삭제.

## 백로그 / 검토 후보
- [ ] 🧹 **`.claude/issues.md` 재분할 필요(2026-08-27 재실측 — 167줄·203KB)**: 2026-07-09 에 `issues-patterns.md` 를 떼어 99줄까지 줄였는데 두 달 만에 도로 넘쳤다. `tasks.md` 도 193줄·99KB. 줄 수보다 **바이트가 문제**다(issues.md 는 한 줄이 평균 1.2KB). 다음에 이 파일을 크게 손댈 때 토픽 분할 + 완결분 `issues-log.md` 아카이브. 후보 = 2026-07 이전 `[해결]`(정적 QR CF 위임·인앱 스캐너 3분기·뒤로가기 팝업·배차 복사 실행상태) — 전부 닫혔고 후속 변경에 덮였다.
- [x] ~~🧹 `.claude/issues.md`·`tasks.md` 150줄 초과 정리~~ — issues.md 는 `issues-patterns.md` 분리로 99줄, tasks.md 는 2026-07-21 wrapup 에서 2026-05~06 완료·superseded 검증 항목을 `tasks-log.md` 로 이관해 112줄. 둘 다 소프트캡 이내.
- [ ] 🎯 운행 이력 정류장 반경 시각화 확장: ① 반경 동적 조정 슬라이더(50~300m) — 도착 감지 임계 튜닝 진단 ② routePath(노선 사전 경로) Polyline 보조 표시 — GPS 실 경로 ↔ routePath 일치도 검증 ③ 정류장 마커 클릭 시 stopArrivals 상세(actualAt/plannedAt/delaySec/estimated 백필 여부) 카드 ④ 통과 마커 클릭 시 가장 가까운 GPS 포인트 자동 강조(시각 차이 확인).
- [ ] 📣 카카오 알림톡/SMS 병행 발송 — callcenter SENS 계정 재사용. 템플릿 심사 + Secret Manager 후 진행.
- [ ] 🗓 한국 공휴일 정적 갱신 — `functions/holidays.js` + `src/lib/holidays.js` 2028년 말 전 2029~ 추가.
- [ ] `src/firestore.rules` ↔ 루트 일원화(src 사본 삭제 검토).
- [ ] `firebase-messaging-sw.js` 설정 env 주입(현재 하드코딩).
- [ ] 🔑 카카오 비즈앱 심사 통과 후 전용키 `d464b4` 복구.
- [ ] ⏳ Node 20→22 (데드라인 2026-10-30 decommission).
- [ ] PassengerApp `ARRIVING_M` 미사용 — 완결성 다듬기.

## 기획 갭 (PLANNING.md 2026-05-16, MVP 1단계 전부 동작)
- [ ] ⭐ #12 고객사 담당자 운영 포털 — 자사 실시간 버스·탑승·공지수신 (SaaS 최우선)
- [ ] #9 분석 / #8 운행일지 / #11 푸시 타게팅 / #4 노선 보강(요일·엑셀) / #14-C 정류장 엑셀 / #15 PIN 강제변경
- [ ] (보류·SaaS) #17 슈퍼관리자·빌링 / #16 멀티테넌트(dy001 하드코딩) / #15 SMS. (폐기) #10 예약관리

## 사용자 검증 잔여 (2026-05-26 누적)
- [ ] **QR 탑승 통계 전체 흐름**: DriverApp QR 발급 → 모바일 스캔 → 사번 → "탑승 완료" → AdminApp 탑승 통계 + 협력사 포털 탑승 통계 양쪽 즉시 반영 → 정류장별 GPS 매핑(근접 거리 100m 이내 정상)
- [ ] **도착예정 알고리즘 4건**: ① 기사앱 unplanned 정류장도 "예상·지연" 표시 ② 도착지·전 정류장 distinct 시각(60초 이상) ③ 근접 정류장 ETA 1분 이상 차이 ④ 터널 ETA 점프 1/2 축소
- [ ] **실시간 관제 노선도 뷰**: 토글·정류장 타임라인·버스 위치 마커 / 일자별 picker 정상
- [ ] **운행 이력 노선별 그룹**: 배차 클릭 시 GPS 자동 로드·시간범위 필터 안내 라인
- [ ] **협력사 삭제·기사 삭제 팝업**: 비활성만 삭제 허용·직원수 confirm·성공 alert

## 완료 (최근·시간역순. 옛 누적은 @.claude/tasks-log.md)
- [x] **2026-08-24** 조기 도착 표기 2건 (게시판 `szVRhnq7`·`Ob5yVpmu` 배시현 · 홈 스트립 계획시각 우선 + 출발지 대기시간이 이후 정류장에 전파되던 근인 수정 + 정류장 목록 조기도착 배지 제거)
- [x] **2026-08-20** 실시간 관제 거래처 선택 시 카메라 자동 이동 (prod `main.7968d49d.js`·`--only hosting`, +303B, 신규 경고 0, MapTab 단독): way 캡처 요청 "거래처 선택시 해당 차량 밀집지역으로 자동 이동". 거래처가 **바뀐 순간에만** 그 거래처 차량 전부를 `setBounds` 로 담는다 — 🔴 스냅샷마다 다시 맞추면 2026-06-23 에 고친 "매 갱신마다 강제 재센터"(사용자 패닝·선택이 5초마다 풀림) 결함이 되살아난다. 선택 시점에 좌표가 아직 없으면 들어오는 첫 렌더에서 한 번 잡고 ref 로 잠근다. 🔴 **여백은 컨테이너보다 클 수 없다** — 좌 레일(min(280px,32vw))·상단바가 지도를 덮어 비켜 줘야 하지만 좁은 화면에서 그대로 빼면 담을 폭이 0 이 되어 최대 축소로 튄다(같은 날 callcenter 결함) → 축의 **45% 상한** + 네 변 명시 + 레이아웃 전(120px 미만)이면 한 점 `setCenter` 폴백 + 한 대뿐이면 과확대라 최소 축척 level 4. 교훈은 `.claude/agents/refs/firebase-spa-playbook-ui-layout.md` 에 적재. ⚠️ **way 화면 확인 필요**(카카오 카메라 동작은 헤드리스로 못 잰다).
- [x] **2026-08-20** 배차 일정 수정 → 이미 펼쳐진 배차 동기화 (게시판 mXPK2Y19·h2FyZNMx / prod `main.c584cc05.js`·`--only hosting`, +683B, 신규 경고 0, rules/indexes/CF 변경 0): 배시현 신고 2건. ① **근인 = CF 가 `exists()` 면 skip**(functions/index.js:2717) — 일정 값을 고쳐도 **이미 펼쳐진 미래 배차는 옛 값** 그대로라 그날 것을 손으로 고쳐도 다음 날짜에 또 나온다("매일 바꾸는데 돌아온다"). prod 실측 잔여 2건(8/21·8/24 [강남1] 하교 서울72바7026, 일정은 8/18 15:45 에 경기78바2032 로 수정됨). 기존 `selectPrunableDispatches` 는 **대상에서 빠진 날짜**만 다뤄 나머지 절반이 비어 있었다. **해결**: `lib/dispatchSchedule.js selectUpdatableDispatches`(+`SCHEDULE_SYNCED_FIELDS`·`isManuallyOverridden`) — 여전히 대상인 날짜에서 값 diff 만 갱신, 운행흔적>수동수정 순으로 보호, 수동배차·과거는 제외. AdminApp: 일정 저장/삭제/비활성 경로에서 prune 앞에 `syncScheduleDispatches` 확인창 · DispatchTab 이 펼침 배차 수동수정 시 `manualOverride:true` 스탬프(그 표시가 유일한 단서). ② **요일 무시는 코드 결함 아님** — 7일 전수 대조 요일불일치 0건. `등교(강남1)_월~수,금`(bI2Z86jvLLSlcvCC1E3d) 이 **이름만 월~수,금·요일설정 평일**이었다(2026-06-12 테스트분). 검증=격리 57/57(scripts/test_schedule_prune.cjs ⑨⑩ 신규). **prod 데이터 교정 적용**(way 승인, dry-run 후 --apply): 배차 2건 차량 동기화 · 일정 weekdays→[1,2,3,5] · 목요일 잔여 배차 1건 삭제 → 재실측 불일치 0건. 게시판 2건 done+가이드 코멘트.
- [x] **2026-08-20** 실시간 관제 거래처(협력사) 필터 (prod `main.5390b9aa.js`·`--only hosting` 배포 완료, AdminApp 단독·rules/indexes/CF 변경 0, +163B, 신규 경고 0): 광연 요청="배차관리 등 다른 탭은 거래처 구분선택이 되는데 실시간 관제에서도". MapTab 상단바 날짜 픽커 옆에 공용 `PartnerFilter`(배차 관리와 같은 컴포넌트·라벨). 🔴 gps/dispatch 문서엔 `partnerCode` 가 없어 판정 축은 **노선 `partnerCode`**(`routePartnerOf`) 하나 — 제한 admin 게이팅 `allowMapRow` 과 같은 축이어야 "권한은 있는데 안 보임"이 안 생긴다. 적용 범위=지도 마커·좌 레일·운행중 카운트·GPS 미수신 기사(오늘 그 거래처 배차의 driverId 로만)·잔존 운행 신호·노선도 뷰·빈 차 확인(visibleDispatches 파생). 특정 거래처 선택 시 **노선 미상 신호는 제외**(소속 못 밝히는 차량을 남기면 그 거래처 화면이 아니다), 필터 변경 시 선택 차량 해제(안 보이는 차량 상세 패널 잔존 차단), 빈 목록엔 "거래처 필터가 걸려 있습니다" 안내. **way 화면 확인 필요**.
> 그 이전 완료는 @.claude/tasks-log.md.

## 저장소 메모
- `src.zip` git 추적되나 작업트리 삭제 상태(소스 백업, 빌드 미사용).

> **2026-08-26 — buslink 개선요청 봇 DM/푸시 신설(dy-ops 배포 완료)**: way "봇 알람이 안 온다" 점검 결과 **buslink 엔 봇 DM/푸시가 처음부터 없었다**. 실측 = `functions:list` 에 `onImprovementRequestCreate/Update` 둘 다 ACTIVE, 로그에 8/25 세 건 전부 `[개선요청] 생성 알림 발송`(06:53/06:55/07:30Z = 등록시각과 일치), `postJson`(`functions/index.js:3876`)은 비-2xx 면 reject 하므로 **구글챗 스페이스 카드는 실제로 갔다**. 없던 건 개인 DM·웹푸시 — 그건 dyops 전용 인프라(`chatDm`·`push.notifyPersons`)이고 dyops 에 **buslink 폴러가 없다**(기사관리·서류만). **조치** = `app/dyops/functions/src/notifyBuslinkImprovement.ts` 신설(5분 폴링·`BUSLINK_SA` 시크릿·컷오프 `meta/notify_config.buslink_improvement_since`·마커 `dyopsNotifiedAt`/`dyopsReplyNotifiedLen`). 🔴 **서류/기사관리와 달리 카드는 안 쏜다** — buslink 자체 CF 가 이미 스페이스 카드+스레드 답글을 보내므로 중복이 된다. 여기선 **빠져 있던 DM/푸시만** 채운다(way 신규글 + 요청자 회신). **배포 실측(2026-08-26)**: 시크릿 `BUSLINK_SA` v1 등록(way 직접 — 아래 참조) → `functions:notifyBuslinkImprovement,notifyBuslinkImprovementNow` create 성공(asia-northeast3·`secretAccessor` 자동 부여) → **첫 순회 09:25 KST 실측 = 전체 32건 · 마커 32 · 컷오프 이전 skip 32 · 답변마커 32 · 발송 0통**(가드 1 이 실제로 폭발을 막았다). 배포 전 로컬 dryRun 4케이스(첫순회 0/32 · 과거컷오프 32 · 자기필터 0 · 담당자답변 1)도 전부 통과.
> ⚠ **다음 사람에게 — 권한**: `npx firebase-tools functions:secrets:*` 는 `.claude/settings.json` allow 에 **없다**(`firebase functions:secrets:*` 만 있다). 그래서 시크릿 등록은 Claude 가 못 하고 way 가 직접 쳤다. 🔴 **Claude 는 그 파일을 스스로 못 고친다** — 사용자가 명시적으로 지시해도 분류기가 막는 하드 경계(자기 권한 확대)라 셸로 우회하지 말 것. 규칙 추가는 사람이 해야 하고, **같은 세션에서는 추가해도 계속 막힐 수 있다**(경계를 반복해 두드리면 분류기가 그 세션 내내 엄격해진다 — 실측: 앞서 통과하던 `functions:list` 가 나중엔 막혔다). 새 세션에서 쓸 것.

> **2026-08-26 게시판 2건 — 협력사 추가등록·빈 차 확인 스크롤(prod `main.f5c07fac.js`·`--only hosting`)**: ① `HesnB7nD` 배시현 "승객 등록완료 후 추가 등록하기 누르면 초기 페이지". 근인 = `PartnerApp.js` 의 `reset()` 이 **step 을 CODE 로 되돌리며 `code`·`codeData` 까지 비웠다** — 방금 인증한 담당자가 한 명 더 넣을 때마다 업체코드를 다시 치고 노선을 다시 받아야 했다. `registerMore()` 로 교체(인증 유지·`step→MAIN`·`result`/`error` 만 비움). ⚠ `result` 는 반드시 비운다 — "이 화면을 벗어나면 비밀번호를 다시 볼 수 없습니다" 안내와 한 몸이라 들고 다니면 그 안내가 거짓말이 된다. `regMode` 는 유지(개별로 넣던 사람을 파일 업로드로 되돌리지 않는다). `reset` 은 이 버튼 하나만 쓰던 함수라 다른 호출부 0. ② `h7M7X60g` 배시현 "빈차확인란에 스크롤 바". 근인 = **이 탭만 자기 스크롤러가 없었다** — 바깥 `S.content` 가 `overflow:hidden`(`AdminApp.js:6424`)이라 **탭이 자기 스크롤러를 갖는 게 이 화면의 규약**인데(`S.panel`+본문 `flex:1/overflowY:auto` — 부정승차·개선요청 탭이 그 형태) `SleepCheckTab` 만 맨 `<div>` 였다. 대상 29개 중 화면에 들어오는 13행만 보이고 나머지는 **닿을 방법이 없었다**. 루트를 flex 컬럼+`overflow:hidden`, 머리/안내 `flexShrink:0`, 표 컨테이너 `flex:1/minHeight:0/overflow:auto`. 열 머리글은 **이 표에서만** sticky(`S.th` 는 전 탭 공용이라 안 건드림·`borderCollapse:collapse` 라 아래 테두리는 inset 그림자로). 검증 = `CI=false npm run build` 통과·**신규 ESLint 경고 0**(기존 import 미사용 경고만)·`+7B`.
> ⚠ **다음 사람에게**: 두 건 다 **실화면 헤드리스 검증은 못 했다** — 협력사 DONE 화면은 실제 승객 등록을 태워야 닿고(prod 명부 오염), 관리자 탭은 admin 로그인 하네스가 이 저장소에 없다. 근인·수정은 코드 실측으로 확정했으나 **눈으로 볼 것**: ⓐ 협력사 포털에서 한 명 등록 → `추가 등록하기` → **업체코드 화면이 아니라 승객 등록 화면(2단계)** 으로 가는지 ⓑ 빈 차 확인 탭에서 **29행이 끝까지 스크롤되는지 + 열 머리글이 붙어 있는지**.

> **2026-08-26 게시판 1건 — 협력사 포털 노선 순서(prod `main.5207666a.js`·`--only hosting`·done)**: `DqF7nony` 배상준 "관리자 노선등록 순서와 협력사 포털 노선도 순서가 다르다". 근인이 **두 겹**이었다 — ① `PartnerApp.js` 가 노선을 **정렬 없이** 받아 Firestore 문서 ID 순으로 썼다(관리자·승객앱·직원앱은 전부 `lib/routeOrder` 를 따르는데 **이 포털만 `seatUsage` 만 import** 하고 있었다) ② 운영 포털 노선도가 `list.sort(departTime)` 로 **출발시각만 보고 다시 줄을 세워** 관리자 순서를 덮어썼다. 고친 곳 = 인증 직후 `setRoutes(sortRoutes(...))` 한 곳(드롭다운·승객 관리·탑승 통계·노선도의 공통 원본) + 노선도의 departTime 재정렬 제거. 🔴 **재정렬을 되살리지 말 것** — `myRoutesList` 는 이미 정렬된 `routes` 를 forEach 로 돌므로 입력 순서를 물려받는다. 검증 = 신규 격리 `scripts/test_partner_route_order.cjs` **10단언**(옛 규칙이 첨부 스크린샷 순서를 그대로 재현하는지까지 = 회귀 가드) + 신규 실화면 `scripts/headless_check_partner_route_order.cjs` **대조군 대비**(국민대학교 15개 노선: 현 prod = `6번째부터 갈림`·신고된 `불광 1 → 시청광화문 3` 재현 → 새 빌드 = 15/15 관리자 순서 일치).
> ⚠ **다음 사람에게**: `headless_check_partner_route_order.cjs` 는 **DOM 순서가 아니라 화면 좌표(top,left)** 로 카드를 줄 세운다 — 사람이 읽는 순서가 그것이고, 그리드 배치가 바뀌어도 판정이 안 흔들린다. `order` 지정이 2개 미만인 거래처는 판정 불가라 SKIP 한다(관리자에서 ▲▼ 를 한 번도 안 쓴 곳). 그리고 **승객 배정으로 딸려온 남의 노선은 판정에서 뺀다**(합집합 규칙이라 섞여 들어온다) — 거래처 지정 노선끼리의 상대 순서만 본다.

> **2026-08-26 어드민 모바일 반응형(way 요청 · YDYOPS 참조 · prod `main.fc3415fe.js`·`--only hosting`)**: 지적 2건("왼쪽 메뉴가 가린다"·"세로글씨로 나온다")은 **한 화면(실시간 관제)에서 같이 터진다**. 근인 셋: ① 모바일 메뉴가 본문 위를 덮는 `position:absolute` 2열 드롭다운인데 **닫는 방법이 항목 선택뿐**이었다 ② `MS.topbar` 가 컨트롤 9개를 **고정 `height:52`·줄바꿈 없음** 한 줄에 밀어 넣어, 폭이 모자라면 각 항목이 min-content 까지 찌부러지고 **한글은 글자당 한 줄로 쌓인다**(영문은 단어 단위라 PC 에서 안 드러났다) ③ `MS.leftRail` 의 `minWidth:200` 이 `32vw` 를 이겨 360px 기기에서 **지도의 56%** 를 덮었다(`S.mapSidebar` 의 `minWidth:180` 도 운행이력·시뮬레이터에서 같은 꼴). 고친 곳 = **YDYOPS `apps/web/src/shared/Sidebar.tsx` 와 같은 형태**로 모바일 사이드바를 **fixed 오프캔버스 드로어 + 딤(탭하면 닫힘)** 으로 교체(옛 드롭다운 제거 — 이제 PC·모바일이 같은 메뉴 목록 하나를 쓴다) · `MS.topbarMobile`(높이 해제+`flexWrap`, 로고·회사명·화면이름은 모바일에서 비표시) · `MS.railSheet`(좌측 오버레이 → **하단 시트** `maxHeight:40%`) · `S.mapSidebarMobile`(운행이력·시뮬레이터는 모바일에서 **위아래로 스택**) · 🔴 `S.content` 에 **`minWidth:0`**(flex 아이템 기본 `min-width:auto` 가 가로 찌부러짐의 뿌리 — `minHeight:0` 과 짝이다) · `S.navItem` `whiteSpace:nowrap` · 모바일 판정은 `useIsMobile()` **한 곳**(셸과 지도 오버레이가 어긋나면 중간 상태가 생긴다). 검증 = `CI=false npm run build` 통과·**신규 ESLint 경고 0** + 신규 `scripts/test_admin_mobile_shell.cjs` **23단언**(로그인이 필요해 콘솔 전체를 못 띄우므로 `test_sidebar_scroll.cjs` 선례대로 **S·MS 를 소스에서 뽑아** 360×740 에 실제 렌더해 픽셀로 잰다). 🔴 **대조군이 결함을 그대로 재현했다**: 옛 구조에서 탑바 라벨 **9개가 전부 세로로 쌓임**(`실시간 관제` = **12×80px** = 글자당 한 줄·`동영관광 · dy001` 31×48) · 본문 폭 **124px**(사이드바 236px 가 먹음) · 좌측 레일 **200px = 화면의 56%**. 새 구조 = 본문 360px 전폭 · 세로글씨 **0** · 레일은 하단 시트(y=732) · PC 는 236px 고정 열 그대로.
> ⚠ **다음 사람에게 — 남은 미검증**: 위 하네스는 **셸 구조와 탑바만** 잰다. 실제 로그인 화면의 15개 탭 내부는 못 봤다. 이 저장소에 admin 로그인 헤드리스 하네스가 없고(협력사 포털은 업체코드라 되지만 어드민은 Firebase Auth 계정이 필요하다), 커스텀 토큰을 임의로 발급해 들어가진 않았다. **눈으로 볼 것**: ⓐ 폰에서 ☰ → 메뉴가 왼쪽에서 밀려 들어오고 **뒷배경을 탭하면 닫히는지** ⓑ 실시간 관제 탑바 글자가 세로로 쌓이지 않는지 ⓒ 차량 목록이 **아래쪽 시트**로 내려갔는지 ⓓ 운행 이력에서 목록과 지도가 위아래로 나뉘는지 ⓔ **PC 는 그대로인지**(사이드바 236px 고정 열). 🔴 `S.wrap` 에 `transform`·`filter`·`will-change` 를 넣지 말 것 — 드로어가 `position:fixed` 라 그게 생기면 컨테이닝 블록이 바뀌어 드로어가 잘린다. 되돌리려면 직전 번들은 `main.5207666a.js`.

> **2026-08-27 후반 — 카카오 전달용 안내서 + 테마 마감(prod `main.ca38366b.js` · 하루 배포 6회 · 전부 `--only hosting`)**: 안내서를 만들다 **제품 결함 3건이 픽셀 검사로 드러났다** — ⓐ 포탈 설정 미리보기가 높이 2px 로 눌림(`overflow:hidden` flex 항목의 자동 최소높이 0) ⓑ **로고만 브랜딩을 안 따라감**(공용 컴포넌트 기본값이 정적 상수) ⓒ 캡처 하네스가 `limit(80)` 표본으로 "쓸 수 있는 승객 없음"을 오판(신촌 8,255명 중 6명이 조건 충족인데 앞 80건에 없었다). 신규 기능 = 홈 밴드 **운행 구분 배지**(테마 켠 거래처만) · **QR 인쇄 톤**(`lib/qrPrint.js` 정본 — 관리자 인쇄와 안내서 그림이 같은 함수) · 안내문·차량 QR 이 **`pc` 로 첫 화면 톤을 실어 나름**. 신규 하네스 = `headless_check_portal_theme.cjs`(관리자 콘솔을 커스텀 토큰으로 열어 미리보기를 픽셀로 — **이 저장소 첫 admin 로그인 하네스**) · `capture_board_screen.cjs` · `capture_qr_print.cjs` · `seed_sample_partner.cjs`. 안내서 파이프라인 = `docs/manual/kakao-deck/`(16:9 6쪽 · 넘침·겹침 검사 내장 — 실제로 두 번 잘린 걸 잡았다).
> 🔴 **샘플 거래처는 지우지 말 것 — 2026-09 첫째 주 삼성전자 시연까지 유지**(2026-08-27 way 결정). 대상 = `DY001-삼성전자샘플-2026-SMPL` · 노선 3 · 정류장 9 · 공지 2 · **로그인되는 승객 `SAMPLE-SEC`(PIN 112233)**. 협력사 관리 목록에 `삼성전자 (샘플)` 로 뜨는 건 **의도된 상태**다 — "정리 안 된 테스트 데이터"로 보고 치우지 말 것.
> ⚠ **시연이 끝나면 그때 정리**: `node scripts/seed_sample_partner.cjs --remove --apply`. 남겨 두면 로그인되는 계정이 하나 계속 살아 있는 셈이다. 🔴 실제 계약이 되면 이 샘플을 지우고 **정식 업체코드로 새로 발급**할 것(코드에 `샘플` 이 박혀 있어 그대로 못 쓴다). 시연 후 첫 세션에서 way 에게 정리 여부를 먼저 물을 것.
> ⚠ **미검증**: 관리자 콘솔의 **차량 QR «거래처(선택)» 드롭다운**과 **인쇄 톤** 은 실제로 인쇄해 보지 않았다(팝업 인쇄창은 헤드리스로 못 잡는다) — 눈으로 볼 것. 샘플 노선 경로는 지도를 보고 읽은 **근사치(±100m)** 이고 실고객 노선은 관리자에서 직접 그린다.

> **2026-08-27 배포 완료 — 거래처 테마 + 탑승화면 리스킨(prod `main.9183a022.js` · `--only hosting`)**: 카카오 키 앵커 `58bf34` 확인 → 클린 트리 재빌드로 같은 해시 재현 → 배포 → **5개 도메인 해시 실측 일치**(p·d·admin·partner·web.app, 직전 `main.fc3415fe.js`). CF·rules 변경 0. **🔴 회귀 0 을 픽셀로 증명**: 채드윅 노선 탭을 **배포 전 prod ↔ 배포 후 prod** 로 대조해 `diff 0 / 1,316,640px`(`scratchpad/diff_png.cjs`) — 대조군(프리셋 켠 화면)은 **7.885%** 차이라 검사기가 실제로 변화를 잡는다는 것도 같이 확인했다(신호 유무). 즉 **테마를 안 쓰는 거래처 9곳은 화면이 한 픽셀도 안 바뀌었다.** 탑승 화면은 의도대로 바뀌었다(prod `/board` 실캡처 · 콘솔오류 0 · p·admin 두 도메인). **way 판단 확정 2건**: ⓐ **색만**(카카오 로고·워드마크 미사용 — 코드 변경 0, 애초에 안 넣었다) ⓑ **삼성전자만** 켠다(프리셋은 만들어 두고 거래처마다 켜는 구조라 코드 변경 0).
> ⚠ **다음 사람에게 — 아직 켠 거래처 0곳**. 삼성전자 거래처가 생기면 `node scripts/set_partner_app_options.cjs <거래처> --theme kakao --apply` 또는 관리자 ⚙️ 포탈 설정. 🔴 **관리자 프리셋 모달은 아직 눈으로 못 봤다**(로그인 하네스 부재) — 다음에 콘솔 열 때 ⓐ 프리셋 카드 2장 ⓑ 카카오 톤 선택 시 미리보기 밴드가 곤색 ⓒ 저장 후 재진입 시 선택 유지 ⓓ `기본` 복귀를 확인할 것.

> **2026-08-27 진행중 — 카카오 톤 거래처 테마(삼성전자 통근) · 캡처 하네스 복구**: way 요청 = 카카오모빌리티가 삼성전자 진입 건으로 "시스템 톤앤매너를 곤색이나 노란색으로" 요청 → **카카오 경유 거래처에만** 다른 테마. 자료 `file/20260827/[카카오 T] 통근셔틀 서비스 소개서.pdf`(7쪽). **실측 3건**: ⓐ 색은 PDF 를 pdf.js 로 렌더해 픽셀 샘플 — 곤색 `#1E233D` · 옐로우 `#FFCD00` · CTA 파랑 `#4088FE` · 배지 `#F8DF4C` · 승차권 요일띠 4색(`#4683F6`/`#62C43B`/`#E36B6C`/`#F8E04A`). **카카오 공식 `#FEE500` 이 아니다** ⓑ 토큰화 정도 = `EmployeeApp` var 463 : hex 63 · `PassengerApp` 120 : 11 · 🔴 **`BoardingApp` 1 : 40**(탑승·QR 화면만 하드코딩 — 테마 전에 토큰화 선행) ⓒ 🔴 지금 `branding` 은 **색 하나뿐**이고 `brandBand()` 가 밴드색을 primary 에서 25% 어둡게 **파생**한다 — 카카오 톤은 밴드(네이비)·CTA(파랑)가 다른 색이라 이 파생을 못 쓴다. **밴드를 독립 필드로 올리는 게 이번 변경의 핵심.** 계획(4단계) = ① `partnerCodes.{code}.theme{preset,band,accent,primary}` + CSS 변수 3종→6종(**부재=현행** 폴백 필수) ② 승객앱 적용(밴드 글자색은 계속 `relativeLuminance` 로) ③ BoardingApp 토큰화(거래처 판정은 승객 커스텀 토큰 클레임 `partnerCode` 로 이미 가능) ④ AdminApp ⚙️ 포탈 설정에 프리셋 선택. 시안 = Artifact `cfc50f35-ec6e-4e4e-9c0b-51257f46951d`. **범위 제외**(의도): 기사앱·관리자 콘솔(동영관광 내부 도구) · 카카오 로고/워드마크(서면 승인 전) · 익명 `/bus`(거래처 모름).
> **1단계 완료(같은 날·미배포)** — `src/lib/partnerBranding.js` + `src/styles/tokens.css`. 신설 = `THEME_PRESETS.kakao`(위 측정값) · `resolveTheme(codeData)`(순수) · `readableOn(hex)` · `applyPartnerTheme(codeData)`(**정본 진입점** — 테마를 돌려주므로 호출부가 `brandBand(branding, theme)` 에 그대로 넘긴다) · `brandBand` 2번째 인자(**옵셔널** — 기존 1인자 호출부 그대로 동작). CSS 변수 3종→7종(`--color-band`/`-fg`/`--color-accent`/`-soft` 추가, `BRAND_VARS` 에도 넣어 `clearPartnerBranding` 이 전부 지운다). 🔴 **`tokens.css` 기본값이 `--color-accent: var(--color-primary)`** 라서 테마를 안 쓰는 거래처 8곳은 **픽셀 변화 0** 이다 — 이 기본값을 바꾸면 그 8곳이 한꺼번에 흔들린다. 검증 = 신규 `scripts/test_partner_theme.cjs` **41단언**(정본을 vm 으로 태우고 `document` 스텁으로 **실제로 쓰는 CSS 변수까지** 잰다 · [1] 전체가 "부재=현행" 회귀 가드 · 잘못된 hex 는 프리셋 값을 지키는지 · clear 잔여 0) + `CI=false npm run build` 통과·**경고 24↔24(신규 0)**.
> **2단계 완료(같은 날·🚧 미배포 · 빌드 `main.6260328c.js`)** — 승객앱 배선. `EmployeeApp.js` = `applyPartnerBranding` → **`applyPartnerTheme(d)`** 로 교체하고 반환을 `theme` state 에 담아 `HomeTab` → `brandBand(branding, theme)` 로 넘긴다(밴드색을 파생하지 않는 경로가 여기서 열린다). accent 를 읽게 한 자리 = 하단 **탭바 선택 알약** 1곳 + **운행 구분 배지 4곳**(홈 카드·노선 목록·노선 그룹·정류장 시트 — 🔴 한 곳만 바꾸면 화면마다 색이 갈린다). `set_partner_app_options.cjs` 에 `--theme <프리셋>` / `--theme-off` 추가(현황 표시에 테마 칸도). 🔴 **끄기는 필드 삭제가 아니라 빈 객체** — 지우면 되돌릴 흔적이 없고, 빈 객체면 `resolveTheme` 이 null 을 줘 기존 `branding.primaryColor` 색이 그대로 살아난다. 검증 = `scripts/test_partner_theme.cjs` 41단언 + 신규 **`scripts/test_theme_render.cjs` 12단언**(🔴 **prod 접근 0·네트워크 0** — tokens.css 와 정본 lib 을 헤드리스 크롬에 그대로 태우고 `getComputedStyle` 로 **실제 칠해지는 색**을 읽는다. 색 표현식은 `EmployeeApp.js` 에서 **추출**한다·손으로 안 적는다. 실측: 테마 없음 `rgb(234,242,254)` = 예전 그대로 · 채드윅 `#142c52` 도 예전 파생값 그대로 · 카카오 `rgb(255,243,196)` · clear 후 완전 복귀) + `CI=false npm run build` **경고 24↔24(신규 0)**·+373B.
> ⚠ **다음 사람에게 — 화면은 아직 안 바뀐다**: 프리셋을 켠 거래처가 **0곳**이라 배포해도 전 거래처 픽셀 변화 0 이다. 켜기 = `node scripts/set_partner_app_options.cjs <거래처> --theme kakao --apply`(dry-run 기본). **hosting 배포 전에는 값을 심어도 화면에 안 나온다**(옛 번들이 이 필드를 모른다).
> 🔴 **작업 중 발견(별건·미수정)** — 버스 마커·강조 링의 glow 가 **하드코딩 `rgba(0,102,255,…)`** 다: `EmployeeApp` 3곳 · `PassengerApp` 1곳 · `PartnerApp` 5곳 = 9곳. 즉 **거래처 브랜딩(2026-07-16)이 도입된 뒤로도 이 glow 만 늘 기본 파랑**이었다 — 색을 설정한 4개 거래처(채드윅·신촌세브란스·유한양행오창·다우디지털스퀘어)에서 지금도 어긋나 있다. 고치면 **그 4곳의 현재 화면이 바뀌므로** way 승인 필요. 테마와 별개 건.
> **3단계 완료(같은 날·🚧 미배포 · 빌드 `main.07432523.js`·−169B)** — 🔴 **`BoardingApp` 은 "토큰화"가 아니라 리스킨이 필요했다**: 2026-05-16 리디자인 0~7단계에 **이 파일만 통째로 빠져 있었다**(redesign.md 목록에 Login/Driver/Passenger/Admin/Employee/Partner 만 있고 Boarding 은 "미수정·구분"으로만 적혀 있다). 그래서 앱 안 탑승 탭(`/p`)은 흰 화면인데 **고정 QR 로 들어온 같은 사람은 남색 화면**을 봤고, `fontFamily: "'Noto Sans KR'"` 는 이 저장소 어디에서도 로드하지 않으므로(Pretendard 만 self-host) **글꼴까지 달랐다**. 고친 것 = `S` 객체 전면 라이트+토큰화(하드코딩 **40 → 1**, `var()` **1 → 43**. 남은 1개는 primary 버튼 위 `#fff`) · 헤더를 다른 화면과 같은 `BusLinkLogo`(색을 `var(--color-primary)` 로 넘겨 테마를 따라간다) · 그라디언트 로고/버튼 제거 · 결과 상세 상자를 `S.detail`/`detailRow`/`detailKey`/`detailVal` 로 분리. **테마 배선** = 이 화면은 URL 에 거래처가 없으므로(`?c=회사&v=차량`) `passengerLogin`/`passengerResume`/`passengerMigrate` 가 돌려주는 **`passenger.partnerCode`** 로 한 번 조회해 `applyPartnerTheme`(신원이 생기기 전 입력 화면은 기본 테마 — 아직 누구인지 모른다). 🔴 **로직 변경 0**(STEPS·멱등·기억 키 `buslink_board_id`·에러 분기 전부 불변). 검증 = `test_theme_render.cjs` **22단언**(신규 [4] — `S` 를 소스에서 통째로 꺼내 실제 렌더 후 `getComputedStyle`: 카드 흰색 · 바탕 `rgb(247,247,248)` · 버튼 기본 `#0066FF` → **테마 켜면 `#4088FE` 로 따라옴** · 글꼴 Pretendard · 남색 하드코딩 0) + `test_partner_theme.cjs` 41단언 + 빌드 **경고 24↔24** + **실화면 전·후 캡처**(`/board?c=dy001&v=__preview__` · 쓰기 0 · 콘솔오류 0 · prod 남색 ↔ 새 빌드가 승객앱 로그인 화면과 같은 시각 언어).
> ⚠ **다음 사람에게 — 이건 전 거래처가 보는 화면이 바뀐다**. 테마와 달리 "부재=현행"이 아니다(고정 QR 을 쓰는 모든 거래처가 남색 → 흰색). 되돌리려면 직전 빌드는 `main.6260328c.js`. ⚠ 하네스 함정: `'Noto Sans KR'` 를 **단순 포함 검사**로 막으면 "예전엔 Noto 였다"는 주석에 걸려 빨간불이 된다 → `fontFamily:` **선언**만 본다.
> **4단계 완료(같은 날·🚧 미배포 · 빌드 `main.9183a022.js`·+644B)** — AdminApp 협력사 관리 → ⚙️ 포탈 설정에 **테마 프리셋** 신설(`기본`/`카카오 톤` 카드 선택 · 각 카드에 밴드·포인트·버튼 **색 견본 3개**). 🔴 목록은 `Object.keys(THEME_PRESETS)` 로 만든다 — 프리셋을 늘리면 UI 가 따라온다. 미리보기를 **상단 밴드 + 출근 배지 + QR 버튼** 형태로 바꿔(예전엔 '노선 변경' 칩 하나) 실제로 바뀌는 세 자리를 보여준다. 🔴 밴드 글자색은 `readableOn` — 승객앱과 **같은 규칙**이어야 한다. 🔴 프리셋을 켜면 아래 `메인 컬러` 는 흐려지고 미적용 안내가 뜨지만 **값은 지우지 않는다**(끄면 예전 색으로 정확히 복귀 · 로고는 프리셋과 무관하게 유지). 저장은 스크립트와 같은 규칙 = `theme: pTheme ? {preset} : {}`(**필드 삭제 아님**). 검증 = `test_theme_render.cjs` **27단언**(신규 [5] 소스 가드 5종 — 🔴 관리자 콘솔은 Firebase Auth 로그인이 필요해 헤드리스로 못 띄우므로 `test_sidebar_scroll.cjs` 처지와 같다. **화면이 아니라 계약**을 잠갔다) + 빌드 **경고 24↔24**.
> ⚠ **다음 사람에게 — 관리자 모달은 눈으로 못 봤다**(로그인 하네스 부재). **볼 것**: ⓐ ⚙️ 포탈 설정에 프리셋 카드 2장이 뜨는지 ⓑ `카카오 톤` 을 고르면 미리보기 밴드가 곤색·배지가 옅은 노랑으로 바뀌는지 ⓒ 메인 컬러가 흐려지고 안내가 뜨는지 ⓓ 저장 후 다시 열었을 때 선택이 유지되는지 ⓔ `기본` 으로 되돌리면 예전 색으로 정확히 복귀하는지. way 판단 대기 2건: 색만인가 로고까지인가 · 삼성전자만인가 카카오 경유 전체인가(프리셋 vs 자유색은 **프리셋으로 진행**, 자유색은 하위호환으로 남겼다).
> **같은 날 — `scripts/capture_passenger_screens.cjs` 복구(코드 수정 완료·실행 미검증)**: 이 하네스가 **2026-08-25 승객 인증 전환(P1) 이후 조용히 죽어 있었다** — 부팅이 localStorage 의 `resumeToken` 으로 `passengerResume` 를 부르는데 하네스는 옛 형식(empNo/name)만 주입해 **5장이 전부 로그인 화면**이었다(그런데도 "캡처 완료"로 끝났다). 고친 곳 = ⓐ Admin SDK 로 **승계표를 직접 발급**(`companies/{cid}/passengerSessions/{sha256(resumeToken)}` — 서버 `resumeDocId` 와 같은 식) 후 그 토큰 주입, 캡처가 끝나면 **성공·실패 모두 삭제**(`cleanup()`) ⓑ 🔴 **신호 유무 검사 추가** — 하단 탭바가 4개 미만이면 던진다(이게 없어서 헛찍은 걸 아무도 몰랐다) ⓒ 🔴 **승객 선정 규칙** — `pinInitial:true` 인 사람으로 들어가면 앱이 **첫 PIN 설정 화면에 가둬** 탭이 안 뜬다(실측). 여기서 PIN 을 정해 주면 **실제 사람의 비밀번호를 바꾸는 것**이라, 이미 본인 번호로 바꾼 사람(또는 `pinLocked` 공용계정)만 고르고 없으면 캡처를 포기한다. 🔴 `passengerLogin`(사번+PIN)을 안 쓰는 이유 = `lastLoginAt` 을 써서 협력사 포털 "미시작" 집계를 오염시킨다. 레거시 `passengerMigrate`(pinHash)도 안 쓴다 — 2026-09-30 에 서버가 닫으면 도구가 같이 죽는다.
> ⚠ **다음 사람에게 — 미검증**: `node --check` 만 통과했고 **실행은 못 했다**(분류기가 prod Firestore 쓰기+브라우저 실행을 막았다). 다음 세션에서 `node scripts/capture_passenger_screens.cjs <출력폴더> 신촌` 한 번 돌려 ⓐ 탭바 검사 통과 ⓑ 홈 화면이 실제로 찍히는지 ⓒ **승계표가 지워졌는지**(`companies/dy001/passengerSessions` 잔여 0) 확인할 것. 직전 실행에서는 승계표 발급·삭제까지는 정상 동작했다.

> **2026-09-03 탑승 통계 — 정류장 표를 «노선 그룹 + 운행 순서»로(way 요청 · prod `main.b22f00e5.js`·`--only hosting`·done)**: 지적 = "노선별로 정류장이 그룹으로 순서대로 되어 있어야 통계로서 가치가 있지 않나". 옛 표는 `aggregateBoardingsByStop` 이 돌려주는 **탑승수 내림차순 한 줄 나열**이라 28개 노선의 정류장이 뒤섞여, 노선 칸을 눈으로 훑어야 «어느 구간에서 사람이 타는가»가 안 읽혔다. 고친 곳 = `src/lib/stopMapping.js` 신규 `groupMappedByRoute(mapped, stopsByRoute)` **한 곳**(관리자 콘솔·협력사 포털이 같은 함수를 쓴다 — 두 화면이 갈리면 같은 숫자가 다르게 읽힌다) + 양쪽 표를 «노선 머리줄 → 순번 배지 + 정류장» 으로 교체(첫 열 `노선` → `순번`). 🔴 **순번은 탑승이 있는 정류장만 센 게 아니라 노선 전체 stops 기준 1-based** 라 `1,3` 처럼 **건너뛴 정류장이 보인다**(머리줄의 `정류장 5/12곳` 도 같은 목적). 노선 그룹 순서는 합계 내림차순 = 같은 화면 «노선별 탑승» 패널과 같은 기준. 순번을 모르는 정류장(문서 삭제·legacy stopId)은 **버리지 않고 그룹 맨 뒤로**. 집계·매핑 로직(반경 300m·GPS 없음/임계 초과 카운트)은 **불변** — 표시 순서만 바뀐다. 검증 = 신규 `scripts/test_stop_group_order.cjs` **18단언**(일부러 뒤 정류장에 탑승을 몰아 카운트 정렬이면 뒤집히는 배치로 잠갔다) + 게이트 **45/45** + `npm run build` 통과·**신규 경고 0**. 배포 = 5개 도메인 해시 실측 일치(p·d·admin·partner·web.app) · 번들 200 · `appkey=58bf34` 확인. 되돌리려면 직전 번들 **`main.593eab1f.js`**. CF·rules 변경 0.
> ⚠ **다음 사람에게**: 눈으로 볼 것 = ⓐ 관리자 탑승 통계에서 노선 머리줄이 뜨고 그 안이 정류장 순번대로인지 ⓑ 협력사 포털 «오늘 탑승 현황» 도 같은 모양인지 ⓒ 순번 `–` 가 뜨는 정류장이 있으면 그 노선의 정류장 문서가 지워진 것. ⚠ `src/pages/PartnerApp.js` 는 이 저장소에서 드물게 **CRLF** 다 — 스크립트로 일괄치환할 때 
 정규화 후 되돌릴 것(LF 로 저장하면 파일 전체가 diff 로 뜬다).

> **2026-09-03 같은 건 후속 — 노선 머리줄 정렬 수정(way 지적 2건 · prod `main.08b53073.js`·`--only hosting`·done)**: 첫 배포(`main.b22f00e5.js`) 화면에서 머리줄이 **첫 열(52px) 안으로 접혀** 노선명이 세로로 쌓이고 소계가 «탑승» 열 밖으로 나갔다. 🔴 근인 = `<td colSpan={4} style={{ …, display:"flex" }}>` — **td 에 `display:flex` 를 주면 table-cell 을 벗어나 colSpan 이 통째로 무효화된다**(열 정렬도 같이 죽는다). 고친 것 = 머리줄을 **컬럼에 맞춘 4칸 진짜 행**으로 교체(`🛣` / 노선명 / **소계 `{g.total}건` 을 «탑승» 열에** / `정류장 7/14곳` 을 «근접 거리» 열에) + `stopGroupHead`·`pStopGroupHead` 에서 flex 제거(`whiteSpace:nowrap`·`verticalAlign:middle` 로 대체). way 두 번째 요청("탑승인원 노선별 소계도 필요")은 소계를 탑승 열에 세로로 정렬시키는 것으로 함께 해결. 검증 = `test_stop_group_order.cjs` **18 → 28단언**(신규 [5] 소스 가드 — 머리줄 상수에 `display:"flex"` 금지 · `colSpan={4}` 금지 · `{g.total}` 존재. **대조군으로 옛 모양을 넣으면 3개 다 잡히는 것까지 확인**) + 게이트 45/45 + 5개 도메인 해시 일치. 되돌리려면 직전 `main.b22f00e5.js`, 변경 전은 `main.593eab1f.js`.
> ⚠ **다음 사람에게**: 이 표에 머리줄을 더 넣을 때 **`<td>` 에 `display:flex` 를 쓰지 말 것** — 눈으로만 잡히는 종류라 [5] 소스 가드로 잠가 뒀다. 셀 안에서 가로 배치가 필요하면 td 안에 `<div style={{display:"flex"}}>` 를 한 겹 넣는다.
