# Tasks

> 작업 시작/완료 시 이 파일만 수정. 체크박스 관리. 어느 PC든 이어작업용.

> **2026-07-31 세션 B(게시판 1건·hosting 배포)**: 배시현 개선요청 `sPnYpmChrcOqSIzwbQEn` "과거 공지가 앱에 다 보인다 · 관리자가 정리하게". prod 실측 = 공지 110건 중 **active 101건**(최고참 3-22·4개월치), **75건이 "전체" 발송**이라 채드윅 학부모 앱에 5월 테스트 공지가 그대로 노출. 근인 = 관리 화면 발송 이력이 `slice(0,10)` 이라 **95건이 앱엔 보이는데 관리 화면엔 없었다** + 제한 admin 은 "전체" 공지가 목록에서 필터돼 존재조차 몰랐다. way 결정대로 숨기기+영구삭제(2단 확인)·제한 admin 은 전체 공지 **읽기만**·자동 만료 미도입. 목록 상한 제거(더보기)·검색·상태 칩·되돌리기 추가. **`isShown = active === true`** 로 소비측 쿼리와 정합(느슨한 `!==false` 는 유령 건수). 격리 26케이스·신규 경고 0·prod `main.8c460627.js`. rules/indexes/CF 변경 0. **운영 후속**: 101건 실제 정리는 전체권한 계정(김희정·황광연)이 화면에서. 상세 issues.md `[해결]`.

> **2026-07-31 세션(게시판 1건·CF 배포)**: 배시현 개선요청 `BiSp9ujKqmPOGgAYzocX` "서울72바7026 / CARID 4964 오전 관제가 안된다". 등록·busin 연동은 전부 정상이었고(좌표 76건·최신 2분 전), 근인은 폴러가 **한 차량당 배차를 1건만** 남긴 것 — 등교·하교를 같이 뛰는 차량에서 하교 배차가 뽑히면 그 시간창(15:20~17:30)으로만 판정해 **오전 좌표가 통째로 차단**됐다. prod 실측 = device 27대 중 다중 회차 10대, 그중 4대가 오늘 오전 포인트 0~4건(등교가 우연히 뽑힌 3대는 93~134건) — **문서 ID 운**에 좌우되던 상태. `pickActiveDispatch` 신설(현재 시각이 든 시간창의 회차 선택·창 겹치면 출발시각 근접)·`functions:pollDeviceVehicleGps` 배포·prod 실호출 확인(7012 가 `08:20 판교역`→`09:20 판교역` 회차로 전환). 격리 29케이스·진단 도구 `scripts/inspect_device_gps.cjs` 신설. **오후 하교 회차는 15:20 이후 재확인 필요**. 상세 issues.md `[해결]`. ⚠ **별건 보고(게시판 미기재)**: 잔존 mobile gps 문서 2건(서울72바7026·경기79사3539 "테스트노선")이 device 차량에 남아 있다 — `cleanupDeviceGpsDoc` 은 `source==="device"` 만 지우므로 영구 잔존. 관제 "잔존 운행 신호"에서 정리 가능.

> **2026-07-29 세션(도착알림 시간표 폴백 + 길안내 카카오 도로경로 연동)**: ① 🔴 도착 임박 알림 76% 미발송 → 시간표 폴백 CF `notifyPreArrivalBySchedule` 배포(회사 옵트인·GPS 살아있으면 미발송·마커 공유로 중복 0). way 가 dy001 켬. 하루 상한 3통(시뮬레이션). ② way 질문에서 출발해 **카카오모빌리티 길찾기 API 연동** — 앱 임베드는 불가하지만 데이터를 받아 우리가 그리면 앱을 안 떠나므로 GPS 가 안 끊긴다. CF `getNaviRoute` 배포·시크릿 `KAKAO_REST_KEY`(buslink 전용 앱 REST 키). prod 실호출 검증=점간격 29m·회전안내 39개. 프리뷰 **`main.c65f3246.js`**·**prod 미배포**. ③ way 현장 확인 반영 2건 — 경로 밖 투영 결함(16km 밖에서 회전 안내가 떴다) 수정 · **차량 마커 진행방향 화살표**(카카오 지도는 회전 미지원 확인). 격리 104케이스. 상세 issues.md.

> **2026-07-28 세션 B(기사 길안내 신설·프리뷰까지)**: way 요청 "초보 기사를 위한 길안내". 실측으로 **기사앱에만 지도가 없다**는 걸 확인(다른 3개 앱엔 다 있음). `src/lib/navGuide.js`(순수·78케이스) + DriverApp "길안내" 탭. 핵심=경로를 **지나온/가야 할/그 이후**로 잘라 가야 할 구간만 굵게 + 출발 전엔 노선 전체가 한 화면에. **프리뷰 `main.3e9079ca.js`**(https://buslink-prod--navguide-aq5atnnv.web.app) — **prod 미배포·way 검토 중**. 음성(정류장 접근 TTS)은 way 결정만 나오고 **미착수**. ⚠ **프리뷰 채널은 카카오 도메인 미등록이라 지도가 통째로 안 뜬다** — way 가 콘솔에 이 채널 주소를 등록해 해결(다음부터는 고정 채널명 재사용 권장). 부수 발견=**도착 임박 알림 76% 미발송**(별건·issues.md `[미해결]`). 상세 issues.md.

> **2026-07-28 세션(강제 운행 종료 신설·게시판 1건)**: 배시현 개선요청 `cv4XzFYLUdUfzqBEuDQw` "기기 로그아웃 후에도 계속 운행 중 — 강제 로그아웃 기능". prod 실측으로 **신고보다 넓은 문제** 확인: gps 문서 10건 중 4건 잔존(309~1102시간)·신고 대상은 **배정 기사가 없는 고아 신호**(로그아웃으로는 영원히 못 지움)·반대로 `status="운행중"` 만 18일째 남은 기사도 1명. mobile gps 문서는 청소 주체가 "기사앱 운행 종료 버튼" 하나뿐인 게 근본. 신규 `src/lib/runSignals.js`(순수 판정)·`src/lib/forceEndRun.js`(쓰기) + AdminApp 실시간 관제 좌측 레일에 "🧹 잔존 운행 신호" 섹션·강제 종료 버튼(인라인 확인·경과시간 노출) + DriverApp 로그아웃 경합 수정(fire-and-forget 쓰기가 signOut 으로 유실되던 것). 격리 34케이스·헤드리스 4화면·신규 경고 0. rules/indexes/CF 변경 0. prod `main.9026b68f.js`(`--only hosting`·curl 확인)·게시판 done + **사용자가이드 댓글**(2026-07-28 way "추후도 반영" → 완료 처리에 가이드 댓글 상시 첨부로 승격·`/board` 스킬 반영). 잔존 데이터도 정리 완료(신호 4건·기사 1명·재검증 0). 상세 issues.md `[해결]`. ⚠ **분류기 함정**: 허용 규칙이 `Bash(firebase deploy:*)` 라 `npx --no-install firebase deploy …` 로 부르면 접두사가 안 맞아 차단된다 — **`firebase` 를 그대로** 호출할 것(cwd 를 프로젝트로 먼저 옮기고 `cd … && firebase …` 결합도 피할 것).

> **2026-07-27 세션(승객 계정 발급·배부 3종·prod 배포·게시판 done)**: 게시판 요청(배시현 `eWh6y3K5NDqNwzkdlxhJ`) "승객 자가 가입" → way·요청자 협의로 **자가가입 미채택**(고정 명부 239~250명·담당자 셋팅이 효율적) 확정 후, 진짜 병목인 **계정 배부**를 자동화. ① 초기 PIN 개인별 발급(prod 실측 = 승객 12명 전원 `000000` 미변경 → 사번만 알면 남의 계정 진입) + 첫 로그인 시 본인 PIN 설정 강제 ② 계정 안내문 A4 인쇄(`src/lib/accountCards.js` 신설·QR=`/p?emp=` 프리필) ③ `passengers.lastLoginAt` + 협력사 포털 "미시작" 집계·배지·일괄 재발급＋인쇄. **구현 중 잡은 버그**: 미시작 판정을 `!lastLoginAt` 단독으로 두면 레거시 사용자(필드 부재·본인 PIN 사용 중) 비밀번호를 날림 → `&& pinInitial` AND 필수. prod `main.2b347671.js`(`--only hosting`)·rules/indexes/CF 변경 0·신규 경고 0. 검증=격리 38케이스+헤드리스 5화면+인쇄물 렌더+포털 실데이터 캡처. 상세 issues.md `[패턴]`.

> **2026-07-27 세션 C(관리자 가시범위 점검·코드 변경 0)**: 김희정 점검요청 "관리자 화면에 제가 test로 입력한 것만 보인다". 코드 결함 아님 — 그 계정 `allowedPartnerCodes:[]` 라 본인 생성 거래처만 노출(createdBy 격리 설계대로). dy001 admin 4명 전수 실측 → **황광연 계정은 0/58 로 화면이 통째로 비어 있었음**(미신고). 김희정·황광연 `["*"]` 부여·재실측 58/58 확인. 신규 진단 스크립트 2종(`inspect_admin_visibility.cjs` 읽기전용 · `set_admin_partner_access.cjs` dry-run 기본). **배포 없음**(users 문서 데이터 변경만). **사용자 안내 필요**: 권한은 `getDoc` 1회라 **새로고침 후** 반영 · 김희정 계정 비밀번호가 `000000` 이라 전체권한 부여와 함께 변경 권고. 상세 issues.md `[패턴]`.

> **2026-07-27 세션 B(게시판 2건·전부 prod 배포)**: ① 🔴 **배차 일정 하루 밀림**(배시현 `CmxQPRtRhUAnDq4kfVna`) — 서버(UTC)에서 `getDay()` 를 써 요일이 하루 전으로 계산됨. prod 실측 = 토요일에 평일 배차 29건 / **월요일 배차 0건**(신고 당일 오후에 그날 배차가 통째로 없었다). `dayOfWeekKST` 를 `getUTCDay()` 로 교정·CF 2종 배포 + `scripts/fix_schedule_weekday_shift.cjs` 로 데이터 정리(way `--apply` 실행: 삭제 36/생성 36, 재검증 0건). ② **노선 탭 "실시간 지도" 경로 직선**(배시현 `qYol841t9rt9yOOA9VUI`) — 데이터는 정상(50개 노선 routePath 보유·전부 number), 그 화면만 정류장을 곧장 잇고 있었음. `routeProgress.toLatLngPath` 신설 + 모달이 routePath 우선. prod `main.c87a40a9.js`. 상세 issues.md `[해결]` 2건.

> **2026-07-22 세션 B(NFC 사원증 탑승 신설·prod 배포)**: way 요청 — 레거시 버스인3 NFC 태깅(`file/20260722/*.pptx` 20슬라이드)과 `swsfirebase/swstagsys`(Web NFC 선례)를 참고해 buslink 에 이식. 탑승 모드 4번째. 신규 `src/lib/nfc.js`(순수·격리 테스트 36케이스) + CF `boardNfc` + DriverApp NFC 탭 + PartnerApp 카드번호 등록 + AdminApp "부정승차" 탭. prod `main.3d678744.js` + `firestore:rules` + `functions:boardNfc`(카나리아 401=우리 핸들러 확인). 신규 경고 0(35↔35). 이어서 **카드 등록 대행**(way 결정=기존 사원증 재사용 → UID 목록 없음 → 기사앱 대행): CF `registerNfcCard` + NFC 탭 모드 토글 + 미등록 결과에서 등록으로 잇는 버튼. prod `main.ac32f83b.js`. 헤드리스 4화면 콘솔오류 0. **실기기 검증 대기** — Web NFC 는 헤드리스 불가. 상세 issues.md `[패턴]`.

> **2026-07-22 세션 A(배차 복사 실행상태 복제 근절·prod 배포)**: 승인된 `clean_stale_arrivals.cjs --apply` 를 실행하려다 dry-run 수치 증가(69/122→120/215)와 **미래 날짜 배차의 도착기록**을 보고 발생원 재수색 → `handleCopyDispatches` 의 `{...data}` 스프레드가 `stopArrivals`·`preArrivalNotified` 까지 복제하던 것 확정(prod 필드 분포 실측). 명시 필드 화이트리스트로 교체·prod `main.3a183a76.js` 배포. 청소 스크립트는 미래일 `preArrivalNotified` 까지 처리하도록 확장. way 가 직접 `--apply` 실행 → **120배차 정리·재검증 0건/0건**. 상세 issues.md `[해결]`.

## 현재 상태 (2026-07-13 세션 종료)
> 이 저장소가 작업 정본. **어느 PC든** `git pull`(origin/master) + `.env.local`(↓부트스트랩, 카카오 키 `58bf34`)이면 빌드·배포 가능.
> prod 라이브 = **`main.4ac4d558.js`**(2026-07-13 curl 실측) + CF 27종. 기사앱 빌드마커 `v2026-06-09-livegps`. 옛 세션 로그는 @.claude/tasks-log.md.
> **2026-07-13 세션(대량·전부 배포·커밋 완료)**: ① 🔴 **정적 QR 권한오류** — 익명 진입점(직원앱 스캐너·폰카메라)이 admin/driver 잠금 배차를 쿼리→거부. CF 위임 `resolveStaticBoarding`/`boardStatic`(Admin SDK·rules 무변경). ② **device GPS 운행 시간창** — 상시신호 단말을 노선 `departTime`+정류장 `offsetMin` 창(`computeGpsWindow`, dep−30~+offset+60분·미설정=게이트없음)으로 폴러가 게이트. ③ **깜빡 마커 위로**(요약바·노선도 스트립 −4px). ④ **개선 요청 게시판**(YDYOPS 이식) — AdminApp "개선 요청" 탭(인덱스 12·companyId 스코프)·`improvement_requests` 컬렉션·상태전이(superadmin)·댓글·**구글챗 알림**(CF 2종·웹훅=Functions 시크릿 `GCHAT_WEBHOOK_URL` 우선→`config/improvementBoard` 폴백·카드 "🚌 BusLink" 구분)·**본문 인라인 이미지**(리치 텍스트·data URI·DOMPurify sanitize 2회+svg차단 훅·총량 800KB 캡). rules `improvement_requests`+`config` 추가·인덱스 1개. 상세 issues.md/backend.md/functions.md/architecture.md. **커밋 62f3886~856b558 · 별도 웹훅 3e081f0.**
> ⚠ **배포 검증 함정(이번 세션 실측)**: 경고 대조용 `git stash` → `npm run build`(HEAD) 하면 **`build/` 가 옛 번들로 덮인다**(build/ 는 gitignore 라 `stash pop` 이 안 되돌림). 그 상태로 배포하면 "Deploy complete" 인데 **prod 번들 해시 불변**. 배포 후 `curl | grep main.*.js` 로 **해시가 실제로 바뀌었는지** 반드시 확인(첫 배포가 no-op 이라 회귀는 없었음).
### git 원격
- `origin/master` = 작업 정본. 3월 폐갈래는 `origin/archive/remote-march-2026`(`f63321c`) 영구 백업.

### 새 PC 부트스트랩 (이어작업 1회)
- `git pull` → `npm install` (functions 쓰면 `cd functions && npm install`).
- `.env.local` 생성: `.env.example` 참고, 값은 사용자 보유분. ⚠ `REACT_APP_KAKAO_MAP_KEY`=운영 공유키 `58bf34…`(`d464b4…`면 prod 지도 사망).

### 배포 절차
1. 앵커 grep: `grep -nE '^REACT_APP_KAKAO_MAP_KEY=' .env.local` → `58bf34` 시작 확인.
2. `CI=false npm run build` → `npm start` → localhost `/p`·`/bus` 지도·노선 그리기 확인.
3. `firebase deploy --only hosting` (rules 변경 시 `--only firestore:rules,hosting`) → curl 검증.
4. 회귀 시 가설-재배포 금지 → 콘솔 Hosting 롤백 먼저, 재현은 localhost.

## 다음 할 일
- [x] **과거 공지 100건 일괄 숨김 완료(2026-07-31, way `--apply` 실행)** — 앱 표시 중 **101건 → 1건**(오늘자 채드윅 공지만 유지). 재실측으로 숨김 대상 0건 확인. 되돌리기 = `node scripts/hide_old_notices.cjs --restore hidden_notices_2026-07-31.json --apply`(ID 목록 커밋됨). 숨김 내역=3월 25건·5월 68건·6월 2건·7월 5건, 전부 테스트·지난 당일공지. **문서는 남아 있다**(삭제 아님) — 관리자 콘솔 공지 발송 이력의 `숨김` 칩에서 열람·되돌리기 가능.
- [ ] **🛰 단말 GPS 다중 회차 — 오후 하교 실측(2026-07-31)** — 오전 회차 수정은 prod 실호출로 확인했으나(7012 가 `09:20 판교역` 회차로 전환), **하교(15:50) 시간대는 아직 실측 못 했다**(그 시각 이전에 세션 종료). 15:20 이후 `node scripts/inspect_device_gps.cjs` 로 다중 회차 차량(5377·7022·7029·6859·3305·3539·7026)이 하교 창에서 잡히는지 확인. 이전에는 등교가 뽑힌 3대가 오후에 죽던 구조였다.
- [ ] **(운영 판단) 잔존 mobile gps 문서 2건(2026-07-31)** — 서울72바7026·경기79사3539 에 `routeName="테스트노선"` mobile 신호가 남아 있다(각 14분·28분 경과 시점 관측). **device 차량인데 mobile 문서**라 `cleanupDeviceGpsDoc`(`source==="device"` 만 삭제) 이 영원히 못 지운다. 관제 → 🧹 잔존 운행 신호에서 정리하거나 `node scripts/clear_stale_run_signals.cjs --apply`. ⚠ **`cleanupDeviceGpsDoc` 의 source 조건을 풀어서 때우지 말 것**(2026-07-16 회귀 가드 — mobile 차량 오삭제). device 차량에 mobile 문서가 왜 생겼는지(기사가 테스트노선으로 운행 시작?)는 미규명.
- [ ] **🪑 좌석예약 승객앱 화면(2026-07-30, 스위치만 배포됨)** — `partnerCodes.seatReservation` 3단 토글은 prod 에 있고 **아직 켠 거래처 0곳**. 고객사가 `optional`/`required` 를 고르면 그때 승객앱 예약 화면 + 예약 데이터 모델 착수. **required 는 탑승 거부 아님** — 기사 화면 "미예약" 표시 + "태우기"(예외 승차 기록)로 설계 확정(way).
- [ ] **🌡 승객→기사 실시간 요청(더워요/추워요) — way 검토 대기** — 부품은 이미 있어 값싸게 됨. 단 3가지가 필수: ① **집계로 표시**(`최근 10분 · 더워요 4 / 추워요 1` — 개별 메시지는 기사가 판단 못 함) ② **쿨다운**(1인 5분 1회·연타 방지) ③ **기사 확인 응답**(승객 화면에 "확인했습니다" — 없으면 불만이 커짐). 버튼 고정 3~4개(자유 입력=민원 창구)·**정차 요청 제외**(안전)·소리·진동 없이 화면 배지만(운전 중 주의 분산). **기사분들 반응 확인 필요**(새 부담을 주는 기능).
- [ ] **🧭 길안내 — prod 반영됨·현장 테스트 대기(2026-07-30)** — 프리뷰 `main.c65f3246.js` 에 **카카오 도로 경로 + 회전 안내**까지 들어감. 남은 것: ⓐ **음성(TTS)** — 회전 안내와 정류장 접근을 읽어주기(실기기 확인 필요·헤드리스 불가) ⓑ **경로 이탈 시 재요청**(지금은 진입 시 1회만 호출) ⓒ 외부 내비 버튼을 운행 중 잠글지 결정(도로 안내가 앱 안에 생겼으므로 필요성이 줄었다) ⓓ prod 배포. **way 결정 사항(기록)**: 카카오내비 전환은 **보류** — 경유지가 **최대 3개**뿐이라 정류장 8~15곳 노선을 한 번에 못 태운다(목적지1+경유지3=4곳). 그리고 **차량 31대 중 27대가 이미 GPS 단말**이라 외부 앱 전환의 GPS 끊김 위험은 폰 GPS 4대에만 해당한다(내가 비중을 과대 경고했음). heading-up 지도 회전은 카카오 로고·저작권 표시 약관 확인이 선행 필요. **현장에서 볼 것**: 회전 안내가 실제 도로와 맞는지 · 안내 시점(거리)이 적절한지 · 지도만으로 따라갈 수 있는지.
- [x] ~~길안내 마무리(2026-07-28 항목)~~ — 아래로 통합 — 프리뷰 `main.3e9079ca.js` 확인 후: ⓐ 정류장 접근 **음성 안내(TTS)** 얹기(500m 전 "잠시 후 ○○입니다"·통과 직후 다음 정류장·정류장별 1회·끄기 가능·음성 없는 기기 자동 폴백. **실기기 확인 필요** — 헤드리스 불가) ⓑ 외부 내비 버튼을 **운행 중 잠글지** 결정(잠그면 이유도 함께 표시) ⓒ prod 배포·게시판 없음(way 직접 요청 건). **미결 질문**: 차고지→첫 정류장 구간을 노선 경로에 포함할지(포함 시 진행률·도착예측 계산에 섞이므로 부작용 확인 필요).
- [ ] **🔴 (way 실행) 도착 임박 시간표 폴백 켜기(2026-07-29)** — CF `notifyPreArrivalBySchedule` **배포 완료·기본 꺼짐**. 켜기 = `node scripts/set_prearrival_fallback.cjs dy001 on --apply`(**분류기가 막아 Claude 가 실행 못 함** — 실제 승객에게 푸시가 나가는 스위치라 우회 안 함). 발송량 사전 확인 = `node scripts/simulate_prearrival_schedule.cjs`(오늘 기준 상한 3통). lead 조정 = `--lead 7`. 켠 뒤 Cloud Functions 로그에서 `[도착임박:시간표]` 확인. 상세·회귀 가드 issues.md `[부분해결]`.
- [ ] **(후속) 시간표 폴백 관리자 토글 UI** — 지금은 스크립트로만 켜고 끈다. AdminApp 회사 설정에 토글+lead 슬라이더 추가 필요(도착 반경 슬라이더 선례). **hosting 배포가 필요해 길안내와 함께 나가야 함**.
- [ ] **⭐ '내 정류장' 지정률이 진짜 병목** — 트리거를 100% 고쳐도 **도달 인원이 5명으로 묶인다**(푸시 토큰 8개 중 '내 정류장' 지정 5명). 명부 239~250명 업로드 시 승객앱에서 내 정류장 지정을 유도하는 경로(안내문·첫 로그인 흐름·홈 배너)를 같이 설계할 것. 이게 알림 체감의 상한선.
- [ ] (선택) **정류장 사진 채우기** — 206개 중 51개(25%)만 등록. 초보 기사 지원의 핵심 자산인데 **코드 작업 0**(관리자 화면에서 등록). 길안내가 이 사진을 "여기서 세우세요"로 크게 띄운다.
- [ ] (선택) **경로 미설정 실노선 4개** — `[등교] 강남1`·`[출근]잠실노선`·`[퇴근]잠실노선`·`등교(강남1)_월~수,금`. 안 그리면 길안내·승객앱이 정류장 직선으로 폴백(관리자 → 노선 관리 → 🛣 경로 그리기).
- [x] **🧹 잔존 신호 정리 완료(2026-07-28)** — `clear_stale_run_signals.cjs --apply` 실행: 신호 삭제 4건(조이형TEST 고아 12.9일·최우석 테스트노선 20.8일·박성환 등교TEST 32.1일·배시현(TEST) 테스트노선 46.0일) + 박상제 status 되돌림 1명. **재검증 0건/0명**(stale 0 · 운행중 오표시 0). 이후 이 스크립트는 위임 목록에 추가돼 Claude 가 직접 실행 가능(설정저장소 `cc719ff`). 재점검 = `node scripts/inspect_driver_status.cjs --gps`.
- [ ] **(운영) 계정 배부 실사용 확인(2026-07-27)** — 배시현님이 239~250명 명부를 올릴 때: ⓐ 엑셀 `초기PIN` 컬럼은 비워두는 게 권장(승객마다 다른 값 자동 발급) ⓑ **등록 완료 화면에서 안내문을 바로 인쇄**해야 함(평문 비밀번호는 저장 안 되므로 화면을 벗어나면 재발급 필요) ⓒ 나중에 다시 뽑을 땐 "승객 관리 → 📄 미시작 승객 계정 안내문 만들기". 인쇄물 = A4 2열·8명/장·점선 절취.
- [ ] **(관찰) 기존 승객 12명 첫 로그인 시 PIN 설정 화면** — 전원 `pinInitial:true` 라 배포 후 다음 접속 때 본인 비밀번호 설정 화면을 만난다(의도). 공용 계정(`pinLocked`)은 제외됨. 문의 들어오면 정상 동작으로 안내.
- [x] **배차 복제 실행상태 정리 완료(2026-07-22, way 직접 `--apply`)**: 근본 원인(배차 복사 스프레드) 수정·배포(`main.3a183a76.js`) 후 데이터 청소 실행 — **정리 배차 120건**(잔존 도착기록 215건 + 미래일 푸시마커 22건 제거). 재-dry-run 검증 = 630배차 전수 **0건/0건**. 도착시각 기록·도착 임박 푸시 정상 경로 복구.
- [ ] **🔴 NFC 실기기 검증(2026-07-22)** — Web NFC 는 헤드리스 테스트가 불가하다. **안드로이드 폰 + 실제 사원증 카드**로:
  - [ ] 기사앱 `/driver` 운행 시작 → **NFC 탭 노출**(탭은 항상 보인다. 크롬 아닌 브라우저면 탭 안에 "크롬으로 열어주세요" 안내가 뜨는 게 정상)
  - [ ] **카드 등록**: NFC 탭 → "카드 등록" → "카드 읽기 시작"(권한 프롬프트) → 승객 이름 선택 → 그 사람 카드 태그 → "○○님 카드가 등록되었습니다"
  - [ ] **탑승**: "탑승 처리" 모드 → 같은 카드 태그 → 초록 "승차 완료" + 카운트 증가
  - [ ] **중복**: 같은 카드 재태깅 → "이미 탑승 처리됨" (카운트 불변·중복 적재 0)
  - [ ] **미등록**: 등록 안 한 카드 태그 → 빨강 "미등록 카드" + UID 표시 → "이 카드 등록하기" 버튼으로 바로 등록되는지
  - [ ] **카드 뺏기 차단**: 이미 A에게 등록된 카드를 B로 등록 시도 → "이미 ○○에게 등록되어 있습니다" 거부
  - [ ] 관리자 "부정승차" 탭에 미등록 태깅이 남고 UID 복사가 되는지
  - [ ] **협력사 포털 일괄 등록**(안드로이드 크롬으로 `partner.buslink.co.kr` 접속): 등록 탭 → "📇 카드 등록" → "카드 읽기 시작" → 이름 선택 → 태그 → **자동으로 다음 사람 대기**로 넘어가는지 + 진행률(N/M) 증가
  - [ ] **엑셀 컬럼**: 양식 다운로드에 `NFC카드번호(선택)` 있는지 → 값 넣고 업로드 시 등록되는지 → **그 컬럼을 비운 채 재업로드해도 기존 카드가 유지되는지**(대량 업로드 삭제 방지 가드)
- [ ] **(운영 판단) NFC 도입 범위 결정** — 현재 등록된 카드 0장이라 기능은 켜져 있어도 아무 동작 없음(회귀 0). ⓐ 어떤 거래처부터 쓸지 ⓑ 카드 등록을 누가 돌릴지(기사 대행 vs 관리자 포털). **기사 단말 = 안드로이드 전용 확정(2026-07-22 way)** — 기사에게 배포/안내할 때 **크롬으로 열도록** 함께 안내할 것(삼성 인터넷 등은 Web NFC 미지원 가능·탭 안에서도 안내하지만 사전 공지가 확실).
- [x] **엑셀 NFC 컬럼 + 협력사 포털 카드 일괄 등록 (2026-07-22, prod `main.80732519.js`)**: way 결정 — 아이폰 사용자 비중이 높아 **승객 자가등록은 만들지 않음**. 담당자가 안드로이드 1대로 줄 세워 일괄 등록(협력사 포털 등록 탭 "📇 카드 등록"). 엑셀은 `NFC카드번호(선택)` 컬럼 추가(빈 셀=기존 등록 보존). CF `registerNfcCard` 가 협력사 익명 인증 경로 지원(업체코드+승객 소속 검증).
- [ ] **(운영 판단) 중복 배차 확인(2026-07-22)**: 오염 구간 07-13~07-24 는 하루 배차 35건, 깨끗한 날(07-17/18/25/28)은 13~15건 — 복사가 배차 행 자체를 늘린 정황. 완전중복(routeId+departTime+vehicleId 동일)은 하루 3건뿐이라 나머지는 코드로 판별 불가. 배차관리 화면에서 07-23·07-24 보고 불필요분 삭제 여부 판단 필요.
- [ ] **RQ/20260716 추가개선사항미팅 6건** (정본=`meetings/2026-07-16_추가개선사항미팅.md`·원 전사 `RQ/20260716/meeting.txt`):
  - [x] **#1 🔴 QR 오탑승 차단 + 선택 노선 매핑 (2026-07-16 prod 배포 `main.ab0d10a9.js` + CF `resolveStaticBoarding`/`boardStatic`·커밋 `2de00df`)**: `resolveStaticDispatchAdmin` selectedRouteId 매칭(불일치=차단·다중 회차=현재시각 근접)·직원앱 스캐너 session.routeId 전달·BoardingApp 무변경. 격리 7 assertion·신규 경고 0. **사용자 실기기 검증 대기**(카메라 스캔 자동화 불가): 7:10 선택→7:50 차량 태깅 시 "선택한 노선의 차량이 아닙니다" 확인. 상세 issues.md `[패턴]`.
  - [x] **#2 운행 중 표시 잔존 해소 (2026-07-16 prod 배포 CF `pollDeviceVehicleGps`·커밋 `f462f2e`)**: 폴러가 창 밖/미배차 시 device gps 문서 삭제(`cleanupDeviceGpsDoc`·mobile 보호) + POST 창 60→30(회의 결정 30/30). 격리 10 assertion. **운영 확인**: 내일 셔틀 종료 30분 후 노선탭 "운행중" 꺼지는지. 상세 issues.md `[패턴]`.
  - [x] **#3 협력사 포탈 노선도 + 노출 옵션 (2026-07-16 prod `main.b37d3f9c.js`·커밋 `5d15ed6`)**: 섹션 B2 정류장 스트립(통과 ✓·버스 위치) + `partnerCodes.opsControlEnabled`(부재=true) 게이팅 + AdminApp "⚙️ 포탈 설정" 토글. 상세 issues.md `[패턴]`.
  - [ ] **#4 그룹(관리회사) 설정 구조** — **설계안 초안 완료**: `.claude/group-design-draft.md`(partnerGroups 컬렉션+routes.groupId+가시성 4지점·G1~G3 단계·결정 3건). **way 검토·확정 후 구현**.
  - [x] **#5 거래처별 UI 커스터마이징 (2026-07-16 prod `main.b37d3f9c.js`·커밋 `5d15ed6`)**: `partnerCodes.branding{primaryColor,logo,logoHeight}` — 신규 `lib/partnerBranding.js`(CSS 변수 3종 덮어쓰기·mixHex 파생), 승객앱 헤더 로고+테마·포탈 테마, AdminApp 포탈 설정 모달(컬러 픽커·로고 200KB·크기 슬라이더·미리보기). 로고 위치 조정은 v2 후보(요청 시). 상세 issues.md `[패턴]`.
  - [x] **#6 협력사 포탈·승객앱 요약 매뉴얼 — 스크린샷 중심 v2 (2026-07-16 커밋 `06ec2c7`)**: 협력사포탈가이드.pdf **4쪽**(실캡처 9장 — DB하이텍 코드로 사용자 `!` 캡처·PII 비파괴 마스킹·카드 크롭) + 승객앱가이드.pdf 3쪽(기존 마스킹 자산). 캡처 하네스 `docs/manual/_serve_build.mjs`·`_capture_partner.mjs`(PARTNER_CODE env)·`_capture_passenger.mjs` + `scripts/manual-capture-ids.cjs`(ID 조회). **운행 시간대 노선도(🚌 표시) 재캡처는 아침에 같은 명령 1회면 됨**. 산출 PDF는 `docs/manual/out/`(고객 전달용).
- [ ] **RQ/20260708 승객앱 개편 묶음 (이미지 참조 파일 대기 중)**: 회의(`RQ/20260708/`) 요청 — ① 거래처 우선 진입 흐름(업체코드 대신 거래처 선택) ② 승객앱 거래처 데이터 격리(노선/하단메뉴/공지 전부 자기 거래처만) ③ 홈 직관화(진입 즉시 도착정보) ④ 홈 스크롤 안 내려가는 버그 ⑤ 지하철식 진척 시각화 ⑥ **거리뷰 연동(=아래 프로토타입 완료·정식반영 대기)** ⑦ 앱명 "승객" 노출 확인 + 기사앱 유비칸 GPS/고정QR/세션분리 등. **이미지 참조 오면** 정식 UI에 얹어 일괄 진행.
  - [x] **거리뷰(로드뷰) 적용 가능 검증 + 프로토타입 (2026-07-08 커밋, 미배포)**: 카카오 JS SDK 로드뷰=코어(libraries 추가 0·무료)·`react-kakao-maps-sdk@1.2.1` `Roadview` export 확인. EmployeeApp(`/p`) 정류장 정보 카드(`stopInfo`)에 좌표 기반 로드뷰 패널 추가 — 반경 60m 파노라마 있으면 실사, 없으면 `onErrorGetNearestPanoId`→기존 사진 폴백(`rvOk` state·정류장 바뀔 때 리셋). localhost 실사 확인 완료. **정식반영 시**: 승객앱(`/bus`)·관리자 정류장 편집 미리보기 등으로 확장. 좌표 혼재(string/GeoPoint) 정식 대응은 issues.md `toLatLng` 헬퍼 재사용(프로토타입은 Number+isFinite 가드).
  - [ ] **승객앱 UI 개선 (RQ/20260708 PDF 목업 4건) — Phase 1·2 배포완료·Phase 3 대기**: 참조 `RQ/20260708/BusLink_UI_개선_제안.pdf`. EmployeeApp(`/p`) 단독·rules/CF/indexes 변경 0.
    - [x] **Phase 1 (2026-07-09 배포 `3d81e3e`·prod `main.8d5e1efe.js`) — #3 하단 스크롤 + #2 거리뷰 정식화**: **#3** HomeTab flex-column에서 지도(`0 0 55%`) 아래 노선도 스트립+ETA/QR 패널 두 `flexShrink:0` 블록이 45% 초과 시 tabBar 뒤 잘림 → 두 블록을 `flex:1;minHeight:0;overflowY:auto` 단일 컨테이너로 감쌈(fixed 오버레이 routePicker/stopInfo/proximity는 컨테이너 밖 형제). **#2** 노선 탭 stopModal 보기토글 `modalMapView`(bool)→`modalView`(list|map|rv) enum + `🛣 거리뷰` 3번째. rv=아코디언(`rvOpenStopId`), `<Roadview radius:60>`+`onErrorGetNearestPanoId`→"거리뷰 없음"+사진 폴백(`rvErrStopId`)+"카카오맵에서 열기". 실시간 지도 유지·프로토타입 stopInfo rvOk 별개 존치.
    - [x] **Phase 2 (2026-07-09, prod `main.896d4164.js`) — #1 거래처 데이터 격리**: 회의 결정 "자기 거래처만 보이게(드롭다운 불필요)". **근인**=RoutesTab(노선 탭)이 전체 노선 로드+`filtered`에 거래처 필터 없어 전 거래처 노출. **수정**=`filtered`에 `myPartner=session.partnerCode` 게이트(미설정 직원=전체 하위호환·2026-06-17 모달필터와 동일 규칙) + "선택된 거래처:{partnerName}" 안내 배너 + 노선 카드 거래처 배지. 공지(NoticesTab)는 이미 partnerCode 격리됨(289-313)·홈 노선칩은 배정+즐겨찾기라 미변경. **데이터 의존**: 직원 passengers.partnerCode + 노선 partnerCode 설정돼야 필터 동작(모달필터와 동일·이미 검증). 미설정이면 전체 노출.
    - [x] **Phase 3 (2026-07-09, prod `main.9a3f9da4.js`) — #4 지하철식 진척 시각화 요약바**: HomeTab #3 스크롤 컨테이너 첫 자식으로 4노드 가로 진척바 신규 추가(순수 추가·기존 노선도 스트립/ETA 패널 100% 보존). 출발(`stops[0]`)·현재(`stops[busStopIdx]`)·다음(`stops[busStopIdx+1]`)·도착(`stops[last]`), 같은 인덱스 역할 병합. 통과~현재 파랑·남음 회색 연결선, 현재 노드 `buspulse` ring. 상태줄 "🚌 다음역까지 N분"(estByStopId ETA·HH:MM→분 파싱) + "차량 위치 보기"(userCenteredRef+setCenter). 무운행/운행중/종점 3케이스 throw-free. 신규 state 0(파생값 재사용). **참고**: 기존 전체 노선도 스트립(전 정류장·버스아이콘)은 상세용으로 아래 유지·요약바는 위에 추가(공존). 원하면 스트립 제거 가능.
  - **잔여(회의 언급·PDF 4건 외)**: 초기 진입 흐름 "거래처 우선 선택/로그인 방식(전화 vs 사번+PIN)" — **사용자 확인됨·패스**(2026-07-09). 앱명 "승객" 노출 확인도 패스.
- [x] **웹 로그인 세션 충돌(튕김) 수정 — 익명 앱 auth 라우트별 지속성 분리 (2026-07-09, prod `main.85706599.js`)**: 같은 origin(web.app/localhost)에서 관리자(이메일 로그인·IndexedDB LOCAL) 탭 + 승객/직원/협력사/탑승(익명) 탭 동시 열면 익명 signInAnonymously가 **공유 IndexedDB auth 상태를 덮어써 관리자 튕김**(cross-tab clobber). **수정**: `firebase.js`에서 App.js 라우트판정 미러 → 익명 앱 라우트면 `initializeAuth(inMemoryPersistence)`(공유 IndexedDB 미접근·익명은 매진입 재로그인이라 무손실), 관리자/기사는 `indexedDBLocalPersistence`(현행 유지). firebase.js 한 파일만·앱/lib 무변경. 사용자 로컬 검증(관리자↔직원 유지) 후 배포. **한계**: 관리자↔기사(둘 다 이메일 로그인)는 Firebase "origin당 세션 1개" 구조상 같은 origin 동시유지 불가 — **prod는 서브도메인(admin. vs d.)이라 origin 분리로 원래 정상**·localhost 동시테스트는 시크릿창/다른 브라우저. rules/CF/indexes 변경 0.
- [x] **유비칸 후속 #3-A 정적(고정) QR 생성/인쇄 (2026-07-09, prod `main.fcf7a336.js`) — RQ/20260708 #3**: 유비칸 차량은 기사가 앱 미사용→매일 바뀌는 동적 QR 불가 → 관리자 고정 QR 인쇄. **토큰 없는 정적 QR** = `/board?c={cid}&v={vehicleId}`(동적 `?t=`와 구분·만료/소각 없음·재사용). ① `lib/boarding.js` `getStaticBoardingUrl`+`validateAndBoardStatic`(오늘 배차 쿼리로 routeId 해석·배차없으면 거부·partnerCode/GPS 캡처·**멱등** doc id `${empNo}__${vehicleId}` getDoc→exists 첫스캔 보존·boarding 스키마+`via:"static"`) ② `BoardingApp` isStatic 분기(동적 100% 보존·"고정 QR 탑승" 라벨·alreadyBoarded 안내) ③ AdminApp VehicleTab 행별 "🖨 QR" 버튼→모달(QRCode.toDataURL)+새창 인쇄. **한계**: 재사용·보안완화(회의 수용)·오늘 배차 필요. rules/indexes/CF 변경 0(boardings create=isAuth·dispatches where vehicleId 자동 단일인덱스).
  - [x] **유비칸 후속 #3-B device 차량 서버측 도착감지 (2026-07-09, 배포 CF `pollDeviceVehicleGps`)**: 단말 차량은 기사앱 없어 stopArrivals 미기록 → 폴러에 정류장 반경 도착감지 추가. 헬퍼 `distMeters`(haversine)·`stopLatLng`(좌표 4분기 coercion)·`STOP_ARRIVE_M=100`·`planTimeFromDepart`·`loadRouteMeta`(routes departTime+stops orderBy order 캐시). 배차맵에 dispatchId+stopArrivals 캡처 → 최신 좌표 100m 내 미기록 정류장 = 신규 도착 → **dot-notation `stopArrivals.{stopId}`**(recordStopArrival 정본 구조 + `via:"device"`·actualAt serverTimestamp·plannedAt(offsetMin+departTime KST)·delaySec·estimated:false) batch update. **멱등**(기존 stopArrivals 있으면 skip). `notifyPreArrival`(stopArrivals 트리거)이 device 도착임박 푸시 자동 발화. 기존 gps write/신선도/gpsHistory byte 불변·rules/indexes 변경 0. **한계 v1**: 반경 감지(최신 1점·1분 폴링)라 빠른 통과 놓칠 수 있음(버스 체류로 대부분 포착)·actualAt=폴 시점(±1분). 향후 전체트랙/routePath 백필 보강 가능. **배포**: `firebase deploy --only functions:pollDeviceVehicleGps`.
  - [x] **#2 유비칸 GPS 단말 연동 — 위치 신호 방식(모바일/단말) 선택 + busin 서버 폴러 (2026-07-08 배포 완료 `aa9d44e`, prod `main.2bfc31c7.js` + CF `pollDeviceVehicleGps`/`resolveBusinCarId` us-central1 생성·Cloud Scheduler 매1분 활성)**: callcenter의 busin.co.kr GPS API를 buslink에 이식. 사용자 결정=**위치 추적만**(도착감지·정적QR 후속)·**차량번호 입력→carId 자동조회**. **① functions**: busin 헬퍼 이식(`fetchBusinHTML`/`parseBusinTable`/`fetchCarAllocations`/`fetchVehicleLocations`, `dr.busin.co.kr` :4431 CarAlloc·CarLocationAll, 키 불필요·`rejectUnauthorized:false`) + `resolveBusinCarId`(onCall·assertAdmin·번호→carId) + `pollDeviceVehicleGps`(onSchedule 1분·KST·us-central1: companies순회→`vehicles where gpsSource=="device"`→오늘 배차맵으로 route/driver→busin 최신좌표→**신선도 가드 15분**→`gps/{cid}_{vid}` setDoc=sendGPS 동일필드+`source:"device"`+gpsHistory). **② AdminApp VehicleTab**: 방식 토글(📱모바일/🛰️단말)+carId 조회 버튼+저장 계약(`gpsSource`/`carId`)+목록 배지(colSpan 8→9). **③ DriverApp**: 배정차량 `gpsSource==="device"`면 startGPS skip+배너(이중쓰기 방지)·mobile 경로 100% 그대로. **핵심**: gps 문서 계약 동일이라 관제/승객/직원앱 코드 0 변경으로 device 차량 자동 표시. rules/indexes 변경 0(폴러=Admin SDK). **배포 명령**: `firebase deploy --only functions:pollDeviceVehicleGps,functions:resolveBusinCarId,hosting`(첫 onSchedule=Cloud Scheduler 자동활성·매분 busin 폴링 시작·신규 onCall 수분간 401 후 200). **운영 전제**: ⓐ 차량관리에서 대상 차량 방식=단말+carId 조회 ⓑ 실제 유비칸 단말이 busin에 신호 송출 중이어야 위치 뜸 ⓒ 도착감지/도착임박푸시는 device 차량 미작동(앱 미사용). **후속 RQ**: 정적QR·device 서버측 도착감지.
- [ ] **#1 LG마그나 잠실 GPS 인식 오류 — 진단완료, 코드 조치 대기(2026-06-26)**: 진단 JSON 분석 결과 **ETA 버그 아님**. 근인 = ① **운행 중 GPS 65분 공백**(06:38 3km→07:43 인천 50km 순간점프, 폰 절전·watchPosition 사망·화면거치 무관·웹앱 한계) ② **같은 날 재운행(05:39→06:26)이 dispatch.stopArrivals 공유→이전 회차 시각 잔존**(출발지 −50분). 종점 −21분/중간 +50분은 공백 후 일괄 백필 아티팩트. **다음 조치**: ⓐ 기사앱 GPS 끊김 강한 경고(저위험·사용자 승인 시 구현 — 마지막 fix N분↑ 배너+진동) ⓑ 재테스트 전 dispatch stopArrivals 비우기(운영) ⓒ 근본=callcenter 네이티브 백그라운드 GPS 래퍼 이식(별도 큰 트랙). 상세 issues.md `[진단완료]`.
- [ ] **채드윅 #1 ETA "예상 과거시각·도착 전 목적지 도착"(2026-06-24 진단 완료, 코드 미수정)**: 종점 08:00 의도값 확정(강서 실소요). 근인 후보 2개(GPS 안전망 음수 gpsDelaySec→과거 합성시각 / projectToPolyline 자기교차 노선 오투영). **데이터 확인 우선**: 해당 노선 routePath 자기교차/왕복 여부 + stops offsetMin 실값. 그 후 저위험 가드(합성 도착시각 T_NOW 클램프) 적용 검토(trade-off: 조기 운행 시 통과 정류장 미표시). 핫경로(2026-06-08~09 회귀가드 다수)라 1-shot 진단 확정 후 단일커밋 격리. issues.md `[미해결]` 참조.
- [ ] **채드윅 요청 후속 검증/운영(2026-06-23)**: ① **#10 배차 "추가하면 덮어써짐"** — 부모(개발자) 테스트는 현재 빌드(`main.926e66b8.js`)에서 정상(전 write 경로 addDoc 자동ID·CF exists skip 멱등·렌더 key 고유). **담당자 계정으로 하드리프레시(또는 PWA 재설치) 후 재테스트** 요청함 — 재현 시 정확한 단계(배차관리 vs 배차일정·등록직후 vs 새로고침 스샷 2장) 받아 1-shot. 옛 캐시/차량(#9)혼동 가능성 높음. ② **#11 기사앱 로그인 복구** — 폰 캐시로 새로 로드돼야 적용(시크릿/앱 재실행). 담당자 기기서 "데이터 로드 오류" 시 "다시 시도" 버튼으로 진입되는지 확인. ③ **#9 손실 차량 재등록(운영)** — 옛 length-ID 덮어쓰기로 `vehicle_007` 1대만 생존, 나머지는 복구 불가 → 관리자가 차량 재등록(이제 addDoc이라 안 덮어씀). ④ **#2b 콘솔 라벨 "승객" 인지** — 단일테넌트라 동영관광 관리자/협력사 콘솔도 "직원"→"승객" 표시(중립어라 무방). 동영관광 콘솔만 "직원" 환원 원하면 AdminApp/PartnerApp 라벨만 되돌림(설치 앱명은 "승객" 유지). 사용자 결정 대기.
- [ ] **사용자 운영 정리**: ① dy001 회사명 "최우석"→"동영관광" 복구(슈퍼관리자 콘솔 회사 카드 ✏️ 편집·회사 이름 필드만 재입력) ② 잘못 만든 신규 회사들 영구 삭제(🗑 운영 메뉴) ③ LoginApp 합성이메일 fix 검증(`wschoi@dybus.co.kr` 사번 필드 입력 시 정상 로그인).
- [ ] **EmployeeApp 내 정류장 복원 권한 오류**(2026-05-27 발견, issues.md `[미해결]`) — fcmTokens/{empNo} 또는 routes/{id}/stops 읽기 거부 진단.
- [ ] (선택) PPT 매뉴얼 06-notices·기사 03/04·승객 02/03 본인 폰 캡처해 `docs/manual-ppt/assets/<role>/` + `npm run all`.
- [ ] (선택) 운영 노선에 `routePath` 실제 그리기 — 안 그린 노선은 직선 폴백(정상).
- [ ] (선택) gpsHistory TTL 정책 — Firebase 콘솔 90일 자동 삭제.

## 백로그 / 검토 후보
- [x] ~~🧹 `.claude/issues.md`·`tasks.md` 150줄 초과 정리~~ — issues.md 는 `issues-patterns.md` 분리로 99줄, tasks.md 는 2026-07-21 wrapup 에서 2026-05~06 완료·superseded 검증 항목을 `tasks-log.md` 로 이관해 112줄. 둘 다 소프트캡 이내.
- [ ] 🎯 운행 이력 정류장 반경 시각화 확장: ① 반경 동적 조정 슬라이더(50~300m) — 도착 감지 임계 튜닝 진단 ② routePath(노선 사전 경로) Polyline 보조 표시 — GPS 실 경로 ↔ routePath 일치도 검증 ③ 정류장 마커 클릭 시 stopArrivals 상세(actualAt/plannedAt/delaySec/estimated 백필 여부) 카드 ④ 통과 마커 클릭 시 가장 가까운 GPS 포인트 자동 강조(시각 차이 확인).
- [ ] 📣 카카오 알림톡/SMS 병행 발송 — callcenter SENS 계정 재사용. 템플릿 심사 + Secret Manager 후 진행.
- [ ] 🗓 한국 공휴일 정적 갱신 — `functions/holidays.js` + `src/lib/holidays.js` 2028년 말 전 2029~ 추가.
- [ ] `src/firestore.rules` ↔ 루트 일원화(src 사본 삭제 검토).
- [ ] `firebase-messaging-sw.js` 설정 env 주입(현재 하드코딩).
- [ ] 🔑 카카오 비즈앱 심사 통과 후 전용키 `d464b4` 복구.
- [ ] ⏳ Node 20→22 (데드라인 2026-10-30 decommission).
- [ ] PassengerApp `ARRIVING_M` 미사용 — 완결성 다듬기.

## 기획 갭 (PLANNING.md 2026-05-16, MVP 1단계 전부 동작)
- [ ] ⭐ #12 고객사 담당자 운영 포털 — 자사 실시간 버스·탑승·공지수신 (SaaS 최우선)
- [ ] #9 분석 / #8 운행일지 / #11 푸시 타게팅 / #4 노선 보강(요일·엑셀) / #14-C 정류장 엑셀 / #15 PIN 강제변경
- [ ] (보류·SaaS) #17 슈퍼관리자·빌링 / #16 멀티테넌트(dy001 하드코딩) / #15 SMS. (폐기) #10 예약관리

## 사용자 검증 잔여 (2026-05-26 누적)
- [ ] **QR 탑승 통계 전체 흐름**: DriverApp QR 발급 → 모바일 스캔 → 사번 → "탑승 완료" → AdminApp 탑승 통계 + 협력사 포털 탑승 통계 양쪽 즉시 반영 → 정류장별 GPS 매핑(근접 거리 100m 이내 정상)
- [ ] **도착예정 알고리즘 4건**: ① 기사앱 unplanned 정류장도 "예상·지연" 표시 ② 도착지·전 정류장 distinct 시각(60초 이상) ③ 근접 정류장 ETA 1분 이상 차이 ④ 터널 ETA 점프 1/2 축소
- [ ] **실시간 관제 노선도 뷰**: 토글·정류장 타임라인·버스 위치 마커 / 일자별 picker 정상
- [ ] **운행 이력 노선별 그룹**: 배차 클릭 시 GPS 자동 로드·시간범위 필터 안내 라인
- [ ] **협력사 삭제·기사 삭제 팝업**: 비활성만 삭제 허용·직원수 confirm·성공 alert

## 완료 (최근·시간역순. 옛 누적은 @.claude/tasks-log.md)
- [x] **2026-07-20** 도착 감지 반경 회사 설정화 (prod `main.2415d9bb.js` + CF `pollDeviceVehicleGps` 배포·`--only functions:pollDeviceVehicleGps,hosting`, rules/indexes 변경 0, +419B, 신규 경고 0): way 요청 — 하드코딩 `STOP_ARRIVE_M=100`(gps.js·functions 2곳)을 관리자 조정 가능화. `companies/{cid}.stopArriveRadiusM`(미설정=100m 폴백). `gps.js startGPS({arriveRadiusM})`·DriverApp 회사 반경 로드→전달·`pollDeviceVehicleGps` 회사 반경 사용·AdminApp HistoryTab 슬라이더(50~300m)+저장+반경원 시각화. 기사앱은 운행 시작 시 캡처(값 변경=다음 운행부터). 상세 issues.md `[패턴]`.
- [x] **2026-07-20** 🔴 노선도 뷰 "조기도착 수천분" 수정 (prod `main.517c8c12.js`·`--only hosting`, AdminApp 단독·stopSchedule 무변경, rules/indexes/CF 변경 0, 신규 경고 0): way 점검 — 타 날짜 stopArrivals(테스트 배차 잔존·복제)의 `actualAt` 을 오늘 도착으로 오인해 `delaySec` 이 며칠짜리(계획 오늘−실측 며칠전). prod 실데이터로 확정(07:50 판교역 actualAt=07-15→−7204분). `RouteTimelineCard` 에 운행 날짜 창 필터 + 과거 조회 `now` 앵커(`parseDateStartMs`). 격리 node 테스트로 수정전/후 검증. 부수: `scripts/clean_stale_arrivals.cjs`(잔존 정리·dry-run 완료·**--apply 미실행**). 상세 issues.md `[해결]`.
- [x] **2026-07-13** 개선 요청 **본문 인라인 이미지(리치 텍스트)** — YDYOPS 이식 (**빌드 `main.4ac4d558.js` 미배포·prod 미반영**, +12.42KB gzip=dompurify, css `main.9371e94c.css` +78B, 신규 ESLint 경고 0, rules/indexes/functions/CF 변경 0=프론트 전용): 요청="본문 글 속에 이미지가 인라인으로". Buslink 는 Storage 미사용 → **압축 data URI 를 content HTML 에 인라인**. `npm install dompurify`(^3.4.12). **신규 파일**: `src/lib/richText.js`(DOMPurify sanitize·looksLikeHtml·htmlToPlainText·htmlByteSize·contentHasImage + **svg 차단 훅**=DOMPurify 가 DATA_URI_TAGS 로 ALLOWED_URI_REGEXP 우회하는 함정 대응)·`src/components/RichTextEditor.js`(contentEditable·순수·compressImageFile 재사용·서식 툴바 없음·동기 붙여넣기·혼합 텍스트 유실 방지). **AdminApp**: ImprovementCreateModal textarea→RichTextEditor(content=HTML)·제출 시 sanitize+총량캡 800KB(인라인+screenshots 합산)·ImprovementDetailModal 표시부 `looksLikeHtml ? dangerouslySetInnerHTML(재sanitize) : pre-wrap 폴백`. **index.css** `.rte-editor img`/`.imp-content` img 폭 제어(style 미허용 대응). 기존 `screenshots` 별도 첨부 공존·레거시 평문 회귀 0. 검증=격리 node 테스트(jsdom+DOMPurify) 22케이스 통과·`CI=false npm run build` 통과(신규 경고 0)·앵커 58bf34 확인. **배포 절대 금지 — 사용자 검토 후**. **배포 명령**: `firebase deploy --only hosting`. 상세 issues.md `[패턴]`.
- [x] **2026-07-13** 개선 요청 게시판 신설 — YDYOPS 이식 (**빌드 `main.eda7bbfe.js` 미배포·prod 미반영**, +3.79KB, 신규 ESLint 경고 0, 기존 25 CF/rules 기존 블록/기존 인덱스/다른 탭/앱 무영향): AdminApp 새 탭 "개선 요청"(인덱스 12·전 admin/superadmin·SUPER_TAB_INDEX 자동 13). **신규 파일**: `src/lib/improvementRequests.js`(data-access+상수·history 는 `Timestamp.now()`)·`src/lib/improvementSeen.js`(localStorage 안읽음·백엔드 0). **신규 컬렉션**: 최상위 `improvement_requests/{autoId}`(companyId 스코핑·회사 admin 자기 회사만/superadmin 전 회사)·`config/{docId}`(웹훅 URL). **rules**(루트+src 미러): improvement_requests·config 블록(isAdmin/isSuperAdmin). **인덱스**: improvement_requests(companyId+createdAt DESC). **CF 2종**(us-central1·best-effort·doc write 0): `onImprovementRequestCreate`(cardsV2 새 스레드)·`onImprovementRequestUpdate`(history 길이 diff 신규만·done skip·threadKey 답글). 웹훅=`config/improvementBoard.gchatWebhookUrl`(시크릿 아님·미설정 skip). **AdminApp**: `ImprovementTab`+등록/상세 모달(상태필터칩·검색·내글만·처리이력 타임라인·댓글·상태전이 superadmin·첨부 압축 3장/300KB/900KB). 검증=`node --check` 통과·`CI=false npm run build` 통과(신규 경고 0)·격리 node 19케이스 통과. **배포 절대 금지 — 사용자 검토 후**. **배포 명령**: `firebase deploy --only firestore:indexes,firestore:rules,functions:onImprovementRequestCreate,functions:onImprovementRequestUpdate,hosting`. **웹훅 URL 설정(배포 후·슈퍼관리자)**: 구글챗 스페이스 → 앱/통합 → 수신 웹후크 생성 → URL 복사 → Firestore 콘솔 `config/improvementBoard` 문서 `gchatWebhookUrl` 필드에 붙여넣기(문서 없으면 생성). 미설정이면 알림만 skip(게시판 자체는 정상 동작). 상세 issues.md `[패턴]`.
- [x] **2026-07-13** device(유비칸 단말) GPS 운행 시간 창 게이트 (**prod 배포 `functions:pollDeviceVehicleGps`**, functions 단독·hosting 무관, rules/indexes/클라 변경 0): 사용자 지적="GPS 상시신호라 수신 시간 설정이 노선설정에서 가능해야". device 폴러가 매1분 24시간 돌며 시간대 창 없이 ①오늘배차 ②15분 신선도로만 판단 → 운행시간 밖 이동(퇴근 후 개인운전 등) 위치가 뜸. **해결(옵션1=기존 데이터 자동도출)**: 순수함수 `computeGpsWindow(meta)` — 노선 `departTime`+정류장 `offsetMin`에서 창 `[dep−30, (dep+max offset)+60]`(offsetMin 전무 시 기본 120분) 도출, 현재 KST 분이 창 밖이면 busin 조회·write skip. **`departTime` 미설정 노선=게이트 없음(기존 동작 보존)**. meta 는 게이트에서 1회 로드→도착감지 재사용(중복 제거). 상수 PRE30/POST60/DEFAULT120·자정 클램프. 운영자 통제점=노선 출발시각·정류장 offsetMin(별도 UI 0). 검증=격리 node 테스트 11케이스 통과·`node --check` 통과. **배포**: `firebase deploy --only functions:pollDeviceVehicleGps`. 상세 issues.md `[패턴]`.
- [x] **2026-07-13** 🔴 정적(고정) QR 탑승 `Missing or insufficient permissions` 수정 — CF 위임 (**prod 배포 `main.184f487d.js` + CF `resolveStaticBoarding`/`boardStatic` 생성·커밋 `da82b19`→origin/master**, -313B, rules/indexes 변경 0, EmployeeApp/BoardingApp diff 0, 신규 ESLint 경고 0): 근인=정적 QR 흐름(`resolveStaticDispatch`)이 오늘 배차를 `getDocs(where vehicleId==X)` 로 읽는데 두 진입점(인앱 스캐너·폰카메라)이 익명 인증 → 배차 읽기 rules(admin/driver 잠금)에서 쿼리 전체 거부. **사용자 결정=CF 위임·rules 변경 금지**: ① `functions/index.js` onCall 2종 신설 `resolveStaticBoarding`(읽기전용 프리뷰)·`boardStatic`(배차 재해석+멱등 boarding)·둘 다 `request.auth` 존재만 확인(익명 통과·assertAdmin 없음)·공용 헬퍼 `resolveStaticDispatchAdmin` ② `src/lib/boarding.js` `resolveStaticDispatch`/`validateAndBoardStatic` 본문만 `httpsCallable` 호출로 교체(반환 계약 보존·클라 가드 "사번을 입력해주세요" 유지)·orphan 된 `getDocs/query/where` import 제거(신규 경고 0). 검증=`node --check functions/index.js` 통과·`CI=false npm run build` 통과(boarding.js 경고 0·나머지 경고는 HEAD 선재)·EmployeeApp/BoardingApp/rules/indexes git diff 0. **배포 절대 금지 — 사용자 명시 요청 시에만**. **배포 명령**: `firebase deploy --only functions:resolveStaticBoarding,functions:boardStatic,hosting`. 상세 issues.md `[해결]`.
- [x] **2026-07-10** 최우석 문의·점검·요청 3건 (prod `main.d42c1bcb.js`, `--only hosting`, +1.06KB, rules/indexes/CF 변경 0, 신규 경고 0): ① 문의=고정 QR 용도 확인(답변만·코드 0) ② 🔴 점검=고정 QR 인앱 스캔 `Invalid segment` 오류 수정(`boarding.js resolveStaticDispatch` 추출 + `EmployeeApp handleTokenScanned` 동적/고정/무효 3분기 + `includes("/")` 가드 + `alreadyBoarded` 안내) ③ 요청=노선 표시 순서(`src/lib/routeOrder.js` `compareRoutes` 신설 → Admin 목록/폼 "표시 순서"+▲▼ `moveRoute`, Employee 노선탭·노선변경 모달·홈 폴백, Passenger 노선선택 5지점 적용). 검증=격리 node 테스트(스크린샷 실패 URL 포함 8케이스+정렬)·헤드리스 Chrome 실로드 3화면 콘솔오류 0·HEAD 경고 diff=줄번호 이동뿐·curl 4 서브도메인 해시 확인. **사용자 실기기 검증 대기**: 카메라 스캔은 자동화 불가. 상세 issues.md `[해결]`+`[패턴]`.
- [x] **2026-06-26** 배시현 점검 4건 UI 개선 (prod `main.f4f4c8b5.js`, `--only hosting`, rules/indexes/CF·css 변경 0, +659B, 신규경고 0): ① #2 노선 새로고침 — `src/lib/forceReconnect.js`(disableNetwork→enableNetwork) + EmployeeApp/PassengerApp `manualTick`·AdminApp MapTab `refreshTick` 재구독 + 헤더/탑바 "↻ 새로고침" 버튼(`blspin` 재사용) ② #3 정류장 탭→지도중앙 — EmployeeApp GPS 자동재센터 useEffect 가 탭 센터 덮어쓰던 것 `userCenteredRef` 억제(activeRouteId 변경 시 리셋)+노선도/마커 탭 setCenter, /bus 는 자동재센터 없어 무변경 ③ #4 버스 yAnchor 1.5→0.5(라인 중심 안착, 3맵) ④ #5 도착시간 라벨 `isMyStop` 게이트(선택 정류장만, 클러터 제거). #1 LG마그나 GPS 인식오류=진단 보류(다음 할 일). 상세 issues.md `[패턴]`+`[미해결]`.
- [x] **2026-06-24** 채드윅 요청 3건 중 2건 (prod `main.8dd2d627.js`, `--only hosting`, rules/indexes/CF 변경 0): ① 기사앱 "조기 2분"→"조기도착 2분"(`formatDelayLabel` 조기 분기·전역 헬퍼·조기출발 오인 방지) ② 노선도 스트립 "버스 2대처럼"→단일 버스+점멸 화살표 `›`(EmployeeApp 노드+커넥터 이중 렌더 근인·`@keyframes busblink` 신설·PassengerApp은 스트립 없음 무변경). 빌드 `main.8dd2d627.js`/css `main.02202394.css`·신규경고 0·curl `/p` no-cache 확인. ③ **계획≠예상·도착 전 "목적지 도착" = 진단만(코드 미수정)** — issues.md `[미해결]` 참조. 상세 issues.md `[패턴]` 2건.
- [x] **2026-06-23** 실시간 관제 선택 차량 추적 (prod `main.83d1e753.js`, `--only hosting`, rules/CF 변경 0): 클릭한 차량이 매 GPS 스냅샷마다 첫 차량으로 자동 복귀하던 것(setCenter(list[0]) 반복) 수정 — 최초 1회만 센터(didInitCenter ref) + 선택 차량 추적 effect + 선택 시 그 차량 마커만 표시 + 레일 "✕ 전체 보기" 버튼. MapTab 단독. 상세 issues.md `[해결]`.
- [x] **2026-06-23** GPS 미수신 표기 + 마커 연속 이동 (prod `main.03ee32c8.js`, `--only hosting`, rules/indexes/CF 변경 0): ① 운행중인데 GPS 미수신(상태만 운행중·gps 문서 없음/stale) 기사를 대시보드(기사현황 amber 칩+경고카드+KPI sub)·실시간관제(좌측레일 amber 경고, drivers prop 추가)에 표기. 헬퍼 `isGpsFresh`(≤60초·pending=fresh). 제한 admin 게이팅 보존. ② 마커 들쑥날쑥(1.5초 easeInOut + GPS 5초 간격=이동/정지/점프) → `useAnimatedPositions` 선형+동적 duration(실제 도착간격 clamp 1200~8000)으로 연속 이동(승객·직원앱 공용 동시 개선). 셔틀 "관제 1대만" 문의 = 코드 정상(차량별 마커)·해당 기사 폰 GPS 미발신이 근인 → 미수신 표기로 가시화. 상세 issues.md `[패턴]`/`[해결]`.
- [x] **2026-06-22** 채드윅 요청 배치 2 — 전역 리네임·셔틀 옵션·기사앱 로그인 복구 (prod `main.926e66b8.js`, `--only hosting`, rules/indexes/CF 변경 0): ① **#2b "직원"→"승객" 전역 리네임** — 설치 PWA 앱명(manifest-employee.json name/short_name "BusLink 승객")+appIcons+EmployeeApp title/sub+엑셀 양식명+AdminApp 13곳/PartnerApp 11곳 화면 라벨. 식별자/boardingMode 값/주석/rules/CF/docs/.claude 불변. **운영주의**: 단일테넌트라 동영관광 콘솔도 "승객" 표시(중립어라 무방·인지 필요). ② **#7 셔틀** 노선 type 옵션 `["출근","퇴근","셔틀"]` 추가(노선 분리 운영·도착기록 구조 무변경). ③ **#11 기사앱 로그인 "데이터 로드 오류"+로그아웃 화면 갇힘** → 복구형(init useCallback 추출+transient 1회 자동재시도+"다시 시도" 버튼+onAuthStateChanged 보강). 빌드 검증(앵커 58bf34·신규경고 0·`926e66b8`)·curl `/p`·`/driver` `926e66b8`+manifest name "BusLink 승객" 확인. 상세 issues.md `[해결]`/`[패턴]` 3건.
- [x] **2026-06-22** 채드윅 요청 점검 + 안전 배치 4건 (prod `main.169c4f4e.js`, `--only hosting`, rules/indexes/CF 변경 0): RQ/20260622 11개 요청 점검 후 안전·고가치 4건 구현·배포. ① **🔴 차량 1대만=ID 덮어쓰기 수정** VehicleTab `vehicle_${length+1}` setDoc→`addDoc`(자동ID·데이터손실 차단) ② **등교/하교 shift 옵션 추가**(AdminApp:2197·학교노선용) ③ **지도 밝기** color-scheme:light meta+css(OS auto-dark 차단) ④ **뒤로가기 종료 확인** `useExitConfirm` 훅 3앱 적용. 빌드 검증(앵커 58bf34·신규 경고 0)·curl 4 클린URL `169c4f4e` no-cache 확인. **점검 결과 — 이미 충족**: #1 채드윅 노선만 정렬(2026-06-17 partnerCode 필터·데이터만), #3 사번=비번변경 필수 아님(기사 createDriver 동시생성·직원 Auth무), #4 1ID 전체노선조회(PartnerApp 운영포털 완성·업체코드 발급만), #6 사번 영문혼용(제약 없음·이미 가능). **보류·후속**: #2b "직원→승객" 전역 리네임(동영관광은 실제 직원이라 운영영향·per-tenant 라벨 필요), #7 셔틀 동일정류장 반복(stopArrivals map 키 구조 제약·노선 분리 정합 or 구조변경 결정 필요), #10 배차 사라짐(createdBy 격리/날짜경계 가설·콘솔 캡처 필요), #11 로그인 "로그아웃 처리" 화면(**코드에 해당 문구 없음**·스크린샷 필요). 상세 issues.md `[해결]`/`[패턴]` 4건.
- [x] **2026-06-16** 노선 createdBy 소유 모델 — 제한 admin "노선 추가해도 안 보임" 해소 (prod `main.8896e8f8.js`, rules/indexes/CF 변경 0): 근인=RoutesTab `filtered`(2153행) Phase B 게이팅이 제한 admin 의 **거래처 미지정 노선을 숨김**(기사/차량 createdBy 격리와 동일 맥락의 잔존 이슈). 거래처 createdBy 패턴대로 노선에도 `createdBy` 적용 — handleSave 신규+handleCopy 복사본에 `createdBy:currentUserUid||null` 기록, filtered 에 `|| r.createdBy===currentUserUid` 추가(거래처 미지정 자기 노선도 노출). RoutesTab 에 `currentUserUid` prop 전달. **회귀 0**: isAllAccess 경로 동일·기존 partnerCode 허용 노선 그대로. **한계**: 레거시(createdBy 부재) 노선은 제한 admin 비노출(전체권한 admin 엔 보임)·신규 등록부터 적용. 상세 issues.md `[패턴]`.
- [x] **2026-06-16** 기사·차량 관리 createdBy ID별 격리 (prod `main.91fa0fdd.js` + functions:createDriver, rules/indexes 변경 0): 사용자 결정 "기사/차량도 등록한 admin ID별 완전 별도 관리·단 전체권한/슈퍼관리자는 전부 조회". 거래처 createdBy 모델(2026-06-15)을 기사/차량으로 확장(격리키=`createdBy===uid`). ① `createDriver` CF 가 `createdBy:request.auth.uid` 기록(서버측) ② DriverTab/VehicleTab 에 `allowed`+`currentUserUid` prop → `isAllAccess` 면 전체, 아니면 `createdBy===uid` 만 표시(목록·카운트·기사폼 배정차량 드롭다운) ③ 신규생성 createdBy: CF(기사)·폴백 addDoc(기사)·setDoc(차량). **공유 vehicles/drivers 배열은 다른 탭(배차/시뮬/이력 등) 무필터**(범위 한정)·차량 id 생성 `vehicles.length` 전체 유지. **회귀 0**: isAllAccess 경로 현재 동작 100% 동일. **한계**: 레거시(createdBy 부재) 기사/차량은 제한 admin 비노출(전체권한 admin 엔 보임)·클라 게이팅이라 우회 가능(rules 강제는 Phase B 후속과 동일). 상세 issues.md `[패턴]`.
- [x] **2026-06-16** 🔴 기사 정보 수정 권한 오류 + 기사 로그인 불가 (firestore:rules 배포, 프론트 변경 0): 고객 점검 2건 동일 근인 = `drivers/{driverId}` 룰에 **admin update 경로 부재**(create/delete=admin, update=기사본인 한정뿐). 관리자 "기사 정보 수정" 저장 시 `updateDoc(drivers)` 거부→throw→뒤의 PIN/Auth 생성 블록 미실행→기사 Auth 미갱신→로그인 실패. **해법(순수 추가)**: dispatches 패턴대로 `update: isAdmin(companyId) || (isDriverOf...)`. 루트+src 룰 동기. 회귀 0. **운영자 후속(필수)**: 관리자가 해당 기사 수정→비번 6자+ 입력→저장 1회 재실행해야 createDriverAuth/updateDriverPassword 발화→기사앱 로그인 가능. 상세 issues.md `[해결]`.
- [x] **2026-06-16** 노선 복사 기능 (prod `main.718c2ded.js`, +309B, rules/indexes/CF 변경 0): 고객(김희정) 요청 "정류장 정보 많은 노선을 복사하는 기능". RoutesTab 관리 칸 수정↔삭제 사이 `📋 복사` 버튼 + `handleCopy(item)` — 노선 문서(명시 필드만·`name+" (복사본)"`·partnerCode/routePath 보존·id 누출 0) addDoc 후 하위 `stops` 전체(name/address/lat/lng/order/photo/description/offsetMin, order 보존·id 제외) 직렬 복제. 성공 시 필터 초기화(handleSave 패턴)로 즉시 노출 + "정류장 N개와 함께 복사" alert. 순수 클라(companies/** write=admin 룰). Phase B: partnerCode 보존이라 제한 admin 도 복사본 자동 노출. cloud-functions-dev 위임→부모 diff/빌드 검증→배포(curl 718c2ded 확인). 상세 issues.md `[패턴]`.
- [x] **2026-06-16** 정류장 주소검색 무반응 + 본인 거래처 배차 미표시 (커밋 `3a0bcda`→`2dd924a`, prod `main.9a5310da.js`, rules/indexes/CF 변경 0): ① **주소검색·지도 동시 사망 = 카카오 키 신규 서브도메인 미등록**(코드 무죄) → 사용자 카카오 콘솔 `admin/d/p/partner.buslink.co.kr` 4개 등록 완료(해결) + 코드 `handleAddrSearch` 8초 워치독+settled 가드(콜백 미응답 "검색 중" 영구정지 차단). ② **createdBy 가시성 전 게이팅 지점 확장** — AdminApp 최상위 `allowed = allAccess ? rawAllowed : rawAllowed∪(createdBy==uid 거래처)` 합쳐 전 탭 동일 prop(이전엔 대시보드/협력사관리만). 본인 등록 거래처 배차/노선이 배차관리 등에서 자동 노출. 잔존: 거래처 미지정 노선은 여전히 제한 admin 비노출(설계)→노선에 거래처 지정 or 전체권한. 상세 issues.md `[패턴]` 2건·architecture.md Phase B 보강.
- [x] **2026-06-15** 거래처 중심 관리·권한 UX 묶음 (커밋 d4a6e5c→de19252, prod `main.a987fde5.js`, rules/indexes 변경 0): ① 권한 "전체 해제" 저장 버그(빈배열 정규화 `length>0` 제거, CF+클라 둘 다 배포 필수) ② 신규 admin 기본 전체해제 ③ 관리자목록 게시판형+자기회사 자동펼침 ④ 대시보드 거래처 관리 현황(노선/배차/운행/탑승+업체코드+포털URL) ⑤ `partnerCodes.createdBy` 본인 발급분 열람 ⑥ 거래처 등록/거래처별 노선관리 버튼 ⑦ 노선 저장후 필터리셋 ⑧ 기사 사번·PIN 안내+autofill 차단 ⑨ SearchableSelect(검색형 기사/차량/노선). 상세 issues.md `[패턴]`.
> 그 이전 완료는 @.claude/tasks-log.md.

## 저장소 메모
- `src.zip` git 추적되나 작업트리 삭제 상태(소스 백업, 빌드 미사용).
